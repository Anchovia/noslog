import { ProfileAchievementsTabSkeleton } from "@/features/profile/components/profileLoading";

// 「업적」 탭 불러오기 — 머리 · 탭은 그대로, 내용 자리만(2026-09-25 D2)
export default function Loading() {
    return <ProfileAchievementsTabSkeleton />;
}
