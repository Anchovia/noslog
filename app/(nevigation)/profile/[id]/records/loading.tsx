import { ProfileRecordsTabSkeleton } from "@/features/profile/components/profileLoading";

// 「기록」 탭 불러오기 — 머리 · 탭은 그대로, 내용 자리만(2026-09-25)
export default function Loading() {
    return <ProfileRecordsTabSkeleton />;
}
