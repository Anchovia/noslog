# CLAUDE.md — NosLog 2.0 디자인 작업 규칙

> **이 파일을 모든 작업 시작 전에 반드시 먼저 읽는다.** 세션이 바뀌어도, 대화가 길어져도,
> 작업을 재개할 때마다 다시 읽는다. 여기 적힌 규칙은 사용자가 반복해서 지시한 것이며
> 어긴 적이 있는 항목만 남겨 두었다.

---

## 0. 절대 규칙 (어긴 이력이 있음 — 반복 금지)

1. **시작 게이트** — 사용자가 명시적으로 "시작"이라고 말하기 전에는 아무것도 만들지 않는다.
   스킬 로딩이나 "phase 진입"도 시작으로 간주된다.
2. **문서 우선** — 만들기 전에 해당 Page Brief를 끝까지 읽는다. 상위 문서만 읽고 시작하지 않는다.
3. **추측 금지** — 문서에 없는 값(패딩·정렬·색·구조·컬럼·라벨)을 감으로 정하지 않는다.
   `AGENTS.md`에 이미 명시: _"Do not make ambiguous decisions from assumptions."_
4. **애매하면 멈추고 질문** — 문서에 근거가 없는 시각 결정이 나오면 **만들지 말고 먼저 묻는다.**
   질문할 때는 말로만 하지 말고 **비교 시안을 그려서** 보여준다.
5. **만든 직후 전수 검증** — 아래 §2 체크리스트를 예외 없이 전부 돌린다.
   "스크립트가 에러 없이 돌았다" ≠ "결과가 맞다".
6. **실행한 검사만 보고** — 안 돌린 검사를 "검증했다"고 말하지 않는다.
7. **Light/Dark 둘 다** — 문서 24는 Light·Dark 양쪽을 요구한다. 한쪽만 만들고 끝내지 않는다.
8. **정의값 그대로 쓴다 — 눈대중 금지** ⚠️ **가장 자주 어긴 항목**
   **레이아웃(패딩·간격·크기·정렬)·색·타이포는 §3에 적힌 값 집합 안에서만 고른다.**
   근사값·중간값·"보기 좋아 보이는 값"을 만들지 않는다. 스케일에 없는 숫자가 필요해 보이면
   그건 값을 새로 만들 신호가 아니라 **멈추고 질문할 신호**다(규칙 4).
9. **매 컴포넌트마다 다시 확인한다** — 레이아웃·색·타이포 세 가지는 §2에서 **예외 없이 매번**
   전수 재확인한다. 앞 컴포넌트에서 지켰다는 이유로 다음 컴포넌트에서 생략하지 않는다.
   패딩·정렬 같은 "기본적인 것"일수록 건너뛰지 않는다 — 실제로 여기서 반복해서 틀렸다.

---

## 1. 권위 순서

① 사용자의 최신 결정 → ② `AGENTS.md` → ③ `README.md` · 문서 57 → ④ 문서 07(뷰어/에디터 절대 보존)
→ ⑤ 문서 24(Foundation 정확값) → ⑥ 문서 25 → ⑦ 해당 Page Brief → ⑧ 문서 22 · 63

- 시각 언어 기준: `output/pdf/noslog-2.0-visual-core-review.pdf` (승인된 편집 언어)
- 실제 콘텐츠: `prisma/data/nosdata-musics.json` (578곡) — 콘텐츠를 지어내지 않는다

---

## 2. 검증 체크리스트 (만들 때마다 전부)

> **레이아웃 · 색 · 타이포 세 블록은 만들 때마다 전부 돌린다. 하나도 건너뛰지 않는다.**
> 안 돌린 검사는 "통과"가 아니라 **미실행**이며, 미실행을 통과로 보고하지 않는다(규칙 6).

### A. 레이아웃 감사 — 패딩 · 간격 · 크기 · 정렬

- [ ] **spacing 스케일** — 모든 padding/itemSpacing이 `0·4·8·12·16·24·32·48·64` 중 하나.
      `2px`는 **아이콘·배지·차트 내부 광학보정 전용** — 일반 레이아웃 간격으로 쓰면 위반(문서 24)
- [ ] **패딩 역할 일치** — `4` 분리불가 디테일 / `8` 인라인 피어 / `12` compact inset /
      `16` 기본 component inset / `24` subsection / `32` section / `48` page-region /
      `64` 희귀 대구획. 역할과 다른 값을 쓰면 근거를 남긴다
- [ ] **좌우·상하 패딩 대칭** — 비대칭이면 의도된 것인지 확인하고 근거를 남긴다
- [ ] **가장자리 아이콘 컨트롤 광학 패딩** ⚠️ 2026-08-13 사용자 지적 — 선언값만 보면 못 잡는다
      양쪽 끝이 고정된 컨테이너의 첫/마지막 자식이 아이콘 전용 컨트롤이면,
      그 컨트롤의 내부 여백 `(타겟−아이콘)/2` 을 컨테이너 패딩에서 뺀다.
      AppHeader = 왼쪽 16 / 오른쪽 8(44 타겟 안 24 아이콘 = 내부 10). **선언 비대칭이 정상이다.**
      타겟이 컨테이너 경계에 닿을 만큼 줄이지 않는다(SearchField 는 그래서 8 유지).
      검사는 `audit.js` `A17_edgeIconOptical` — 잉크·아이콘 박스 기준으로 본다
- [ ] **죽은 여백(FILL 자식이 남는 공간을 삼킴)** ⚠️ 2026-08-14 사용자 지적 — `A15`는 반대 경우만 잡는다
      부모가 FIXED이고 자식이 세로 `FILL`이면, 자식은 남는 공간을 흡수하고 정렬이 `MIN`일 때
      그 공간이 **콘텐츠 아래 죽은 여백**으로 깔린다. 선언 패딩만 보면 안 보인다.
      **고정 높이는 반드시 내용에서 유도한 숫자여야 한다** — 유도 안 되는 숫자면 HUG로 바꾼다.
      실제 사고: Music Grid 카드가 297 고정인데 내용은 277 → 난이도 마커 아래 20px 죽은 공간
      (하단 패딩 12까지 32px). 검사: `자켓 + gap + (body 패딩+내용) + 카드 패딩 == 카드 높이`
- [ ] **컴포넌트 크기 일관** — 같은 역할 컴포넌트는 높이가 서로 일치.
      variant 간(Default/Focus/Filled…) 크기가 들쭉날쭉하면 위반
- [ ] **아이콘 크기** — 렌더 `20`(일반 액션) / `16`(라벨 옆 보조) / `24`(강조)만.
      아이콘 전용 컨트롤 타겟 **모바일 44×44 · 데스크톱 40×40** 충족
- [ ] **정렬** — ① `counterAxisAlignItems=CENTER` 선언 지점의 자식 centerY 오차 ≤0.5px
      ② `primaryAxisAlignItems` 선언과 실제 배치 일치
      ③ 같은 행 텍스트끼리 baseline 어긋남 없음
      ④ 세로로 쌓인 라벨/값의 좌측 정렬선 일치
- [ ] **원형 요소** — 원이어야 하는 것(배지·dot·아바타)은 width=height 고정
- [ ] **radius 역할** — control `4` / container `8` / overlay `10` / full `50%`.
      focus 는 컨트롤의 기존 radius 를 그대로 사용하며 파생 radius 를 만들지 않는다
- [ ] **strokeWeight** — 1 또는 2
- [ ] **페이지 겹침** — top-level 노드 간 bounding box 교차 0
- [ ] **자식 대 자식 겹침** ⚠️ 2026-08-20 사용자 지적 — 같은 섹션 안 **형제 프레임끼리** 겹치는지 별도로 본다.
      `자식 ⊂ 섹션` 검사만으로는 안 잡힌다. 실제 사고: 320 검증 섹션의 행 간격이 상수 `1582` 로 박혀 있었는데
      상세 프레임이 커지자 KO 행이 JA 행을 20px, JA 행이 EN 행을 80px 덮었다.
      **행 피치를 상수로 두지 말고 그 행의 최대 높이에서 유도한다** — 프레임 높이는 내용에서 나오는데
      간격만 상수면 내용이 조금만 커져도 깨진다.
- [ ] **섹션 자식 포함** ⚠️ 2026-08-19 — 모든 섹션 자식의 `absoluteBoundingBox` 가 자기 섹션 안에 있는가.
      섹션 자식 좌표는 상대값이라 절대값을 넣으면 섹션 밖으로 밀려 **다른 섹션을 덮는다.**
      프레임 단위 감사·개별 스크린샷은 이걸 절대 못 잡는다 — **페이지 전체를 한 장으로 렌더해 눈으로 본다**
- [ ] **텍스트 넘침** — 부모 가용폭 초과 0
- [ ] **아이콘 클리핑** — 인스턴스 크기 = 내부 프레임 크기, clip 해제
- [ ] **그리드/컨테이너** — Compact <672(4col g12 m16) / Intermediate 672–1055(8col g16 m24) /
      Wide 1056+(12col g16 m32). container 최대값은 **천장이지 고정 캔버스가 아니다**

### B. 타이포 감사

- [ ] **Text Style만 사용** — 노드에 size/weight/line-height를 직접 찍지 않는다.
      39개 Text Style(13 composite × ko/ja/latin) 중 하나를 적용
- [ ] **composite 정확 일치** — size/line-height/weight 조합이 §3 표의 **13종 중 하나와 정확히 일치**.
      임의 조합(예: 16/20, 14/24) 금지
- [ ] **weight** — `400·500·600·700`만. `100–300`·`800–900`·중간값 금지
- [ ] **최소 크기** — 사용자 대면 텍스트에 12px 미만 없음
- [ ] **tabular figures** — `metric-display`·`metric-value`에만. 나머지는 proportional
- [ ] **역할 오용 없음** — `entity-title`을 섹션 제목으로, `body`를 컨트롤 라벨로 쓰는 식의 혼용 금지.
      `control`=액션/선택/필드 라벨, 입력된 값=`body`

### C. 색 감사

- [ ] **바인딩 100%** — 모든 fill/stroke/effect가 변수 바인딩. 하드코딩 0
- [ ] **NI-A 준수** — interaction fill이 hover/selected/menu-set/disabled **외의**
      resting 요소에 칠해져 있지 않음. 보편적 hover/selected 배경은 없다
- [ ] **경계 오용 없음** — `divider`는 장식 리듬 전용. quiet 경계(`divider`·`border-subtle`)를
      유일한 필수 단서로 쓰지 않음. Dark 컨테이너를 전부 외곽선 두르지 않음
- [ ] **Light·Dark 양쪽** — A·B·C 전부를 두 모드 각각에서 재확인 (한쪽만 돌리면 미실행)

### 구조 변경 함정 (실제로 화면을 깨뜨린 적 있음)

- **auto-layout 부모에서 `appendChild`/`insertChild`는 z-순서만 바꾸는 게 아니라 flow 위치까지 바꾼다.**
  2026-08-14: P1 셸(VERTICAL auto-layout)에서 z를 맞추려고 `appendChild(header)` 했더니
  헤더가 flow 끝(y=1488)으로 밀려 화면에서 사라졌다. 일반 프레임(절대배치)에서는 z만 바뀐다.
  **오버레이 z는 `layoutPositioning='ABSOLUTE'` 로 해결하고, flow 자식의 순서는 건드리지 않는다.**
- 이 사고는 겹침·이탈·넘침 검사를 **전부 통과했다** — 헤더가 셸 안에 있고 크기도 그대로였기 때문이다.
  셸을 만들면 `헤더가 flow 최상단(index 0 · y=0)이고 폭이 셸 폭과 같은지`를 따로 확인한다.
- **`figma.createAutoLayout()`·`createFrame()` 은 SECTION 이 아니라 현재 페이지에 붙는다.** ⚠️ 2026-08-18 사용자 지적
  좌표만 섹션 위치에 맞춰 두면 화면에서는 섹션 안처럼 보이지만 실제로는 섹션의 자식이 아니다.
  Z1 결정 시안 4개가 이 상태로 만들어졌고 `section.children.length` 가 0 으로 찍혀서야 발견했다.
  **새 판은 `section.appendChild(node)` 로 먼저 붙이고 좌표는 그다음에 준다.**
  검사: `page.children` 에 SECTION 아닌 노드가 남아 있으면 위반이다.
- **섹션 자식의 `x`·`y` 는 섹션 기준 상대좌표다 — 절대 캔버스 좌표를 넣지 않는다.** ⚠️ 2026-08-19 사용자 지적
  `node.x`/`node.y` 는 부모(섹션) 원점 기준이고, `absoluteBoundingBox` 만 캔버스 절대값이다.
  여기에 절대값을 넣으면 자식이 **섹션 y 만큼 한 번 더 아래로 밀려** 다른 섹션 영역을 덮는다.
  규약: `x = 48` · `y = 48`(첫 자식) · `y = 직전 형제.y + 직전 형제.height + 64` — **전부 상대값**.
  섹션 크기는 내용에 맞춘다. 내용을 고쳐 높이가 바뀌면 **그 섹션을 통째로 다시 흘려보낸다.**
  실제 사고: P3 6개 섹션·P2 6개 섹션(프레임 56개)이 이 상태로 만들어져 페이지가 두 배 높이로 늘어나고
  내용이 서로 겹쳐 뭉갰다. **개별 프레임만 스크린샷하면 멀쩡해 보여서 안 걸린다.**
  검사: 섹션마다 `child.absoluteBoundingBox` 가 `section.absoluteBoundingBox` 안에 들어오는지,
  그리고 **페이지 전체를 한 장으로** 렌더해 섹션 박스와 내용이 어긋나지 않는지 본다.
- **생성 API 전부가 `figma.currentPage` 에 붙는다 — 내가 지정한 노드가 아니다.** ⚠️ 2026-08-18/19 반복 발생
  `createAutoLayout()` · `createFrame()` · `createSection()` · `clone()` 이 전부 해당한다.
  2026-08-19 에는 Z1 을 currentPage 로 둔 스크립트 안에서 P2 용 섹션 2개를 만들어
  **Intermediate·1440 섹션이 통째로 Z1 페이지에 생겼다.** 좌표를 P2 기준으로 계산했기 때문에
  위치만 그럴듯해서 감사·스크린샷에도 안 걸렸고, handoff 를 쓰려고 `p2.children` 을 세다가 발견했다.
  **다른 페이지의 노드를 만들려면 그 페이지를 currentPage 로 두거나, 만든 직후 `targetPage.appendChild()` 한다.**
  검사: 작업 후 `page.children` 개수와 섹션 목록을 직접 세어 본다.
- **`clone()` 도 원본이 SECTION 자식이어도 페이지에 붙는다.** ⚠️ 2026-08-18
  상태 화면 3개를 복제했더니 전부 페이지 직속으로 생겼다. 복제 직후 `section.appendChild()` 한다.
- **`layoutPositioning='ABSOLUTE'` 자식의 `x`/`y` 는 부모 프레임 기준이다.** 절대 캔버스 좌표를 넣으면
  프레임 밖으로 나가 clip 되어 아예 안 보인다. `absoluteBoundingBox` 차이로 계산한다.
- **인스턴스를 배치하기 전에 그 컴포넌트의 컨테이너 계약을 먼저 읽는다.** ⚠️ 2026-08-18 사용자 지적
  `ResultCollection · Music List` 행은 `surface/surface` 면 + radius 8 을 가진 **독립 카드**인데
  gap 0 으로 쌓아 모서리가 부딪히고 자켓(`surface/sunken`)이 회색 기둥으로 이어졌다.
  또 팝오버를 만들면서 승인된 오버레이 계약(`DestinationPanel`: `surface/overlay` + `border/default` 1px
    - radius 10 + **`elevation/overlay-light` 그림자 스타일**)을 확인하지 않아 그림자가 통째로 빠졌다.
      **재사용 = 인스턴스만 가져오는 것이 아니라 그 컴포넌트가 놓이도록 설계된 면·간격·고도까지 맞추는 것이다.**
- **`resize()` 는 auto-layout 의 `primaryAxisSizingMode` 를 FIXED 로 되돌린다.** ⚠️ 2026-08-19 한 세션에 3회 발생
  HUG 로 두려던 축이 임의값으로 굳어 **죽은 여백**이나 **높이 0** 이 된다. 전부 §2 의
  "고정 크기는 내용에서 유도한 숫자여야 한다" 위반으로 나타난다.
  실제 사고: Checkbox 폭 200 고정(내용 66 → 죽은 여백 134, 오른쪽 정렬이 무력화됨) ·
  상세 카드 높이 1 로 붕괴 · 밴드 오버레이 높이 10 으로 붕괴.
  **규약: `resize()` 직후 HUG 여야 하는 축의 `primaryAxisSizingMode`/`layoutSizing*` 을 다시 설정한다.**
  검사: 만든 뒤 `width`/`height` 를 내용 합과 대조한다. 선언만 보면 안 걸린다.
- **FILL 자식의 폭은 형제가 다 붙은 뒤에야 확정된다.** ⚠️ 2026-08-19
  카드를 한 개씩 추가하며 `jacket.resize(w, w)` 로 1:1 을 맞추면 첫 카드는 행 전체 폭을 받아
  358, 두 번째는 173 이 되는 식으로 어긋난다. **모든 자식을 배치한 뒤 두 번째 패스에서 맞춘다.**
  이 렌더러는 `targetAspectRatio` 를 지원하지 않으므로 1:1 은 수동 유지이며,
  폭이 바뀌는 검증본(320 등)을 만들 때마다 다시 맞춰야 한다.
- **`vectorPaths` 를 다시 넣으면 노드의 bbox 원점이 재정규화된다 — `x`/`y` 를 경로 최소 좌표로 다시 잡아야 한다.** ⚠️ 2026-08-20
  경로만 갈아끼우고 좌표를 안 건드리면 도형이 통째로 밀린다. 노드는 제자리에 있고 크기도 맞아서 감사에 안 걸린다.
  실제 사고: 가중치 차트 곡선이 밀려 상수 14.5 지점이 **약 145 로 읽혔는데 실제 값은 177.7** 이었다.
  규약: 경로를 만들 때 `minX`·`minY` 를 같이 계산해 두고 `vectorPaths` 대입 직후 `node.x = minX; node.y = minY`.
- **아이콘 인스턴스는 정사각으로 둔다 — 마크의 종횡비는 컴포넌트 안에서 이미 처리돼 있다.** ⚠️ 2026-09-01 사용자 지적
  `Icon/discord` 는 **24×24 상자 안에 23.75×18 마크**가 들어 있는 구조다. 원본 SVG viewBox 가 64×48 이라는
  이유로 인스턴스를 `20×15` 로 리사이즈했더니 가로 0.833 / 세로 0.625 로 **서로 다른 배율이 걸려
  마크가 세로 75% 로 눌렸다.** 브랜드 마크를 왜곡한 것이라 `IC-06` 위반이기도 하다.
  ⚠️ 더 나쁜 것: 사용자가 지적했을 때 내가 **배율을 64×48 기준으로 계산해 「정상」 이라고 단정**했다.
  기준을 컴포넌트 상자(24×24)로 잡았어야 했다. **검사는 `width === height` 하나면 된다.**
- **새로 만든 노드는 `y`가 0이라 `y` 정렬 재배치에서 항상 맨 앞으로 간다.** ⚠️ 2026-08-30 사용자 지적 · 2회 발생
  섹션 자식을 `sort((a,b)=>a.y-b.y)` 로 흘려보내면 방금 만든 보드가 **매번 최상단**을 차지하고
  기존 보드가 아래로 밀린다. 겹침·이탈 검사는 통과하므로 눈으로만 보이고, 더 나쁘게는
  **내가 대화에서 말한 순서와 화면 순서가 반대가 된다.**
  규약: 재배치는 **`children` 순서 기준**으로 한다 — 원하는 순서대로 `appendChild` 를 다시 불러
  자식 순서를 확정한 뒤 y 를 부여한다. 보류 보드는 이름 앞에 `① ② ③` 번호를 붙여 대조 가능하게 한다.
- **`insertChild(index, node)` 로 같은 부모 안에서 순서를 바꾸면 인덱스가 이동 전 기준으로 해석된다.** ⚠️ 2026-08-20
  원하는 자리에 안 들어간다. **순서를 바꿀 때는 원하는 순서대로 전부 `appendChild` 하는 쪽이 결정적이다.**
- **`appendChild` 는 대상 서브트리의 폰트가 로드돼 있지 않으면 던진다.** 텍스트를 품은 노드를 옮기기 전에
  쓰이는 폰트 패밀리·스타일을 먼저 `loadFontAsync` 한다.
- **프레임의 `clipsContent` 가 기본 true 라 경계 밖으로 나가는 자식이 잘린다.** ⚠️ 2026-08-19 사용자 지적
  슬라이더 손잡이처럼 트랙 밖으로 튀어나오는 요소가 위아래가 잘린 채 렌더됐다.
  자동 감사는 못 잡는다 — 노드는 제자리에 있고 크기도 맞기 때문이다. **눈으로만 보인다.**
  해결: 튀어나오는 요소를 **그 요소 크기와 같은 높이의 프레임으로 감싸** 아무것도 밖으로 나가지 않게
  구조를 바꾸고, 그래도 필요하면 조상의 `clipsContent` 를 푼다.
- **`findAll` / `findAllWithCriteria` 는 INSTANCE 내부로 내려가지 않는다.** ⚠️ 2026-08-18
  로케일 프레임을 만들 때 일반 프레임의 목적지 라벨은 번역됐는데 AppHeader·SearchField·OrdinaryFooter
  **인스턴스 안의 텍스트만 한국어로 남았다.** 인스턴스 내부를 훑어야 하면 그 인스턴스를 기점으로
  다시 `findAll` 하거나 `children` 을 직접 재귀한다.
  ⚠️ 2026-08-21 재측정: `page.findAll(n=>n.type==="TEXT")` 은 `children` 수동 재귀와 **같은 3,216개**를
  돌려줬다(P4). 즉 이 렌더러에서 `findAll` 자체는 인스턴스 내부로 내려간다. 2026-08-18 누락의 원인은
  다른 데 있었을 수 있으므로, 인스턴스 내부가 걸린 작업에서는 **두 방식의 개수를 대조해 확인한다.**
- **다른 페이지를 `currentPage` 로 둔 채 `page.findAll` 을 부르면 조용히 빈 배열이 나온다.** ⚠️ 2026-08-21
  에러도 경고도 없다. 푸터 문구를 P4 에서 찾는데 0건이 나와 "저장소에도 화면에도 없다" 고 보고할 뻔했다 —
  실제로는 50곳에 있었고, `setCurrentPageAsync(p4)` 후 다시 부르니 전부 나왔다.
  `getNodeByIdAsync` 는 교차 페이지로 동작하지만 `findAll` 은 그 페이지가 로드돼 있어야 한다.
  **검색 결과가 0이면 "없다" 가 아니라 "현재 페이지가 맞는가" 를 먼저 의심한다.**
- **이름이 같은 프레임이 Light·Dark 양쪽에 있으면 `findOne(name)` 이 Dark 를 잡을 수 있다.** ⚠️ 2026-08-21
  P3 필터 레이어를 이름으로 찾아 규격을 읽고 비교판에 복제했는데 **판이 통째로 검게 나와서야** 알았다.
  그 전에 읽은 "P3 규격" 도 Dark 프레임 것이었다(다행히 Light 와 같은 값이었다).
  교차 페이지 노드를 이름으로 찾을 때는 `explicitVariableModes` 로 **실효 모드를 같이 확인한다.**

### 색 바인딩 함정 (실제로 파일을 망가뜨린 적 있음)

- 변수 바인딩 paint를 만들 때 **raw 색을 반드시 실제 값으로 채운다.**
  `{r:0,g:0,b:0}`으로 두고 `setBoundVariableForPaint`만 걸면, 바인딩 해석이 안 되는 순간
  **전부 검정으로 렌더**된다. Dark 플레이트가 통째로 검게 변한 사고의 원인.
- Dark 노드는 `explicitVariableModes`로 모드가 걸려 있다. 검사·수정 시 **노드의 실효 모드**
  (자신 또는 조상의 explicit mode)를 기준으로 값을 해석한다. Light 기준으로 비교하면
  Dark 노드가 전부 "불일치"로 오판되고, 그걸 고친다며 덮어쓰면 파일이 깨진다.
- **대량 변경(수십 개 이상) 전에는 반드시 소수로 시험하고 눈으로 확인한 뒤 확대 적용한다.**

### 시각 검증

- [ ] **1x 배율** 스크린샷 (축소본만 보고 판단 금지)
- [ ] **실제 사용 크기로 확대** (예: 20px 아이콘을 8배 확대해 잘림 확인)
- [ ] **Light + Dark 양쪽** 실제 배경 위에서 확인
- [ ] **320px reflow** (반응형 주장을 하는 화면일 때)
- [ ] **최장 실제 KO/JA/EN 콘텐츠**로 잘림 확인

### 의미 검증

- [ ] 브리프가 요구한 항목 누락 없음 (예: 난이도별 레벨 숫자)
- [ ] 도메인 용어 정확 (**◆JUST**(S-Just 아님), **기타 지역**(Global 아님), Not listed/Not published 구분)
- [ ] 색만으로 상태 전달 금지 — 형태·라벨·순서 같은 비색 단서 병행
- [ ] WCAG 대비 계산 (텍스트 4.5:1, 비텍스트 3:1)
- [ ] **결정 반영의 마지막 단계 — 보류 비우기** ⚠️ 반복해서 빼먹은 항목
      결정이 끝난 Z1 시안은 즉시 `✅ 승인 완료` 섹션으로 옮기고 이름에 결과를 적는다.
      `⏸ 보류 — 결정 대기` 에 승인·채택·확정된 것이 남아 있으면 위반.
      `audit.js` 의 `Z1_decidedLeftInPending` 이 이걸 잡는다
- [ ] **설명 문장은 dev 주석에만** ⚠️ 반복 위반 — 2026-08-14 사용자 지적
      컴포넌트 페이지의 보이는 텍스트에 규칙·근거·설명 문장을 넣지 않는다.
      보이는 스캐폴딩 라벨은 **짧은 식별 이름**만(`DARK VERIFICATION · Button`),
      설명은 그 프레임의 `annotations`(dev 주석)에 넣는다.
- [ ] **폐기안은 C 페이지에 두지 않는다** — 2026-08-14 사용자 결정
      폐기·미채택·비교 보드는 **Z1로 옮긴다.** 제품 컴포넌트 페이지(C1~C8·P1)에는
      승인된 것만 남긴다. 폐기안이 제품 페이지에 섞여 있으면 다음 세션이 승인된 패턴으로 착각한다
- [ ] **폐기안 표시** ⚠️ 할루시 원인 — 2026-08-13 사용자 지적
      결정 시안에 남기는 미채택 칸은 이름을 `폐기 · …` 또는 `미채택 · …` 로 바꾸고
      주석에 "채택되지 않았다 · 구현 근거로 쓰지 말 것 · 채택안은 X" 를 적는다.
      채택된 칸은 `채택 · …`. **표시 없는 폐기안을 남기지 않는다** — 다음 세션이 그걸
      승인된 패턴으로 착각한다. `audit.js` 는 `폐기·미채택` 조상 아래의 위반을
      `rejectedSpecimenCounts` 버킷으로 분리해 본 카운트에서 뺀다

### 검수 보고 형식 (매 빌드마다 이 형태로 보고)

컴포넌트를 만든 뒤에는 **A 레이아웃 / B 타이포 / C 색**을 항목별로 결과와 함께 적는다.
"감사 통과"·"이상 없음" 같은 뭉뚱그린 문장으로 대체하지 않는다.
안 돌린 항목은 통과가 아니라 **미실행**으로 적는다.

```
A 레이아웃  spacing 0위반 · 패딩역할 OK · 크기일관 OK · 아이콘 20/16 OK · 타겟 40×40 OK
            정렬 centerY 최대오차 0.0px · 겹침 0 · 넘침 0 · radius OK · stroke OK
B 타이포    Text Style 100% · composite 13종 내 · weight OK · tabular 위치 OK
C 색        바인딩 100%(하드코딩 0) · NI-A 위반 0 · Light/Dark 양쪽 확인
```

---

## 3. Foundation 핵심 값 (문서 24) — **이 집합 밖의 값은 만들지 않는다**

### 3.1 타이포 composite 13종 (이외 조합 금지)

| Role               | size/line-height · weight | Figures      | 용도 경계                                  |
| ------------------ | ------------------------- | ------------ | ------------------------------------------ |
| `display`          | `40/48 · 700`             | proportional | 별도 승인된 표현적 순간만. 자동 배정 금지  |
| `page-title`       | `24/32 · 700`             | proportional | 페이지/집중 태스크 정체성                  |
| `section-title`    | `20/28 · 600`             | proportional | 실제 주요 콘텐츠 경계. 카드 장식 라벨 아님 |
| `component-title`  | `16/24 · 600`             | proportional | 다이얼로그·드로어·패널·묶음 모듈 정체성    |
| `entity-title`     | `16/24 · 600`             | proportional | 일반 목록/카드 엔티티 정체성               |
| `entity-companion` | `14/20 · 400`             | proportional | 승인된 Music Detail 팝오버의 보조 정체성   |
| `body`             | `16/24 · 400`             | proportional | 일반 읽기·설명·시스템 메시지, 입력된 값    |
| `body-secondary`   | `14/20 · 400`             | proportional | 간결한 보조 맥락. 유일한 핵심 의미 금지    |
| `control`          | `14/20 · 500`             | proportional | 액션·선택·필드 라벨·내비게이션 라벨        |
| `metadata`         | `12/16 · 400`             | proportional | 진짜 3차 정보만                            |
| `metric-display`   | `32/40 · 700`             | **tabular**  | 라벨·단위·범위가 보이는 지배적 수치 1개    |
| `metric-value`     | `14/20 · 500`             | **tabular**  | 행·그룹·시각화 안의 비교 가능한 수치       |
| `emphasis-label` ⭑ | `14/20 · 600`             | proportional | StatusMessage 제목 · 선택된 세그먼트 라벨  |

⭑ **13번째 composite. 2026-08-11 사용자 승인**(R2 CONFLICT-05).
Text Style `emphasis-label/ko·ja·latin` 3개를 포함해 문서 24와 Figma에 반영 완료(총 39개).

- weight는 `400·500·600·700`만. 사용자 대면 텍스트 12px 미만 없음.
- **수치에 IBM Plex Mono 를 쓰지 않는다**(2026-08-11 결정, R2 CONFLICT-06).
  배지·개수 같은 3차 수치는 `metadata`, 비교 대상 수치는 `metric-value`(tabular).
- `page-title`은 1056px+ 12col에서 **8트랙 이상 또는 측정 인라인 폭 640px 이상이고 `reading`이 아닐 때만**
  `32/40 · 700`으로 승격 가능. 유동 보간·로케일별 크기·페이지 로컬 크기는 없다.

### 3.2 spacing 역할 (이외 값 금지)

`0` 의도적 부재 · `2` **아이콘/배지/시각화 내부 광학보정 전용** · `4` 분리불가 디테일 ·
`8` 인라인 피어 · `12` compact inset · `16` 기본 component inset · `24` subsection ·
`32` section · `48` page-region · `64` 희귀 대구획

> `2px`를 일반 레이아웃 간격으로 쓰지 않는다. `48/64`를 여백이 허전해 보인다는 이유로 쓰지 않는다.

### 3.3 나머지

- 중립 = **Adobe Spectrum S2 정확값** (Tailwind 금지)
- focus = **FI-C** Light `#000000` / Dark `#FFFFFF` · **컨트롤 자신의 1px INSIDE 경계 하나**
  (`FOCUS-1B`, 2026-08-13 사용자 결정)
    - 평상시 경계가 있으면 **그 색만** focus 색으로 교체 (invalid 가 빨강으로 바뀌는 것과 같은 방식)
    - 평상시 경계가 없으면 **포커스에서만** 1px 경계를 부여. 평상시 외형은 그대로
    - INSIDE 라서 크기·위치·주변 레이아웃이 변하지 않는다. **두께를 바꾸지 않는다**
    - 면이 진해 focus 색이 3:1 을 못 맞추면 그 면의 on-fill 색(`primary/on-primary`) 사용
    - 자켓처럼 **bleed 자식이 경계를 덮는 카드**는 최상단 절대배치 `focus-border` 오버레이로 그린다
    - ⚠️ 1px 색상 단서는 WCAG 2.2 SC 2.4.13(2px 최소)을 못 맞춘다 — 사용자가 알고 승인함
    - **포커스에 면 변화를 함께 주는 보조안은 제안했으나 미채택**(2026-08-13). focus 는 경계만 건드린다
    - **hover 는 면(fill), focus 는 경계(stroke)** — 축이 달라 동시에 표시된다. 서로 대체하지 않는다
- **NI-A**: 보편적 hover/selected 배경은 **없다**. interaction fill은
  hover / selected / menu-set / disabled **에만** 사용. 평상시(resting) 요소에 칠하지 않는다.
- 아이콘 = **Lucide** 실제 geometry (`node_modules/lucide-react` 1.24.0에서 추출, 손으로 그리지 않음)
    - 렌더 20px(일반 액션) / 16px(라벨 옆 보조) / 24px(강조)
    - **아이콘 전용 컨트롤 타겟: 모바일 44×44, 데스크톱 40×40**
- 도메인 색: DU-01 난이도 4 / JD-02 판정 5(FAST/SLOW 아님) / LD-03 SAP / FS-BN 피드백
- **눌림(pressed)** = `transform: scale(0.98)` + `100ms`(MO-02 Pressed feedback 슬롯) · 상수 `motion/press-scale`
  (2026-08-13 승인). transform 이라 **박스 크기·레이아웃 불변** — 축소값을 레이아웃 크기로 만들지 않는다.
  작은 컨트롤(버튼·아이콘 버튼·페이지 번호)만. 리스트 행·그리드 카드에는 쓰지 않는다.
  `prefers-reduced-motion` 에서는 제거되고 Neutral·Ghost 는 색 단계가 남는다(Primary·Destructive 는 변화 없음).
  ⚠️ Primary·Destructive 의 pressed **색** 차이는 1.13~1.26:1 로 비가시 — 승인 램프에 여유가 없어서이며
  `primary/pressed`·`feedback/destructive-border-pressed` 는 reduced-motion 잔여 단서로만 존재한다.
- **`border/empty-slot`** ⭑ 신규 별칭 토큰(2026-08-13 승인, CONFLICT-17) — 아트 없는 자켓 슬롯의 1px 경계.
  Light = `surface/sunken` 별칭(자켓 fill 과 동일 → 비가시) / Dark = `border/subtle` 별칭(#393939).
  Dark 에서 `surface/sunken` = `surface/canvas`(#111111) 이라 슬롯이 사라지는 문제 해결. 이미지 fill 이
  들어오면 stroke 를 제거한다. 문서 24와 Figma에 반영 완료.
- grid: Compact <672(4col g12 m16) / Intermediate 672–1055(8col g16 m24) / Wide 1056+(12col g16 m32)
- container: reading 768 / standard 1280 / wide 1440 / workspace fluid

---

## 4. 보존 경계 (절대)

- **채보 viewer/editor 전체 잠금**(문서 07) — 재설계·recolor·Foundation 적용 금지, 진입 링크만
- **관리자 `/admin/*`** 광범위 재설계 없음
- **user chart-contribution 흐름** 신규 생성 금지
- **레거시 NOSTORY Figma**를 현재 권위로 사용 금지
- **문서 63 regression harness**를 최종 구성으로 복사 금지
- **개인정보 release blocker**(문서 18)를 해결된 UI로 위장 금지
- PDF의 GitHub Primer 편집 색은 **제품 색이 아님**

---

## 5. 현재 작업 환경

- Figma 파일: `NosLog v2.0.0` (`cVbWCxhkfxFfHmAKLCyKrD`)
- **Text Style 48개** = 13 composite × ko/ja/latin(39) + `nav-fit`·`nav-fit-current`(6)
    - **`page-title-promoted/ko·ja·latin`(3, 2026-08-19 신설)**.
      `page-title-promoted` 는 32/40 · 700 · proportional 이며 **문서 24 에 이미 승인돼 있던 승격 규칙**
      ("page-title may step to 32/40")을 쓰기 위한 구현이다. 문서 24 개정은 필요 없다.
      적용 조건 3개를 모두 만족할 때만 쓴다 — ① 12열 컨테이너 1056px 이상 ② 구역이 8트랙 이상이거나
      측정 인라인 폭 640px 이상 ③ `reading` 조성이 아님. **뷰포트가 넓다는 이유만으로 쓰지 않는다.**
      이름에 `wide` 를 쓰지 않은 이유도 이것이다. `metric-display` 가 같은 32/40 이지만 tabular 라 제목에 쓰면 역할 오용.
      ⚠️ Home Wide 의 검색 구역 **640 은 승격 임계값 640 에 정확히 걸쳐 있다.** 이 폭을 줄이면 승격이 무효가 되어
      `page-title` 24/32 로 되돌려야 한다.
- **PC 전용 타입 스케일은 만들지 않는다.** 문서 24 가 "There is no fluid interpolation, locale-specific size,
  or page-local title size" 로 닫았다. 13 composite 는 모든 폭에서 같은 크기이며, 승인된 폭 기반 변화는
  위 `page-title` 승격 하나뿐이다. 데스크톱에서 커 보이게 하려면 폰트가 아니라 문서 03 이 지목한 레버
  (`gaps · padding · label wrapping · shared block height`)를 쓴다.
- **폰트**: 규범값은 `Pretendard JP Variable`이나 이 Figma 렌더러가 로드 불가
  → 현재 렌더는 `IBM Plex Sans KR/JP/Latin`. 수치는 문서 24의 proportional/tabular
  figures 계약을 따르며 `IBM Plex Mono`를 사용하지 않는다
  → 교체는 `font/family/ko·ja·latin` 변수 3개만 바꾸면 됨 (교체 후 레이아웃 재검수 필요)
  → **2026-09-04 결정: Figma 파일은 IBM Plex 로 마감한다.** 제 런타임은 로컬 폰트를 못 보고
  Pretendard 는 Figma 공유 폰트(Professional)로만 올릴 수 있는데 무료 플랜 유지를 택했다.
  **Pretendard 검증은 구현 단계에서 실제 브라우저로** — 폰트 폭에 걸린 판정 목록은 문서 84 §8
- 상태 파일 위치: **`~/.claude/projects/-Users-carol-Desktop-project-noslog/design-state/`**
  (`state.json` · `R2-conflicts.md` · `audit.js` · `brief-contracts-notes.md`)
  ⚠️ **세션 스크래치패드에 쓰지 말 것** — 2026-08-12 에 작업 중 통째로 삭제됐다.
  트랜스크립트 `.jsonl` 재생으로 복구했으나 같은 사고를 반복하지 않도록 위 영구 경로만 쓴다.

---

## 6. 사용자 결정 기록

| 항목                          | 결정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 일자       |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| focus indicator               | **단일 1px INSIDE 경계**(`FOCUS-1B`) · 평상시 경계 있으면 색만 교체 / 없으면 포커스에서만 부여 · 두께·크기 불변. 문서 22·24·25·63 개정 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-13 |
| 필터 적용 표시                | **배경 없음** + 배지 + `border/strong`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 2026-08-11 |
| 뷰모드 선택 표시              | **surface/raised 세그먼트**(sunken 트랙 위)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-11 |
| 대체 폰트                     | IBM Plex 사용, 완성 후 사용자가 Pretendard로 교체                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-08-11 |
| 강조 라벨                     | `emphasis-label 14/20 · 600` **신규 composite** (§3.1 ⭑)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-11 |
| 수치 표기                     | **mono 폐기** — 배지=`metadata` / 비교수치=`metric-value`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-11 |
| Text Style                    | C1~C5 **소급 적용 완료**(222 노드). 앞으로 raw 지정 금지                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-11 |
| 스캐폴딩 라벨                 | 문서 장치 라벨도 `metadata`(12/16·400) 준수                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-11 |
| 판정 바 radius                | JudgementMarker 28×10 바 = `radius/full`(알약)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-08-11 |
| C3·C4 Dark                    | DARK VERIFICATION 플레이트 추가 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-11 |
| 선택-채보 마커                | **D안** — 이름+레벨 묶음 폭에 맞춘 상단 액센트 바(높이 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-12 |
| `View chart` 라벨             | **`채보 보기`** (짝 라벨 `플레이 영상`은 `05:223` 기승인)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-12 |
| Dark 빈 자켓 슬롯             | **해결** — 1px `border/empty-slot`(§3.3 ⭑). Light 비가시·Dark 만 표시                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-08-13 |
| 상세 자켓 크기                | **96×96** (리스트 64 / 그리드 173 과 별개)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2026-08-12 |
| 헤더 난이도 반복              | **제거** — 바로 아래 DifficultySelector 가 4개 전부 표시                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-12 |
| Real 없는 곡                  | **칸 제거** — 3개로 채움 (비활성 잔여 칸 두지 않음)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-08-12 |
| Neutral 버튼 경계             | **`border/strong`** (CONFLICT-18 A) — 3:1 통과 4.88/5.47. 문서 24 NB-A 준수 수정이라 개정 불필요                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-13 |
| MetricSummary 타이포          | **A** (CONFLICT-24) — label `control` / dominant `metric-display` / grouped `metric-value` / unit·비교 `body-secondary`. 기존 빌드와 동일                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-13 |
| 선택 세그먼트                 | **Approved** — `ViewModeSwitch`와 `DifficultySelector`의 선택 세그먼트만 지속적인 비색상 단서로 1px inside `border/strong` 경계를 사용한다. 다른 세그먼트 계열로 자동 확장하지 않는다. 문서 24의 명시적 예외와 동일                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-08-14 |
| 로케일 Text Style             | **측정 전제다** — `*/ko·ja·latin`은 폰트 패밀리 스위치이므로 내용의 문자 체계와 반드시 일치. 일본어에 `*/ko`가 걸리면 한자 폭이 절반으로 오산돼(`楽曲` 14px vs 28px) fit 판정이 통째로 틀린다. 파일 전체 268곳 정정 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-14 |
| 내비 라벨 fit 스타일          | `12/16·500`은 13 composite에 없으므로 **전용 스타일** `nav-fit/ko·ja·latin`(500) · `nav-fit-current/ko·ja·latin`(600) 6개로 처리. **2026-08-18 `HOME-21`로 Home 목적지 타일까지 적용 범위 확대** — 그 밖의 화면으로 자동 확장하지 않는다. raw 값 금지 — 폰트 변수 재지정 시 누락된다. 320px fit 대상은 **4개**(데이터 동기화·譜面ビューア·ご意見・報告·Chart Viewer), `店舗`·`Feedback`은 14/20 복귀                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-14 |
| 패턴 레이더 대비              | **Q1-A** 패널 면을 `surface/surface` 로 (계열 대비 Light 3.12 / Dark 3.74 → 3:1 통과. sunken 면은 2.73 미달) · **Q2-A** 격자·축선 `border/default` (격자는 0–4 를 세는 근거라 장식 아님) · 계열 반투명 채움 **미제작**(승인 불투명도 없음 → 문서 24 개정 사안) · 1단위 = 격자 한 겹 24px                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-13 |
| 서열 투표 분포                | **관측값만 렌더**(0표 값은 칸 없음 · 0.5 밴드 금지) · 높이는 **범위 전체 최대 표수** 기준 · 창은 **평균 중심**, 관측값 한 칸씩 이동 · 관측값 ≤ 창이면 컨트롤 비활성 · 6범위는 압축 행(**C안**)로 나열하고 선택 범위만 펼침                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2026-08-13 |
| 분포 막대 색                  | **B안** — 최고 표수 `local-data/bucket-6` 단독, 나머지는 `bucket-3 → bucket-2` 를 **위에서부터**(2등은 항상 bucket-3). 최고값 vs 2등 Light 2.23 / Dark 2.54. 램프 총 폭이 3.31:1 이라 4단 순위를 색으로 표현하는 건 불가능 — 색은 최고값 강조만, 순위는 높이+표수 숫자. 회색 금지(비활성 의미). 평균은 헤더에만 적고 막대 아래 밑줄 금지                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-13 |
| 미평가 표현                   | **B안** — 선택지는 `0`–`4` 다섯 개뿐이고 미평가는 **선택 없음**. 값이 있을 때만 축 헤더 오른쪽에 `선택 해제` 액션이 나타난다. `미평가`를 여섯 번째 선택지로 넣는 A안은 **기각**(척도 안에 데이터 없음 상태를 섞는다). 축 행 = 12 + 헤더 44 + 12 + 선택지 48 + 16 = **132**, 선택해도 불변                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-08-15 |
| 서열 투표 값 컨트롤           | **A안 단일 드롭다운** — 1.0–14.5 · 0.1 단위 136개를 한 목록에서. 두 컨트롤로 쪼개는 B안은 한쪽만 고른 중간 상태 때문에, 스텝퍼 C안은 1.0→13.2 가 122탭이라 기각. 값 → 범위 도움말 12 → 액션 12 · 오류 시 도움말 → 오류 패널 12 → 액션 16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-15 |
| 패턴 평가 숫자 선택           | `0`–`4` 선택 버튼에서 라디오 원과 내부 점을 제거하고 **숫자만 중앙 배치**. 선택은 기존 면·텍스트·1px 경계 상태로, 미선택은 subdued 상태로 구분한다. 일반 Radio 컴포넌트에는 적용하지 않는다.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-15 |
| Music Detail 교정             | 헤더 `시험`→`검정` · 패턴 성향 레이더는 `채보 정보` 제목 바로 아래·BPM 위로 이동 · 성장 추이는 그래프와 날짜–베스트 스코어 표만 유지 · 최근 플레이와 랭킹 등급은 자체 호스팅 공식 이미지를 고정 열로 점수 왼쪽에 배치 · 랭킹 우측 열은 FC 전용(`P` 중복 금지) · `점수 분포`를 최상단으로 이동 · 베스트 기록의 S/FC/전체 인원/순위 비교 문구 제거                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-15 |
| 긴 제목 맞추기                | **A안** — 예산(자켓 96) 안에 들어오는 **가장 큰** composite 선택: `page-title 24/32` → `section-title 20/28` → `component-title 16/24`. 아티스트는 `body-secondary` 고정. 폭 확장안은 번역 트리거 44 때문에 총합이 늘어 기각. **문서 24 개정 필요**(역할 확장)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-08-13 |
| 색 정리의 의미                | **시각적 변화 없음** — 2026-08-16 Z 보드의 Core UI/Domain 구분은 **picker 노출과 역할 분류를 정리하는 정보 구조**이지 색상값 변경안이 아니다. 향후 정리는 ①동일값 alias 병합 ②노출 역할 수 단순화 ③Core/Domain 분류 정리 ④**resolved color 를 완전히 보존하는 재바인딩** 으로 한정한다. 변수 삭제·이름 변경·재바인딩을 하더라도 기존 화면의 실제 색이 달라지면 위반이다. Z 보드의 Dark high-emphasis 램프 반전안(`#FFFFFF → #F2F2F2 → #DBDBDB`)은 **버튼 변경 보류·버전 복원 결정으로 superseded** 됐다. `primary/*` Dark 는 현재 값 `Default #DBDBDB / Hover #F2F2F2 / Pressed #FFFFFF` 를 그대로 유지한다. 버튼 시스템 전체 변경은 별도 보류이며 Home 에 적용하지 않는다. **Home 을 나중 색 이관으로 색이 바뀔 대상으로 취급하지 않는다**                                                                                                                                                                                                                                                                                                           | 2026-08-18 |
| Home 타일 라벨 fit            | **여백 12 · 문서 15 순서** — 타일 안쪽 여백 `12`(라벨 폭 64). 라벨은 `control 14/20` 에서 시작해 ① 측정 셀에 안 들어가면 `nav-fit 12/16` ② 그래도 안 되면 승인된 축약 문구 ③ 두 줄은 최후. 셀이 허용하면 `14/20` 복귀(320px 부근에서만 12/16 이 보인다). 블록의 폭·높이·면은 8개 모두 동일하고 글자 크기만 달라진다. 새 축약 문구 2개: `데이터 동기화`→**`동기화`** · `譜面ビューア`→**`譜面`**(`データ連携`·`채보 뷰어`는 유지). 두 줄 wrap 안과 여백 8 안은 **기각**. 문서 03 `HOME-21` · 03:306 · 03:553 · 문서 15:423 · SHELL-29 개정 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-08-18 |
| Home 편집·공지 영역           | **중대 공지 = C4 StatusMessage(`100:41`) 그대로** — 링크·액션 슬롯을 추가하지 않는다(C4 확장안 기각: 다른 화면 전체 영향 · 문서 03 이 링크를 요구하지 않음). 공지 없으면 컨테이너·여백 없음 · **공식 뉴스 = 공식 X 링크를 제목 행 오른쪽에 상시 노출**, 정상·소식없음·로드실패에서 임베드만 사라지고 영역 구성은 불변(실패 시에만 링크 노출하는 A안, StatusMessage 로 대체하는 C안 기각). 임베드 높이는 X 소유라 고정값을 만들지 않는다 · **Wide 편집영역 = 8 : 4** (공지 805 / 공식 소식 395 · 1열 86.67 · 좌우 합 1216). 7:5 는 공지 우위가 약해져, 9:3 은 임베드 292 로 실용 최소폭에 근접해 기각                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-18 |
| Home 타일 면·프리뷰           | **타일 면 = `surface/surface` + `radius/container 8`** (A안) — 근거: C6 ResultCollection 카드와 같은 언어 · 문서 03 이 금지한 건 content-heavy card 이지 면이 아님 · 모바일에서 탭 영역이 보이는 편이 유리. 경계만(B) 은 Dark 외곽선 조항과 충돌, 면·경계 없음(C) 은 탭 영역 불가시로 기각. **Dark 에서 재확인 예정** · **키보드 제약 = 스크롤 없이 동적 행 수** — 포커스 시 화면을 스크롤시키지 않고(문서 03 의 흐름 불변 조항), 표시 행 수만 가용 높이에서 유도하며 남은 매치가 있으면 핸드오프를 항상 유지. 390×844 + 키보드 336(검증값) 최악이 **2행 + 핸드오프**. 필드를 상단으로 올리는 안은 문서에 없는 새 동작이라 기각                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-18 |
| 320px·로케일 검증             | **통과** — 320 KO/JA/EN 프레임 이탈 0 · 텍스트 넘침 0 · 타일 높이 72 고정. fit 캐스케이드가 설계대로 작동(320 JA `ランキング`·`データ連携`, 320 EN `Data Sync` 만 `nav-fit 12/16`, 나머지 전부 `control 14/20`). WCAG 대비 Light 365건·Dark 365건 전수 계산 **실패 0**(최저 Light 6.64 / Dark 7.25). 검증 중 **공유 컴포넌트 결함 2건 발견·수정**: ① `OrdinaryFooter` Compact 의 `service-notice` 가 폭 358 FIXED 라 320 에서 54px 이탈 → FILL+HEIGHT (Wide 는 HUG 유지) ② `SearchField` 값·placeholder 가 줄바꿈돼 필드 높이가 늘어남 → 한 줄 + 말줄임 ENDING. **placeholder 결정**: 320 에서 JA `楽曲名・アーティストを検索`(208/163) 와 EN `Search songs or artists`(164/153) 가 말줄임되는 것을 **수용**하고 현재 제품 문자열을 유지한다. 한국어는 156/166 으로 320 에서도 보이고 390 이상에서는 세 로케일 모두 온전히 보인다. 짧은 문구 안은 일본어 후보가 160/163 으로 여유 3px 뿐이라 스코프 변경·Pretendard 교체 시 재검증 부담이 크고 `を検索`·`Search` 가 빠져 동작 설명이 약해져 기각                                                      | 2026-08-18 |
| Home Wide 1280                | **A안 채택** — 컨테이너 1280 · 12열 · gutter 16 · 여백 32 · 1열 86.67 · 정체성+검색 **640 중앙 bounded** · 목적지 **12열 전체 1216 · 4×2 · 타일 292×96**(inset 24 = `24+20+8+20+24`, compact 은 inset 12 로 111×72) · 편집영역 **8:4 = 805/395** · 헤더는 C8 셸 조립(`596:1792`) 선례대로 인스턴스를 1280 으로 늘리고 **패딩 16/8 유지**(32 로 바꾸지 않는다) · 푸터 `Layout=Wide`. 기각: 검색·목적지를 같은 좁은 폭으로 묶는 안(데스크톱 폭 활용 부족), 타일 72 유지안(292×72 = 4:1 로 납작). **`NosLog` 는 `page-title` 승격 32/40 적용** — 셋을 함께 넣어야 타일 확대와 검색 우위가 서로 상쇄된다                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-19 |
| 중간 폭·1440 셸               | **편집영역은 Wide(1056+)에서만 나란히** — Intermediate(672–1055)에서는 소스 순서대로 세로로 쌓는다. 임계값을 문서 24 의 Intermediate/Wide 경계와 일치시키고 편집영역 전용 숫자를 만들지 않는다. 8열 5:3 안은 768 에서 공식 소식이 260 이 되어 292 를 좁다고 기각했던 판단과 모순되므로 기각 · 중간 폭에서 목적지는 **4×2 유지**(영역 720/976 이 448 임계값 초과), 타일 inset 은 compact 과 같은 **12**(24 는 Wide 전용), `page-title` 승격은 12열 1056px 조건 미달이라 **자동 해제**되어 24/32 로 복귀 · **컨테이너 상한 초과 폭(1440)에서는 헤더·푸터가 뷰포트 전체, 본문만 1280 중앙**(문서 15 `SHELL-34`). 헤더는 자기 패딩 16/8 을 유지하므로 좌측 키라인이 96 어긋나며 이는 의도된 것. 페이지 전체를 1280 으로 캡하는 안은 스티키 헤더가 잘린 띠가 되어 기각. **이 셸 규칙은 모든 페이지 패밀리에 적용된다**                                                                                                                                                                                                                                     | 2026-08-19 |
| Home 문구 인벤토리            | 화면 텍스트를 **두 종류로 나눠 다룬다.** ① **대표 데이터**(공지 3건 제목·날짜, 중대 공지 본문) — 런타임에 DB 레코드로 대체되는 자리라 승인 대상이 아니다. 노드 이름에 `(문구 미승인)` 이 아니라 대표 데이터로 표시한다 ② **제품 UI 문구** — 한국어는 확정. 저장소에 이미 3로케일이 있는 것은 재사용한다(`common.retry` 다시 시도/再試行/Try Again · `home.announcements` · `home.officialNews` · `home.officialX`). **신규 키 7개**: `전체 공지` · `검색 중…` · `일치하는 악곡이 없습니다.` · `검색 결과를 불러오지 못했습니다.` · `전체 결과 N개 보기` · `표시할 공식 소식이 아직 없습니다.` · `공식 소식을 불러오지 못했습니다.` · **공식 소식 부재와 로드 실패는 서로 다른 문구로 구분한다**(문서 03 이 별도 상태로 승인). `home.officialXFallback` 재사용안은 제목 옆 공식 X 링크와 중복되어 기각 · **ja/en 은 문서 03 Phase Approval 이 localized production copy 를 downstream 으로 미뤄둔 항목**이라 handoff 로 넘긴다                                                                                                                         | 2026-08-19 |
| Discovery 결과 뷰             | **List 는 폭과 무관하게 1열** — 카드가 결과 영역 전체를 채운다(`DISC-42` 신설, 문서 04 의 2열 허용 개정). 근거: 목록의 통상 동작이고 제목~부가정보 사이 여백은 정상 성질 · List(한 행 한 곡)와 Grid(자켓 중심 다열)의 역할이 깔끔히 갈림 · 2열을 없애면 카드당 440–460 임계값과 레일 활성 시 2열→1열 불연속이 통째로 사라짐. 1440 에서 행이 약 1216 까지 늘어나는 것은 받아들인다 · **Grid 는 컨테이너 용량 규칙 유지**(2열 288–535 · 3열 536+ · 4열 720+ · 5열 904+ 상한 · 새 열은 `n×168+(n−1)×16` 에서 열림 · 2열 하한 288 은 compact gutter 12 로 `2×138+12`). 실측으로 전부 검증 · **C6 반응형 결함 2건 수정**: Music Grid jacket `FIXED 173` → `FILL`+1:1 잠금(자켓 안 절대배치 자식 제약도 함께 — Icon `CENTER/CENTER` · boundary-bottom `STRETCH/MAX`), Chart grouped `target/*` → `FILL`. 기존 인스턴스 13개는 원래 폭이라 픽셀 변화 0, P1 은 세 결과 컴포넌트를 쓰지 않음                                                                                                                                                                   | 2026-08-19 |
| Discovery 필터 레이어         | **레벨 범위 = 두 손잡이 슬라이더 · 손잡이 24** — 현재 제품과 같은 구조를 유지하되 손잡이를 16→24 로 키워 WCAG 2.2 타겟 최소를 충족한다(문서 04 가 필터 컨트롤에 명시). 트랙 4 · step 1 · **선택한 난이도마다 한 벌**. 상한은 난이도별 실데이터 **Normal 8 · Hard 11 · Expert 12 · Real 3** — 하나의 공용 범위로 묶지 않는다. 손잡이 지름 24 때문에 손잡이 중심 이동 구간은 트랙 양끝에서 12 씩 들어온다. 최소·최대 드롭다운(범위 감각 상실)과 레벨 칩(집합 선택이라 inclusive range 정의와 불일치)은 기각 · **카테고리는 6개 전부 노출**하고 순서는 `lib/musicCategories.ts` 정의(`pops · anime · BM · Org · Var · Cl/Jz`)를 따른다. `pops` 1건 · `anime` 2건도 **실제 데이터이며 임포트 오류가 아니다**(2026-08-19 사용자 확인) · 레이어 구성: Sort 를 맨 위 · 스테이징 · 스티키 하나가 커밋+닫기 · 서명 안 된 상태에서 개인 기록 그룹은 통째로 생략 · 최신 악곡 정렬은 발매일 게이트 미충족으로 생략                                                                                                                                                | 2026-08-19 |
| C5 컨트롤 radius              | **6 → 4 · `radius/control` 바인딩** — `FilterSortControl` 4 variant · `ViewModeSwitch` 2 variant 의 컨트롤 모서리 10곳. 문서 24 승인 radius 는 `control 4 · container 8 · overlay 10 · full 50%` 이고 6 은 집합 밖이었다. 파일에서 6 을 쓰던 건 이 둘뿐(Button · SearchField · ContentScopeSwitch · FormField · StatusMessage 는 전부 4). **결정 이유는 시각이 아니라 6 이 어떤 변수에도 연결되지 않은 하드코딩이라 radius 스케일 조정 시 이 둘만 안 따라온다는 것** — 실제 크기 시각 차이는 거의 없다. 인스턴스 25개 회귀 확인, 잔여 0. Discovery 조립에서 처음 드러났다 — 그전까지 감사 범위가 C5 를 포함하지 않았다                                                                                                                                                                                                                                                                                                                                                                                                                                | 2026-08-19 |
| 필터 그룹 OR 안내             | **노출하지 않음**(C안 · `DISC-43`) — 필터 레이어·레일에 그룹 선택이 OR 임을 설명하는 상시 문장을 두지 않는다. 그룹 제목·복수 선택 컨트롤·스테이징 결과 개수가 그 의미를 전달한다. 저장소의 `music.filter.anyMatch`(ko/ja/en 실재)를 이 화면에 배치하지 않는다 — 현재 제품에는 렌더되고 있으므로 안내 하나를 없애는 변경이며 사용자가 알고 결정했다. 앞서 내가 넣었던 `복수 선택` 라벨은 애초에 승인 문구가 아니라 제거했다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2026-08-19 |
| 느린 교체 약화                | **`content/pending` 신설**(`DISC-44` · 문서 24 `F-A` 개정) — Light `#717171` / Dark `#8A8A8A` = `neutral/717171`·`neutral/8A8A8A` 별칭, **신규 원시값 0**(`border/strong` 과 같은 램프 단계) · `scopes` `TEXT_FILL`·`SHAPE_FILL`. 계약: 요청이 진행 중이라 **일시적으로 보류된 유효한** 콘텐츠에만. 낮은 중요도는 `subdued`, 사용 불가는 `disabled`. **`sunken` 면 위 텍스트 금지**(Light 4.02 미달). 색이 단독 단서가 되지 않으며 진행 표시·`aria-busy`·활성화 차단과 함께 간다. **opacity 안은 전부 기각** — 문서 24 에 콘텐츠 opacity 개념이 없고(그림자 alpha 뿐), 문서 04 의 유일한 opacity 언급이 부정 맥락이며, 레퍼런스 처방 없고, 코드베이스 사용 0건. 면 강등안도 `surface` 대 `canvas` 가 Light 1.06 / Dark 1.10 로 **비가시**라 기각. 대비 surface 위 4.60/4.99 · canvas 위 4.88/5.47이고 램프의 다음 단계는 3.25/3.53 이라 **4.5:1 을 지키는 가장 옅은 값**이다. Discovery 밖 다른 페이지에서도 같은 상태에 재사용한다                                                                                                                   | 2026-08-19 |
| Tier 자켓 점수 표기           | **A′안 · `surface/media-scrim` + `content/on-media` 신설**(문서 24 `M-A`·`F-A` 개정) — 자켓 하단 반투명 밴드 위 흰 점수. 알파는 **양쪽 모드 60%**: 순백 아트에서도 흰 전경이 5.74:1 로 통과하는 값이고 계산상 최소는 55%, 기존 40% 는 Light 에서 2.85(밝은 아트)·3.37(빈 슬롯) 로 미달이라 폐기. **60% 는 이미 `surface/scrim` 의 Dark 값이라 신규 원시값 0**. `content/on-media` 는 밴드가 양쪽 모드에서 어두워 흰색 고정 — `primary/on-primary` 는 Dark 에서 `#111111` 로 뒤집히고 `content/default` 는 Dark 에서 밝은 아트 위 4.10 미달이라 재사용 불가. **그라디언트 금지**(문서 24 에 개념 없음·유일 사용처가 잠금 대상 뷰어). 밴드 높이는 내용 유도 **36 = 20 + 8 + 8**. 반투명이 금지된 것은 콘텐츠 약화용 opacity 이며 이미지 위 가독성 바닥은 별개 역할이다                                                                                                                                                                                                                                                                                  | 2026-08-19 |
| Tier 계산 안내 구성           | **펼침 = C안** — 목록 설명은 펼침에서 빼고(상단에 이미 보임) `필터 해석 → 업데이트 → 가중치 차트` 순서. 차트가 목표에 따라 붙었다 떨어졌다 하는 264 짜리 최대 블록이라 꼬리에 두면 여섯 조합 전부에서 본문 상단이 고정된다 · **차트는 서열표마다 노출** — 곡선 모양은 상수² 로 같지만 정규화 분모가 그 표의 상위 70곡 합이라 y 눈금이 표마다 다르다 · 차트 형태·문구는 현재 제품 `TierRatingWeightChart` 와 `tiers.weight.*` 10키를 그대로 옮긴 것이라 결정 대상 아님 · 패널 `surface/surface`+radius 8 · 격자 `border/default`(패턴 레이더 Q1-A·Q2-A 와 동일) · 계열 `local-data/single` · **y축 라벨 필수** — 없으면 격자가 유일한 크기 단서가 되는데 `border/default` 는 1.61/1.77 로 3:1 미달. 라벨이 있으면 수치는 7.59/7.85 텍스트가 전달                                                                                                                                                                                                                                                                                                       | 2026-08-20 |
| NosLog 레이팅 범위            | ⚠️ **Superseded 2026-08-21** — 아래 「레이팅 출처」 행으로 대체. (원안: 서열표 6개가 각자 레이팅을 정의)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-20 |
| 레이팅 출처                   | **모드별 Pianist 서열표 1개**(`TIER-29`) — 공식 서열 상수는 각 모드의 Pianist 표에서만 나오고 레이팅은 **Basic Pianist · Recital Pianist 두 개**다. 서열표가 6개이고 각자 구간을 갖는 사실은 불변이며 바뀌는 건 출처뿐. 다른 표를 볼 때도 기여값은 그 모드의 Pianist 상수로 계산한다(v-archive 방식). 근거: ① 문서 08 의 메트릭 스위치가 `공식 Grd \| NosLog 레이팅` 두 개라 레이팅이 6개면 한 랭킹으로 줄 세울 수 없다 ② `rankings.ratingBasis` 와 `BASIC_RATING_TIER_GOAL='pianist'` 가 이미 이 모델을 전제 ③ 비-Pianist 표에서 Pianist 곡선을 보여주던 오해가 해소된다. 파생: **가중치 차트는 Pianist 표에만 노출**하고 S·FC 표는 목표 요건 문장으로 끝냄 · 차트 하단은 항상 점수별 반영 비율표 · **Pianist 표에 없는 채보는 레이팅 기여 행 생략** · 문서 08 `RANK-03`·`RANK-04` → `Superseded`, `RANK-21` 신설로 **두 모드 모두 메트릭 스위치 노출**                                                                                                                                                                                              | 2026-08-21 |
| 계산 안내 본문 인셋           | **인셋 없음(flush)** — 본문 좌측 기준선 = 트리거 좌측. 기존 `좌 32 / 우 0` 은 C4 `Disclosure` 본문 계약을 그대로 쓴 결과인데 그 인셋은 셰브론 아래로 텍스트를 맞추려던 것이라 전체폭 패널에 안 맞고 좌우 비대칭이었다. 검증된 선례: Bootstrap 5.3 은 `$accordion-button-padding-x` 와 `$accordion-body-padding-x` 가 동일, Carbon 은 `indented`/`flush` 두 변이 모두 제목과 본문의 좌측 기준선이 같다(그 indent 는 컴포넌트 전체가 그리드에 맞춰 들어가는 것). USWDS·GOV.UK·Spectrum·MUI 는 **미확인**. 플롯 폭 252 → 284 · **이 안내는 Disclosure 인스턴스가 아니라 계약 참고 조성으로 취급**하며 컴포넌트 자체는 안 고쳤다(다른 사용처 영향)                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-08-20 |
| Tier 페이지 배치              | **설명·안내를 모드·목표 뒤로**(`TIER-24`) — `제목 → 모드 → 목표 → 목록 설명 → 계산 안내 → 밴드 → 필터+뷰 → 요약 → 결과`. 근거: 안내 제목 `{goal} 서열표 안내`·목록 설명·차트의 `Basic {goal} · 1곡 기준` 이 전부 모드·목표에 종속되어 선택 전에 설명하면 아직 하지 않은 선택을 설명하게 된다 · 문서 06 자체가 "guide secondary to choosing a mode, goal, band, and chart" 라고 한다 · 현재 제품이 `TierControls` 뒤에 `details` 를 둔다 · 펼치면 모드·목표가 화면 밖으로 밀린다. **아코디언의 페이지 내 위치를 규정한 디자인 시스템 레퍼런스는 확인되지 않았다** — Carbon·Primer·Fluent·USWDS 는 내부 구조와 키보드 동작만 정의한다. Wide 는 레일 안에서 같은 순서를 유지(문서 06 의 소스 순서 조항) · 문서 06 계층 목록 개정 완료                                                                                                                                                                                                                                                                                                                    | 2026-08-20 |
| Tier 신규 문구                | `tiers.detailedView` = `상세 보기` / `詳細表示` / `Detailed view` (`music.listView`·`music.gridView` 어순 계승) · `tiers.loadError` = `서열 데이터를 불러오지 못했습니다.` / `難易度データを読み込めませんでした。` / `Could not load the tier data.` (`tiers.loading` 의 짝. 재시도 버튼 `tiers.retry` 가 옆에 있으므로 문장에 "다시 시도"를 중복하지 않는다) · **목록 설명 ja/en 은 문구 결정 사안이 아니라 데이터 모델 결함** — `TierList.description` 이 단일 `String?` 이라 로케일 변형 자체가 없다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-20 |
| Tier 밴드 모델                | **다중 선택 + sticky 헤더 스택**(`TIER-26`~`TIER-28`) — 구간은 1.0–14.5 · 0.1 단위 **136개**이고 채보 2,159개가 그 위에 배치된다(구간당 평균 16). 기존 레일은 6줄만 그려 구간 수를 담지 못했다. ① 내비 = 스크롤 다중 선택(행 44 · 밴드 값은 Checkbox 자체 라벨 · 구간별 개수 표시) ② 결과 = 선택 구간을 발행 순서로 이어 붙이고 각 구간 위에 sticky 헤더(`section-title` 값 + `metric-value` 달성 수 · `surface/canvas` · 하단 1px `border/default`) ③ 기본 = 커밋된 필터에 맞는 구간 전부. 문서 06 `TIER-05` → `Superseded`, "한 문서로 쌓기" Superseded 항목은 **복권**. 실측: 필터 없음 2,159채보 = 약 112,000px(폰 133장) · Expert 578 = 28,400px · Expert 12 = 169 = 9,300px → 점진 로딩과 스크롤 복원이 선택이 아니라 필수                                                                                                                                                                                                                                                                                                                      | 2026-08-20 |
| 밴드 체크 도착 상태           | **체크 0개 = 구간 제약 없음**(b안) — 체크 1개 이상이면 그 구간만, 마지막 체크를 해제하면 빈 결과가 아니라 전체로 복귀. 구간 선택은 난이도·레벨 필터와 AND. 근거: ① 같은 페이지의 난이도·레벨 필터가 이미 "쿼리가 비면 제약 없음" ② 좁히기가 1단계(a안은 130개를 끌 수 없어 결국 "전체 해제 후 재선택" 2단계) ③ URL 기본 상태가 비어 있으면 전체라 문서 06 의 shareable committed conditions 와 Back 복원에 그대로 맞는다(a안은 136개 값이나 전체 토큰이 필요). ⚠️ `전체 표시`·`범위로 선택`·`전체 해제` 는 **승인 문구 아님** — 신규 키 필요                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-20 |
| 그리드 부분 행                | **카드는 열 폭 고정 · 짧은 행은 왼쪽 정렬** — 카드가 `FILL` 이면 행에서 카드를 지웠을 때 남은 카드가 행 전체로 늘어나 자켓이 1:1 을 잃는다(Z1 판에서 358×111 발생). 스택 모델은 밴드마다 부분 행이 생기므로 상시 발생 조건이다. 열 폭은 행에 몇 개가 들어있든 컨테이너에서 유도한 값으로 고정한다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-08-20 |
| 구간 범위 선택                | **목록 안에서 시작·끝 두 번 탭** — `범위 선택` 을 누르면 목록이 범위 지정 모드가 되고 두 행을 탭하면 사이 구간이 전부 켜진다. 컨트롤이 안 늘고 조작면이 체크박스 목록 하나로 유지된다. 두 손잡이 슬라이더는 P3 레벨 범위에서 승인된 형태지만 **그곳은 슬라이더가 유일한 컨트롤**이었고 여기서는 체크박스와 같은 상태를 쓰는 이중 조작면이 되어 기각 · 시작·끝 Select 2개는 P3 에서 "범위 감각 상실"로 이미 기각한 형태라 기각. 신규 키 2개(한국어만 확정, ja/en 은 핸드오프): `tiers.selectRange`=`범위 선택` · `tiers.selectRangeEnd`=`끝 구간을 고르세요`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-20 |
| 커밋된 구간 표기              | **연속 범위를 토큰 하나로 축약** — 결과 옆 적용 조건 토큰이 `12.5–13.5` 하나로 표기하고, 지우면 구간 제약이 사라져 전체로 복귀(체크 0개 = 제약 없음 결정과 일치). 비연속은 실재 문자열 `tiers.moreSelected`(`{first} 외 {count}`)로 접는다. **토큰 라벨은 밴드 값만 적어 새 문구가 없다** — 레벨 토큰이 `Lv.` 접두를 달아 구분되고 문서 06 이 정확한 소수 라벨 보존을 요구한다. 구간마다 토큰 하나는 11개가 3줄로 접혀 난이도·레벨 토큰이 묻혀 기각 · 개수만 요약(`구간 11개`)은 어떤 구간인지 안 보여 기각. 토큰 구성: radius/control 4 · 1px `border/default` · 라벨 `control` · 제거 `Icon/x` 16                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-08-20 |
| 필터 배지 계수                | **조건 그룹 수** — 난이도 1 · 공식 레벨 1 · 구간 1 = 3 이며 구간을 몇 개 골랐든 1로 센다. 구체적 내용은 적용 조건 토큰이 전달하므로 배지와 토큰이 역할을 나눈다. 문서 04 는 "배지는 적용된 필터만 센다"까지만 정하고 그룹/항목을 구분하지 않았으므로 이 결정이 공백을 메우며 **Discovery 에도 같은 규칙을 적용**한다. 선택 항목 수(13)는 구간이 숫자를 지배해 난이도·레벨 적용 여부가 안 보이고 범위를 넓히면 세 자리가 되어 기각 · 점만 표시는 좁힘 정도가 사라지고 기존 배지와 형태가 갈라져 기각                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-08-20 |
| sticky 구간 헤더 면           | **현행 유지** — `surface/canvas` + 하단 1px `border/default`. 스크롤 중 상태로 그려 확인: 헤더가 구분되는 근거는 헤더 면이 아니라 **카드 자켓이 `surface/sunken` 이라 페이지보다 어둡다**는 점이며, canvas 면이 그 위를 덮으면 색 차이가 생겨 1px 선에만 의존하지 않는다. `surface/surface` 면은 canvas 대비 1.06/1.10 으로 2026-08-19 느린 교체 면 강등안을 비가시로 기각했을 때와 같은 값이라 기각 · `border/strong` 은 구간마다 진한 가로줄이 반복돼 기각                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-20 |
| 기본 프레임 조건 상태         | **조건 없음 = 문서 최상단** — 스택이 14.5(1곡) → 14.4(2곡) → 14.3(2곡) 로 시작하고 결과 요약은 전체 개수, 필터 배지·토큰 없음. 밴드 재작업 때 결과 영역만 바꾸고 조건 상태를 안 맞춰 **36개 프레임이 "조건 없음인데 구간 3개 21곡"** 이라는 불가능한 상태였다. 필터 걸린 상태로 정정하는 안은 `Expert`+`Lv.12` 가 169채보라 구간 3개에 안 들어가고, 맞추려 구간 토큰까지 넣으면 `구간 좁힘` 프레임과 같아져 기각. **파일은 조건 없음(기본)과 구간 좁힘(배지 3) 두 극단만 둔다.** 서명 안 됨 밴드 헤더는 밴드 값만 — 문서 06 이 달성 수를 서명된 경우에 붙인다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 2026-08-21 |
| 카드가 곧 자켓인 구조         | ⚠️ 서명 안 됨 카드는 **카드 프레임 자체가 자켓**이고 자식이 `Icon/music` 하나뿐이라 `jacket` 이름 자식이 없다. `jacket` 을 찾아 정사각을 맞추는 패스가 이 카드를 통째로 건너뛰어 **30장이 높이 11~33px 로 무너졌다.** 정사각 검사는 `jacket` 노드와 **"auto-layout 이 아니고 jacket 자식도 없는 card 프레임"** 두 가지를 모두 대상으로 해야 한다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-21 |
| layer header 광학 패딩        | **아이콘 24 · 패딩 8**(A안) — close 컨트롤이 타겟 44 안 아이콘 20 이라 내부 여백 12, 우 패딩 8 과 합쳐 잉크 20 이 되어 좌 16 과 **4px 어긋났다**(`A17` 허용 2 초과). 24 로 올리면 내부 여백 10 → 잉크 18 · 오차 2 로 통과하고 AppHeader 와 같은 규격이 된다. 좌 패딩 24 인 Intermediate 헤더는 같은 규칙으로 우 패딩 16. **패딩 4 안은 기각** — "타겟이 컨테이너 경계에 닿을 만큼 줄이지 않는다(SearchField 8 유지)" 와 충돌                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-21 |
| 버튼 인스턴스 교체            | **32개 전부** — P4 의 `Button` 이 손으로 그린 FRAME 이었다. C2 `Button` 은 20 variant 전부 높이 40 인데 sticky 커밋 버튼 16 개가 44 였다. 폭·라벨(3로케일)은 보존하고 인스턴스로 교체. 결과 영역 Neutral 16 개는 높이가 같아 **화면 변화 없이 구조만** 바뀐다. 근거: 손그림은 `State` variant 가 없어 **`FOCUS-1B` 와 pressed 계약을 표현할 수 없고** 컴포넌트 개정을 따라오지 않는다. `audit.js` 는 색·spacing·타이포가 맞게 그려져 있어 이걸 못 잡는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-21 |
| 부재·오류·로딩 밴드 헤더      | **B안 · 달성 수만 제거** — 헤더와 밴드 값 `14.1` 은 유지하고 `달성 2/5` 만 뺀다. 네 상태(공개 서열표 없음·조건 불일치·밴드 요청 실패·초기 로딩)에 같은 규칙, **`느린 교체` 는 기존 결과 유지라 그대로**. 근거 `06:231` — 밴드 헤더는 밴드 값과 _when signed in_ 달성 수를 싣는데, 달성 수는 그 밴드에 채보가 있을 때 성립하는 값이다. A안(카드 0 이면 헤더 제거)은 상태마다 규칙이 갈려 기각                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-21 |
| 필터 레이어 규격              | **결정 아님 · P3 선례로 정정** — A/B 두 안을 올렸으나 사용자가 "동일한 거 아니냐" 고 지적했고 맞았다(가시 차이는 sticky 높이 8px 뿐, 선 색은 대비 1.31 대 1.40 으로 구분 불가). P3 `1194:262` 계약을 그대로 따른다: 헤더 = `surface/canvas` + `border/divider` **하단** 1px INSIDE · sticky = 같은 토큰 **상단** 1px INSIDE · 패딩 16 · 커밋 버튼은 `Button` 인스턴스(40). **높이 68 대 72 는 손그림 버튼(44)이 만든 증상이었다** — 인스턴스로 되돌리니 72 가 저절로 나왔다. `body (스크롤)` 의 `clipsContent=false` 와 내용 넘침은 **두 페이지 공통 관례라 결함이 아니다**                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-08-21 |
| P5 포디엄                     | **두지 않음**(`RANK-22`) — 결과는 1위부터 하나의 순위 목록. `RANK-10` 이 같은 공개값에 같은 순위를 주므로 1위가 여러 명일 수 있고, 문서 08 이 "공유 순위일 때 고유 메달 암시 금지" 를 못박아 높이·메달·1·2·3 위계를 줄 수 없다 → **카드 3개가 나란한 형태만 남아 목록 상단 3행과 정보량이 같다.** 딸린 비용도 소멸: 데이터 이중 노출(또는 목록이 4위부터), `RANK-08` `내 위치` 의 포디엄 예외, 320px 세로 스택 시 목록과 동일, 2페이지부터 포디엄 소멸. 문서 08 은 포디엄을 요구한 적이 없고 Handoff Boundary 가 미뤄둔 항목이었다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 2026-08-21 |
| P5 Wide 결과 형태             | **테이블형 목록**(`RANK-23`) — 문서 08 이 `semantic table or table-like ranked list` 로 둘 다 허용했으나 ordered-list 채택. 근거: ① 시맨틱 테이블은 헤더 라벨 `순위`·`플레이어` 가 **새 문구**(저장소에 키 없음, `rankings.metric.grade` 만 실재) ② 320px 에서 헤더를 버려야 해 **좁은 폭 ordered-list / 넓은 폭 table 로 시맨틱이 갈린다** ③ 열이 `순위·정체성 그룹·값` 3개뿐이고 가운데가 복합 그룹이라 헤더가 설명할 것이 적다. 값의 정체는 메트릭 컨트롤이 전달하며 이는 문서 08 Compact 조항이 이미 승인한 방식                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-21 |
| P5 랭킹 행 컴포넌트           | **`PlayerRankingRow` 신설** — C7 `LeaderboardRow` 에 variant 를 추가하지 않는다. 두 행의 공통 요소는 **순위·아바타·이름뿐**이고 등급이미지·FC마크는 문서 08 행에 있으면 안 되며 국가마커·2행 시험은 문서 05 행에 없다. 이 파일은 구조가 다르면 항상 별도 셋으로 나눠 왔다(`ResultCollection`·`AreaSwitcher`·`DestinationPanel`) — **variant 축으로 묶은 사례 0**. 딸림: `MyRankSummary` 도 재사용 안 함(내용이 채보용이고 `RANK-08` 의 `내 위치` 액션 없음) · `Pagination` `361:2387` 은 그대로 재사용(**정정 2026-08-22**: 이전 인수인계문의 "Focus variant 미제작" 은 사실이 아니다 — `State=Focus` 가 `focus/ring` 1px INSIDE 로 실재하며 `FOCUS-1B` 충족, 보완 불필요). ⚠️ 두 안은 렌더 차이가 0 이라 그림으로 비교할 수 없는 결정이었다                                                                                                                                                                                                                                                                                                          | 2026-08-22 |
| P5 기타 지역 마커             | **lucide `globe`** — `RANK-13` 이 `globe` 라고만 정해 글리프 선택이 남아 있었다. `globe-2` 는 `earth` 의 재수출 별칭이라 실제 후보는 둘. 마커는 이름 뒤 **16px 보조 크기**인데 그 크기에서 `globe`(원+경선+적도선, path 3)는 대칭이라 형태가 유지되고 `earth`(대륙 윤곽, path 4)는 뭉친다. `earth` 를 택할 이유는 현재 코드가 `Globe2` 를 쓴다는 것뿐인데 현재 코드는 2.0 권위가 아니다. 경로는 `lucide-react` 1.24.0 에서 그대로 추출. **C1 에 없으므로 `Icon/globe` 신설 필요**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-08-21 |
| 국가 마커 모서리              | **`r=0`** — 기존 `r=2` 는 문서 24 승인 집합(`control 4 · container 8 · overlay 10 · full 50%`) **밖이고 미바인딩**이었다(2026-08-20 판에서 생겨 복제로 23곳). `radius/control 4` 는 40px 급 컨트롤 전제라 **높이 12** 요소에 쓰면 각 모서리에서 높이의 1/3 이 깎인다. 국기를 둥글리라는 조항이 문서 08 에 없다. C5 radius `6→4` 정정과 같은 부류로 **문제는 미감이 아니라 집합 밖 하드코딩**. 23곳 적용 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-21 |
| P5 국기 에셋                  | **결정 아님 · `public/flags/kr.png`·`jp.png`** — 두 나라를 같은 매체로 커버하는 세트가 이것뿐이라 선택지가 없다. `public/flag/ko-KR.svg` 는 한국만 있고 현재 코드의 일본 마커는 CSS 로 그린 사각형+점이라 매체가 비대칭이었다. 렌더 16×12(원본 4:3). ⚠️ `country.korea` 영문이 `South Korea` 라 승인 라벨 `Korea` 와 다르다 — 문구 정정 대상                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-21 |
| P5 메트릭 컨트롤              | **밑줄 탭 + 지역 compact**(`RANK-24`) — 사용자 지적으로 발견한 **2중 위반 정정**이다. 메트릭을 모드와 같은 `SegmentedControl`·같은 폭 358·같은 높이 44 로 만들어 동급으로 세웠고(`08:168`·`08:343` subordinate 위반), 지역 Select 를 358 full-width 로 만들어 peer 로 세웠다(`08:177` compact ·`08:187` secondary 위반). 채택: `MetricSwitch` 신설(밑줄 탭) + 지역 compact 으로 **한 보조 행** — 실측 `206 + 8 + 73 = 287 / 358` · 컨트롤 세로 `132 → 88`. **밑줄 탭은 `AreaSwitcher · Wide` `241:78` 언어를 그대로 따른 것이고 지어낸 형태가 아니다**(항목 pad 12/16/0/16 · 라벨 `control`→`emphasis-label` · indicator 2px `border/strong` · Focus 는 항목에 `focus/ring` 1px INSIDE). 선택 단서가 굵기+밑줄 둘이라 색 단독이 아니다. `AreaSwitcher` 는 **P1 에만** 쓰여 내비와 혼동되지 않음(실측). 폐기: 체크 마크(체크는 관례상 적용됨/완료 · 토글 시 라벨 이동) · 텍스트 토글(단서가 굵기+색뿐이라 클릭 가능해 보이지 않음). 불가능: 메트릭 HUG(내부 세그먼트 FILL) · 높이 36(타겟 44 위반) · 메트릭을 Select 로(`08:168` 이 두 값 노출을 전제) | 2026-08-23 |
| P6 Recital 레이팅             | **노출**(`PROF-33`) — `PROF-09`(Recital 레이팅 생략)은 `Superseded`. 출처는 그 모드의 Pianist 서열표이며 `TIER-29`·`RANK-21` 과 정합. 출처가 런타임에 없으면 P5 가 승인한 rating-unavailable 상태를 재사용한다. 「발행됐을 때만 칸 노출」 안은 플레이어마다 요약 항목 수가 달라져 기각. 문서 09 의 10곳 정정 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-08-26 |
| P6 모드 선택기                | **SegmentedControl 2칸 · 358×44**(`PROF-34`) — P5 가 같은 역할(성능 영역 전체 지배)에 승인한 형태. 밑줄 탭은 `RANK-24` 가 subordinate 로 확정했으므로 여기 쓰면 위계가 뒤집힌다. 현재 제품의 높이 36 · 채운 반전 세그먼트는 승인 집합 밖                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-26 |
| P6 Progress 컨트롤            | **밑줄 탭(메트릭) + Select compact(범위) · space-between 한 행**(`PROF-35`) — `RANK-24` 배치 승계. Select 2개는 메트릭 두 값을 감추고, 메트릭을 358 세그먼트로 두면 모드 선택기와 형태가 겹쳐 기각. 범위 라벨 `30일`·`90일`·`1년` 은 **신규 키**(`profile.all`=`전체` 만 실재)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-08-26 |
| P6 아바타 폴백                | **사용자명 이니셜**(`PROF-36`) — 첫 **문자** 자소 1개 · 대소문자 있으면 대문자화 · 비문자는 건너뜀(`_test01`→`T`) · 문자가 없거나 이름이 없으면 `Icon/user`. **이니셜은 접근성 트리에서 제외**(사용자명 중복 낭독 방지), 슬롯은 자체 접근 이름 유지. ⚠️ 내 추천은 `Icon/user` 였고 사용자가 뒤집었다 — 「구현 규칙이 필요하다」는 기각 사유가 아니다. `MDET-85` 의 avatar 부분은 이 결정으로 분리됨. P5 `PlayerRankingRow` 4 variant + 인스턴스 558개 교체 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-26 |
| Pianist 는 등급이다           | **문서 24 `AC-01` 정정**(2026-08-27) — Pianist 를 Full Combo 와 나란한 「성취 마크」 로 정의한 것이 오류였다. Pianist 는 퍼펙트(`score>=1,000,000` 또는 `fc_type===3`)라 **점수 등급 사다리의 최상위**이며(`Pianist > S > A+ > A > B+ > B > C > D`) 공식 `P` 등급 이미지로 전달된다. Full Combo 는 **콤보 축**이라 별개 칸이고 둘은 배타가 아니다 — `isTierGoalAchieved('fc')` 가 `fc_type>=2 \|\| score>=1,000,000` 이므로 **Pianist 판에도 FC 마크가 붙는다.** 문서 05:828 이 금지한 것은 FC 칸에 `P` 를 넣어 등급을 반복하는 것뿐. `achievement/pianist` 변수는 색 불변으로 유지하되 역할 미배정 · 행 계약의 권위는 문서 05 Resolved row composition                                                                                                                                                                                                                                                                                                                                                                                               | 2026-08-27 |
| 행 규격은 C7 에 있다          | **`LeaderboardRow`(`359:98`)가 문서 05 확정 구성의 실제 구현이다.** `mark FC` = 26×20 · 패딩 `2/4/2/4` · radius 4 · 1px `achievement/full-combo` + `FC` 라벨 / `grade S` = 18×18 원형 슬롯. 등급·FC 를 그리기 전에 **이 컴포넌트 내부를 먼저 연다** — 나는 이름과 variant 만 보고 안을 안 봐서 FC 마크를 지어냈다. 공식 등급 이미지는 `public/grade/` 8종(원형 메달)이며 `fc_bg` 만 원격(`p.eagate.573.jp`)이라 자체 호스팅 반출이 구현 과제                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-08-27 |
| P6 Best Plays 행              | **2축 구성**(`PROF-37`) — 왼쪽 「무엇을 어떻게 쳤나」(자켓+우상단 FC 마크 · 원제 · 난이도·레벨·점수·**공식 등급 이미지**) / 오른쪽 「그래서 얼마를 주나」(기여도 · 우측정렬 tabular). 좌 패딩 0 은 자켓 bleed 때문이며 C6 Music List `[0,12,0,0]` 과 동일. 등급은 글자가 아니라 `public/grade/` 공식 메달 이미지이고 Pianist 는 그 사다리의 최상위다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-27 |
| P6 Best Plays 메트릭          | **전용 MetricSwitch**(`PROF-38`) — 기본값 **공식 Grd**, 대안 NosLog 레이팅. ⚠️ **표시 토글이 아니라 다른 질의다** — Grd 는 상위 50곡·하한 없음, 레이팅은 상위 70곡·950,000 하한이라 **목록에 들어오는 곡과 순서가 함께 바뀐다.** 딸림: `RANK-31` 형 Updating 규칙 · 정렬 라벨 동시 전환 · 한 페이지에 MetricSwitch 둘(Progress+Best Plays). 게이지는 곡별 Grd 최대값이 저장소에 없어 Grd 모드에서 불가                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 2026-08-27 |
| P6 동기화 상태 표기           | **자체 관측값만**(`PROF-40`) — 소유자 동기화 줄은 NosLog 자체 임포트 잡이 관측한 것만 말한다: `동기화한 적 없음` · `마지막 동기화 {시각}` · `동기화 중` · `일부만` · `실패`. **`최신`·`동기화 필요` 판정은 제거** — 외부 게임 서비스와 비교해야 알 수 있는 값인데 NosLog 에 조회 권한도 경로도 없다. 사용자 지적: "최신인지 알 수 있는 방법이 없잖아 외부 api를 사용하는건데 우리가 어떻게 불러와 무슨 권한으로". 문서 09 `PROF-05` 계약 본문 교체 완료                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-08-27 |
| P6 아바타·국기 크기           | **인접 composite 에서 유도**(`PROF-41`) — 아바타는 정사각이고 **옆 정체성 스택 높이에서 나온다**(현재 136×136). 고정 숫자로 두면 스택이 바뀔 때 첫/마지막 줄과 어긋난다. 국가 마커는 **사용자명 composite 를 따라간다** — 승격 `page-title` 옆 `24`, `control` 옆(P5 행) `16`. `24` 는 승인된 렌더 크기이고 문서 24 가 그 크기에 붙여 둔 "두드러진 단독 어포던스·빈 상태" 역할에 대한 **경계 있는 예외**로 문서 09 에 기록했다(Music Detail 자켓 96 예외와 같은 부류). 국기는 그 정사각 박스 안에서 원본 4:3 유지, 기타 지역 globe 는 꽉 채움                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 2026-08-27 |
| 지표 표기 통일                | **값 → 단위 · 순위 `#` 접두 · 요약 네 값 동일 크기**(`PROF-42` · `RANK-32`) — `5,713 Grd` · `6,240 pt` · `#2`. **P5 도 `Grd {값}` → `{값} Grd` 로 정정**(문서 08 `RANK-32` · 문서 71 개정, Figma `PlayerRankingRow` 인스턴스 558개 반영 필요). 경쟁 요약은 **네 값 전부 `metric-display`** — 공식 Grd 만 32/40 이고 나머지가 14/20 이면 지표 하나에 캡션 셋이 붙은 모양으로 읽힌다. 공식 Grd 의 우위는 **순서와 칸 폭**이 전달한다(`CONFLICT-24` 개정). 국가 순위 라벨은 나라 이름을 넣지 않고 `국가 순위` — 값이 국가 카테고리 내 위치이기 때문                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-27 |
| 브랜드 마크 예외              | **문서 24 `IC-06` 경계 있는 예외** — Discord 처럼 Lucide 에 대응 글리프가 없는 서드파티 브랜드는 **브랜드 자신의 공식 심볼**을 쓴다. 손으로 다시 그리지 않고 제품 소스에서 vendoring(`components/ui/DiscordIcon.tsx` 의 공식 Clyde 경로, viewBox `0 0 64 48`), 같이 놓인 아이콘과 같은 렌더 단계, 접근성 처리 동일. 브랜드 마크는 **채운 아트워크라 `2px` 스트로크 규칙이 적용되지 않는다.** 적용 범위는 **외부 서비스를 지목하는 정체성 메타데이터**뿐 — 액션·내비·상태에는 브랜드 마크를 쓰지 않는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 2026-08-27 |
| P6 목록 확장 방식             | **개요에서 인라인 확장**(`PROF-39`) — `더 보기` 로 다음 배치를 **fetch 해서 제자리에 덧붙이고** `접기` 로 5개로 되돌린다. **전용 자식 라우트(`/profile/[id]/best`·`/recent`)는 만들지 않는다** — 라우트 두 개와 그에 딸린 정체성·복귀 경로·URL 복원 계약을 짓는 비용이 얻는 것보다 크다는 사용자 판단. 확장 상태는 view state 라 URL 에 안 남고 새로고침·곡 상세 왕복 시 5개로 복귀한다(없는 복원을 지어내지 말 것). 배치는 클라이언트 슬라이스가 아니라 fetch 라 숨김 이력이 방문자 페이로드에 실리지 않는다. 문서 09 개정 완료 — `Complete-List Destination Contract` → `In-Place List Expansion Contract`, `PROF-13` Revised · `PROF-17` 범위 재정의 · Superseded 항목 교체 · 매핑·픽스처·접근성·문구 조항 13곳                                                                                                                                                                                                                                                                                                                                    | 2026-08-27 |
| P6 간격 스케일 정정           | **인셋 24 · 항목 내부 12 · 항목 사이 24** — 승인된 Wide 본문 안에 문서 24 스케일 밖 값 3종(`padding 20` · `gap 6` · `gap 20`)이 남아 있었다. 내가 Z1 보드를 그릴 때 눈대중으로 넣고 시각 승인만 받아 간격 감사를 안 거친 결과다(규칙 8 위반). 카드는 제목·컨트롤·본문을 담은 subsection 이라 `24`, 아이콘+라벨 `12` 는 **이 파일이 294곳에서 쓰는 실측 선례**라 새 값을 만들지 않는다, 항목 사이는 항목 내부(12)보다 뚜렷이 넓어야 네 덩어리로 읽혀 `24`. 딸림 수정: 인셋이 커져 콘텐츠 폭이 `765→757`·`355→347` 로 줄면서 `trend line` 벡터·`ProfilePlayRow` 10장·판정 누적막대 5구간이 넘쳐 전부 재유도(막대는 표수에서 largest-remainder 로 재계산, 합 347 정확). 노드 이름 `seg · S-Just`→`seg · ◆JUST`, `meta · Discord`(오락실 항목)→`meta · 오락실` 정정                                                                                                                                                                                                                                                                                       | 2026-08-27 |
| P6 Compact 390 유도값         | **6건 확정**(`PROF-43`) — ① 카드·구역 인셋 **16**(Wide 24) · ② 정체성 **아바타 옆 3행**(아바타 64 = 옆 이름그룹 높이) · ③ 최근 플레이 **시각을 제목 줄 끝**으로 · ④ 섹션 제목/컨트롤 **2행**(PROF-35 의 "두 컨트롤 한 행"은 유지) · ⑤ 경쟁 요약 **2×2**(두 행 좌측 키라인 일치) · ⑥ 곡 제목 **한 줄 + 말줄임 · 행 64**. ① 은 미감이 아니라 실측 — 인셋 24 면 콘텐츠 310 이라 재생 행 meta(164)가 안 들어간다. ② **레퍼런스 실측(375px)**: osu!·Steam 은 아바타 왼쪽, Mastodon 은 위지만 좌측 정렬 — **가운데 정렬은 3곳 모두 안 쓴다**(계정·설정 화면의 관용). osu! 는 `세계 순위 #759,387 \| 국가 순위 #15,636` 2열이라 우리 ⑤·`#` 접두·라벨 문구까지 겹친다. 아바타 위(B안) 는 정체성 블록이 212→312 로 100px 늘어 미채택                                                                                                                                                                                                                                                                                                                           | 2026-08-27 |
| 320 컨트롤 라벨 축약          | **B안 · 라벨을 줄여 한 줄 유지**(`PROF-44`) — 320 에서 메트릭 탭 206 + 8 + 범위 74 = 288 > 카드 콘텐츠 256. **P5 `MetricSwitch` 가 390·320 양쪽에서 이미 `Grd` / `Rating` 을 쓴다는 실측**이 근거라 새 형태를 만드는 게 아니다. 적용 후 130 + 8 + 74 = 212 / 256 으로 한 줄이 되어 **`PROF-35` 를 고칠 필요가 없다.** 두 줄로 접는 A안은 컨트롤 세로가 44→98 로 늘고 조항 예외가 필요해 기각, 범위를 아이콘만 남기는 C안은 문서 09 Compact 조항이 「한 줄을 지키려고 라벨을 설명 없는 아이콘으로 줄이지 말 것」 으로 **명시 금지**해 기각. 라벨 축약은 아이콘 대체와 다른 층위다                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-27 |
| A17 은 고스트에만 적용        | **가장자리 아이콘 컨트롤에 보이는 상자가 있으면 A17 을 적용하지 않는다**(2026-08-27) — 정렬 단서는 잉크가 아니라 **상자 경계**이고, 상자는 이미 컨테이너 패딩에 맞아 있다. P6 소유자 액션(40×40 · 1px `border/default`)은 상자가 기준선 24 에 정확히 닿아 있는데 잉크만 34 라서 `A17` 이 122건을 잡았고 **전부 오탐**이었다. 「아이콘 24 · 우 패딩 16」 으로 고쳤다면 잉크는 맞지만 **상자가 기준선을 8px 넘어가 지금보다 나빠진다**(비교판을 실제로 그려 렌더를 보고서야 알았다). AppHeader 가 16/8 을 쓴 이유는 그쪽 `nav-trigger` 가 `fills[] · strokes[]` 인 **고스트 아이콘**이라 잉크가 유일한 단서였기 때문이다. `audit.js` A17 에 `ghost17()` 조건 반영 완료                                                                                                                                                                                                                                                                                                                                                                                  | 2026-08-27 |
| P6 로케일 시각 형식           | **A안 · JA 날짜를 `8/27 21:40` 로 단축**(`PROF-45`) — Wide 최근 플레이 카드는 395 라 `8月27日 21:40` 이 KO 보다 넓어 identity 열이 164→163 이 되고 meta(난이도·레벨·점수·등급 164)가 **1px 넘쳤다**. 단축하면 identity 182 로 여유가 생긴다. B안(Wide 도 시각을 제목 줄로 = Compact 패턴 통일)은 identity 259 로 가장 넉넉하고 렌더도 정돈돼 보였지만 **원인이 JA 날짜 폭 하나인데 전 폭·전 언어의 승인된 구성을 바꾼다**는 이유로 미채택. EN `Aug 27, 21:40` 은 넘치지 않아 그대로 둔다 — 로케일마다 날짜 형식이 다른 건 정상이다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 2026-08-27 |
| P6 상태 문구                  | **4건 전부 A안**(`PROF-47`) — `아직 동기화된 기록이 없습니다.`(기록 없음 · 성능 영역에 **하나만**) · `프로필을 불러오는 중입니다.`(로딩) · `프로필을 불러오지 못했습니다.`(오류 · 옆 `다시 시도` 버튼과 중복하지 않음 — `tiers.loadError` 선례) · `일부 기록만 동기화됐습니다.`(동기화 일부만). **전부 저장소에 이미 있는 어법**(`{대상}을 불러오는 중입니다.` `tiers.loading`·`arcades.mapLoading` / `{대상}을 불러오지 못했습니다.` `common.pageError` / `일부 …만` `sync.notice`). ⚠️ `profile.bestEmpty`·`recentEmpty`·`judgementEmpty`·`gradeTrendEmpty` 는 **쓰지 않는다** — 문서 09 가 카드별 빈 상태 반복을 금지해 현재 구현을 대체한다                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-28 |
| P6 초기 로딩 표현             | **A안 · 목록만 스켈레톤 + 상태 문구**(`PROF-46`) — 사용자 지적으로 발견: **P3 Discovery 에 이미 승인된 `skeleton row (접근성 트리에서 숨김)` 358×64** (`surface/surface`·radius 8·자켓 64 `surface/sunken`)과 Z1 「스켈레톤 막대 = 컬럼 비율 선언(첫 막대 100% · 둘째 75/166)」(2026-08-19)이 있다. 문서 09 는 **택일이 아니다** — 「스켈레톤 콘텐츠에 지어내지 말 것」 이 스켈레톤을 전제하고, 스켈레톤 행이 a11y 트리에서 숨겨지므로 **낭독되는 건 상태 문구뿐**이다(눈 / 귀로 역할이 갈린다). 베스트 성과·최근 플레이만 스켈레톤, **차트·요약·분포는 기하만 유지** — 스켈레톤 곡선은 문서가 금지한 「차트를 지어내는」 쪽이다. 순위·점수·축 날짜·기준 채보 수는 전부 `—`. B안(P4·P5 식 기하만)은 **「기록 없음」 화면과 문구 한 줄 차이라 눈으로 구분되지 않아** 기각                                                                                                                                                                                                                                                                              | 2026-08-28 |
| 구역 상태 = 그릇을 고친다     | **구역 오류 그릇 = B · C4 `StatusMessage` 에 본문 boolean 속성 추가**(`PROF-48`) — 사용자 지적으로 발견: 「위쪽 쏠림」 의 원인은 정렬이 아니라 **빈 줄**이었다. 루트는 이미 `MIN/CENTER` 인데 내부가 제목+본문 2줄 구조라 본문을 빈 문자열로 두면 44px 블록이 남는다. **변형 축을 늘리지 않고 boolean 속성**(`Body#2404:0`)을 본문 레이어 visible 에 바인딩 — 4 variant 유지, 기존 인스턴스 전부 기본값 true 라 **P2·C7·P6 픽셀 변화 0**(전수 스냅샷 대조). 덤으로 인스턴스 자식을 직접 감출 때 `findAll` 에서 사라지던 함정도 사라진다(속성으로 끄면 `componentProperties` 로 되돌릴 수 있다). 라벨+문장 2줄 A안은 「불러오기 실패」 라는 새 문구가 생기고 구역마다 반복돼 기각 · 문장은 `내용을 불러오지 못했습니다.` 하나로 네 구역 공용 — 한국어 목적격 조사가 갈려(`베스트 성과를`·`판정 요약을`) 템플릿이 불가하고, 어느 구역인지는 **오류가 그 카드 안에 있다는 사실**이 말한다                                                                                                                                                                | 2026-08-28 |
| 구역 갱신엔 문장을 안 쓴다    | **`content/pending` 재사용 · 보이는 문장 없음**(`PROF-48`) — 사용자 지적("왜 텍스트를 굳이 써서 시선분산시켜?")이 맞았다. `DISC-44`(2026-08-19)가 「요청이 진행 중이라 일시적으로 보류된 유효한 콘텐츠」 용으로 만든 토큰이고 계약에 **「Discovery 밖 다른 페이지에서도 같은 상태에 재사용한다」** 고 적혀 있다. P3 「느린 교체 · 기존 결과 약화」 가 정확히 이 방식이며 문장을 쓰지 않는다(실측 126곳). 문서 09 의 `restrained live status` 는 **낭독용이라 보일 필요가 없다** — 시각 단서는 pending 색 + 진행 표시 + `aria-busy`. ⚠️ 약화는 **색으로만** — `DISC-44` 가 콘텐츠 약화용 opacity 를 전부 기각했다(처음 그릴 때 차트에 opacity 를 걸었다가 되돌림)                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-28 |
| 랭크 분포에 FC 를 넣지 않는다 | **등급 사다리만**(`PROF-49`) — 사용자 지적. 문서 09 가 `Pianist, Full Combo, S, A+, …` 로 적어 두었지만 **FC 는 콤보 축이지 등급이 아니다**(2026-08-27 `AC-01` 정정과 같은 논리). `isTierGoalAchieved('fc')` 가 `fc_type>=2 \|\| score>=1,000,000` 이라 **모든 Pianist 는 FC 이기도 하다** — 막대 하나로 세면 채보가 중복 집계돼 합이 카탈로그와 안 맞고 「분포」 가 성립하지 않는다. FC 는 재생 행의 마크로 남는다. 표시 4칸은 `P · S · A+ · A`, 나머지 `B+ 이하 4칸` 은 펼치기. 문서 09 조항 교체 + P6 40곳 반영(`grade_a.png` 업로드)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-08-28 |
| 랭크 분포 수 열 폭            | **분포의 최대값 폭으로 고정 · 오른쪽 정렬 · 트랙은 FILL**(`PROF-51`) — 사용자 지적: 자릿수가 적은 행의 막대가 더 길어 보였다. 수 열이 자연폭이라 `5`(9px) 행의 트랙이 `421`(26px) 행보다 **17px 길었고**, 그래서 막대 길이가 값이 아니라 **자릿수**를 반영했다. 수 열을 그 분포의 최대값 폭으로 고정하면 네 트랙의 시작·끝이 같아진다. 폭은 상수가 아니라 **데이터에서 유도**한다(네 자리 프로필은 그만큼 넓어짐) · `metric-value` 가 tabular 라 자릿수가 달라도 흔들리지 않는다. 40곳 반영. 메달 가로 나열안·막대 제거안은 미채택 — 사용자가 현재 형태 유지를 선택                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-08-28 |
| 성장 추이 플롯 높이           | **폭의 16:9 · 상한 344**(`PROF-50`) — 사용자 지적으로 발견: 플롯이 **모든 폭에서 344 고정**이라 1280 2.2:1 · 390 0.95:1 · **320 은 0.74:1 로 세로가 더 길었다.** 344 는 내가 Wide 에서 정해 복제한 상수이고 어디서도 유도되지 않았다. 실측 → 320 **144** · 390 **183** · 768 이상 **344**(상한). 상한 344 는 Wide 행 파리티(성장 추이 = 기록 개요)에서 **이미 유도된 값**이라 Wide 는 변화 0. 상수안(96·128)은 「왜 그 숫자인가」 를 설명 못 해 기각 — 96 만 저장소 `chart.tsx` 의 `h-24` 라는 근거가 있었다. **레퍼런스 실측**(실제 브라우저): osu! 랭크 추이는 375 에서 355×90, 1280 에서 639×90 으로 **높이가 상수**이고 폭만 바뀐다 — 상수 자체는 관용이지만 우리는 카드 안에 컨트롤·요약이 함께 있어 더 크다. 3:2 안은 768 에서 이미 상한에 닿아 중간 폭 둘이 같은 값이 돼 미채택                                                                                                                                                                                                                                                                | 2026-08-28 |
| busy 는 disabled 가 아니다    | **버튼 busy = 라벨만 진행형**(`다시 시도 중`) · `State=Busy` variant 는 보류(2026-08-28) — 문서 19 가 「busy 는 라벨을 없애지 않는다」 를 명시하는데 내가 `State=Disabled` 로 그렸다가 대비 **1.41** 로 실패했다. **WCAG 의 비활성 면제는 busy 에 적용되지 않는다.** 저장소가 `{동작} 중` 어법을 18곳에서 쓴다(`settings.saving`·`feedback.submitting`·`sync.regenerating`). variant 신설은 **진행 표시(스피너) 계약이 문서 24 에 없어** 선행 과제이고 20→24 로 전 페이지 영향이라 별도 Foundation 안건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-08-28 |
| Skip link                     | **`SkipLink` 컴포넌트 신설**(103×36 · 면 `content/default` · 글자 `surface/canvas` · radius 4 · 패딩 8/12, 2026-08-28) — 문서 19·15 가 일반 셸의 first-focusable 로 요구하는데 **P1~P6 어디에도 없었다.** 지어낼 것은 없었다: 문구 `skip.main` 3로케일 실재 · `components/layout/skipLink.tsx` 가 반전 알약 형태를 이미 고정. 배치는 **`SHELL-31`**(셸마다 대표 포커스 컨트롤 하나)에 따라 대표 프레임 한 벌에만 절대배치 — 평상시 뷰포트 밖이라 나머지 렌더 불변. **최소 셸(치명적 오류·점검)에는 넣지 않는다.** Button 재사용안은 문서 19 가 링크와 버튼 역할을 구분해 기각                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 2026-08-28 |
| P1 조성·임계값 3종            | **탭 424 · 레이더 320 s 0.80 · 푸터 840**(2026-08-28) — 셋 다 「디바이스 폭이 아니라 내용이 들어가는지」 로 유도했다. ① 영역 전환기는 가용 인라인 **424**(EN 라벨 65·68·52·109 + 항목 pad 16/16)에서 combobox→탭. ② `320` 레이더는 플롯 264 에 안 들어가 오각형·격자·계열만 **0.80** 배(라벨 크기 유지) — **임계 로케일은 ja 가 아니라 latin**(`Repetition` 56·`Polyrhythm` 62), 여유 ko 23.3/ja 19.9/en 8.2. ③ 푸터는 Wide 한 줄에 `패딩24+링크+gap24+고지+패딩24` 가 필요해 ko 648·en 780·**ja 840** → **840 이상 Wide**. P1·P4·P5 의 768 을 Compact 로, P6 의 1024 를 Wide 로 바꿔 여섯 페이지가 한 규칙이 됐다. ⚠️ **라벨을 번역하면 폭만이 아니라 중심 위치도 움직인다 — 계산하지 말고 그려서 재라**(내가 두 번 틀렸다)                                                                                                                                                                                                                                                                                                                          | 2026-08-28 |
| 문구는 브리프가 먼저다        | ⚠️ **「저장소 카탈로그에 없음」 을 「미승인」 으로 읽지 않는다**(2026-08-28) — 푸터 서비스 고지 3종을 지어낸 문자열로 단정하고 `home.tagline` 로 바꿨는데, **문서 15 Footer Contract 에 승인돼 있는 값**이었고 `SHELL-32` 가 「불특정 팬 서비스 표기를 세 로케일 모두 유지하고 줄이지 말 것」 을 명시한다. 아직 구현 안 된 2.0 신규 문구라 카탈로그에 없었을 뿐이다. 컴포넌트 기본값 2 + 인스턴스 154 를 되돌리고 문서 68·69·70·71 에 재정정 절을 남겼다. **문구를 바꾸기 전에 문서 15·해당 브리프를 먼저 본다.** `footer.privacy` 로케일 정정은 유지                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-08-28 |
| 컴포넌트가 폭에 반응하게      | **인스턴스 자식은 `x` 를 못 바꾼다**(`relative-transform` 오버라이드 불가) — 폭 반응이 필요하면 **컴포넌트 제약을 고친다**. 2026-08-28 에 셋을 고쳤다: `CommunityPatternRadar` 플롯 자식 `MIN→CENTER` · `OrdinaryDataChart` 격자·선·벡터 `STRETCH` + 점 `MIN/CENTER/MAX` · `PatternAxisRow` `choices` 와 버튼 5개 `FILL`(57 은 390 행 폭에서 나온 유도값이 상수로 굳은 것). ⚠️ **아이콘 7종(`languages`·`graduation-cap`·`grid-3x3`·`layers`·`trophy`·`list-music`·`shield-user`)의 내부 프레임이 `MIN` 이라 24→20 축소 시 내용이 안 따라와 4px 크게 그려지고 있었다 — 770곳**. `SCALE` 로 고쳐 0. 정상 아이콘(`globe`·`music`)은 원래 `SCALE` 이었다                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-08-28 |
| 프로필 공유 카드 순서         | **모든 페이지 작업이 끝난 뒤 맨 마지막에 한다**(2026-08-28 사용자 지시) — 남은 페이지(P1 보강 · 브리프 10~19)를 전부 끝내기 전에는 착수하지 않는다. 계약은 문서 09 `Share Card Contract`, 사용자가 건 제약(다크 전용 · 등급 메달 없음 · NosLog 브랜드 강조 · 브리프 무시 · 레퍼런스 다수 조사 후 여러 안 제시)과 레퍼런스 표는 `design-state/RESUME.md` 에 있다. **먼저 하자고 제안하지 말 것**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-08-28 |
| FC 라벨 대비                  | **A안 · 라벨 `content/default` · 테두리 `full-combo` 유지**(문서 24 `AC-01` 개정, 2026-08-28) — `#75980B` 는 Light 에서 canvas 3.36 · surface 3.16 · sunken 2.77 로 **비텍스트 3:1 은 넘지만 텍스트 4.5:1 은 미달**이다(Dark 는 9.70~~10.63 로 자력 통과). 라벨만 중립으로 바꾸면 Light 11.98~~14.55 · Dark 12.44~13.64. **색 역할은 테두리에 그대로 남고 값도 안 움직인다** — 문서 24 가 「색이 단독 단서가 아니다 · 마크는 항상 FC 라벨을 갖는다」 라고 못박았으므로 계약 위반이 아니다. 램프 이동은 기각(집합 안 4.5 초과 초록은 `difficulty/068850` 4.52 뿐인데 sunken 3.72 로 재미달 + 역할 오용), 면 채우기도 기각(`content/on-media` 의 짝이 될 **어두운 고정 별칭이 새로 필요**하고 마크가 너무 강해진다). 반영: P6 222곳 · C7 컴포넌트 정의 3개 → C7 35 · P1 6 · Z1 30 인스턴스 자동 전파. **Z1 옛 보드 79곳은 기록이라 그대로 둔다**                                                                                                                                                                                                        | 2026-08-28 |
| P8 Wide 영역은 상태를 따른다  | **`SYNC-26` · 안 B**(2026-08-30) — Wide 두 영역을 「왼쪽=결과 / 오른쪽=설치」 로 고정했더니 **결과가 없는 첫 사용 상태에서 주 영역이 48, 보조가 1116** 이 되어 브리프가 금지한 `empty desktop dashboard` 가 됐다. 상태에 결과가 없으면 **설치 안내가 주 영역(805), 도움말·보안이 보조(395)** 다. ⚠️ 내 추천은 「1열 `reading 768` 중앙」 이었고 사용자가 「다른 페이지 wide 이점과 동떨어진다」 며 뒤집었는데 **실측이 사용자 편**이었다 — 일반 셸(P2·P4·P5·P6)은 전부 1280 에서 1216 컨테이너 다영역이고, `measure 768` 은 **P7 최소 셸 전용**이며 그마저 `MIN`(좌측) 정렬이라 내가 그린 중앙 정렬은 인용한 선례와도 달랐다                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 타이포는 역할까지 본다        | **「Text Style 100%」 는 감사가 아니다**(2026-08-30) — P8 에서 스타일 *적용 여부*만 세어 「B 타이포 통과」 로 보고했는데 사용자가 「타이포 계층 맞는거야?」 라고 묻자 열어 보니 **역할 오용 2건**이 있었다. ① 설치 섹션 소제목에 `emphasis-label` — 문서 24 가 이 composite 를 **StatusMessage 제목·선택된 세그먼트 라벨** 둘로 못박았고 둘 다 아니다. 묶음 모듈 정체성이므로 `component-title 16/24` (P1 `점수 분포` 선례 158곳). 게다가 20→14 로 **16 단이 통째로 비어** 소제목과 본문이 같은 크기였다 ② 설치·실행 단계 문장이 `body-secondary`+`subdued` — 정의가 「간결한 보조 맥락 · **유일한 핵심 의미 금지**」 인데 신규 가입자에겐 이게 핵심 의미이고 유일한 자리이며 문서 13 이 페이지를 `text-led` 로 규정한다. **§2 B 블록은 「스타일이 붙었나」 가 아니라 「그 역할이 맞나」 를 묻는 것이다 — 선례 페이지에서 그 composite 가 실제로 무엇에 쓰이는지 세어 대조한다**                                                                                                                                                                      |
| 조성은 재보고 정한다          | **로그인 조성 실측 9곳**(2026-09-01) — 브리프에 조성 근거가 없을 때 다른 페이지 계열에서 유추하면 틀린다. P7 최소 셸(`measure 768` 좌측)을 로그인에 옮겼다가 사용자가 「누가 저딴식으로 하냐」 고 지적했고 실측이 사용자 편이었다: Stack Overflow 248 · Atlassian 320 · Spotify 324 · **GitHub 352** · Notion 360 · Dropbox 368 · Twitch 488 · Bandcamp 516 — **전부 좁은 중앙 열이고 제목도 중앙**(Last.fm만 좌측). 폭은 1280 에서도 안 늘어난다. **오류 화면과 입력 폼은 같은 계열이 아니다** — 전자는 읽는 문장, 후자는 조작면                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 설명 문장은 예산이 있다       | **실측 대비 초과분만 지운다**(2026-09-01) — GitHub 로그인은 보이는 텍스트 13개·200자에 **설명 문장 0개**, Dropbox 7개·64자, Spotify 15개·225자(법적 1줄). 내 P9 로그인은 8개·168자인데 **설명 문장이 3개**였고 한국어는 글자당 정보량이 2~3배다. 원인은 **브리프 계약 조항 N개를 화면 문장 N개로 옮긴 것** — 계약은 「의미가 전달되어야 한다」 이지 「각각 별도 문장으로 그려라」 가 아니다. 규칙: ① 제목·버튼·라벨이 말한 것을 반복하지 않는다 ② 조항 N개 ≠ 문장 N개 ③ 한 구역 설명 1개 기본. ⚠️ 전수 조사 결과 **P1~P7은 문제 없었다**(P3 0개·P6 1개, 나머지는 실제 콘텐츠나 상태 메시지) — 「전부 장황하다」 고 넘겨짚지 말 것                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 렌더러 함정 · 텍스트          | **`maxLines` 미지원** — 대입해도 `null`. `textTruncation="ENDING"` 만으로는 줄바꿈이 안 막히고, `textAutoResize="NONE"`+1줄 높이로 고정하면 **둘째 줄이 프레임 밖으로 나와 아래 메타와 겹쳐 그려진다.** 텍스트 박스는 24 라서 **좌표 기반 넘침 검사가 못 잡는다 — 눈으로만 보인다.** 말줄임이 필요하면 문자열에 `…` 를 직접 박고 폭에서 이진탐색으로 자른다 · **`SPACE_BETWEEN` + `FILL` 자식이면 `itemSpacing` 이 통째로 무효**(실측 선언 12 → 실제 0, 제목과 시각이 붙었다). `MIN` 정렬로 바꿔야 간격이 산다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-08-27 |

| P10 탈퇴 버튼 | **채운 빨강**(`SET-43` · 문서 24 「Filled destructive action」 신설) — `Button · Style=Destructive Filled` 5 variant 신설, 기존 아웃라인 `Destructive` 는 모든 사용처 불변. 별칭 3개 · **신규 원시값 0**: `feedback/destructive-surface`(L `C9372C` 흰 5.16 / D `AE2E24` 흰 6.53 — Dark 마커 `F15B50` 은 3.31 미달이라 면으로 안 씀) · `destructive-surface-hover`(양쪽 `AE2E24` = pressed 면 겸용, Dark hover 비가시는 pressed 잔여 단서 선례와 같은 사유) · `on-destructive`(양쪽 흰색, `content/on-media` 구조·별도 역할). focus 는 FI-C 그대로(면이 양쪽 극성 3:1 통과라 on-fill 대체 불필요). C2 DARK VERIFICATION 에 행 추가 완료 | 2026-09-01 |
| P10 뒤로가기 | **`Icon/chevron-left` 16 + 라벨 · 타겟 44**(`SET-42`) — 계층 위로의 이동이라 개요 행 chevron-right 와 대칭. 현행 텍스트 글리프 「←」(아이콘 시스템 밖·타겟 20)와 arrow-left 신설안(히스토리 back 관례라 부적합) 기각. 6프레임 교체 완료 | 2026-09-01 |
| P13 Wide·스테이지 | **W-A′ 급수 레일 + 자켓 트랙리스트**(`EXAM-29`~~`31`) — 805/395 초판은 상단 절반이 비어 기각(레퍼런스 7곳 실측: MS Learn·AWS·Coursera·LeetCode·Khan·chess.com·Spotify). wide = 좌 레일 292(모드 + 10급 상태 목록, 팝오버의 승격) + 콘텐츠 908. **과제곡은 전 폭에서 자켓 64 트랙리스트 행**(검정 = 3곡 셋리스트 · C6 Music List 언어 · 우측 스코프+값 tabular) · 난이도는 **C3 `DifficultyMarker`**(dot 12 난이도색 + 중립 텍스트)로만 — 글자에 색 입히지 않음. 응시 정보 = exam head card 라벨/값 클러스터(컴팩트 2×2 · wide 한 행), metric-display 안은 텍스트 상태가 셀을 넘쳐 기각. ⚠️ 교훈: wide 를 그리기 전에 「이 페이지의 핵심 대상이 가로 공간을 받는가」부터 묻는다 · 그리기 전에 C 페이지에 이미 있는 컴포넌트를 연다 | 2026-09-02 |
| 난이도 = 색 글자 | **`EXAM-32` · 문서 24 DU-01 텍스트 램프 개정** — dot 마커·아웃라인 배지 둘 다 사용자 기각("AI 티 나는 칩 디자인"). 게임·현행 제품의 관례 = 색 글자이고, 대비 미달은 색 포기 사유가 아니라 **텍스트 램프 유도 사유**였다. `difficulty/text-*` 별칭 4(Light/Dark): N `09834A`/`219563` · H `BA5500`/`E06400`(기존) · E `CD2E1D`(기존)/`D85C4F` · R `9452CE`/`AD69E9`(기존) — 전부 4.5:1+, 신설 원시값 5. C3 `DifficultyMarker` 는 dot 제거·색 글자로 개정(사용처 = C3+P13 뿐, 타 페이지 0). **비텍스트 마커(2×20 바·12×2 라인)의 DU-01 값은 불변.** ⚠️ 대비는 기준면을 명시: `068850` 은 canvas 4.52 / surface(F8F8F8) 4.26 미달. ⚠️ 교훈: 배지·칩은 학습된 SaaS 기본값 — 도메인의 네이티브 관례부터 찾는다 | 2026-09-02 |
| P10 문구 2차 | **정적 카테고리 요약 채택**(`SET-46` — 값 요약은 프로필·공개 설정에서 압축 불가) · 닉네임 도움말 정정(`한글·일본어·영문·숫자·공백과 . _ -` — 첫 빌드는 일본어·공백 누락으로 SET-16 위반) · `내 프로필 보기` 는 저장 구역 위 우측(`SET-44`) · 플레이 활동에만 결합 설명 줄(`SET-45` · FormField 도움말 선례 metadata·subdued·간격 8) · 나머지 신규 문구 전부 한국어 승인, ja/en 은 로케일 패스로. **제거 버튼은 폴백 아바타 픽스처에서 삭제**(브리프: custom avatar 있을 때만) · dirty 픽스처는 닉네임 `노스굿이2` 로 실제 변경 부여 | 2026-09-01 |
| 입력 컨트롤 경계 | **`border/strong`**(문서 24 「Input control boundary」 신설, 2026-09-04) — FormField·TextArea·SearchField·Select·FilterSortControl 의 평상시 경계. `border/default` 는 1.71/1.94 로 WCAG 1.4.11 비텍스트 3:1 미달인데 이 컨트롤들엔 경계가 유일한 존재 단서였다. 깊은 검증의 대비 전수에서 발견 · Neutral 버튼 CONFLICT-18 과 같은 논리. 컴포넌트 5종 14 스트로크 + C5 Dark 인스턴스 오버라이드 9 정정. `border/default` 는 장식 구분선·카드 경계·차트 격자·틴트 상태 컨테이너에 그대로 | 2026-09-04 |
| P12 지도 클러스터 라벨 | **정정(같은 날): 버블 면 = `surface/media-scrim` 60% · 라벨 `content/on-media`**(양 모드 버블-지도 5.5 · 라벨 6.33) — 처음 채택한 `primary/default` 는 Dark 에서 #DBDBDB 라 밝게 남는 지도 위에서 1.2:1 로 사라졌다(비교판이 Dark 를 실제 지도 위에 안 그렸던 내 실수 · Codex 교차검증의 플로팅 버튼 지적을 고치다 발견). 지도는 미디어이므로 이미지 위 가독성 쌍을 쓴다. 지도 위 플로팅 버튼도 오버레이 면으로. 범례 `파란 원`→`숫자 원`. 원래 기록: 안 B(primary/default, 14.55/13.64) — 현행 `local-data/single` 면 위 흰 라벨은 3.31/4.11 미달, `content/default` 라벨(안 A)도 4.4/3.32 로 미달이라 기각. 86 버블 반영. ⚠️ 대비 스크립트는 버블이 텍스트의 형제(ELLIPSE)라 배경을 못 잡는다 — P12 클러스터 라벨 52건은 오탐 | 2026-09-04 |
| P14 셀 저장 중 | **menu-set 면 제거 · pending 글자 on `surface/surface`**(안 A, 4.6/4.99) — `content/pending` on `interaction/menu-set` 은 4.02/3.71 미달. DISC-44 가 pending 을 보류 콘텐츠의 시각 단서로 정했고 선택 강조는 저장 중엔 중복. 안 B(subdued 유지)는 pending 계약 위반이라 기각 | 2026-09-04 |
| 로딩·진행 표시 | **A2 펄스 스켈레톤 · B4 Busy 버튼(스피너 16 + 진행형 라벨) · C1 중립 스피너 트랙 없음 · D1 pending 유지 + D3 라우트 상단 2px 바**(문서 24 「Progress indication」 신설, 2026-09-04) — 레퍼런스 24 실측(Carbon·Atlassian·Spectrum·Primer·NN/g·Material 등). `Button · State=Busy` 5 variant 신설 + 인스턴스 24 교체 · C8 `RouteProgressBar` 신설. ⚠️ 내 추천은 A1 정적이었는데 사용자가 뒤집었고 맞았다 — NN/g 「가장 흔한 형태」 는 형태 얘기지 모션 얘기가 아니었고, 라이브러리 기본값(shadcn·Chakra·MUI·Carbon)은 전부 애니메이션이다. **인용을 결론에 끼워 맞추지 말 것** | 2026-09-04 |
| 오버레이 경계 | **`border/overlay` 토큰 신설**(Light = surface/overlay 별칭 · Dark = border/default 별칭, 2026-09-04) — 팝오버·리스트박스·메뉴·시트·다이얼로그·툴팁의 1px 경계. Light 에서 「1px 헤어라인 + 넓은 그림자」 는 shadcn/Radix 기본값이라 AI-slop 카탈로그가 생성 UI 시그니처로 지목 · Spectrum·Material·Carbon·Atlassian 은 그림자만. Dark 는 그림자가 안 보여 경계 유지. border/empty-slot 과 같은 방식. 50 노드 재바인딩 | 2026-09-04 |
| Home 목적지 타일 | **가로 타일(아이콘 20 + 라벨)**(`HOME-22`) — Compact 2열 173×44 · 320 138×44 · 768/1024 4×2 inset 12 · 1280 4×2 inset 24 (292×68). 아이콘-위-라벨 동일 타일 그리드는 두 독립 검토(slop 규칙 스캔 · Codex)가 가장 범용적 표면으로 지목한 「feature-card 템플릿」 형태. 타일 면·fit 캐스케이드·축약은 HOME-21 그대로. 424 타일 전부 control 14/20 fit(nav-fit 0). ⚠️ 옛 320 fit 셀(고정 폭 60 · auto-resize NONE)이 남아 둘째 줄이 상자 밖으로 그려졌다 — 텍스트 auto-resize 는 항상 다시 확인 | 2026-09-04 |
| 랭킹 행 구분선 | **≥720 목록에 1px `border/divider`**(`RANK-33`, Codex 지적) — 1,216 행에서 이름(좌)과 값(우단) 사이가 멀고 행 경계가 없어 옆 행 값과 붙여 읽힘. 얼룩말·폭 768 축소안 기각. 24 목록 120 구분선, compact 불변 | 2026-09-04 |
| P16 카드 장식 | **C-5**(Z1 ⑲) — 골드 링 2겹(24/12%) + 좌하단 대각 밴드 5% + 우상단 골드 **선형** 기운 5% + 그라디언트 강화(`#1a1a26→#0b0b10→#241b0c`). 사용자 요청(1.0 처럼 그라디언트·도형으로 더 예쁘게). 미디어 블러·워터마크·밴드·원형 면 안과 C-1~~4(더 강한 링/기운) 폐기 — C-4 는 링이 순위 값을 가려 사용자가 C-5 선택. 슬롭 규칙 회피: 방사형 글로우·격자·그라디언트 텍스트 미사용. 빈 상태 카드의 오른쪽 여백을 채우는 효과 | 2026-09-04 |
| P15 요약 그룹 제목 | **`component-title 16/24`**(Z1 ⑳, PRIV-36 개정, 2026-09-04) — PRIV-36 이 emphasis 제목을 승인했지만 문서 24 의 `emphasis-label` 역할(SM 제목·선택 세그먼트) 밖이었고 승인 당시 예외를 기록하지 않은 내 누락. Codex 2차 검증이 잡음. P8 소제목 정정과 같은 논리. **역할을 확장하는 승인은 반드시 문서 24 에 경계 있는 예외로 적는다** | 2026-09-04 |
| 난이도 표기 통일 | **B · 결과·재생 행도 P13 색 글자 문법**(Z1 ㉒, `DISC-45`, 2026-09-05) — C6 Music List/Grid/Chart grouped 셀 · P3 행 · P4 카드 · P6 재생 행의 레벨 숫자를 `difficulty/text-*` + `metric-value 14/20` 로, 12×2 색 바 제거. 비색 단서는 위치·순서. C6 24 셀 + 인스턴스 736 셀 + P6 370 텍스트. 문서 24 DU-01 소비자 확장 절 · 브리프 04 | 2026-09-05 |
| 대표 프레임 실제 자켓 | **C (b) 채택**(Z1 ㉒, `DISC-46`, 2026-09-06) — P1 hero 96 · P3 390 목록/그리드·1280 · P4 390 기본/서명됨·1280 · P6 390/1280 · P13 390/1280 의 **기본 프레임 Light+Dark 에만** `public/bg` 4장(64px)을 image fill. 빈 슬롯 아이콘·`border/empty-slot` 숨김. 상태·로케일·320 프레임은 자리표시자 유지. 픽스처이지 계약 변경 아님. 이미지 해시는 파일에 남으므로 재적용 시 base64 재전송 불필요 | 2026-09-06 |
| 비평 86 소소 3건 | **D-1·D-3·D-7 (b) 채택**(Z1 ㉒) — D-1 랭킹 1~~3위 = `emphasis-label/latin`(Text Style, 600) · D-3 공지 월별 헤더 = **section-title 20/28**(문서 14 개정 · Codex 적용분 유지 — 내 보드는 emphasis-label 로 그렸으나 역할 밖이라 적용본이 권위) · D-7 오락실 결과 행 64 썸네일(`ARC-DV-02`, 목록+Wide list column 96행, 행 92/88 내용 유도). D-2·D-4·D-6 은 내 수치가 틀려 철회, D-5 는 A-2 와 동일 폐기 | 2026-09-06 |
| 병렬 세션 적용분 확인 | ⚠️ **사용자가 Codex 로 같은 결정을 이미 파일에 적용해 뒀을 수 있다**(2026-09-06 발견) — D-1 이 P5 에 raw `IBM Plex Sans Bold 700` 으로 231곳 적용돼 Text Style 이 비어 있었고 문서 24 에 「700 예외」 까지 적혀 있었다. 규칙 B(raw 금지)를 어긴 상태라 `emphasis-label/latin` 으로 재바인딩하고 문서 24·25·08·71 에 정정 절을 남겼다. **결정을 적용하기 전에 대상 노드의 현재 상태와 최신 커밋의 문서 개정 절을 먼저 읽는다** — 내가 만든 적 없는 변경이 있으면 덮어쓰지 말고 대조한다 | 2026-09-06 |
| 프로필 공유 다이얼로그 | **B′ · 현행 3버튼 구성**(Z1 ㉓, `PROF-52`, 2026-09-06) — `이미지 저장`(Primary) · `클립보드 복사` · `공유`(시스템 공유 시트, 미지원 시 문서 09 폴백 X Web Intent). 문서 09 배치안(공유 시트 주 액션)은 폐기 — 사용자가 「현행이 낫다, 대신 X 공유가 아닌 공유로」. 그릇 = C8 `ProfileShareDialog` 신설(Compact 334 / Wide 768 × 5 상태) + P6 12 프레임. 카드 오류는 StatusMessage + 액션 행 `다시 시도`(인라인 액션은 286 에서 문장과 안 맞음). **Wide 768 승인**(Z1 ㉔) — 334(기존 다이얼로그)·640(Home bounded) 비교판에서 사용자가 768 선택. 문서 24 「Overlay width」 신설: Compact 334 / Wide 768(미디어 미리보기가 있는 다이얼로그만 reading 측정폭 재사용). 내 추천은 640 이었다 | 2026-09-06 |
| 프로필 겹친 표면 덜어내기 | **Codex V05 채택 + 사용자 수정 2건**(Z1 ㉖, `PROF-53`~~`55`, 2026-09-06) — ① 정체성·본문 4구역 카드 면·인셋 제거, 스택 48, 경쟁 요약만 카드 유지(Light 1.06 이라 카드 위계는 Dark 전용이었음) ② **모드 선택기는 카드 밖**(프로필 전체 적용 · 1.0 배치) ③ **보조 메타 4종은 각각 칩**(surface/surface · radius 8 · inset 12 · HUG · wrap 8 = HOME-22 언어). 등폭 2열은 390 에서 179 > 175 로 안 맞아 HUG 채택. 54 프레임 · 칩 214 · 판정 막대 largest-remainder 재유도 · 1280 그리드 행 재유도(열 gap 16 유지 — 32 로 바꾸면 1232 > 1216 넘침). **다른 페이지로 자동 확장 금지** — P1 레이더 면은 대비용(Q1-A) | 2026-09-06 |
| Music Detail 헤더 액션 | **정체성 행 오른쪽 HUG · Intermediate 이상**(Z1 ㉕, `MDET-91`) — 1280 에서 버튼이 각 604 로 늘어난 건 Compact FILL 규칙이 유도 없이 넓어진 것. C6 `MusicEntityHeader` 에 `Layout` 속성 신설(Compact 4 + Wide 4), 인스턴스 24 교체, 768 은 아티스트 한 줄 말줄임(박스 408). 헤더 152 → 96. 사용자 지적으로 시작 | 2026-09-06 |
| 검정 급수 배지 | **B 티어 플레이트**(Z1 ㉗, `PROF-56`, C3 `ExamBadge` 신설) — 24 플레이트(inset 4 · gap 8 · 좌 패딩 0 은 밴드 bleed 의도) · 좌 6px 밴드 = 급 색(10~~8 회색 · 7~~5 파랑 · 4~3 보라 · 2 빨강 · 1 금) · 모드 이니셜 B/R(`nav-fit-current/latin`) + `N급`(`metadata`), 글자는 `content/default`. 토큰 6개 신설(원시값 0): `surface/plate`(L E9E9E9 / D 2C2C2C — 양 모드에서 보이는 배지 면) · `exam/tier-*` 5. 10안 비교(필·육각·띠·메달·리본·방패·인장·건반·링)에서 사용자 선택. ⚠️ 그림은 이니셜이 급 색이었지만 12px 텍스트로는 어느 면에서도 4.5 를 못 넘어 **밴드만 색**으로 적용 — 색 글자로 하려면 새 원시값이 필요 | 2026-09-06 |

> **폰트 교체 전제 복구됨** — 이전에는 텍스트가 Text Style 을 참조하지 않아
> `font/family/ko·ja·latin` 3개를 바꿔도 전파되지 않았다. 소급 적용으로 이제 동작한다.
