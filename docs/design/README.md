# NosLog 디자인 가이드

2026-09-14 새로 씀. **짧게 유지한다** — 규칙이 바뀌면 해당 절을 고치고, 이유는
[결정 기록](./decisions.md)에 한 줄 남긴다. 다른 문서에 겹치는 규칙을 쓰지 않는다.

## 0. 이 문서의 자리

- 우선순위: ① 사용자의 최신 결정 ② 이 가이드 ③ 코드 —
  [토큰](../../app/styles/tokens.css) · [공용 스타일](../../app/styles/foundation.css) · `components/ui`.
- **Figma(NosLog v2.0.0), 옛 브리프 · 핸드오프 · PDF, 레거시 NOSTORY 는 참고하지 않는다.**
- 동작과 데이터 의미는 [기능 규칙](./product-rules.md), 과정과 보존 경계는 루트 `AGENTS.md`.
- 규칙을 바꾸면 같은 작업에서 **가이드 → 코드 → 테스트 기대값** 순서로 맞춘다.

## 1. 기초값

### 테마 · 글꼴

- **다크만.** `NEXT_PUBLIC_ENABLE_THEME_SWITCHING=false` 동안 라이트 스타일은 남겨 두되 새로 만들지 않는다.
- Pretendard JP Variable 1.3.9 한 파일을 자체 호스팅(분할 · 서브셋 · 전역 preload 없음). 한국어에만 `ss05`.

### 색 — 역할 이름으로만 쓴다 (다크 값)

| 역할                                                  | 값                       | 쓰는 곳                                                |
| ----------------------------------------------------- | ------------------------ | ------------------------------------------------------ |
| `surface/canvas` · `sunken`                           | #111                     | 페이지 바탕, 트랙 · 자켓 빈자리                        |
| `surface/surface`                                     | #1b1b1b                  | 카드, 입력칸, 보조 버튼 면                             |
| `surface/raised` · `overlay`                          | #222                     | 선택된 세그먼트 · 칩, 떠 있는 창                       |
| `content/default` · `subdued`                         | #dbdbdb · #afafaf        | 본문 · 보조 글자                                       |
| `content/pending`                                     | #8a8a8a                  | 요청 중이라 잠시 보류된 값 (흐리게 하는 opacity 대신)  |
| `border/divider` · `subtle` · `default`               | #323232 · #323232 · #444 | 구분선 · 세그먼트 트랙 · 카드 경계                     |
| `border/input` · `strong`                             | #717171 · #8a8a8a        | 입력 · 보조 버튼 · 태그 경계 · 올림 · 선택 경계        |
| `border/overlay`                                      | #444                     | 떠 있는 창 경계                                        |
| `interaction/hover` · `selected-pressed` · `menu-set` | #fff 6% · 10% · 10%      | 올림 · 누름 · 목록에서 선택된 행 (면 위에 겹침)        |
| `primary/default` · `on-primary`                      | #dbdbdb · #111           | 주 버튼 면 · 그 위 글자                                |
| `content/interactive`                                 | #f2f2f2                  | 제목 링크 · 동작 칸 글자                               |
| `content/disabled` · `interaction/disabled-content`   | #444                     | 비활성 글자 · 값 없는 줄                               |
| `surface/scrim`                                       | #000 60%                 | 대화상자 뒤 가림막                                     |
| `focus/ring`                                          | #fff                     | 포커스 선                                              |
| `stage/canvas`                                        | #070910                  | 채보 무대 바탕(낙하형 · 전체 악보, 테마 무관)          |
| `hand/left` · `hand/right`                            | #4fc8dc · #e85f5d        | 채보 왼손 · 오른손(노트 · 범례 · 눌린 건반 = 흰색 45%) |

- 도메인 색은 이미 있는 것만 쓴다: 난이도 글자 `difficulty/text-*`(다크 값은 난이도 판 면 `raised` 위에서도 4.5 를 넘게 한 칸 밝힘 — 2026-09-18), 판정 `judgement/*`, 카테고리
  `category/*`, 달성 `achievement/*`, 검정 `exam/*`, 상태 `feedback/*`, 차트 `local-data/*`.
  **새 색이 필요해 보이면 만들지 말고 묻는다.**
- 상호작용 면(올림 · 누름 · 선택 · 비활성)은 그 상태에만 칠한다. 평소 요소에 칠하지 않는다.
- 올림 · 누름 · 선택은 **제 면 위에 겹친다**(2026-09-23 I2) — 면 색을 바꾸지 않고
  `--nl-state-layer: var(--nl-interaction-…)` + `background-image: linear-gradient(var(--nl-state-layer), var(--nl-state-layer))`.
  그래서 어느 면(바탕 · 카드 · 떠 있는 창) 위에서도 한 단계씩 밝아진다. `--nl-state-layer` 는 `@property` 로 등록돼 ① 전환을 탄다.
  예외 = 배경 그림을 이미 쓰는 요소(투표 줄의 구분선)와 `::selection` 은 `background-color` 로 칠한다.
  공용 부품의 올림 규칙이 면 색을 바꾸면(예 `.nl-button:hover`) 변형 쪽에서 제 면 색을 다시 적는다.
- 글자 대비 4.5:1, 글자 아닌 단서 3:1. 색만으로 상태를 전달하지 않는다.

### 글자 — 14종 (`tokens.css` 의 클래스). 이 밖의 조합 금지

| 클래스                                   | 크기/줄 · 굵기            | 쓰는 곳                                            |
| ---------------------------------------- | ------------------------- | -------------------------------------------------- |
| `nl-display`                             | 40/48 · 700               | 따로 승인된 곳만(프로필 머리 아바타 첫 글자)       |
| `nl-page-title`                          | 24/32 · 700               | 페이지 제목 — 1056 이상에서는 모든 페이지 32/40    |
| `nl-section-title`                       | 20/28 · 600               | 페이지의 구역 제목                                 |
| `nl-component-title` · `nl-entity-title` | 16/24 · 600               | 상자 · 창 · 필터 그룹 제목 / 목록 · 카드 항목 이름 |
| `nl-body` · `nl-body-secondary`          | 16/24 · 400 · 14/20 · 400 | 본문 · 입력된 값 / 보조 설명                       |
| `nl-body-reading`                        | 16/28 · 400               | 공지 · 이벤트 글 본문만(읽기 본문, 2026-09-26 H1)  |
| `nl-control`                             | 14/20 · 500               | 버튼 · 탭 · 라벨                                   |
| `nl-emphasis-label`                      | 14/20 · 600               | 상태 메시지 제목, 선택된 항목, 격자 카드 항목 이름 |
| `nl-metadata`                            | 12/16 · 400               | 3차 정보 (12 미만 금지)                            |
| `nl-metric-display` · `nl-metric-value`  | 32/40 · 700 · 14/20 · 500 | 비교하는 숫자 — **이 둘만 tabular**                |

- 굵기는 400 · 500 · 600 · 700 만. 입력칸 글자는 모든 폭에서 16 (그보다 작으면 iOS 가 확대한다).
- **예외를 만들지 않는다** — 표의 조합 밖(예: 16 굵게 + 고정폭, 12 굵게)은 쓰지 않고, 고정폭 숫자도 `metric-display` · `metric-value` 밖에서는 쓰지 않는다. 다른 절이 조합을 따로 정하지 않는다(2026-09-18).

### 간격 · 모서리 · 높이 · 아이콘

- **간격:** 0 · 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64. `2` 는 아이콘 · 배지 · 차트 안 광학 보정에만.
  **역할별 값(2026-09-15):** 구역 사이 32 (모든 폭) · **페이지 제목(24/32) → 설명 · 메타 글줄 8, 제목(또는 설명 끝) → 첫 내용 16** ·
  구역 제목(20) → 첫 내용 12 · 상자 제목(16) → 내용 8 ·
  카드 안쪽 16 (목록 항목 카드 12 · 촘촘 격자 카드 8 · 1056 이상 큰 카드 24) · 떨어진 목록 줄 사이 8 (구분선 목록 0) · 문단 사이 16 ·
  선으로 나눈 묶음(필터 그룹 · 개인정보 요약)은 선 위아래가 같다 — 레일 · 팝오버 16, 전체 화면 창 24, 개인정보 요약 12.
- **모서리:** 누르는 상자 `radius/field` 8 (입력 · 셀렉트 · 검색 · 버튼 · 트리거 · 칩 · 적용 조건 · 페이지 번호 ·
  패턴 투표 버튼) / 상자 안 항목 · 체크박스 · 태그 `radius/control` 4 / 카드 `container` 8 / 떠 있는 창 `overlay` 10 / 원 `full`.
- **높이 — 두 단계(2026-09-15):**
  **큰 L** `--nl-control-height` 44 (1056 이상 40) — 입력 · 셀렉트 · 검색 · 필터 트리거 · 2단 탭 · 페이지 주 액션
  (하단 바 · 레일 · 목록 카드 액션 · 폼 제출 · 폰 대화상자 버튼 · 설정 폼 행의 버튼). 672 이상 대화상자 발 버튼은 M(2026-09-26).
  **중간 M** `--nl-control-height-compact` 36 (1056 이상 32) — 카드 안 버튼(오락실 기체) · 상자 없는 인라인 보조
  (팝오버 머리 줄 「초기화」 · 적용 조건 「전체 지우기」 · 「영역 초기화」 · 알림 안 「다시 시도」 · 시트 요약 줄 정렬) · 필터 칩 · 적용 조건 토큰.
  전체 화면 창 발의 「초기화」 는 주 액션과 같은 줄이라 L.
  버튼은 `size="sm"` 이 M. 아이콘 버튼은 자기 줄의 단계. **같은 줄은 한 단계.**
  폼 제출이라도 상자 안 인라인 칸의 버튼(의견 작성 칸 「취소 · 의견 작성」, 평가 잠금 카드 「로그인」)은 M — 이 둘 밖에 예외 없음.
  폼 안 **첨부 버튼**은 입력칸 역할이라 L — 붙인 뒤 들어서는 파일 줄(L)과 높이가 같아 자리가 튀지 않는다(2026-09-18).
- **선 두께 — 두 단계(2026-09-22):** **기본** `--nl-stroke-default` 1 — 구분선 · 경계(카드 · 입력 · 태그 · 떠 있는 창) · 포커스 선 · 칸 사이 선(gap + divider 바탕).
  **강조** `--nl-stroke-strong` 2 — 선택 밑줄(밑줄 탭 · 수치 전환) · 강조 테두리(서열 달성 · 체크박스 · 라디오 링 · 빙고 선택) · 위쪽 진행 막대.
  CSS 는 선 두께를 숫자로 쓰지 않는다 — `border` · `outline` · `box-shadow` 선 · 음수 오프셋(`calc(var(--nl-stroke-default) * -1)`) · 테두리를 빼는 여백 보정도 이 토큰. 선이 아닌 1 · 2(모서리 · 광학 보정 · 그림자 흐림)는 해당 없음.
  1단 탭 48. 펼쳐지는 메뉴 · 목록 행(셀렉트 · 정렬 · 검정 목록 · 「⋯」 메뉴 · 레일 체크 목록 · 설정 오락실 목록)은 L,
  헤더 메뉴(≡) 이동 줄만 48(1056 이상 40).
- **아이콘:** Lucide 실제 모양. 20 기본 · 16 글자 옆 보조 · 24 강조와 창 머리 가장자리 닫기.

### 포커스 · 상태 · 움직임

- **포커스:** 컨트롤 자신의 1px 흰 선 — 평소 경계가 있으면 색만 바꾸고, 없으면 포커스에서만 그린다.
  입력 · 셀렉트 · 검색 · 버튼은 여기에 바깥 4px 후광(`--nl-state-halo`)을 더한다.
- 목록(셀렉트 목록 · 정렬 메뉴) 항목의 흰 선은 **키보드로 움직일 때만** — 마지막 입력을
  `lib/inputModality.ts` 가 `<html data-input>` 에 적는다.
- **올림(hover) 면은 포인터 장치에서만** — 모든 `:hover` 규칙은 `@media (hover: hover)` 안에(터치는 탭한 뒤 hover 가 남아
  선택된 것처럼 보인다). `:not(:hover)` 로 평소 색을 정하는 규칙은 밖에 둔다.
- **누름**은 색 단계만. 크기 줄이기(scale) 없음. **문서에 없는 움직임을 넣지 않는다** — 상태는 즉시 바뀐다.
- **바쁨(busy)은 비활성이 아니다:** 버튼은 스피너 16 + 진행형 라벨. 화면 · 영역 로딩은 아래 「로딩」.
- **떠 있는 창**(목록 · 메뉴 · 팝오버 · 대화상자 · 차트 툴팁): 면 #222 · 1px `border/overlay` · 모서리 10 · `--nl-overlay-shadow`.
  예외 없음 — 헤더 메뉴 패널만 헤더에 붙는 위쪽 경계를 긋지 않는다. 미디어 위 판(자켓 카테고리 · 서열 카드 난이도)은 떠 있는 창이 아니다.

### 움직임 (2026-09-19)

근거: 디자인 가이드 6곳(Material 3 · Carbon · Apple HIG · Fluent 2 · Atlassian · Primer) + 실제 서비스 19곳 실측(데이터 · 랭킹 7 · 제품 12).
NosLog 는 데이터 서비스라 움직임은 **상태 · 위치 · 로딩**에만 쓰고 장식은 없다. **아래 목록에 없는 곳은 즉시 바뀐다.**

- **값은 `tokens.css` 의 움직임 토큰만** — CSS 에 숫자 시간 · `cubic-bezier` · `ease`/`linear` 를 직접 쓰지 않는다(인라인 · 부품별 값 금지, 테스트가 막는다). 새 움직임이 필요하면 이 목록과 토큰을 먼저 고친다.

| 토큰                                                        | 값                                                    | 쓰는 곳                                                       |
| ----------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------- |
| `--nl-motion-duration-state`                                | 100ms                                                 | ① 올림 · 누름 색                                              |
| `--nl-motion-duration-popup`                                | 150ms                                                 | ③ 떠 있는 메뉴 열림                                           |
| `--nl-motion-duration-overlay-in` · `-out`                  | 200 · 150ms                                           | ④ 대화상자 · ⑤ 토스트 나타남 · 사라짐                         |
| `--nl-motion-duration-move`                                 | 200ms                                                 | ⑫ 헤더 숨김 · 오락실 시트                                     |
| `--nl-motion-duration-chart-in` · `-change`                 | 800 · 300ms                                           | ⑯ 그래프 나타남 · 값 바뀜                                     |
| `--nl-motion-loop-spin` · `-pulse`                          | 800 · 1600ms                                          | ⑩ 스피너 한 바퀴 · ⑪ 스켈레톤 한 번                           |
| `--nl-ease-enter` · `--nl-ease-exit`                        | `cubic-bezier(0.4, 1, 0.6, 1)` · `(0.6, 0, 0.8, 0.6)` | 들어올 때 감속 · 나갈 때 가속(Atlassian practical 과 같은 값) |
| `--nl-ease-spin` · `--nl-ease-pulse`                        | `linear` · `ease-in-out`                              | 스피너 · 스켈레톤                                             |
| `--nl-motion-distance`                                      | 8px                                                   | 대화상자 · 토스트가 떠오르는 거리                             |
| `--nl-motion-delay-progress` · `--nl-motion-delay-skeleton` | 100 · 300ms                                           | 이 안에 끝나면 위쪽 진행 막대 · 스켈레톤을 띄우지 않음        |
| `--nl-motion-duration-progress`                             | 1500ms                                                | 위쪽 진행 막대가 80% 까지 차오름                              |

- **① 올림 · 누름 색** — 누를 수 있는 모든 것(`a` · `button` · `summary` · `label` 로 만든 줄 · 옵션 · 메뉴 항목 · 탭, 누를 줄을 품고 올림 면을 칠하는 카드 — 펼침 카드 · 오락실 결과, 올림 면을 `::before` 로 그리는 버튼)에 같은 값. 배경 · 글자색 · 겹침 층(`--nl-state-layer`)만 움직이고 포커스 선 · 경계는 즉시. 겹침 층은 켜질 때만 움직이고 꺼질 때는 즉시(층이 올림 규칙에만 있어서).
- **③ 떠 있는 메뉴**(셀렉트 · 정렬 · ⋯ · 필터 팝오버 · 빙고 칸 · 지도 범례 · ≡ 메뉴 · 폰 ≡ 뒤 어둠 · 업적 배지 카드 — 2026-09-25 A1) — 열 때 페이드만, **닫을 때 즉시**
  (셀렉트 · ≡ 메뉴는 닫는 순간 떼어져 닫힘 움직임을 줄 수 없어 모두 즉시로 맞춤).
- **④ 대화상자 · 전체 화면 창** — 나타날 때 페이드 + 8 떠오름, 사라질 때 페이드만. 떠오름은 `translate` 속성(가운데 맞춤 `transform` 과 따로).
- **⑤ 토스트** — 나타날 때 페이드 + 8 올라옴, 사라질 때 페이드만(sonner 기본 400ms · 전체 높이 이동을 토큰으로 덮음).
- **⑩ 스피너** 회전 · **⑪ 스켈레톤** 불투명 1 → 0.5 깜빡임.
- **⑫ 위치 이동** — 헤더: 나타날 때 감속 · 숨을 때 가속. 오락실 시트: 화면 안 이동이라 감속. 손가락으로 끄는 동안은 움직임 없이 따라간다.
- **⑯ 그래프**(막대 목록 · 누적 막대 · 선 그래프, 2026-09-19 A) — 나타날 때 막대 · 선이 왼쪽부터 드러나고(바닥선 · 틀은 그대로),
  같은 막대 · 같은 점의 값이 바뀌면 새 값으로 옮겨 간다. 선 그래프는 점 구성(날짜)이 바뀌면 다시 드러난다. SVG 선의 옮겨 감은 `lib/motion` 이 같은 토큰을 읽어 움직인다.
  그래프를 그리는 곳은 공용 부품이든 페이지(프로필 랭크 분포 · 판정 요약 · 관리자 대시보드)든, 값이 바뀌는 진행 막대(빙고 상세 진행 · 악곡 머리 그레이드)든 공용 클래스 두 개만 붙인다. 빙고 목록 카드의 완료 막대는 값이 바뀌지 않고 여러 장이 한꺼번에 움직여 장식이라 움직임 없음 — 드러남 `nl-chart-reveal` · 막대 길이 옮겨 감 `nl-chart-bar`.
- **⑮ 지도** — 카드를 누르면 핀으로 미끄러진다(카카오맵 `panTo`). 목록 스크롤은 즉시.
- **즉시(움직임 없음)** — ② 누름 크기 줄이기 · ⑥ 탭 밑줄 · ⑦ 구역 펼침 · ⑧ 툴팁(그래프 · 용어 도움말 — 같은 부품이라도 업적 배지 카드는 ③) · ⑨ 체크박스 · 라디오 · 토글 · ⑬ 스크롤 연출 · ⑭ 페이지 이동.
- **동작 줄이기** — 기기 설정이 켜지면 `tokens.css` 한 곳에서 시간 토큰을 모두 0 으로(스피너 한 바퀴만 남김 — 멈추면 고장처럼 보인다). 지도는 JS 라 같은 설정을 읽어 곧바로 옮긴다.
- 손대지 않는 것: 채보 캔버스, 카카오맵 안 타일 페이드(라이브러리), 공식 사이트 위 북마클릿 창.

### 로딩 (2026-09-19)

근거: 디자인 가이드 7곳 · 실서비스 15곳 실측 · 우리 사이트 측정 — [로딩 시안 · 조사](./drafts/loading/). 로딩 표시는 **한 영역에 하나**.

- **사이트 안 이동 = 위쪽 진행 막대**(`NavigationProgress`, 일반 · 관리자 셸에 각각 하나) — 사이트 안 링크를 누르고 `--nl-motion-delay-progress` 안에 끝나지 않으면
  맨 위 2px `primary/default` 막대가 80% 까지 차오르고, 새 화면이 오면 끝까지 채우고 사라진다. **이전 화면은 그대로**(흐리게 하지 않음).
  다른 처리가 클릭을 막아 이동하지 않으면(저장 안 한 변경 안내 등) 뜨지 않는다. 동작 줄이기에서도 막대는 남는다.
- **스켈레톤 = 실제 부품의 틀 · 클래스 그대로** — 컨테이너 · 간격 · 안쪽 여백 · 높이가 저절로 같다. 제목 · 탭 · 필터 · 라벨처럼 **고정된 부분은 실제 글자 · 실제 부품**,
  이름 · 수치 · 사진 · 그래프 · 목록 자리만 스켈레톤. 부품마다 `…Skeleton` 을 부품 곁에 둔다(결과 카드 · 채보 묶음 · 기록 줄 · 순위 줄 · 곡 순위 줄 · 서열 카드 · 빙고 카드 · 의견 줄 · 막대 목록 · 수치 칸 · 수치 띠).
    - 글자 자리 `SkeletonText` — 그 글자 스타일 클래스를 받아 줄 높이(1lh)를 차지하고 막대는 글자 크기(1em). 폭은 네 단계(25 · 50 · 75 · 100%)만,
      짧은 값(순위 · 숫자)은 보이지 않는 표본 글자(「0,000」) 폭만큼 — 기준선 정렬 줄에서도 실제 글자와 같은 선에 선다. 값 칸에는 실제와 같은 글자 스타일 클래스를 붙여 줄 높이를 맞춘다.
    - 채움 = `border/subtle` 하나(어느 바탕 · 면 위에서도 보인다) · 모서리는 실제 부품 것 · 펄스(⑪). 입력 · 셀렉트 자리 = 컨트롤 L 높이 · 모서리 8.
    - **스켈레톤 안에 스피너를 넣지 않는다.** 모양을 모르는 한 덩어리(지도 등)만 그 자리 가운데 스피너 20.
- **시간** — 첫 진입(서버가 그림)은 지연 없이 스켈레톤. 화면 안 다시 불러오기는 `nl-loading-delay`(`--nl-motion-delay-skeleton`) 동안 숨겨 짧으면 번쩍이지 않게.
  최소 표시 시간은 두지 않는다. 다시 불러오는 동안 이전 내용이 있으면 그 내용을 흐린 색(pending)으로 두고 스켈레톤 · 글자를 끼우지 않는다.
  단 **값의 뜻이 바뀌는 전환**(지표 Grd ↔ 레이팅 · 기간 · 모드 Basic ↔ Recital — 성장 추이 · 베스트 성과)은 이전 내용을 두지 않는다. 처음 `--nl-motion-delay-skeleton` 동안만 흐린 색, 넘으면 같은 틀의 스켈레톤(그래프 면 · 요약 값 · 목록 줄, 라벨은 실제 글자). 같은 지표에 줄만 이어 붙는 「더 보기」 는 해당 없음.
  관리자 대시보드의 기간 전환도 같은 규칙 — 제목 · 기간 버튼 · 지표와 패널 라벨은 실제 부품으로 두고, 수치 · 그래프 · 목록만 현재 패널과 행 수가 같은 스켈레톤으로 바꾼다. 기간이 붙는 보조문구는 고정 안내를 두고 기간 값만 글자 스켈레톤으로 바꾼다. 같은 기간의 지표 전환은 이미 받은 값으로 즉시 바꾸므로 로딩을 만들지 않고, 주소·뒤로 가기와 기존 그래프 움직임을 유지한다.
- **안내** — 「…불러오는 중」 문장은 화면 읽기에만(`LoadingStatus`, 한 번), 로딩 중인 영역은 `aria-busy`. 보이는 곳에 로딩 문장 한 줄만 두지 않는다.
- 실제와 같은지 확인: 요청을 늦춰 스켈레톤일 때와 내용이 들어온 뒤 요소 위치 · 크기를 재서 비교한다(폭 390 · 1280).

## 2. 셸과 레이아웃

- 헤더 · 본문 · 푸터 내용은 `max-width: 1200px` 가운데. 바탕과 구분선은 화면 전체.
- 전역 좌우 여백 16 (672 미만) / 24 / 32 (1056 이상). 페이지가 여백을 한 번 더 넣지 않는다.
  1200 이상에서 셸 1200 = 안쪽 내용 1136, 1440 에서 양옆 120 씩 빈다.
- **페이지 모드** — viewport media query 로만, CSS 와 JS 가 같은 기준:

    | 모드         | 폭        |                          |
    | ------------ | --------- | ------------------------ |
    | Compact      | 672 미만  | 모바일 구성              |
    | Intermediate | 672–1055  | 중간 구성                |
    | Wide         | 1056 이상 | 넓은 구성 (셸 폭은 같음) |

- Wide 레일(악곡 · 서열표 · 검정 · 개인정보 목차 · 설정) = 내용 폭의 1/4, `calc((100% - 3 * 16px) / 4)` (셸 1200 에서 272).
  헤더 60. 동기화 페이지 구역 펼침은 다른 구역처럼 사이 32 (붙여 쌓지 않는다).
- 악곡 격자는 결과 영역 container query — 2열 288 · 3열 536 · 4열 672 이상(상한 4열, 셸 1200 에서 카드 200), 카드 최소 156.
  촘촘한 격자(`view=dense`)는 단계마다 한 열 더. 5열 단계는 두지 않는다(Intermediate 끝 → Wide 레일 등장에서 열 수가 뒤집힘).
- 홈의 검색 · 목적지 · 공지 · 공식 소식은 안쪽 최대 640. 공지 바로 아래 「진행 중인 이벤트」(진행 중일 때만 · 끝나는 순 2장, 2026-09-18 H1) = 늘 보인다(이벤트로 가는 길). 큰 배너 카드(2.4 : 1 · 모서리 8 · 사이 8) 위에 제목 `entity-title` · 기간 `body-secondary` 흰 글자(`content/on-media`), 글자 자리는 `surface/media-scrim` 층(위 32 는 투명에서 번짐) + 같은 모양으로 옅어지는 흐림 12. 진행 중인 이벤트가 없으면 같은 크기 빈 카드(면 surface · 경계 divider) 가운데 안내 `body-secondary` →12→ 「이벤트 글쓰기」 보조 M(E1). 홈 공지 줄은 목록과 같은 제목 16/600 · 고정 줄은 면 없이 「고정」 테두리 태그(N2). 푸터는 840 이상 한 줄, 그 아래는 가운데로 쌓는다.
  공식 소식은 준비된 원문·번역만 읽고 수집·번역을 기다리지 않는다(2026-09-19). 수집 동작은 [기능 규칙](./product-rules.md#official-x-news-2026-09-19)을 따른다.
- 부품이 자기 폭을 재서 글자를 맞추는 것은 되지만, 그걸로 페이지 모드를 따로 바꾸지 않는다.
- 페이지 구역 제목은 `section-title`, 상자 안 제목은 `component-title`.
- **제목 줄** `nl-heading-row`: 제목 · ⓘ · 메타가 열 8 · 행 4로 줄바꿈. 오른쪽 끝 이동은 **제목 링크** `nl-heading-link`
  (글자 `control` + 꺾쇠 16 · 간격 4 · 광학 −4 · 누르는 영역 위아래 +12). 로그아웃 사용자에게 구역 행동을 권할 때도 이 자리를 쓴다.
  같은 자리의 **동작**(「선택 해제」 · 「다시 시도」)은 제목 링크가 아니라 고스트 M 버튼 — 줄 높이를 늘리지 않게 위아래, 잉크가 오른쪽 선에 서게 오른쪽 16 을 당긴다(2026-09-18).

## 3. 부품

- **버튼** (`ActionButton`, `Button appearance="foundation"`): 주 = #dbdbdb 면, 보조 = 입력칸과 같은 면 · 경계,
  고스트, 위험 = 빨간 테두리, 채운 위험 = **되돌릴 수 없는 삭제를 확정하는 버튼 전부**(탈퇴 · 공지 삭제 · 기록 삭제 · 의견 · 투표 삭제 — 확인 창의 확정 버튼은 예외 없이 채운다, 2026-09-18). 높이는 L(기본) 또는 M(`size="sm"`), 패딩 7/15(M 은 세로 0).
  **페이지에서 크기를 덮어쓰지 않는다** — 칸이 좁으면 열 수를 줄인다(주 액션 한 줄 + 보조 2열).
  로그인이 필요한 동작은 `data-locked`(비활성처럼 보이되 이유를 보여 줌).
- **아이콘 버튼** `IconButton`: 공용 버튼 `size="icon"` — 컨트롤 높이 정사각 · 모서리 8 · `label` 필수 · 기본 고스트. M 줄에서는 `size="compact"`(공용 버튼 `icon-sm` = 컴팩트 높이 정사각, 폰 36 · 1056 이상 32).
  아이콘 20, 창 머리 가장자리 닫기만 24. 패딩 있는 칸의 첫 · 마지막 자식이면 `(컨트롤 − 20) / 2` 만큼 밖으로
  당겨 잉크를 패딩에 맞춘다. 사진 위 넘김 · 지도 범례처럼 **미디어 위 버튼**만 자기 모양을 쓴다.
- **입력 · 셀렉트 · 검색:** 면 `surface` + 1px `border/input`, 올림은 경계만 `strong`. 폼 `Select` 는 Radix
  (`value` · `onValueChange` · `options`, react-hook-form 은 `Controller`).
  **필드 안 잉크(2026-09-16): 첫 · 마지막 잉크는 안쪽 12, 잉크 사이 8** — 입력칸 글자 12(11+경계 1) · 셀렉트 글자 12 · 꺾쇠 20 은 오른쪽 12 ·
  검색 돋보기 20 은 왼쪽 12 → 글자 40 · 범위 셀렉트 라벨 12(바깥 4 + 버튼 8) · 지우기 × 20 은 오른쪽 12(중심 22 = 셀렉트 꺾쇠).
  글자는 모든 폭 16/400, 플레이스홀더 `content/subdued`, 아이콘 20(범위 셀렉트 꺾쇠 16).
  검색창 안 범위 셀렉트 · 지우기는 **4px 안쪽 층** — 면만 M 정사각(모서리 4)이고 잉크는 위 12 규칙을 따른다. 누르는 높이는 검색창 그대로.
- **목록**(셀렉트 목록 · `SortMenu` · 좁은 셀렉트): 떠 있는 창 규격, 판 안쪽 8 · 트리거에서 8, 행 = 컨트롤 높이(폰 44 · 데스크톱 40) · 안쪽 8/12 · 4 간격 ·
  `body-secondary` 14/400, 선택 = `menu-set` 면 + 600 + 오른쪽 체크 16. **소제목(`metadata` subdued)은 그룹이 둘 이상일 때만** —
  정렬 하나뿐이면 트리거 라벨이 이미 이름이라 소제목 없이 행부터(2026-09-16). **목록 정렬은 항상 `SortMenu` 하나** (두 가지뿐이어도).
  **정렬 트리거는 모든 폭에서 고스트**(2026-09-23 T2) — 폭에 따라 높이만 그 줄의 단계를 따른다(결과 줄 M · Wide 툴바 L). 테두리 · 면은 「필터」(창을 여는 트리거)만 갖는다.
  **정렬 메뉴는 1단 배타 선택** — 오름 · 내림은 따로 고르지 않고 항목에 녹인다(「레벨 높은 순 / 레벨 낮은 순」, 2026-09-23 D2. 실측 16곳 중 방향을 따로 고르는 곳은 2곳뿐).
  방향이 없는 기준(관련도)은 한 줄, 종속 선택이 있는 기준(레벨 순의 난이도)만 고른 뒤 아래 절이 붙는다.
- **필터:** 트리거 「필터」 + 적용 그룹 수 배지(`nl-filter-count` — 원 20 · 면 `raised` · `metadata`, 개수 전용이라 태그와 다른 모양). 그릇 = Compact 전체 화면 창 / 악곡 · 서열표 Wide 레일 /
  오락실 672 이상 팝오버 (바텀시트 없음, 빙고는 상태 칩 한 줄이라 필터 그릇 없음 — 2026-09-22). 그룹은 `FilterGroup`(제목 + 오른쪽 슬롯 + 사이 1px 구분선).
  짧은 열거는 창 · 팝오버에서 `FilterChips`(창 밖에서 늘 보이는 한 줄은 `row` — 줄바꿈 없이 가로 스크롤), 레일에서는 체크박스 목록 `SelectionList`. 적용 조건은 `AppliedTokens`.
  주소가 곧 상태인 페이지 필터(공지 분류)는 같은 모양의 링크 칩 `FilterChipLinks`(선택 = `aria-current`).
- **마크다운 입력** `MarkdownEditor`(2026-09-18 E2 — GitHub 구조를 우리 규격으로): 상자 = 입력칸 경계 · 모서리 8 · 면 surface, 포커스는 입력칸처럼 상자 경계 + 후광.
  머리 줄 = 가라앉은 면 + 아래 divider · 왼쪽 「쓰기 / 미리보기」 2단 탭 · 오른쪽 서식 아이콘 버튼(모두 컨트롤 L — 한 줄 한 단계, 넘치면 가로 스크롤).
  서식 버튼은 렌더러가 그리는 것만 — **소제목(메뉴: 큰 `##` · 작은 `###`) · 굵게 · 기울임 · 취소선 · 인용 · 코드 · 표 · 구분선 · 목록 · 번호 목록 · 링크 · 이미지**(2026-09-23 T-c). 서식은 접지 않고 넘치면 가로 스크롤(Zulip · Discourse · GitLab 확인).
  단축키는 Cmd/Ctrl + B · I(굵게 · 기울임)와 **Cmd/Ctrl + Enter = 주 동작(게시 · 게시 요청)**, 이름표에 적지 않고 툴팁에만. 버튼을 눌러도 입력칸 포커스 · 고른 자리를 지킨다.
  이미지는 버튼 · 붙여 넣기 · 끌어다 놓기로 올리고(올리는 동안 자리 표시 → `![](주소)`), 도움말 줄에 넣는 방법을 적는다(2026-09-18). 입력 = 본문 16/24 · 안쪽 8/12 · **처음 8줄 · 35줄까지 늘고 그 뒤 스크롤**(2026-09-23 C).
  **미리보기(2026-09-23 P2)** = 미리보기 탭을 누르면 **전체 화면 창**에 공개 화면 그대로(같은 렌더러 · 읽기 폭 768 · 제목 · 분류까지), 발에 「이어 쓰기」. 상자 안에서 글만 바뀌지 않는다.
  칸 아래 도움말 줄 = 왼쪽 안내(오류면 오류) · 오른쪽 「쓴 수 / 최대」는 **한도의 75% 를 넘을 때만**(넘겨도 입력은 막지 않고 게시만 막는다). 오류는 칸을 벗어날 때 검사한다.
  **글 안 서식 값(2026-09-23 M1 · K1 · B1 · TB1 · Q1 · I2)**: 고정폭은 기기 글꼴(`--nl-font-family-mono`, 파일 추가 없음) · 글 안 코드 = 면 `raised` · 모서리 4 · 안쪽 2/4 · 14 · 코드 덩이 = 면 `sunken` · 경계 `subtle` · 모서리 8 · 안쪽 12 · 넘치면 그 상자만 가로 스크롤 · 표 = 칸 안쪽 8/12 · 줄 아래 divider · 머리 `subdued` 600 · 좁으면 표만 가로 스크롤(열을 줄이지 않는다) · 인용 = 왼쪽 2px `border/strong` + 안쪽 12 + 글자 `subdued`.
  **기울임만 예외로 합성을 켠다**(`font-synthesis: style`, 본문 `em` 한 곳) — 우리 글꼴에 기울임 글꼴이 없어 합성을 끄면 글자가 전혀 바뀌지 않는다(2026-09-23 I2). 다른 곳의 합성 끄기 규칙은 그대로.
  **아래 고정 바(2026-09-23 L2)** `nl-form-bar` = 왼쪽 묶음(나가기 · 삭제 · 공개 페이지) · 오른쪽 묶음(저장 상태 → 임시저장 → 주 버튼). 저장 상태는 「저장 안 한 변경 · 저장 중 · 저장됨 · 저장 실패」 `role="status"`.
  **전체 화면 창(`FullScreenDialog`)**: 머리 60(8+44+8) = 왼쪽 16 제목 `component-title` + 오른쪽 닫기 24(패딩 8) · 아래 1px 선. 본문 안쪽 24/16, 그룹 제목 → 내용 8,
  그룹 사이 선 위아래 24. **발 = 왼쪽 「초기화」 고스트 L(`onReset`, 잉크를 16 선에 맞춰 당김) + 남는 폭의 「결과 N개 보기」** · 위 1px 선 · 안쪽 16 (2026-09-16).
  팝오버는 즉시 적용 + 머리 줄 제목 | 「초기화」 M (발이 없어 자리가 다르다).
- **밑줄 탭** `nl-tabs`: 안 선택 subdued 500 · 선택 default 600 + 2px 밑줄 · 올림은 글자만.
  **1단** `AreaTabs`(페이지 구역) = 높이 48 · 내용 폭 전체 1px 구분선 · **항목이 줄 폭을 똑같이 나눔**(안쪽 8, 글자가 칸보다 길면
  그 칸만 글자만큼) · 모든 폭 탭, 넘치면 가로 스크롤.
  **2단** `MetricSwitch`(같은 목록의 기준) = 구분선 없음 · 컨트롤 높이. 1단은 tablist, 2단은 `aria-pressed` 버튼 묶음.
- **세그먼트** `SegmentedControl`: 트랙 `sunken` + 1px `subtle`, 선택 = `raised` + 1px `strong` + 600.
  보기 방식 · 모드 · 난이도 · 한 차트 안에서 보는 지표 전환(최근 판정 추이)에만. 아이콘만 있는 세그먼트는 항목 40×36.
  1056 이상에서 세로로 쌓는 변형 `nl-segments--wide-vertical`(행 = 컨트롤 높이 · 이름 왼쪽 · 값 오른쪽, 항목 안쪽 `8 − 1` — 트랙 경계 1 + 트랙 안쪽 3 을 더해 잉크가 옆 칸과 같은 12 에 선다, foundation.css).
  **난이도 세그먼트**(악곡 상세): 항목 = 이름 + 난이도 색 레벨 숫자(`metric-value`, 사이 8, 항목 안쪽 4) · L 한 줄 ·
  375 미만은 약칭 N · H · EX · R · 1056+ 왼쪽 열에서는 세로 변형(행 = 컨트롤 높이 · 이름 왼쪽 · 숫자 오른쪽 · 잉크 12).
- **수치 상자 아래 동작 줄** `StatStrip footer` + `nl-action-group`(foundation.css): 수치 띠에 딸린 이동 링크 2–3개를 같은 상자(모서리 8) 아래 칸에 둔다.
  칸 = 같은 폭 · 사이 1px 선(수치 띠와 같은 gap + divider) · 컨트롤 L 높이 · `surface` 면 · 글자 `emphasis-label` `content/interactive` + 끝 › 16 `subdued`(사이 4) — › 가 누를 곳 표시.
  hover `interaction/hover` · 누름 `selected-pressed`. 없는 동작도 칸은 두고 글자 · › 모두 `interaction/disabled-content`, 반응 없음(2026-09-17).
- **수치 띠** `StatStrip`: 핵심 숫자 몇 개(최대 5)를 한 줄에 — 값 `component-title` 조합(16/24 · 600) 위, 라벨 `metadata` subdued 아래.
  칸 면 `surface` · 칸 안쪽 8 · 칸 최소 64 · 칸 사이 1px `border/divider`(좁으면 줄바꿈해도 선이 맞음) · 모서리 8.
  **값이 없는 칸은 만들지 않는다**(「—」 칸 없음) — 예외는 악곡 머리 「공식 레벨」 칸 하나(값이 없으면 흐린 「—」, 2026-09-18). 큰 숫자(32) 대신 이것을 쓴다 — 새 글자 단계를 만들지 않는다(2026-09-16).
  새 값을 받는 중(`pending`)이면 이전 값을 그대로 두고 값 · 그레이드 숫자 · 진행 막대를 `content/pending` 으로. 1056 이상 세로 목록 변형 `nl-stat-strip--wide-list`(칸 = 컨트롤 높이 · 라벨 왼쪽 · 값 오른쪽 · 안쪽 12).
  값 색 `tone`(2026-09-17): 순위 = 순위표와 같음(1 · 2 · 3 = `exam-tier` top · high · mid, 그 밖 subdued) · 점수 = 점수 목표 색(950k 이상 S 노랑 · 990k 이상 살구 · 100만 Pianist 분홍, FC 무시) ·
  Grd · 레이팅 = 구간 색(2026-09-19 B2 · R1, 두 모드 · 두 값 같은 기준): 6,000 아래 기본 · 6,000 판정 NEAR 파랑 · 6,500 GOOD 하늘 · 7,000 S 노랑 · 7,500 990k 살구 · 8,000 Pianist 분홍(`--nl-grade-band-1…5`, 기준값 `GRADE_BAND_CUTS`). **같은 Grd · 레이팅 숫자는 어디서나 같은 색**(보이는 숫자 기준) — 프로필 수치 띠(`MetricSummary tone`) · 유저 랭킹 값 · 성장 추이 요약 줄(시작 · 현재, 변화는 차이라 없음) · 공유 카드(이미지라 `STAT_TONE_DARK_HEX` 다크 값, 테스트가 토큰과 대조). 값 색을 받는 칸은 `nl-toned` + `data-tone`. 순위 칸은 위 순위 색(공유 카드 포함).
  등급 = S 노랑 · P 분홍(등급에 990k 없음) · A+ · A · B 는 게임 등급 아이콘 색을 있는 토큰으로 빌린 별칭 `--nl-score-grade-*`(A+ = `difficulty/text-expert` · A = `difficulty/expert` · B = `local-data/single`, 새 값 없음 — 2026-09-26 C2a, 막대 색에만) · 최대 콤보 = FC(fc_type 2 이상 · Pianist)면 `achievement/full-combo` 초록. 점수 목표 색은 `--nl-score-goal-*`(서열표 테두리와 같은 값).
- **선 그래프** `LineChart`: 값 표는 툴팁과 겹치면 화면 읽기용으로만 둔다(성장 추이 · 최근 판정 추이 · 프로필).
  플레이 기록 툴팁(성장 추이 — 악곡 상세 · 프로필 — · 최근 판정 추이)은 osu! 처럼 **값 위 · 「오늘 / 어제 / N일 전」 아래**, 날짜 줄은 두지 않는다. 성장 추이 값은 「956,666점」 · 「5,723.05 Grd」 만(라벨 없음). 라벨이 있는 툴팁(최근 판정 추이)은 **선 이름만 그 선의 색**, 값은 기본 글자색(2026-09-19 T1 — 라이트 SLOW 글자는 대비 3.3 : 1 로 기준 미달, 새 색을 만들지 않아 그대로). 툴팁은 플롯 폭 안에 머문다. 점이 하나여도 틀 · 축을 그대로 두고 점 하나를 가운데 찍는다(점 표시를 끈 차트도).
  「기록이 1건이라…」 같은 설명 문장이나 값만 따로 크게 쓰지 않는다. 기록이 없으면 틀 유지 옵션으로 빈 틀 + 가운데 짧은 「기록 없음」.
  **기록 추이 그래프(성장 추이 · 최근 판정 추이, 2026-09-19)** = 면 없음(구역 바탕 위에 바로) · 가로선은 맨 밑 바닥선 하나(`baselineOnly`, 빈 틀도) · 성장 추이(악곡 상세 · 프로필 같은 모양) = 세로축 숫자 없음 · 점은 가리킨 자리만(점이 하나면 그 점) · 날짜 「2026. 09. 10.」 처음과 끝 · 선은 주황 `local-data/categorical-2`(`tone="growth"`, osu! 계열), 최근 판정 추이 선은 판정 색 그대로. 점을 끈 그래프(`showPoints={false}`)도 가리킨 자리에는 선 색으로 채운 점(반지름 4)을 찍는다(P1).
- **누적 막대** `StackedBar`: 전체 중 부분(판정 비율)을 한 줄로, 줄을 겹쳐 나 · 평균 비교. 줄 = 라벨 `metadata`(최소 40, 모든 줄에 라벨이 없으면 라벨 열 없이 막대만 — 프로필 판정 요약) ·
  트랙 8(모서리 4 · `raised`), 줄 사이 8 · 열 사이 12, 조각 사이 틈 없음 · 빗금 없음. 줄들이 한 격자의 열을 나눠 써(subgrid) 라벨 폭이 달라도 막대 시작이 맞는다.
  줄 오른쪽 값 칸(`metric-value`, 선택 — 한 줄이라도 주면 모든 줄에, 2026-09-26). 정확한 값은 곁의 목록이 말하고 막대는 화면 읽기에서 숨김.
- **끝 페이드** `nl-fade-end`: 한 줄로 자른 글자의 오른쪽 32를 흐리게(말줄임표 대신). 눌러서 전체를 볼 수 있는 곳에서만 쓴다.
- **막대 목록** `BarList`: 항목 몇 개의 크기 비교(레이더 대신). 줄 = 라벨 `control` · 트랙 8(모서리 4 · `raised`) ·
  값 `metric-value` 오른쪽, 줄 사이 8 · 열 사이 12, 채움 = 차트 단일 색. 값이 없는 항목은 막대 없이 값 자리 「—」, 모두 없어도 빈 트랙을 그린다(문장으로 바꾸지 않음).
- **체크박스 · 라디오 · 슬라이더:** 체크박스 20 · 모서리 4 · 테두리 2 `strong` · 체크 선 2. 라디오 원 20 · 테두리 2 →
  선택 시 `primary` 링 + 점 8. 슬라이더 = 높이 24 · 트랙 4 `border/default`(양끝 12 안쪽) · 채운 점 20(타겟 24) · 올림 · 끌기 · 포커스에 후광 6.
  올림 변화는 슬라이더에만.
- **숫자 척도** `ScalePicker`(0~4 같은 짧은 척도 · 슬라이더 대신): 숫자 버튼 한 줄 = 컨트롤 M 정사각 · 모서리 8 · 1px `border/default` ·
  숫자 `control` 흐림, 고른 값 = `primary` 면 + `on-primary` 600. 값을 고른 줄의 나머지 숫자 = `content/pending` · 1px `border/divider`(빈 줄은 그대로). 사이 8(375 미만 4). 같은 값을 다시 누르면 해제. 라디오 묶음(방향키 이동 · 고른 값만 탭 순서).
  양끝 말 라벨은 두지 않는다.
- **태그 · 배지** `nl-tag`(2026-09-23 재설계 — 다크 실측 35곳 · 배지 100여 개 · 가이드 8종): 높이 24 · 모서리 4 · `metadata` 12/**600** ·
  **면 `surface/raised` + 1px `border/input`** · 글자 `content/default`. 무채색 테두리만 두면 형태 단서가 1.94 : 1 이라 덩어리로 보이지 않고(실측에서 그렇게 두는 곳 0곳),
  면만 두면 카드(1.08) · 팝오버(1.00) 위에서 사라진다 — 테두리가 어디서나 3 : 1 을 넘긴다(캔버스 3.87 · 카드 3.53 · 팝오버 3.26).
  **분류 · 표시는 색을 쓰지 않는다**(공지 분류 · 「고정」 · 미션 종류). 강조형 `--strong` = 반전(면 `content/default` · 글자 `surface/canvas`), 한 줄에 하나까지.
  **상태형 `--status`** 는 같은 면에 **글자만 알림 표시색**(성공 6.52 · 정보 4.54 · 경고 10.15 · 위험 4.81) — 면 색을 상태마다 만들지 않아 분류와 무게가 같고, 배지 글자가 늘 상태 이름을 말한다(색 단독 금지).
  누르지 않는 라벨은 이 하나 — 공지 분류(제목 위 「분류 · 날짜」 줄) · 빙고 미션 종류 · 상태. 알약 모양 라벨은 쓰지 않는다.
  **카테고리 변형**(악곡 상세 머리, 자켓 밖): 크기는 태그 그대로(24 · 좌우 8 · 모서리 4), 면은 `surface/overlay` · 글자는 카테고리 색 · 경계 없음 — 자켓 위 판과 같은 색이라 목록 카드와 이어진다(2026-09-18).
- **대화상자** `ModalDialog`(2026-09-26 A, GitHub · Primer 3칸 창): **머리 · 본문 · 발** 세 칸, 면 · 경계 · 그림자 · 모서리 10 은 떠 있는 창 그대로 · 스크림 `surface/scrim`.
  크기 `size` = small 440 · **medium 560(기본)** · large 768(읽기 폭), 화면 가장자리 16 은 늘 비움 · 높이 상한 화면 − 48.
  머리 = 제목 `component-title` 왼쪽 + 오른쪽 닫기(`IconButton` 컴팩트 · 아이콘 20), 안쪽 12 · 오른쪽 16 · 왼쪽 24, 아래 구분선.
  본문 = 여기만 스크롤 · 안쪽 24 · 세로 간격 16 · 설명은 `body-secondary` 흐림. 발 = 위 구분선 · 안쪽 16 · 24 · 버튼 M · **오른쪽 끝에 「보조 → 주」**.
  **확인 창** `variant="confirm"`(되돌릴 수 없는 결정 · 경고) = small · 제목 `section-title` 20 · 닫기 없음 · 구분선 없음 · 한 판(안쪽 24 · 사이 12).
  **창 안 내용(2026-09-26 — 종류별)**: 입력 칸이 있으면 확인 창이 아니라 기본 창(회원 탈퇴 = 지워지는 것 면 상자(surface · 모서리 container · 안쪽 16) → 「1. Discord로 다시 인증」 → 「2. 확인 글자 입력」(인증 전 잠김) · 버튼은 발).
  **정보 창** = 설명 한 줄 → 소제목 `metadata` →8→ **구분선 줄 목록** `nl-info-rows`(줄 = 이름 칸(가장 긴 이름 폭) · 값/설명 칸, 줄 높이 44 · 위아래 8 · 좌우 4, `--value` = 값 오른쪽 끝) — 패턴 경향 기준(단계 · 항목) · 서열표 안내(달성 기준 · 업데이트) · 곡 정보 제안(지금 값).
  **고르기 창** = 고른 것 칩 한 줄 `nl-pick-slots`(칩 규격 · 「n · 이름」 + 빼기 × 24, 빈 칸은 빈자리 경계) → 돋보기 검색 칸 → **고르기 줄 목록** `nl-pick-list`(최대 min(360, 45dvh) 스크롤 · 줄 = 이름 `emphasis-label` + 보조 `metadata` · 고른 줄 오른쪽 체크 · 줄 사이 구분선, 줄은 줄어들지 않음) — 선호 오락실(검색어가 없으면 「지금 선택」 묶음) · 고정 기록 · 프로필 업적(격자는 그대로) · 검정 채보 선택 · 관리자 과제곡 검색.
  **폼 창** = 버튼은 모두 발(본문 안 버튼 없음). 로그아웃으로 열면 **로그인 안내** `LoginPrompt`(자물쇠 원 40 · 제목 `emphasis-label` 「로그인하면 ~할 수 있어요」 · 한 줄 설명, 발 = 취소 · 로그인, 창 폭 medium) — 신고 · 피드백 · 오락실 제보. 선택지가 적고 늘지 않을 곳은 라디오(신고 사유 4개), 늘어날 곳은 셀렉트(피드백 종류 — 사용자). 라디오 묶음 오류는 묶음 아래 한 줄(`FormField` 오류와 같은 모양).
  **672 미만** = 입력 칸(입력 · 글상자 · 셀렉트 · 콤보 · 슬라이더)이 있는 창은 전체 화면(전체 화면 창과 같은 머리 60 · 발, 버튼 L 등폭), 그 밖은 아래 시트
  (폭 가득 · 위 모서리만 둥글게 · 버튼 L 폭 가득 · 주 버튼이 위) — 둘 다 아래에서 올라옴(⑫). 페이지에서 창 규격을 덮어쓰지 않는다.
  `sheet` 선택(2026-09-26) = 1055 이하까지 아래 시트 — 채보 뷰어 「더보기」. 이미지 내보내기 창(`nl-image-share`, 프로필 카드 · 서열표)은 **1055 이하 전체 화면**(2026-09-26 A — Instagram 프로필 카드 · Telegram QR · 카카오톡 QR: 바탕 `surface/canvas` · 머리 60 · 이미지는 화면 폭 끝까지(모서리 없음) · 본문 안쪽 16 · 버튼 줄은 아래 고정(위 구분선 · 안쪽 16 + 안전 영역, 주 「이미지 저장」 위 · 「복사 · 공유」 아래 등폭 L) · 아래에서 올라옴 ⑫), 1056 이상은 가운데 창(3칸 창 이전 모양 그대로 — 단 본문만 스크롤하고 버튼 줄은 창 아래 고정, 2026-09-26 A: 서열표처럼 내용이 길어도 저장 버튼 · 제목 · 닫기가 늘 보임).
  필터 · 제보처럼 긴 창은 672 미만에서 전체 화면(공용 `FullScreenDialog` — 머리 × · 아래 고정 주 액션, 닫기는 머리 × 가 맡아 발에 취소를 두지 않는다). 기준 · 도움말 설명 창(「패턴 경향 기준」)은 짧은 정의 목록이라 Compact.
  **피드백 · 오류 제보 창**(2026-09-18): 종류(오류 제보 / 제안 · 의견 — 셀렉트 L, 2026-09-19) → 내용(입력칸 5줄 · 자리표시자 없음 · 아래 도움말 줄 = 왼쪽 종류별 안내 · 오른쪽 「쓴 수 / 1,000」) →
  **창 안 두 탭(2026-09-18 F1)** 「새로 쓰기 · 내 제보」 1단 밑줄 탭 `AreaTabs`(로그인했을 때 · 2026-09-19 세그먼트에서 바꿈) →24→ 폼 또는 내 제보 목록. 내 제보 줄 = 상태 태그(접수 = 경고 점 · 처리 완료 = 성공 점) · 날짜 · 종류 →4→ 내용 `body-secondary` →8→ 답변 상자(면 surface · 모서리 8 · 안쪽 12 · 「NosLog 답변 · 날짜」 `metadata` →4→ 답변). 새 답변 = 메뉴 「피드백」 줄 · 탭 글자 뒤 점 8(위험 표시색 · 접근 이름 「새 답변」, `nl-unread-dot`). 내 제보 발은 「닫기」 하나.
  이미지 「(선택)」(보조 L 버튼 → 붙이면 같은 높이의 파일 줄 = 썸네일(줄 높이 − 8) · 이름 · 크기 · 지우기 ×, Carbon 파일 목록 모양) · 보내기는 늘 켜 두고 비면 칸 아래 오류 · 보낸 뒤는 창 안에 체크 · 한 줄 · 설명 + 닫기.
- **상태 메시지** `StatusMessage`, 결과 상태 `ResultState`. 느린 교체는 기존 값을 `content/pending` 으로.
- **판정 이름:** 항상 `◆JUST · JUST · GOOD · NEAR · MISS` — `components/ui/judgementMarker.tsx` 의 `judgementLabels`.
  빙고 문구 용어는 `BingoTermHelp` 가 대소문자 무관으로 찾아 이 표기로 보여 준다.
  악곡 상세(판정 분석 · 최근 판정 추이 · 최근 플레이)는 NEAR 를 보여 주지 않는다 — 게임에서 거의 켜지 않는 옵션(합계 · 비율 계산에는 넣음, 2026-09-18).
- **펼침** `Disclosure`: 화살표는 줄 끝 ∨ · 열리면 ∧(180°) · 20 · 들여쓰기 0. 두 단계 —
  **구역** = 요약 줄 48 · 제목은 자리대로(페이지 구역 `section-title`, 상자 안 `component-title`) · 내용까지 16 /
  **보조** `compact` = 컨트롤 높이 · `control` · 내용까지 8.
  흐름 속 펼침은 면 없이 연달아 쌓이면 사이 1px 구분선(열린 쪽은 내용 아래 16), 혼자 떠 있는 상자는 `card`.
  올림 = 줄 전체에 `interaction/hover` — 면 없는 줄은 좌우 12 를 넓혀 칠하고(글자 선은 그대로), 카드는 카드 면.
  목록 한 줄을 여는 줄(서열 투표 · 최근 플레이 · 영업시간 · 오락실 카드)은 줄 크기는 그대로 두고
  화살표 `nl-disclosure__chevron` 과 올림만 따른다. 열린 내용은 보조 펼침처럼 위 8 · 아래 8, 들여쓰기는 그 줄의
  값 열에 맞춘다(정보 행 = 아이콘 20 + 12). 프로필 「전체 랭크 보기」 는 24 글자 버튼 그대로.
- **도움말:** 두 모양, 둘 다 마우스 올림 · 포커스 · 탭으로 연다.
  **용어 뜻** `TermHelp` = 점선 밑줄 글자 + ? 16(간격 4) → 위쪽 작은 창(떠 있는 창 규격 · 최대 280 · 여백 12 ·
  제목 `control` + 설명 `body-secondary`). 오락실 기체 상태 이유만 줄 끝을 맞추려고 ? 없이 점선.
  **기준 설명** = 라벨 뒤 ⓘ 16(`nl-info-trigger`, 누르는 영역은 컨트롤 높이 · 자리 차지 없음) → 같은 작은 창.
  설명이 긴 곳(「패턴 경향 기준」)만 ⓘ 를 누르면 대화상자. 모두가 알아야 할 기준은 도움말로 숨기지 않고 늘 보이는 글로 둔다.

### 아직 결정 전 — 부품 인벤토리에서 갈라진 곳 (결정하면 위로 올린다)

## 4. 페이지별 규칙 (코드가 틀어지지 않게 최소한만)

- **악곡 상세(2026-09-16 재설계):** **머리** = 자켓 80(위 고정, 카테고리 배지 없음) · 카테고리 배지(태그 카테고리 변형 · 제목 바로 위 왼쪽, 배지 → 제목 4) · 제목(페이지 제목 고정 · 글자 줄이기 없음 · 한 줄, 넘치면 끝 32 페이드) + 제목 글자 끝 8 뒤 내 등급 공식 아이콘 = 제목 줄 높이(폰 32 · 1056 이상 40, 제목 줄 세로 가운데 · 기록 있을 때만) → 번역 제목(설정에서 켰을 때, 본문 보조 · 기본 글자색 · 제목에 붙임 · 한 줄 · 등급 자리 앞에서 끝 페이드) →8→ 아티스트(흐린 본문 보조 · 전체 폭 한 줄 페이드) — 잘린 글자가 있으면 글자 묶음을 눌러 모두 펼침(펼치면 단어 단위 줄바꿈 · 등급 아이콘 숨김) · 글자 묶음이 80보다 짧으면 자켓 옆 세로 가운데 →16→ 난이도 세그먼트
  →12→ 수치 띠(선택한 난이도의 공식 레벨(라벨 「공식 레벨」, 칸 값 = 서열 값 그라데이션 색) + 공개 서열 값 Basic S · 990k · Pianist · Recital, 미등재는 칸 없음) (같은 상자 위 칸 = 「그레이드」: 위 줄 왼쪽 「그레이드」 control, 오른쪽 내 Grd 정수 `page-title` 조합(24/32 · 700) · 소수 `component-title` 조합(16/24 · 600)(글자색 = 막대의 그 위치 색, 최대값을 모르면 기본색) + 「/ 최대 Grd」 흐린 본문 보조 · 최대값 Pianist 색(공식 레벨 · 노트 수가 없으면 흐린 「/ —」), 기록 없음 「—」 · 로그아웃 「로그인하고 보기 ›」 →8→ 진행 막대(막대 목록 트랙 8 · 0 → 최대 Grd · 채움 = 등급 그라데이션 B·B+ 판정 NEAR 파랑 0–40.3% → A·A+ `danger-marker` 빨강 49.5–59.6% → S 73.7% → 990k 81.9% → Pianist 87.9–100%, oklch 섞기 · 최대값을 모르면 빈 트랙) · 안쪽 8/12, 아래 12. 아래 칸 = 「채보 보기 › · 플레이 영상 ›」 늘 두 칸, 없는 쪽은 비활성 글자색). 공식 레벨 칸은 값이 없으면 흐린 「—」 →16→ **1단 탭 4개 「개요 · 내 기록 · 랭킹 · 평가」**(주소 `tab` 은 `detail · record · ranking · tier` 그대로).
  **개요** = 패턴 경향(가로 막대 5줄 · 막대 색은 평균의 가까운 정수 0–4에 `--nl-pattern-level-*`(기존 토큰 별칭: 데이터 파랑 · 판정 NEAR · 판정 JUST · 난이도 Hard · 난이도 Expert) · 제목 줄에 ⓘ(「패턴 경향 기준」 창 = 기준 문장 「모든 곡 대비 세기 · 곡 안 비중 아님」 · 단계 0–4 이름(없음 · 조금 나옴 · 뚜렷하게 나옴 · 심한 편 · 악곡의 특색일 정도)과 막대 단계 색 네모 8(모서리 없음 — 검정 「합격까지」 범례와 같은 막대 범례) · 공개 규칙 · 축 정의, 평가 탭 패턴 투표와 같은 창) · 평가 인원 · 오른쪽 끝 「평가하기 ›」 = 구역 제목 링크 규칙, 평가 1명부터 평균, 평가 없는 축은
  빈 막대 · 「—」) → 내 기록 요약 띠(최고 점수 · 내 순위/참가자(순위가 없으면 칸 없음) · 플레이, 로그아웃은 제목 줄 오른쪽 끝 「로그인하고 보기 ›」) → 채보 정보(BPM · 노트 수 · 길이 · 수록일은 **비어 있어도 줄을 둔다** — 값 자리 흐린 「—」(`content/pending`) + 밑줄 글자 「추가」(로그아웃은 로그인으로), 값이 있으면 끝에 ⋯(고스트 아이콘 · 위아래 · 오른쪽 당김, 메뉴 「수정 제안」, 로그아웃은 없음), 내 제안이 대기 중이면 상태 태그 「검토 중」(경고) — 모두 줄 높이를 늘리지 않게 당긴다. 제안 창 = Compact(지금 값 · 값 칸(칸마다 도움말) · 근거 세그먼트 「영상 · 공식 사이트 · 직접 확인」 · 주소 · 설명 · 「운영자가 확인한 뒤 반영됩니다」 · 주 버튼 「제안하기」), 2026-09-23. 그 밖의 줄은 값 있는 행만,
  해금 조건은 단계마다 한 줄 「(→ )이벤트 이름 · 별가루 N」, 이름은 보는 언어로 · 숫자는 이 난이도 몫만) → 서열 변경 이력(구역 펼침 안에 **날짜 묶음 하나 = 카드 하나**(공용 펼침 `card` · 요약 줄 컨트롤 높이 · 안쪽 16 · 카드 사이 8), 모두 기본 접힘 —
  구역 · 날짜 · 항목 세 단계를 들여쓰기 대신 상자로 가른다, 2026-09-18). **내 기록**(한 열) = 최고 기록 · 누적 요약은 **구역 펼침(기본 펼침)** 안 수치 띠(등급은 화면 표기 A+ · B+ — DB 코드 A2 · B2 를 그대로 쓰지 않는다), 판정 분석(맨 위 · 기본 펼침) · 성장 추이(그래프만, 표는 화면 읽기용 · 기본 접힘 · 점이 없으면 빈 틀) · 최근 플레이(카드 선 목록 · 기본 접힘 · 줄 = 날짜 · 등급 16 · 점수, 펼친 내용 = 위아래 8 · 들여쓰기 없음 · 면 없음 · 숫자 행만 metric-value · Grd 소수 둘째 자리 · 「최고 점수 대비」)는 구역 펼침 세 줄(사이 구분선), 추이는 선 그래프 규칙, 판정 분석은 소구역(요약 · 판정 · 음표별 성공률 · 최근 판정 추이) 사이 24 · 소제목(component-title) → 내용 8, 「요약」(「나」 · 「평균」 누적 막대 두 줄 늘, 값이 없는 줄은 `content/disabled` 한 줄 · 기준 인원은 제목 줄 오른쪽 끝) → 「판정」 목록(색 점이 범례) · 「음표별 성공률」, 유사 Grd 평균은 켜기 없이 값 아래 메타(평균 없는 항목은 줄 없음) · 최근 판정 추이 지표 = ◆JUST · MISS · FAST/SLOW 세그먼트, 선 색은 판정 색(◆JUST 분홍 · MISS `danger-marker` 빨강 · FAST 판정 NEAR 파랑 · SLOW 주황), 비면 빈 틀 + 가운데 「기록 없음」.
  **내 기록이 없어도 다섯 구역 틀 · 펼침 기본값 그대로**(2026-09-19 E1) — 내 값 자리만 흐린 「—」(누적 수치도 「0」 아닌 「—」, 판정 줄은 비율 없이 「—」 하나), 요약 「나」 는 비활성 한 줄, 유사 Grd 평균은 이 곡 기록 대신 유저 Grd 기준으로 그대로 보인다. 성장 추이 빈 틀 · 최근 플레이 「최근 플레이 없음」.
  **랭킹** = 제목도 카드 면도 없이 점수 자 → 32 → 순위표(탭 → 점수 자 24). **「참가자 N명」 · 구역 제목 「순위」 · 「내 순위 N / M」 글자는 두지 않는다**(2026-09-18 사용자: 쓸데없는 글자가 화면을 난잡하게 한다).
  구역 이름은 낭독용 sr-only 제목으로만 남기고, 내 순위는 순위표 안 내 행(면 + 낭독용 「내 순위」)이 말한다. 점수 자(가로 점수 925k(폰 폭은 좁힘 · 하위 5%가 낮으면 거기까지) → 100만 · divider 선 하나, 아래 라벨 950k · 990k · Pianist(**각 목표 색** — S 노랑 · 990k 살구 · Pianist 분홍, 2026-09-18 · 좁아 겹치면 990k 라벨만 뺌).
  **기록 0명이면 점수 자 · 페이지 넘김 없이 순위표 머리 줄 → 줄 한 칸(48 · 아래 divider) 가운데 흐린 body-secondary 「아직 플레이된 기록이 없습니다.」**(2026-09-19 사용자 — 점수 자는 1명 이상일 때부터).
  **면 · 제목 없음** · 좌우 안쪽 12 로 자 시작선을 순위표 줄 안쪽과 맞춘다 · 높이 72 = 핀 줄 48 + 자→라벨 8 + 라벨 줄 16, 끝 점수 핀은 자 안쪽으로 16 당긴다(점은 제자리)).
  **세로축(분포)을 두지 않는다** — 참가자가 대부분 한 자릿수라 밀도 곡선은 없는 봉우리를 그리고, 핀 높이가 실력으로 오독됐다(유저 지적). 참가자는 한 명도 빠짐없이 자 위의 점 6(나 = 8 · 판정 JUST 노랑, 그 밖 `local-data/bucket-5`)으로 남고,
  그 위에 사진 핀을 **한 줄로** 세운다(높이에는 뜻이 없다) +
  사진 핀: 원 24(나 28) + 2 테두리(strong · 나는 판정 JUST 노랑), 끝이 자 위 점수 자리를 가리킴. 30명 이하면 모두, 넘으면 상위 3명 + 나.
  52 보다 가까운 핀은 점수가 가장 높은 대표 + 오른쪽 위 「+N」(**사진끼리 겹치지 않는다 — 나도 같은 거리 규칙을 따르고, 내가 낀 핀은 대표가 나가 되고 나머지가 「+N」 으로 접힌다**)(필터 개수 배지 모양 원 20 · raised · metadata 12 + 1px `border/default` — 늘 핀 오른쪽 위).
  **핀을 누르면 창을 열지 않고 바로 그 사람(묶음이면 대표) 순위 줄로 간다**(2026-09-18 — 사람이 늘수록 창이 불편해진다). 순위로 = 그 줄이 있는 페이지로 넘기고 줄을 가운데로 · 이름 링크에 포커스
  「순위」 구역(제목 줄 없음 — 로그아웃일 때만 목록 위 오른쪽 끝에 「로그인하고 내 순위 확인 ›」 한 줄) → 순위표(유저 랭킹 페이지와 공용 — 여기서 고치지 않는다.
  다만 곡 순위 · 핀 창 목록의 **점수 칸은 만점 「1,000,000」 폭(7ch + 쉼표 2)으로 고정**해 등급 아이콘이 행마다 한 줄에 서게 한다 — 여섯 자리 점수는 등급과 한 글자만큼 벌어진다).
  **평가** = 세 구역(패턴 투표 · 서열 투표 · 의견) 모두 **구역 펼침(기본 펼침)** — 제목 줄이 펼침 줄이 되므로 그 줄에 있던 저장 상태 · 로그인 링크는 펼친 내용 첫 줄 오른쪽 끝으로 내린다(2026-09-18).
  패턴 투표(카드 면 없는 구분선 목록 · 줄 = 이름 + 숫자 척도 0–4 · 펼친 내용 첫 줄 왼쪽에 기준 한 줄 「모든 곡 기준 · 0 없음 – 4 많음」(metadata · 숫자만 semibold 기본 글자색) + ⓘ(개요와 같은 기준 창) — 잠겨 있어도 보이고, 좁으면 저장 상태가 다음 줄 오른쪽으로. 이 줄은 공용 제목 줄(「선택 해제」 는 잉크를 오른쪽 선에 당김) · 높이 28 고정(버튼이 나타나도 목록이 밀리지 않게) →8→ 척도(2026-09-23) · 저장 버튼 없음 · 누르면 바로 저장(연달아 누르면 0.4초 모아 한 번) · 제목 줄 오른쪽 끝 한 곳에 저장 중 · 저장됨(2초) · 실패 + 다시 시도, 평소엔 「선택 해제」(확인창) · 평가할 수 없으면 목록 흐림 5px + 가운데 떠 있는 창 규격 카드(이유 한 문장 · 로그아웃이면 주 버튼 M 「로그인」) — 흐림은 입력 영역 잠금에만 — 의견 칸 없음) → 서열 투표(카드 하나 안 선 목록 「Basic · S … 값 · 투표 N명 ⌄」,
  줄 = 컨트롤 높이 · 여는 줄 올림/누름 = 줄 전체 면(좌우 12 넓힘, 구분선은 글자 선 안쪽), 평균이 없으면 값 자리 「—」 + 인원(가운뎃점 없이 사이 8), 펼친 내용은 같은 카드 안 위 8 · 아래 8 · 사이 8 = 분포(안쪽 상자 · 제목 없음) · 「값 선택 셀렉트(내 투표가 기본값) + 투표 저장」 한 줄(한 단계 더 누르지 않음). 설명 문장 없음 — 자격이 없으면 입력을 두지 않고,
  펼칠 것이 없으면 누르지 않는 줄(⌄ 없음), 로그아웃은 제목 줄 오른쪽 끝 「로그인하고 투표하기 ›」 제목 링크 · 결과는 그대로 공개, 블러로 가리지 않음) → **의견**(공용 제목 줄 →12→ 작성 칸 →8→ 목록 · 빈 안내, 목록 위 왼쪽 정렬(고스트 · 최신순 · 좋아요순, 의견 둘 이상일 때) · 줄 = 이름 · 날짜 · ⋯ →4→ 본문 →(번역 안내 줄) → 동작 줄(좋아요 하트 · 번역 보기 · 답글, 텍스트 버튼 컴팩트 높이, 눌린 하트 = 채움 + `local-data/categorical-4`) → 「답글 N개 ⌄」 → 답글(한 단계 · 사진 24 · 줄 사이 12 · 오래된 순 · 작성 칸은 목록 끝 제자리, 2026-09-22)).
  작성 칸 = 아바타 32 + 한 줄 입력 L(쉬는 상태) → 누르면 여러 줄 96 · 도움말 8 · 「취소 · 의견 작성」 M 오른쪽 12, Esc 로 접힘.
  내 의견이 있으면 맨 위 작성 칸 대신 내 줄 ⋯ 「수정」 이 그 줄 안에서 펼친다. 로그아웃은 제목 줄 오른쪽 끝 「로그인하고 의견 남기기 ›」(작성 칸 없음) · 기록 없음은 안내 한 줄. 저장 · 삭제 · 신고 중에는 진행형 라벨, 입력칸은 비활성 면 대신 읽기 전용.
  의견 줄 = 상자 없음 · 아바타 32 열 + 내용 열(사이 12) · 첫 줄 이름 `emphasis-label` · 날짜 `metadata` · ⋯ · 본문 `body-secondary` 기본색 ·
  위아래 16 · 줄 위 1px divider. 집계 결과는 개요 · 머리에만.
  구역 사이 32 · 구역 제목(20) → 내용 12(펼침 요약 줄의 구역 제목은 3절 펼침 규칙대로 16) · 탭 → 첫 제목 24.
  **접히는 구역끼리도 다른 구역과 같이 「구역 사이 32」** — 펼침 사이에 구분선을 넣지 않는다(2026-09-18 사용자 결정). 레이더 · 점수 분포의 세로 막대는 쓰지 않고, 점수 분포는 세로축 자체를 두지 않는다(2026-09-18).
  탭 · 난이도를 바꾸는 동안 머리는 이전 값을 pending 색으로 두고(서열 칸 · 그레이드를 비우지 않음) 탭 내용 자리는 **그 탭의 실제 틀 스켈레톤**(2026-09-19 — 큰 상자 하나 대신): 개요 = 패턴 경향 막대 · 내 기록 띠 · 채보 정보 네 줄(BPM · 노트 수 · 길이 · 수록일), 내 기록 = 다섯 구역(로그아웃은 안내 그대로), 랭킹 = 점수 자 자리 72 · 순위표 머리 · 곡 순위 줄 10, 평가 = 척도 · 투표 네 줄 · 의견 줄 2. 곡마다 있거나 없는 구역(서열 변경 이력 · 해금 조건 · 의견 작성 칸)은 그리지 않는다.
  **1056+** = 왼쪽 열(악곡 레일 폭) 머리 카드 sticky(위 24, 자켓 열 폭 정사각 · 난이도 세로 행 = 컨트롤 높이 · 수치 띠 세로 목록 — 난이도 · 그레이드 · 수치 칸 · 동작 칸의 잉크는 모두 열 가장자리에서 12) + 오른쪽 탭 내용.
  개요는 두 열(왼쪽 패턴 경향 · 오른쪽 나머지, 열 사이 16), 평가는 2:1 두 열(왼쪽 패턴 투표 · 서열 투표 · 오른쪽 의견, 열 사이 16), 내 기록 · 랭킹은 한 열. 폭을 바꿔도 선택 · 주소 · 입력이 유지된다.
  **시안은 구조만 따르고 간격 · 글자 · 색 · 크기는 이 가이드 값**(시안의 회색 막대 · 띠 경계 같은 대략값은 쓰지 않는다).
- **악곡 · 채보 발견 · 서열표:** Wide 는 왼쪽 필터 레일, 그 아래는 전체 화면 필터. 결과는 끝이 240px 안에
  들어오면 자동으로 20개씩 3번(80개)까지, 그 뒤는 「더 보기」 버튼. Wide 정렬 트리거는 기준만 보인다(접근 이름엔
  「정렬:」 포함).
  **채보 보기(2026-09-25 A2 · R1 · I2 · D1 · V0):** 채보마다 한 줄 = 링크 하나(같은 곡의 다른 난이도는 줄이 따로 — 곡 단위는 「악곡」 보기). 정렬 · 페이지 · 개수 · 필터 적용 버튼 수가 모두 채보 단위.
  줄 = 악곡 목록 줄 틀(`nl-music-card--list`, 사이 8)에 자켓 80 정사각(카테고리 배지 그대로) · 이름 `entity-title` / 아티스트 `body-secondary` 흐림 두 줄(작성자 · 출처 · 공개일 메타 줄은 2026-09-26 삭제 — 뷰어에서 본다), 모두 한 줄 말줄임 · 위아래 4 로 자켓 안에 가운데.
  오른쪽 끝 **난이도 칩** `nl-chart-chip` 「Real 3」 = 레벨 판(`nl-level`)을 이름까지 넓힌 것(높이 24 · 모서리 4 · `raised` · 이름 `control` + 레벨 `metric-value` · 난이도 글자색). 보기 전환 · 노트 · BPM · 길이 열 · 채보 미리보기 없음.
  서열표는 이 한도를 구간마다 적용하고, 화면 밖 구간은 기존 카드 스켈레톤 6개만 두어 전체 곡 수만큼 DOM을 미리 만들지 않는다. 서열표 구간 머리 숫자(13.5 · 13.0 …)는 악곡 상세 머리와 같은 서열 값 그라데이션 색(`lib/music/tierValueColor.ts`).
  **서열표(2026-09-22 제목 스위처 · 검색 · 정렬):** 제목 줄 = **제목이 곧 서열표 셀렉트**(「S 서열표 ⌄」 — `page-title` 글자 + 꺾쇠 24 subdued, 테두리 없음 · 잉크는 왼쪽 선, 누르면 셀렉트 목록에 Basic(S · 990k · Pianist) · Recital 두 묶음 소제목, 접근 이름 「서열표 선택」 — 서열표 페이지만의 예외)
  →8→ **메타 글줄**(`metadata` subdued, 항목 사이 「 · 」, 좁으면 항목 단위 줄바꿈) = 672 미만은 「2,159곡 · 9월 2일 업데이트 · 안내」, 672 이상은 곡 수 없이 「9월 2일 업데이트 · 안내」(곡 수는 결과 쪽) →16→ 검색.
  업데이트는 목록 갱신일(서울 기준 월 · 일, 올해가 아니면 연도까지). 「안내」 = 밑줄 글자 링크(`nl-link` + `nl-text-link--underlined`, 접근 이름 「{목표} 서열표 안내」)가 Compact 창을 연다 — 표 설명 · 쓰는 법 · 업데이트 · 목표 기준.
  제목 뒤에 ⓘ 를 두지 않는다(꺾쇠와 아이콘 둘이 붙음 — 서열표만 「기준 설명」 ⓘ 대신 이 링크).
  672 미만 머리 두 줄(사이 12) = 악곡 목록 폰 머리와 같은 틀 — 검색(남는 폭, 「악곡 제목·아티스트 검색」) + ☰ 필터 아이콘 44(적용 개수 배지) /
  결과 줄 = 왼쪽 정렬 `SortMenu` 고스트 M · (로그인 · 격자 보기) 띠 값 `CompactSelect size="sm"`(공용 M 크기) · 오른쪽 보기 전환 세그먼트 M(격자 · 목록, 아이콘) →24→ 구간.
  Wide 는 제목 아래 검색 전체 폭 → 레일(필터 그룹) | 정렬(L) · 띠 값(L) · 보기 전환(L) →12→ 결과 수 → 구간.
  제목 줄 오른쪽 끝 = **⋯ 더보기 메뉴**(공용 `ActionMenu` · 고스트 아이콘 L · 접근 이름 「서열표 더보기」, 제목 글줄 가운데) — 「이미지로 내보내기」(내보내기 창) · 「링크 복사」(지금 주소 · 토스트 「링크 복사됨」) · 「{목표} 서열표 안내」(메타 줄 「안내」 와 같은 창).
  메뉴에서 연 창을 닫으면 포커스는 ⋯ 로 돌아간다(2026-09-22 S7-a). 프로필 카드 공유는 제목 줄 오른쪽 아이콘 그대로. 구간 머리는 sticky · 아래 1px divider · 로그인하면 오른쪽 「달성 N/M」.
  **정렬** = 구간 안 곡 순서만 바꾼다(구간 순서는 그대로) — 서열표 순(기본) · 레벨 순(높은 레벨 먼저) · 일본어 읽기 순(가나, 없으면 원제) · 로그인하면 점수 낮은 순(기록 없는 곡 먼저).
  같은 값은 서열표 순. 구간은 곡 전체를 한 번에 받으므로 20개씩 그리기 전에 정렬한다. 주소 `sort=`(기본이면 생략, 로그아웃이면 점수 순은 서열표 순으로 읽음).
  **검색** = 악곡 목록 검색과 같은 필드(곡 코드 · 제목 · 가나 · 아티스트 · 승인된 번역 제목) · 대소문자 무시 부분 일치 · 300ms 쉬면 반영(한글 조합 중엔 기다림) · 주소 `q=`(기록 덮어씀).
  지금 목록 · 필터 안에서 좁히고, 맞는 곡이 없는 구간은 숨긴다. 결과가 없으면 `ResultState` 한 줄 「「검색어」에 맞는 곡이 이 서열표에 없습니다.」 + 「검색어 지우기」 보조 버튼.
  **격자** = 672 미만 4열 · 사이 8(672 이상 auto-fill 최소 111 · 사이 16), 카드 = 자켓 1:1 + 오른쪽 아래 난이도 판(375 미만은 약칭 N · H · EX · R) + 로그인하면 아래 점수(달성하면 목표 색) · 달성 테두리.
  로그인하면 **목표 미달성 곡(미플레이 포함)은 자켓 그림만 흑백**(`grayscale`) — 판 · 띠 · 점수 글자 · 누름 반응은 그대로라 disabled 가 아니다(목록 행 자켓도 같음).
  **자켓 위 띠**(로그인 · 격자) = 자켓 위 전체 폭, 바탕 `surface/media-scrim` · 글자 `content/on-media` `metadata` · 안쪽 2 · 4(난이도 판과 같은 규격) · 테두리 아래.
  값은 결과 줄 셀렉트로 고른다 — 공식 Grd(기본 「+41.26」) · NosLog 레이팅(「+36.5」) · 표시 안 함, 목록 보기 기여와 같은 형식. 고른 값이 없는 곡은 띠 없음(다른 값으로 채우지 않음).
  주소 `strip=`(기본 Grd 면 생략 · `rating` · `off`).
  **목록** = 행(자켓 48 · 모서리 4 · 사이 12 · 위아래 8 → 행 64 · 아래 divider) · 곡 이름 `emphasis-label` / 난이도 `metadata` 난이도 색(+ 번역 제목) 한 줄씩 말줄임 ·
  로그인하면 오른쪽 점수(달성하면 목표 색, 미플레이 흐림) / 공식 Grd 기여(없으면 NosLog 레이팅). 보기는 주소 `view=list`(예전 `view=detailed` 는 목록으로 읽음).
  필터 창 · 레일의 구간 목록은 지금 조건에서 0곡인 구간을 뺀다(이미 고른 구간은 남김, 범위 선택은 보이는 구간 안에서).
  **이미지 내보내기** = 프로필 카드와 같은 창(`nl-image-share`): 담기는 범위 한 줄(「담기는 것 · {서열표 · 조건} · N구간 M곡」, 지금 필터 · 검색 그대로 + 창에서 고른 구간) · 줄 오른쪽 끝 「구간 고르기」 밑줄 링크 →
  펼치면 레일과 같은 구간 체크 목록(지금 조건의 구간만) →8→ 「선택 N곡 / 500곡 · N곡 남음 · 넘음」(`metadata`) → 미리보기 → 체크박스(곡 이름 표시 · 로그인하면 내 달성 표시, 이 브라우저에 기억) → 이미지 저장(주) · 클립보드 복사 · 공유(안 되면 X 공유).
  한 장 500곡까지 — 넘으면 목록을 펼친 채 열고 **위(높은 구간)부터 이어서 500곡 안까지 미리 체크**(2026-09-22 R2). 고른 구간은 이미지에만(페이지 필터는 그대로), 넘게 고르면 저장을 막고 경고. 이미지 = 폭 1200 · 한 줄 자켓 12 · JPEG(복사만 PNG), 브라우저 캔버스로 지금 페이지 토큰을 읽어 그린다 —
  머리(서열표 · 조건 `page-title` 크기 32 · 달성 요약 · 오른쪽 NosLog · 날짜) → 구간 행(왼쪽 서열 값 색 · 달성 수, 오른쪽 자켓 격자: 화면 카드와 같은 테두리 · 흑백 · 띠 · 난이도 판 · 자켓 없으면 음표) → 구분선 · 주소.
  파일 이름 `noslog-{모드}-{목표}-{조건}-{날짜}.jpg`.
  **악곡 목록(2026-09-16):** 672 미만은 목록 위 두 줄 — 검색줄(검색 + 오른쪽 ☰ 필터 44 아이콘, 적용 개수 배지) →12→
  결과 줄(왼쪽 정렬 `SortMenu` 고스트 M · 오른쪽 보기 전환 세그먼트 M) →12→ 목록. 결과 수는 제목 아래 메타 글줄(「579곡」).
  Wide 는 검색 전체 폭 → 레일 | 정렬(L) · 보기 → 결과 수 → 목록 그대로.
  카드: 상자(surface) · 자켓 64(목록) / 정사각(격자) · 이름 목록 16 `entity-title` / 격자 14 `emphasis-label` ·
  아티스트 목록 14 / 격자 12 · 촘촘은 이름 한 줄만. **난이도 판** `DifficultyLevels` = 고정 순서 4칸(Normal · Hard · Expert · Real),
  판 24(목록) / 20(격자 · 촘촘) · 모서리 4 · 면 `raised` · 사이 4 · 숫자 `metric-value` + 난이도 글자색 · 없는 난이도는 자리만 비움(테두리 없음).
  격자 카드 안쪽 12, 촘촘 8.
- **빙고(2026-09-22 재설계):** 목록 머리 = 제목 → (672 미만) 개수 메타 글줄 8 → 검색(전체 폭) →12→ 상태 칩 한 줄(`FilterChips row` 하나 고르기 · 개수, 로그인만 — 게스트 없음. **줄바꿈 없이 넘치면 가로 스크롤**, 2026-09-23) →12→
  결과 줄(왼쪽 정렬 `SortMenu`(로그인) 고스트 · 폰 M · 1056 이상 L · 오른쪽 보기 전환 `SegmentedControl`) →12→ 목록. 필터 창 · 팝오버 없음. 목록은 한 번에 전부(더 보기 없음).
  **보기** = 격자(기본) · 목록형, 주소 `view=list`. 격자 = 폰 3열 · 사이 8, 672 이상 auto-fill 최소 156(악곡 격자 카드 최소와 같음) · 사이 16 · 카드 = 자켓 1:1(모서리 8) +4+ 이름 `emphasis-label` · 상태 `metadata` 한 줄씩 말줄임,
  로그인하면 자켓 오른쪽 아래 미니 판(칸 6 · 사이 2 · 판 안쪽 4 · `media-scrim` · 아래 트랙 위 8 — 자켓 카테고리 레이블과 같은 자리) + 아래 진행 트랙 4.
  목록형 = 서열표 목록 행 규격(자켓 48 · 모서리 4 · 사이 12 · 위아래 8 · 아래 divider) — 이름 → 상태 → 진행 막대(위 4, 상세 진행 막대 규격) · 오른쪽 끝 미니 판(바탕 없이 칸만), 1056 이상 두 열(사이 16).
  상태 글 = 「칸 n/25」 · 「해금 완료」 · 「풀보드 완료」 + 찬스가 있으면 「빙고 찬스 n」(기본 글자색), 게스트는 버전 · 보상.
  **상세** = 정체성(자켓 96 · 제목 · 버전 · 진행) → 보상 한 줄(`metadata` — 필요 줄 · 줄당 · 풀보드 · 총 보상, 펼침 없음) → 판 → 미션.
  **판 칸 = 미션 글**(원본 빙고처럼 좌표 A1 없음) — `metadata` 12/16 · 안쪽 4 · 칸 높이만큼 말줄임(폰 칸 68 → 3줄 · 375 미만 2줄 · 1056 이상 5줄), 위치는 화면 읽기 「n행 m열」.
  칸을 누르면 672 미만 · 1056 미만은 칸 팝오버(334), 1056 이상은 **판 | 오른쪽 1:1**(열 사이 16 — 악곡 상세 개요와 같은 비율)의 오른쪽 선택 칸 상세(미션 행과 같은 면), 고르기 전엔 안내 한 줄.
  1056 이상 · 로그인은 판 가장자리에 12줄 진행(왼쪽 가로 · 위 세로 · 두 모서리 대각, 칸 `spacing-32`, 완성 ✓ 600 · 4/5 기본 글자색 · 나머지 subdued).
  **미션** = 펼침(기본 열림) 안에 필터 칩(전체 · 미완료 · 완료 · 찬스) + 판 순서 한 목록, 행 왼쪽 = 칸 위치 미니 판(그 칸만 `content/default`, 나머지 `border/empty-slot`). 줄(행) 묶음 없음.
  찬스 줄 이름은 번호(「5행 · 1열 · 대각선」). 상세 초기화 버튼은 미션 열 전체 폭 + 확인창.
- **투표(2026-09-23 V2 · W1 · R1):** 글에 딸린다 — 본문에 흔적을 남기지 않고 글 하나에 하나, 공개 화면에서는 **본문 끝**에 위젯.
  글쓰기 구역 `nl-poll-section` = 제목 「투표」 + 「선택」 →12→ 만들기 전에는 「투표 만들기」 보조 버튼, 만든 뒤에는 요약 카드(면 surface · 모서리 8 · 안쪽 12 —
  질문 `emphasis-label` · 오른쪽 「고치기 · 지우기」 고스트 M →4→ 「선택지 n개 · 한 개만 고름 · 마감」 `metadata`).
  **만들기 창** `nl-poll-builder`(672 이상 Compact 창 · 아래 전체 화면) = (공지만) 언어 탭 → 고르는 방법 세그먼트 → 질문 → 선택지(엔터로 칸 추가 · 「선택지 추가」 · 「여러 줄 붙여넣기」, 2~20개) →
  「누가 무엇을 골랐는지」 세그먼트 → 펼침 「자세히」(마감 · 결과 공개 · 게시 뒤 선택지 추가). **만들기 창에 미리보기는 두지 않는다.**
  **위젯** `nl-poll` = 질문 `emphasis-label` → 마감 · 익명 `metadata` → 고르는 줄(경계 1px · 모서리 8 · 컨트롤 높이, 고른 줄은 2px + 600) 또는 결과(라벨 + 오른쪽 퍼센트 →4→ 막대 트랙 8, 내 표는 ✓ + 밝은 채움) → 아래 줄 「N명 참여」 + 버튼.
  한 개만 고르는 투표는 **누르면 바로**, 여러 개는 「투표하기」. 비로그인 · 마감은 **결과만**(고르는 줄을 보여 주지 않는다). 표를 낸 사람에게는 「다시 고르기」.
- **오락실:** 지도 실패는 짧은 위험 알림 + 「다시 시도」, 목록은 유지. 마우스 휠 확대는 목록 지도에만.
  카드·핀 선택만 바뀔 때는 기존 핀의 선택 표시와 겹침 순서만 갱신하고 키보드 포커스를 유지한다. 지도 이동·크기·검색 결과가 바뀔 때 묶음을 다시 계산한다.
  672 미만은 필터가 검색줄 오른쪽 아이콘 버튼(44, 적용 개수는 오른쪽 위), 정렬은 시트 요약 줄 오른쪽 ghost `SortMenu`,
  지도 · 시트는 좌우 끝까지(시트 안쪽 16). 672 이상은 다른 목록과 같은 정렬 | 필터 툴바 — 이 예외는 오락실만.
  상세는 이름 아래 상태 줄 없이 바로 정보 행(주소 · 영업시간 · 요금). 영업시간을 펼친 7일 표는 토 · 일 줄 전체(요일 · 날짜 · 시간)를
  `judgement/near` 파랑 · `feedback/error-text` 빨강(휴무 줄도 주말 색). 사진은 모든 폭에서 밀어 넘김 + 좌우 가장자리 세로 가운데 ‹ › 44 + 카운터 · 키보드 ← →(2026-09-23 N2 — 밀어 넘김만 두면 WCAG 2.5.7 위반).
  1056 이상 · 2장 이상은 모자이크(큰 사진 + 작은 사진 최대 2, 순서 고정) — 위에 ‹ › · 카운터 없이
  오른쪽 아래 「사진 N장 모두 보기」(카운터와 같은 어두운 판 · 32). 어느 사진을 눌러도 **사진 뷰어**가 그 사진으로 바로 열린다(격자 단계 없음). 기체는 한 대 = 카드(surface · 안쪽 12/16 · 사이 8),
  「가동 확인」 흰 주 버튼 · 「고장 신고」 빨간 테두리, 둘 다 글자만(카드 안 버튼 = M). 「길찾기」 도 글자만 — 아이콘은 글자 없는 버튼(하트 · 제보)에만.
  기체 카드는 두 열 — 왼쪽 이름(14/20) / 위치 메모(12/16), 오른쪽 상태(14/20) / 확인 시각(12/16), 열 안 4.
  기체 색 점(늘 글자와 함께)은 가동 · 양호 초록 · 보통 노랑 · 주의 빨강 · 이용 불가 빨강 · 미확인 빈 원.
  **시설 · 태그 · 특징(2026-09-22):** 있다 / 없다 = 아이콘 + 글자, 정도 = 정해진 보기 키-값. 입력한 것만 · 없으면 자리 없음.
  시설 = 정보 행 다음 · 위치 앞 구역 「시설」, 아이콘 20 흐림 + 글자 `body-secondary` 두 열(폰도) · 사이 12/16.
  기체 카드 = 이름 · 상태 줄 → 태그(아이콘 16 흐림 + `metadata` 흐림 · 사이 가로 12 세로 4) → 특징 판(`surface/raised` · 모서리 4 · 안쪽 8/12,
  키 흐림 · 값 기본색 `body-secondary` 두 열 · 모르는 항목은 줄 없음, 기타 글은 판 안 구분선 아래) → 버튼.
  세부 모양은 결정 기록 2026-09-12 · 13 을 따른다.
- **검정(2026-09-22):** 종류 = 세그먼트(Basic · Recital · Event) → 급 = 셀렉트(폰) · 레일(1056+). 셀렉트 목록 줄 · 레일 줄은 **「급 · 상태」** 두 칸(명판 없음, 트리거는 급만 — 고른 급의 상태는 급 머리 태그) —
  합격 = 체크 16(성공 표시색) · Grd. 부족 = 자물쇠 16, 둘 다 흐린 글자 / 응시 가능 · 심사 중 · 반려 = 상태 점 8(성공 · 경고 · 위험) + 기본 글자 / 데이터 연동 필요 = 흐린 글자만. 로그아웃 · 이벤트는 상태 없음(이벤트는 셀렉트 위에 「참고용」 한 줄).
  급 머리 = 제목 `section-title` + 상태 태그(`nl-tag` 안 같은 상태, Grd. 부족은 모자란 값까지) →12→ 수치 띠(요구 Grd.(넘었으면 값을 합격 체크와 같은 `feedback/success-marker`, 라벨에 내 값 없음) · 검정료 · 합격 보상(칭호면 검정 명판 풀 이름형 「BASIC | 2급」)).
  과제곡 = 곡 카드(자켓 · 순서 · 난이도 · 곡 이름 · 오른쪽 내 베스트 — 난이도 줄 · 곡 이름 줄은 한 줄, 넘치면 끝 페이드라 카드 높이 = 자켓 64) 사이에 **통과선 줄**(컴팩트 높이 · 자켓 가운데 1px 세로선 위 ✓/✗ 16 · 합격선 글자 `metadata` 「1st 925,000」 · 「누적 1,875,000」 — 여유 숫자는 두지 않음. 내 베스트 숫자는 그 아래 통과선 기준으로 통과 `local-data/slow` 주황 · 미달 `feedback/info-marker` 파랑). 누적은 앞 곡 기록이 모두 있어야 비교, 없으면 합격선만.
  합격까지(로그인한 점수형) = 상자(surface · 안쪽 16 · 사이 12) · 제목 「합격까지 N점」 · 막대 트랙 8 위에 곡별 베스트 조각(⑯ `nl-chart-reveal` · `nl-chart-bar`)(차트 범주 색 1 · 2 · 3, 조각 사이 2 틈) · 합격선 눈금(2 × 16 기본 글자색) · 눈금 아래 「N차 / 합격」 + 그 합격선 대비 여유(±, 내 베스트와 같은 주황 · 파랑 — 비교 못 하면 합격선 값)(모두 눈금 왼쪽에 오른쪽 맞춤) · 범례 「색 · 곡 · 베스트 점수」 늘 · 합격선 아래면 「먼저 연습할 곡」 · 베스트 한계 안내.
  합격 인증 = 구역 제목 → 안내 한 줄 → 찍을 것 다섯 줄(체크 16 흐림, 사진 고르기 전부터) → 주 버튼 L(카메라 20). 1056 이상에서는 합격까지와 두 열 · 같은 상자. 합격한 급은 인증 구역 없음(상태는 머리 태그).
- **사진 뷰어(2026-09-23 V3 · B2 · C2 · S2 · U2):** 사진을 크게 보는 자리는 대화상자가 아니라 **모든 폭에서 화면을 채우는 뷰어**(`PhotoViewer`) —
  바탕 `surface/canvas`(새 색 없음) · 사진 `contain` · 시스템 영역은 safe-area 로 피한다. 조작부는 사진 위 어두운 판(카운터와 같은 문법):
  닫기 24 우상 8 · ‹ › 44 좌우 8 세로 가운데(폰 포함) · 아래 칸에 「N / M」 `metadata` →8→ 썸네일 줄(컨트롤 높이 · 4 : 3 · 모서리 4 · 사이 8 · 고른 것은 `border/strong` 2 테두리, 넘치면 가로 스크롤).
  Esc · 닫기 · **뒤로 가기**로 닫히고(`useOverlayHistory`, 주소는 그대로) 닫으면 눌렀던 사진으로 포커스가 돌아온다. 격자 단계는 두지 않는다.
- **프로필(2026-09-25 D2 개편 1단계 · 2026-09-26 3단계):** 머리 + 구역 탭 — `profile/[id]/layout` 이 머리와 1단 탭(`AreaTabLinks` — `AreaTabs` 와 같은 모양, 항목이 링크 · 선택 = `aria-current`)을 한 번 그리고, 탭마다 주소가 다르다(개요 `/profile/[id]` · 기록 `/records` · 통계 `/stats` · 업적 `/achievements` · 활동 `/activity` — 점수 비공개 프로필을 남이 보면 기록 · 통계 탭 없음, 「플레이 활동 비공개」 면 활동 탭 없음). 고른 모드(`?mode=recital`)는 탭을 옮겨도 유지된다.
  머리 격자 = 폰: 아바타 64 · 이름 묶음 → 정보 두 줄 → 모드 세그먼트(폭 전체 · L) → 핵심 수치 상자 / 672 이상: 세그먼트가 이름 줄 오른쪽(폭 200) · 정보 두 줄 · 수치 상자는 아래 / 1056 이상: 아바타 108 · 세그먼트(폭 240)와 핵심 수치 상자가 머리 오른쪽. 머리는 모든 탭에서 같다 — 업적 · 활동 탭에서도 모드 세그먼트 · 수치 상자를 숨기지 않는다(탭을 옮길 때 머리 높이가 바뀌지 않게, 2026-09-26 사용자).
  정보 두 줄(2026-09-26 H1, osu! 정보 칸) = 활동 줄 「마지막 플레이 값 · (본인) 동기화 N일 전」(아이콘 없음, 라벨 `metadata` subdued + 값 default 색 — 12 굵게는 쓰지 않음, 동기화가 끝나지 않았으면 상태 문장) →4→ 계정 줄 「Discord · 선호 오락실 · NOSTALGIA ID」(링크 성격인 Discord · 오락실만 아이콘 16, NOSTALGIA 는 라벨 글자). 항목 사이 16 · 좁으면 줄 안에서만 줄바꿈 — 계정과 활동을 한 줄에 섞지 않는다(실측 5곳 0/5, 2026-09-26 사용자).
  핵심 수치(2026-09-26 B1) = 칸 나눈 상자(수치 띠와 같은 만듦새 — 면 `surface` · 칸 사이 1px `border/divider` · 모서리 8): 왼쪽 절반 라벨 자리 고르기(`CompactSelect` 고스트 M 「공식 Grd ⌄ · NosLog 레이팅」 — 줄 높이를 늘리지 않게 위아래 · 왼쪽 보정, T1) 위 · 값 `metric-display` 32(Grd 는 소수 둘째 자리 · 레이팅은 정수, 구간 색), 오른쪽 절반을 위아래로 나눠 「세계 순위」 · 「국가 순위」(라벨 `control` 14 왼쪽 — 앞에 세계 = 지구본 16 · 국가 = 그 사람의 국기, 순위 `component-title` 16/600 오른쪽(2026-09-26 사용자 — 중요한 값이라 한 단계 크게), 순위 색, 없으면 「—」 — 레이팅을 고르면 순위도 레이팅 기준, 랭킹 페이지 캐시에서). 모든 폭 같은 상자(넓은 화면은 머리 오른쪽) — 다만 머리 칸이 340 보다 좁으면(폰 320) 공식 Grd 가 윗줄 전체 · 세계 순위 | 국가 순위가 아랫줄 두 칸(라벨 위 · 순위 아래), 숫자는 꺾지 않는다. 레이팅 줄 · 모드 이름 · 상위 % · 90일 변화는 두지 않는다(2026-09-26, 사용자). 그 모드 기록이 없으면 같은 상자에 「아직 Recital 기록이 없습니다」. 점수 비공개 · 기록 없음이면 모드 · 수치 없음.
  개요 = Wide 2 : 1(사이 16) — 주 열(성장 추이 · 베스트 · 최근 플레이) | 옆 열(고정 기록 · 레벨별 달성 요약 · 업적 · 기여), 태블릿(672–1055) = 성장 추이 → 베스트 → 최근 → 옆 열 두 칸(고정 기록 | 레벨별 달성, 업적 | 기여 — 고정 기록이 없으면 레벨별 달성이 두 줄), 폰 = 성장 추이 → 레벨별 달성 → 고정 기록 → 베스트 → 최근 → 업적 → 기여(2026-09-26 사용자, F2 를 바꿈). 고정 기록(S2) = 구분선 목록, 줄 = 자켓 48 · 곡 `entity-title` / 난이도 · 점수 · 등급 + FC / 한 줄 소감(따옴표 · `body-secondary`) · 오른쪽 위 Grd(지금 모드, `metric-value` 「120 Grd」) · 아래 「#n」 `metadata`(곡 순위 — 「곡」 글자 없음, 2026-09-26 D8) — 베스트 성과 폰 줄과 같은 자리, 위아래 8(가이드 목록 줄, T4). 고르지 않았으면 Basic 베스트 상위 3곡. 구역 사이 32. 최근 플레이 제목 옆 「모두 보기 ›」 = 활동 탭.
  성장 추이 = 제목 | 지표 세그먼트 M(「Grade · Rating」 — 짝이 되는 영어 용어, 베스트 성과 전환과 같은 말, 2026-09-26 사용자) + 기간 `CompactSelect` M(테두리) → 그래프 플롯 200(`LineChart plotHeight`, 기록 추이 그래프 규칙 그대로 — 2026-09-26 D1 · 여백도 공용 값 그대로 — 악곡 상세와 같음, D3). 폰 · 태블릿은 조작부가 제목 아래 줄이고 세그먼트가 남는 폭을 채운다. 요약 문장 · 요약 줄(현재 · 변화 · 최고)은 두지 않는다 — Grd 는 내려가지 않아 늘 「지금 = 최고 · 변화 +」 뿐인 중복 정보(2026-09-26 D2, 사용자).
  레벨별 달성(R2 · 2026-09-26 L1) = 구역 제목(요약은 제목 줄 오른쪽 끝 「모두 보기 ›」 = 통계 탭) →12→ (통계 탭만: 난이도 세그먼트 M 전체 · NORMAL · HARD · EXPERT · REAL, 긴 이름이 넘치는 칸 328 아래만 N · H · EX · R) →12→ 한 막대(`StackedBar` + 값 칸 「N%」) →12→ 범례. 줄 = 전체는 NORMAL · HARD · EXPERT 합으로 「1–8」 한 줄 + 9 · 10 · 11 · 12 + REAL 1–3 따로, 난이도를 고르면 그 난이도 레벨마다, 요약은 9 이상 + REAL. 막대 칸 = 채보마다 가장 높은 한 칸 Pianist(= P) → FC → S → A+ → A → B 이하, 색 = 등급 색(랭크 분포와 같음), 칠하지 않은 트랙 = 안 함(범례 없음). 「실패」 는 따로 세지 않는다(그 판의 랭크 칸). 범례 = 칸 강조 전환(글자 그대로 누르는 버튼 · 누르는 높이 24 · 칸 12 · 모서리 control 4 · `metadata`, 고른 항목만 기본 글자색): 누르면 그 칸만 제 색 · 나머지 칸은 `content/disabled`, 값 = 그 레벨 전체 채보 중 그 칸의 비율, 한 번 더 누르면 풀림. 고르지 않았으면 값 = 여섯 칸을 합친 비율(친 채보). 범례 끝에 지금 값이 무엇인지 한 마디(「% = 친 채보」 · 「% = S」). 바뀔 때 움직임 없음.
  통계 탭 = 폰: 레벨별 달성 → 성장 추이 → 판정 요약 → 랭크 분포 / 672 이상: 판정 | 랭크 두 칸 / 1056 이상: 성장 추이(2) | 레벨별 달성(1, 두 줄), 성장 추이 아래 판정 | 랭크(사이 32 · 16). 판정은 제목 줄 오른쪽에 기준 한 줄 `metadata` →12→ 공용 누적 막대 한 줄(라벨 없음) →8→ 판정 줄(색 표시 = 원 8, 악곡 상세 판정 분석과 같음 — 2026-09-26 D1 · D2). 노트 종류별 성공률은 두지 않는다(2026-09-26 사용자 — 의미 없는 통계). 랭크 분포 = P · S · A+ · A(「전체 랭크 보기」 로 B+ 이하) + 맨 아래 FC 막대(라벨 = FC 태그 `nl-full-combo`, 값 = FC 램프 + Pianist) · 플레이 횟수(「전체 랭크 보기」 는 누르는 높이 24), 트랙 = 막대 목록 트랙 8(모서리 4 · `raised`, D1), 막대 색 = 등급 색 — P `score-goal-pianist` · S `score-goal-s` · A+ `score-grade-a-plus` · A `score-grade-a` · B+ · B `score-grade-b` · FC `achievement/full-combo`, C · D 는 `content/subdued`(2026-09-26 D7 · C2a).
  활동 탭 = 구역 제목 →12→ 수치 띠(최근 1년 · 이번 달 판 · 연속(지금 이어지는 일수, 오늘 아직 안 쳤으면 어제까지 — 2026-09-26 사용자) · 가장 긴 연속 일) →16→ 활동 달력(7행 × 53주, 칸 16 · 사이 4 · 모서리 control 4 — 모든 폭 같은 값, 2026-09-26 T2 · T3, 색 = 플레이 없음 raised · 그 사람의 가장 많이 한 날 대비 `local-data/bucket-1…6`, 칸 폭만큼 보이고 옆으로 밀면 1년 · 처음엔 오늘 쪽 끝) →8→ 「최근 1년 기록」(모든 폭) · 범례(칸 12 · 모서리 4) →32→ 최근 플레이(20판씩 「더 보기」).
  기록 줄(B2) = 구분선 목록, 칸 폭 560 이상은 열 고정 표(베스트: 순번 20 · 자켓 36 · 곡 · 난이도 84 · 점수 76 · 등급 + FC 56 · 곡 순위 56 · 날짜 88 · Grd 64 / 최근: 자켓 · 곡 · 난이도 84 · 점수 76 · 등급 56 · New 64 · 플레이 시각 104, 표 머리 `metadata` 32, 행 52), 좁으면 두 줄(자켓 44 · 곡 / 난이도 · 점수 · 등급 · 오른쪽 값 · 시각, 행 64). 최근 플레이는 묶지 않고 한 판씩(사용자), 그 판이 직전 최고 점수를 넘었으면 「New」 상태 태그(`nl-tag--status` 성공 — 이벤트 「진행 중」 과 같은 부품, 세 언어 같음, 2026-09-26 T5)(좁으면 오른쪽 아래).
  본인용 공유 · 설정 버튼(보조 아이콘 버튼 M, 2026-09-26 D4)은 이름 줄 오른쪽 끝에만 붙고, 아래 명판 · 라벨 · 업적 배지 줄은 이름 묶음 폭 끝까지 간다. 이름 옆 국기는 `CountryMarker size="large"`(칸 24 · 그림 18, D5).
  기록 탭(2026-09-25 2단계) = 위 줄 세그먼트 M 「최고 기록 · 모든 기록」(개수 없이 — 「베스트 50」 에서 바꿈, 위 구역 탭과 겹치던 2단 탭을 바꿈 — 2026-09-26 R1) | 고스트 정렬 M(Grd · 점수 · 최근 달성 · 곡 이름 순, 같은 줄 한 단계), 모든 폭 한 줄 →16→ 표(20개씩 「더 보기」). 검색칸 · 필터 · 「베스트 = …」 설명 줄은 두지 않는다(2026-09-26, 사용자).
  기록 표 열 = 베스트 순번(다른 순으로 봐도 베스트 50 안의 순번 그대로) · 자켓 · 곡 · 난이도 · 점수 · 등급 + FC · 곡 순위(#n) · 달성 날짜(88, 올해면 월 · 일) · Grd — 개요 베스트도 같은 열. 개요 베스트 · 최근 플레이 · 레벨별 달성의 「모두 보기 ›」(기록 · 활동 · 통계 탭)는 제목 줄 오른쪽 끝 제목 링크(구역으로 옮길 때 머리 아래 76 띄움) — 같은 구역의 전환(베스트 「Grade · Rating」 세그먼트 M — 위 구역 탭과 겹치던 2단 탭을 바꿈, 2026-09-26 S1)은 제목 줄 다음 줄, 목록 바로 위(2026-09-26 A · 모든 폭 같은 모양). 프로필 구역 안 세그먼트는 글자를 꺾지 않는다. 폰은 오른쪽 위 Grd · 아래 곡 순위(날짜 없음). 최근 플레이 표의 시각 칸은 104.
  검정 명판 24 · 금속 사다리 · Recital 은 파인 모서리 · 들어갈 때만 `BASIC`/`RECITAL`, 둘 다 없으면 「검정 기록 없음」.
  본인이 자기 프로필을 보면 공개 설정으로 숨긴 항목도 보인다(2026-09-26 P1) — 정보 줄의 숨긴 값은 원래 아이콘(Discord · 오락실 핀) 대신 자물쇠 16 + 흐린 값(`content/subdued`, 아이콘이 없던 항목은 자물쇠만 앞에), 숨긴 구역(최근 플레이 · 활동 탭)은 제목 아래 「나에게만 보입니다」(자물쇠 16 + `metadata` 흐림), 숨긴 플레이 횟수는 값 앞 자물쇠 + 흐린 값. 「비공개 · 항목들 · 프로필 설정」 요약 줄은 두지 않는다(2026-09-25).
- **기여 라벨 · 프로필 「기여」(2026-09-24 N1 · L2 · P1):** 라벨 = 기존 태그 「기여 Lv.N」(「기여」 보조 글자 · 보통 굵기, Lv 만 굵게), 운영자는 기본 태그 「Admin」(한 · 일 · 영 모두 · 아이콘 · 앞글자 없음, 2026-09-25 B3).
  이름 옆은 검정 명판 + 라벨 하나까지 · Lv.3 이상만(Lv.1–2 는 프로필에만) · 곡 상세 순위표에는 없음. 전체 랭킹 줄(명판 뒤 · 이름 글줄 20 높이 · 명판 줄이기 폭 계산에 포함 — 자리를 지키는 순서는 이름 > 명판 > 라벨, 명판을 B · R 로 줄여도 안 들어가면 라벨을 숨긴다) ·
  의견 · 답글 머리(이름 뒤 · 같은 20 높이)에서는 누르거나 올리면 용어 도움말 작은 창(「기여 Lv.3 · 42점」 · 다음 등급까지). 프로필 머리는 명판 뒤에 Lv.1 부터, 누르면 「기여」 구역으로.
  프로필 「기여」 구역 = Wide 오른쪽 열(기록 개요 · 최근 플레이 아래) · 폰 맨 아래: 등급 줄(태그 · 점수 · 다음 등급까지) →8→ 진행 막대(막대 목록 트랙 8) →12→ 종류별 수치 띠
  (곡 정보 · 오락실 제보 · 기체 확인), 본인에게만 내 제안 선 목록 3건(머리 줄 「내 제안 · 나에게만 보입니다」 는 두지 않음 — 2026-09-26 사용자)(상태 태그 · 반려 사유 오류 글자색) + 「모두 보기」(Wide 창 · 최근 100건).
  남의 프로필에서 기여가 없으면 구역을 두지 않고, 점수 비공개 프로필에도 기여는 보인다.
- **업적(2026-09-24 T2 · I1 · L2 · K1 · R1 · P5 · N1 · D1 · A1 · B1 · C1 · D1 · E1):** 그림 = 육각 안 Lucide 아이콘(업적마다 하나, `ACHIEVEMENT_ICONS`).
  등급 다섯(2026-09-25 C1) — 동 I · 은 II · 금 III(검정 명판 금속 사다리 `exam/tier-mid` · `high` · `top`) · 플래티넘 IV(`judgement/good` 청록) · 다이아 V(`judgement/s-just` 분홍). 업적마다 가진 등급이 다르다(다섯 모두 · 동 · 은 · 금 셋만 · 다이아 하나만). 알약 숫자는 로마 숫자로 등급을 뜻한다.
  테두리 = 그 등급 색, 못 얻은 것은 `content/disabled` 선 + `content/pending` 그림 · 이름(K1, 조건은 늘 공개 · 숨긴 업적 없음).
  크기 세 단계 — 목록 44 × 48(선 2 · 그림 24 · 오른쪽 아래 단계 숫자 알약 16 = 금속 면 + `surface/canvas` 글자 `metadata`) · 한 줄 22 × 24(선 1 · 그림 16 · 숫자 없음, 이름은 접근 이름 · title) · 정보 카드 58 × 64(선 2 · 그림 32, 2026-09-25).
  프로필 머리 = 명판 · 기여 라벨 뒤에 이어서 한 줄 육각 3개만(설정에서 고른 것, 없으면 높은 단계 → 달성 인원 적은 순 자동, 개수 글자 없음 — 2026-09-25). 배지마다 올리거나 누르면 정보 카드(2026-09-25 M1, osu! 메달 창 순서 · 동작은 기여 라벨과 같은 `TermHelp plain`, 창 안은 `content`): 폭 240 · 안쪽 16 · 가운데 정렬 — 분류 `metadata` →8→ 큰 육각 58 × 64(선 2 · 그림 32, 숫자 알약 없음) →8→ 「이름 III」 `section-title` →8→ 조건 `body-secondary` →구분선(`border/overlay`, 창 끝까지)→ 「N명 달성」 · 「날짜 달성」 `metadata`. 업적 페이지 입구는 「업적」 구역의 「모두 보기」. 명판 줄이기 폭 계산에는 넣지 않고 넘치면 다음 줄.
  프로필 「업적」 구역(2026-09-25 A2) = 옆 열(기록 개요 아래 · 기여 위, 폰은 기여 위): 구역 제목 + 오른쪽 「얻은 수 / 전체 ›」(`nl-heading-link` → 업적 탭) →12→ 얻은 업적 육각 44 × 48 늘 한 줄(높은 단계 → 최근 순, 구역 폭에 들어가는 만큼 — 폭 356 = 7칸 · 408 = 8칸 …, 더 있으면 마지막 칸이 「⋯」(아이콘 20 흐림, 누르면 업적 탭), 2026-09-26 A1) →12→ 「최근 달성 · 이름 III」 `metadata` 한 줄(가장 최근 하나, 2026-09-26 사용자). 금 · 은 · 동 개수는 두지 않는다. 남의 프로필에서 얻은 업적이 없으면 구역 없음.
  업적 탭 `/profile/[id]/achievements`(2026-09-25 D2 — 머리 · 탭 아래, 돌아가기 · 제목 없음) = 「업적 n / N」 `emphasis-label` → 분류 칩 한 줄(`FilterChips row`, 칸마다 얻은 단계 / 전체) →16→ 구분선 목록.
  결과 줄(2026-09-25) = 분류 칩 →12→ 왼쪽 정렬 `SortMenu` 고스트 · 오른쪽 보기 전환 `SegmentedControl`(목록 · 촘촘한 격자, 일반 격자 없음) — 폰 M · 1056 이상 L(악곡 · 서열표와 같은 문법) →12→ 목록.
  정렬 = 분류 순(기본) · 높은 등급 순 · 최근 달성 순 · 희귀한 순(얻은 등급의 달성 인원 적은 순) · 다음 단계에 가까운 순(본인만). 못 얻은 업적은 뒤로. 촘촘한 격자 = 육각 44 × 48 만(줄 사이 12 · 칸 사이 8, osu! 메달 벽), 누르거나 올리면 머리와 같은 정보 카드(못 얻은 것은 첫 단계 조건).
  목록 줄 = 육각 44 × 48 →12→ 이름(상태 글자 없음 — 2026-09-25) · 조건 줄(본인 = 다음 단계 조건 — 현재 / 목표, 남 = 얻은 단계 조건 · 달성일) · (본인) 진행 막대(막대 목록 트랙 8, 채움 = 쫓는 다음 등급의 색 · ⑯ 드러남 · 옮겨 감 — 2026-09-25) · 오른쪽 펼침(아이콘 버튼 M 고스트). 목록에 핀 · 고르기 버튼을 두지 않는다(2026-09-25).
  펼치면 단계 사다리(한 줄 육각 · 「I · 조건」 · 달성일 또는 (본인) 진행 · 「N명 달성」, 명단 없음).
  머리 진열 고르기(2026-09-25 D1) = 설정 「프로필」 탭, 선호 오락실 칸 아래 「프로필 업적」 칸(선호 오락실 칸과 같은 모양: 값 자리에 한 줄 육각 3개 + 고른 것이 없으면 「자동 · 높은 단계 순」 · 오른쪽 보조 「변경」, 아래 도움말 한 줄). 창의 고르는 격자 = 칸 최소 88 · 사이 8(칸 = 육각 + 이름, 안쪽 12 / 4, 2026-09-26 E3).
  「변경」 → 창(설정 창과 같은 부품): 3칸(고른 순서, 빈 칸은 `border/empty-slot`) →12→ 「얻은 업적」 격자(육각 44 × 48 + 이름, 고른 것은 선택 상태, 3칸이 차면 나머지 비활성) · 발 = 보조 「자동으로」 + 주 「적용」. 고른 값은 폼에 들어가 「저장」 때 함께 저장. 얻은 업적이 없으면 「아직 얻은 업적이 없습니다」 만.
  고정 기록 고르기(2026-09-26 S2) = 같은 탭 「프로필 업적」 칸 아래 「고정 기록」 칸(같은 모양: 값 자리에 고른 곡 이름 「 · 」 로 이음, 없으면 「자동 · 베스트 상위 3곡」 · 오른쪽 보조 「변경」, 아래 도움말 한 줄). 창 = 고른 3칸(칸 = 곡 `emphasis-label` · 난이도 · 점수 `metadata` · 오른쪽 고스트 M 「빼기」 →8→ 한 줄 소감 입력칸(80자, 선택), 칸 경계는 「프로필 업적」 창과 같음 · 빈 칸은 빈자리 경계) →12→ 기록 찾기(선호 오락실 창과 같은 검색칸 · 결과 수 · 목록, 고른 항목 600 · 3칸이 차면 나머지 비활성) · 발 「자동으로 · 적용」. 「저장」 때 함께 저장.
  동기화 결과(E1) = 수치 띠에 「새 업적」 칸(있을 때만) + 아래 한 줄(「새 업적」 · 한 줄 육각 + 「이름 II」 3개까지 · 「외 N개」 · 「업적 보기」 밑줄 링크).
  점수 비공개 프로필을 남이 보면 실력 · 수집 업적은 머리 · 구역 · 페이지 어디에도 없다(분모도 보이는 것만). 진행 값은 본인에게만.
- **개인정보:** 처음엔 첫 절(또는 주소의 절) 선택. 목차를 누르면 강조 · 포커스 이동, 스크롤하면 읽는 절이 따라간다.
- **최근 기록 연동(2026-09-20):** Pass 여부와 관계없이 기존 프로필·악곡·서열 화면을 사용한다. 수집되지 않은 값은 「—」, 점수별 출처 배지는 추가하지 않는다. 연동 도움말에서 최근 플레이 누적과 전체 연동 보정을 설명한다.
- **데이터 동기화:** 설치 · 공식 사이트 로그인 · 북마클릿 실행 3단계, 예시 이미지와 결과 · 도움말은 펼침.
- **관리자 공지 작성(2026-09-18 E2):** 언어 1단 탭 → 제목 → 본문(`MarkdownEditor`)만. 분류 · 종류 · 노출 기간 · 우선순위 · 공개 주소는 「게시」 를 누르면 뜨는 창
  (672 이상 Compact 창 · 아래는 전체 화면). 아래 줄 = 왼쪽 삭제 · 공개 페이지, 오른쪽 보조 「임시저장」(공개 중이면 「비공개로 전환」) + 주 「게시」(공개 중이면 「업데이트」).
  오류는 그 자리로 데려간다 — 번역 칸이면 창을 닫고 그 언어 탭, 창 칸이면 창을 연다. 주소가 비었으면 저장할 때 제목에서 만든다.
- **이벤트(2026-09-18 L1 · E2):** 목록 = 제목 줄(오른쪽 「내 글」 고스트 M · 「글쓰기」 보조 M, 쓸 수 없으면 「글쓰기」 비활성 + 제목 아래 이유 한 줄 — 서열 투표 잠금과 같은 방식)
  — 제목은 「소식」 + 입구 탭(위 「소식 입구」) →24→ 상태 필터 칩 「진행 중 N · 예정 N · 종료 N」(`FilterChipLinks` M, 주소 `?tab=`, 2026-09-26 E1) →24→ 배너 카드(폰 한 열 · 672 이상 두 열, 사이 세로 24 · 가로 16).
  카드 = 면 없음 · 배너 2.4 : 1(모서리 8, 없으면 같은 비율 가라앉은 면) →12→ 제목 `entity-title` →4→ 기간 `body-secondary` subdued. 칩이 상태를 말하므로 카드엔 상태 태그 없음. 빈 탭은 한 줄 안내.
  상세 = 공지 상세 문법(상태 태그 · 기간 · 작성자 한 줄 →8→ 제목) · 배너 · 본문(공지 렌더러). **본문 이미지(I1)** = 본문 폭을 넘지 않게 · 원래 비율 · 모서리 8 · 설명은 화면 읽기용(캡션 없음). 글쓰기 = 관리자 공지 작성과 같은 편집기 · 흐름, 「게시 요청」 창(Compact)에 시작일 · 종료일(한 줄에 두지 않음 — 334 에서 잘림) · 대표 이미지(선택).
  수정 요청 · 반려 사유는 편집 화면 맨 위 알림. 내 글 = 공지 목록 줄 부품(상태 태그 · 제목 · 오른쪽 날짜 한 줄).
  삭제(2026-09-18 D1) = 고치기 화면 아래 줄 왼쪽 끝 빨간 테두리 「삭제」(반려된 글은 안내 →16→ 「삭제」 하나) → Compact 확인 창(닫기 없음 · 「취소」 + 채운 빨강 「삭제」), 공개 중이면 확인 문장에 「목록과 홈에서도 바로 사라진다」 한 줄.
- **상태 태그** `nl-tag--status`(2026-09-18 T3) = 기본 테두리 태그 + 앞에 색 점 8(사이 8 — 오락실 기체 「색 점 + 글자」 문법). 점 = 알림 표시색: 성공(진행 중 · 공개) · 정보(예정 · 수정 요청) · 경고(검토 대기) · 위험(반려), 중립(종료 · 임시저장)은 점 없음. 글자에 알림 색을 쓰지 않는다(12px 대비 4.5 미달 조합).
- **소식 입구(2026-09-26 N1 · E1):** 공지 · 이벤트 목록의 제목은 「소식」 →16→ 1단 탭 「공지사항 · 이벤트」(`NewsTabs`, 두 페이지 주소 그대로 — 게시판 · 데이터는 따로, 유저 글과 공식 공지를 섞지 않는다) →24→ 필터 칩(공지 분류 · 이벤트 상태). 헤더 메뉴는 그대로.
- **공지(2026-09-18 B1 · 2026-09-26 R1 · G1 · H1 · L2):** 목록 = 「소식」 머리 →24→ 분류 필터 칩(전체 · 업데이트 · 점검 · 데이터 · 안내, `FilterChipLinks` M · 주소 `?category=`) →24→
  고정 줄(활성 중대 공지 · 1쪽에만 · 목록 항목 카드 = surface · 모서리 8 · 안쪽 12 · 분류 태그 뒤 「고정」, 여럿이면 사이 8, 날짜 목록에서는 뺌) →24→ 날짜 목록.
  **줄(R1)** = 한 줄 — 분류(· 고정) 태그 →12→ 제목 16/600(남는 폭, 넘치면 줄바꿈) →12→ 오른쪽 날짜 `metadata` subdued · 위아래 12 · 줄 아래 divider. 672 미만은 「태그 … 날짜」 위 →4→ 제목. 요약 없음 · **월 제목 없음(G1)**.
  홈 공지 카드 · 내 이벤트 글도 같은 줄 부품. 홈 공지 카드는 제목 굵기 · 고정 표시를 바꾸지 않는다.
  **상세 본문** = 읽기 본문 `nl-body-reading` 16/28(H1 — 공지 · 이벤트 본문 · 편집기 미리보기만) · 폭은 읽기 폭 768 그대로 · 문단 사이 16 · 점 목록 · 번호 목록 모두 들여쓰기 24 · 본문 안 굵게는 같은 크기 · 행간에 600.
  소제목 h2 = 구역 역할 값(앞 문단과 32 · 첫 내용까지 12), h3 = 상자 제목 → 내용 8.
  **라벨 항목(L2)** = 「**라벨:** 설명」(「**라벨**: 설명」 도) 문단이 둘 이상 이어지면 렌더러가 라벨(`component-title`, 「:」 뺌) →8→ 설명, 항목 사이 16 목록(`dl`)으로 그린다. 하나뿐이면 문단 그대로. 글 쓰는 방식은 바뀌지 않는다.
  상세 끝 = 본문 →24→ 이전 글(더 오래된) · 다음 글(더 새) 두 칸(목록 항목 카드 · 사이 8 · 라벨 `metadata` subdued →4→ 제목 `body-secondary`, 다음 글은 오른쪽 정렬, 없는 쪽은 빈 칸 · 둘 다 없으면 칸째 없음) →16→ 「목록으로」 보조 버튼 L.
- **공개 채보 뷰어 배치(2026-09-26 A, 유튜브 시청 페이지):** 돌아가기 → **보기 방식 세그먼트(낙하형 · 전체 악보) →12→ 무대(조작 줄이 위에 겹침)** → 머리 → 채보 의견. 무대 폭 = 폭 가득, 단 첫 화면 안에 들어오게 (화면 높이 − 헤더 60 − 위 여백 24 − 돌아가기 44 − 24 − 컨트롤 높이 − 12 − 16) × 16/9 까지(가운데).
  좁은 화면(671 이하)은 낙하형 무대가 화면 끝까지(모서리 없음). 넓은 화면(1056+) 머리(무대 아래, 사이 12) = 제목 묶음(제목 · 난이도 → 현지화 제목 → 아티스트, 사이 4)
  → 채보 줄(왼쪽 작성자 = 동그란 프로필 이미지 32 · 「채보 · 이름」 + 라벨, 오른쪽 끝 동작 버튼 = 보조 M 아이콘+글자 「고치기」 · 「출처 보기」(영상 추출만) · 「공유」(주소 복사, `?t` 뺌), 사이 8, 넘치면 다음 줄)
  → 정보 상자(면 `surface` · 모서리 8 · 안쪽 12 16 · 줄 사이 8: 노트 수 · 공개 버전 · 길이 → 영상 추출 줄 → 손 범례).
  **1055 이하 머리(2026-09-26 Y1, 유튜브 폰 시청 화면)** = 무대 아래 12 → 제목 한 줄(`page-title`, 넘치면 줄임표 — 2026-09-26 T 모든 페이지 제목 규칙) →4→ 메타 한 줄(`metadata` subdued: 난이도 · 아티스트 · 노트 수 · 길이, 넘치면 줄임표) + 끝 「더보기」(`metadata` · `content/interactive`)
  →4→ 컨트롤 높이 한 줄(왼쪽 프로필 이미지 32 · 이름 · 라벨, 오른쪽 끝 글자 없는 고스트 아이콘 버튼 고치기 · 공유 — 이름은 `aria-label` · 툴팁, 접히지 않음. 출처 ⓘ 는 넓은 화면만).
  「더보기」 = 공용 대화상자 「채보 정보」 **아래 시트**(`ModalDialog sheet`, 2026-09-26 — 유튜브 폰 설명 창처럼): 제목 묶음 →16→ 정보 상자(노트 수 · 버전 · 길이 · 손 범례) →16→ 출처(「출처 보기」 창과 같은 목록: 채보 = 작성자 · 공개 버전 · 날짜, 영상 추출이면 노트 배치 설명 · 논문 · GitHub, 2026-09-26 M1).
  아래 시트 = 1055 이하에서 화면 아래 폭 가득 · 위 모서리만 `radius-overlay` · 아래 안쪽 24 + 안전 영역, 면 · 경계 · 머리는 대화상자 그대로, 나타날 때 아래에서 올라옴(⑫ `duration-move` 감속) · 닫힐 때 페이드. 1056 이상은 가운데 창.
  보기 방식 세그먼트는 무대 위에 늘 보인다(2026-09-26 V1 — 조작 줄 안 아이콘으로 옮겼더니 「낙하형만 있다」 로 보였다). Safari 도 막지 않는다(2026-09-26, 사용자 — 경고 없음).
- **공개 채보 뷰어 출처(2026-09-24 C2):** 채보 줄 = **「채보 · 이름」 + 운영자면 기본 태그 「Admin」**(`nl-tag`, 태그 24 는 글자 줄 20 안으로 당김 · 이름 = 공개한 사람 `username`).
  영상 추출 채보만 정보 상자에 **「노트 배치 · 영상에서 추출(vid2bmap)」(`metadata` subdued)**, 채보 줄에 「출처 보기」 버튼.
  「출처 보기」 = 공용 대화상자 「출처」: 채보(이름 + 태그 · 공개 버전 · 날짜) · 노트 배치(자동 추출 뒤 운영자 검수 · vid2bmap — 김영훈 · 최성희(KAIST) · MIT · 논문 · GitHub 새 탭), 항목 사이 16 + 구분선(2026-09-26) · 제목 → 내용 8. 캔버스는 그대로.
  **유저 기여 채보(2026-09-24 E1)** = 작성자 줄이 실제 작성자 + 이름 옆 기여 라벨(Lv.3 부터 · 운영자 「Admin」 — 공용 `ContributionLabel`), 출처 창 채보 항목 둘째 줄 「v18 · 운영자 이름 공개 · 날짜」.
- **공개 채보 뷰어 무대 · 전체화면(2026-09-25 A1 · B1):** 낙하형 무대 = 폭 가득 · **16:9 고정**(`aspect-ratio`). 그림은 16:9 논리 크기(1280 × 720)에 그려 폭에 맞춰 통째로 줄인다 — 원근 · 노트 간격 · 두께 · 선 굵기가 폭에 정비례라 어느 기기에서나 같은 그림(크기만 다름), 기기 픽셀 밀도로 그린다. 노트 속도(도착까지 시간)는 설정 그대로.
  가로 박자선(2026-09-25 G1) = 게임처럼 박마다 같은 선이 노트와 함께 내려온다(노트 뒤) — 레인 0–28 끝을 같은 원근으로 잇고 가까울수록 굵게(논리 2 × 0.6–1.4), 판정선 흰색 30%(2026-09-25 O2 — 55% 는 진해서 레인 안내선(38% 회색)과 비슷한 무게로). 마디선을 따로 진하게 하지 않는다.
  **조작 줄(2026-09-26 A)** = 인라인 · 전체화면 같은 줄, 무대 아래쪽에 겹친다 — 판 = 아래에서 위로 옅어지는 `surface/media-scrim` → 투명(위 32 · 옆 8 · 아래 4), 글자 `content/on-media`, 빈 곳은 무대로 지나감.
  재생 막대 한 줄(좌우 8) →4→ 버튼 줄(사이 4): 재생 · 처음으로 · 「시각 / 길이」 … 노트 속도 값(「2.0×」, 누르면 설정 창) · 톱니 · 전체화면. 미디어 위 버튼 44, 버튼 판 없음(올리면 `interaction/hover`). 좁은 화면은 처음으로 · 속도 값을 뺀다.
  재생 중 3초 가만히 있거나 마우스가 무대를 벗어나면 즉시 숨고(움직임 없음 · 커서도) 움직이거나 누르거나 포커스가 오면 다시. 멈춰 있으면 늘 보인다.
  톱니 「재생 설정」(2026-09-25 C1) → 떠 있는 창(⋯ 메뉴와 같은 규격 · 안쪽 12 · 줄 사이 12): 노트 속도 · 메트로놈 · 메트로놈 음량 · 엄밀한 연주 · 로컬 음원(버튼 →4→ 파일 이름 또는 안내, 280 안에서 줄바꿈). 창이 열려 있는 동안은 조작 줄을 숨기지 않는다.
  창과 셀렉트 목록은 전체화면이면 전체화면 요소 안에(밖에 뜨면 안 보임), 인라인이면 문서에(무대에 잘리지 않게 · `.noslog-ui` 감쌈).
  전체화면 = 조작 줄 오른쪽 끝 → 무대만 브라우저 전체화면(요소 전체화면이 없는 iPhone Safari 는 화면 채우기 · Esc 로 나감), 16:9 를 지켜 가운데(남는 곳 무대 바탕색).
  조작(2026-09-25, 유튜브와 같게) = 무대를 누르면 재생 · 일시정지(설정 창을 닫으려고 누른 것은 제외) · 스페이스 재생 · 일시정지 · ← → 한 마디 · Shift + > · < 노트 속도 ±0.1(2026-09-26) · 폰은 무대 왼쪽 · 오른쪽 절반 두 번 두드리기 ∓한 마디(이어서 두드리면 계속, 한 번이면 300ms 뒤 재생 · 일시정지).
  한 마디(2026-09-26, 사용자) = 앞으로는 다음 마디 처음, 뒤로는 그 마디 처음 — 마디 처음에서 300ms 안이면 앞 마디 처음(이어서 누르면 계속 뒤로). 글 입력 · 버튼 · 막대 · 셀렉트에 포커스가 있으면 그 부품 동작을 둔다.
  손가락인지 · 설정 창이 열려 있었는지는 누르기 시작(pointerdown)에서 가린다 — iOS WebKit 의 click 에는 pointerType 이 없고, 손가락으로 바깥을 누르면 창 닫힘이 click 뒤로 밀리기 때문(2026-09-25).
  낙하형 · 전체 악보 설명 안내 창과 「28칸 · 열당 4마디」 글자는 두지 않는다(2026-09-25, 사용자).
  전체 악보 = 읽기 줄(시각 / 길이) 오른쪽 끝에 같은 버튼 → 띠가 화면 높이를 채운다(안쪽 16, 열 폭 · 배율 상한 2.0 그대로라 한 번에 보이는 마디가 는다).
  **부품 고유 크기(2026-09-26 감사에서 가이드로 옮김):** 전체 악보 틀 높이 폰 560 · 넓은 화면 `max(560, 화면 − 헤더 60 − 48)` · 미니맵 폭 64(폰) · 96(672+) · 배율 0.16 ·
  미니맵 보이는 영역 틀 = `feedback/warning-marker` 2 + 같은 색 14% 면 · 모서리 4 · 이음새 글자 `metadata` `content/pending`(고정폭 아님) ·
  설정 창 음량 막대 폭 96 · 퍼센트 칸 40 · 음원 안내 줄바꿈 폭 280 · 손 범례 점 12 원 · 빈 채보 자리 = 무대처럼 16:9 ·
  무대 뒤 자켓 흐림(가장자리 −20 · 흐림 24 · 1.1배 · 투명 0.25) → 원형 빛(무대 검정 + 흰색 25% 를 22%) · 가장자리 어둠(무대 검정 96%).
  재생 막대 · 음량 막대는 공용 슬라이더 값(손잡이 20 · 후광 6), 미디어 위 버튼 포커스도 공용 포커스(1px 흰 선).
  **캔버스 색(2026-09-26 — 게임 화면을 흉내 낸 그림 재료라 토큰 대신 여기 적는다):** 낙하형 = 노트 면 #f8f7f1 · 판정선 #f2f0e9 / 가장자리 #8f929d · 레인 안내선 #727786 · 박자선 면 #1c2029 · 선 #565a63 · #bfc2c4 · 트릴 마름모 #f2c75c,
  건반 = 흰 #e7e6e1 · #d2d4d4 · 검은 #161820. 전체 악보 = 열 바탕 #111117 · 칸 줄 #20202a · #2a2a34 · #343441 · 마디 번호 #c2c2cc · #8e8e9a · #656574 · 노트 테두리 #f7f7f2 · 강조 #fff8df,
  글자 = 그림 안 8 · 9 · 10(그림째 최대 2배로 보임). 무대 바탕 · 손 색은 위 토큰.
  열 이음새 라벨(열 맨 위) = 그 경계에서 BPM · 박자가 바뀌면 「BPM 112 · 4/4」 를 먼저, 시각을 그 아래에(2026-09-25) — 캔버스가 그리면 열 아래 여백과 함께 잘려 반만 보이고 시각과 겹쳐서 글자로 옮겼다.
- **공개 채보 뷰어 기여(2026-09-24 A1 · D1):** 「고치기」 = 채보 줄 동작 버튼(내 초안이 있으면 「내 초안 · 상태」, 공용 `ChartDraftEntry`).
  **「채보 의견」 구역** = 머리 아래 · 공용 제목 줄 「채보 의견 (N)」(로그아웃이면 오른쪽 끝 「로그인하고 의견 남기기 ›」) →12→ 쓰기 칸 →16→ 목록 · 빈 안내.
  쓰기 칸 = 시각(control · 링크색 · 고정폭 숫자, 지금 재생 위치를 따라가다 글을 쓰기 시작하면 멈춤 · 누르면 지금 위치로 다시 맞춤) · 한 줄 입력 · 주 버튼 M 「남기기」, 사이 8.
  목록 줄 = 의견 줄 문법에서 아바타만 뺌(이름 줄 20 = 시각(`emphasis-label` 링크색) · 이름 · 기여 라벨 · 날짜 · 해결이면 상태 태그 성공 「해결」 · ⋯ →4→ 본문), 줄 사이 16 · 시각 순.
  시각 글자는 링크색 — 시안의 경고색 글자는 가이드(글자에 알림 색 금지)에 맞춰 바꿈. 시각을 누르면 낙하형으로 그 시각에 서고 캔버스 위 끝이 머리 아래(76)에 오도록 올라가며 주소에 `?t=42.3`(초).
  진행 막대 눈금 = 해결 안 된 의견 시각마다 `--nl-feedback-warning-marker` 2 × 12(누르지 않음 · 손잡이가 움직이는 폭에 맞춤). ⋯ = 내 의견 「의견 삭제」(해결 전) · 운영자 「해결로 표시」 · 「가리기」.
- **채보 에디터 모드(2026-09-24 B1 · C1):** 캔버스 · 속성 칸 · 재생 막대는 세 모드 공통, 위 막대 · 레일만 다르다. 에디터 글은 ko · ja · en(관리자 전용 버튼 · 저장 이력 · 영상 추출 창은 한국어 그대로).
  관리자 = 지금 그대로(버전 저장 · 공개 · 미리보기 · 가져오기 메뉴 · 저장 이력). 기여자(`/music/…/pattern/draft`) = 자동 저장 글 · 실행 취소 · 다시 실행 · 채보 파일 불러오기 · 내보내기 → 상태 태그(기존 상태 태그 12/600) + 주 버튼 「검토 요청」(요청 중이면 테두리 「요청 취소」, 읽기 전용 · 작성 도구 숨김).
  검토(`/admin/music/…/pattern/review/[id]`) = 「읽기 전용」 · 내보내기 → 상태 태그 · 작성자 + 기여 라벨 · 테두리 「수정 요청」 · 주 버튼 「공개」(검토 대기일 때만).
  레일 세 번째 탭 「검토 · N」(남은 댓글 수) = 단계 줄(작성 중 · 검토 대기 · 수정 요청 · 공개, 막대 4 · 지금까지 채움) → 「0:42.300 에 댓글」 + 여러 줄 입력 + 「남기기」 → 댓글 목록(시각 누르면 그 시각으로 · 운영자 「해결」 · 해결 전 내 댓글 「삭제」). 이 탭에서도 캔버스는 채보 작성 화면, 오른쪽은 노트 속성 칸 그대로.
  해결 안 된 댓글 시각 = 캔버스 가로 점선 2px(판정선 위에도 보이게 맨 위) · 파형 위아래 눈금 2 × 12, 둘 다 `--nl-feedback-warning-marker`(스냅 확인 점선과 같은 색).

- **채보 트릴(2026-09-24 B′):** 낙하형 · 전체 악보 · 채보 에디터 모두 게임식 — 머리 · 끝 막대 = 두 자리를 합친 범위 전체(머리 가운데 마름모 두 개 `#f2c75c` · 끝 막대는 캔버스 바탕색 + 손 색 테두리),
  몸통 = 같은 범위 폭 육각형을 사분음표 1/12 마다 쌓고 육각형마다 그때 칠 자리 쪽만 손 색(투명도 0.82), 반대편은 범위 끝까지 옅어짐(첫 육각형 = 첫 자리, 다음부터 번갈아). 채보 에디터 캔버스도 같은 모양(사용자 — 선택 · 충돌은 몸통 테두리). 공용 계산 `lib/chart-pattern/trillShape.ts`.

## 5. 손대지 않는 곳

[AGENTS.md 「손대지 않는 곳」](../../AGENTS.md)을 따른다 — 채보 에디터 · 관리자 · 음원.

## 6. 확인

- 바꾼 화면은 실제 브라우저에서 CSS px 로 잰다: 320 · 390 · 768 · 1280, 바뀐 경계의 양쪽, 한 · 일 · 영.
  가로 넘침이 없다는 것만으로 레이아웃을 통과시키지 않는다 — 같은 줄 높이 · 여백 · 정렬을 함께 본다.
- typecheck · lint · 관련 테스트. **e2e 는 로컬 테스트 DB 에서만** — 사용자의 localhost:3000(개발 DB)에 돌리지 않는다. 명령은 [AGENTS.md](../../AGENTS.md) 「스택 · 명령」.
- 보고는 실제로 돌린 것만. 안 돌린 검사는 「안 함」 이라고 쓴다.
