# NosLog 2.0 Authoritative Remaining-Work and Preservation Audit

## Document control

- Status: `Corrected after history audit — six evidence-backed remaining blocks`
- Canonical language: English
- Korean companion:
  [57-noslog-2.0-authoritative-remaining-work-audit.ko.md](./57-noslog-2.0-authoritative-remaining-work-audit.ko.md)
- Date: 2026-08-10
- Inputs: root `AGENTS.md`, root `README.md`, documents `01`–`56`, current
  repository evidence, and the user's explicit viewer/editor preservation correction
- Purpose: prevent stale future checklists from reviving completed or prohibited work

## Authority rule

This document and the root `README.md` progress table are the current remaining-work
authority. Earlier documents remain evidence for decisions already made, but their
future-tense checklists do not create work when this audit marks them superseded,
downstream, closed, or prohibited.

## Locked chart viewer/editor boundary

The existing chart viewer and chart editor are preserved in their entirety. This lock
includes their pages, DOM shell, controls, labels, accessibility behavior, responsive
composition and containment, PixiJS/WebGL Falling renderer, Canvas Full-sheet
renderer, note and left/right-hand palettes, geometry, animation, chart mathematics,
and editor rendering model.

No remaining block may redesign, recolor, restyle, reorganize, replace, reinterpret,
or create a NosLog 2.0 variant of either experience. Earlier viewer/editor page briefs
are historical functional records, not active redesign scope. Only a later explicit
user decision naming the reopened scope can change this lock.

## Rejected percentage baseline

Git history shows that the fixed eighteen-package denominator was introduced in commit
`1341352` during the Material Geometry documentation. It was not derived from a prior
user-approved roadmap. The user explicitly rejected this inflated count on 2026-08-10.
Therefore `12.5/18 = 69%`, fractional package credit, and the eighteen-package ledger
are superseded. Do not report another percentage until the user approves a denominator
and counting method.

## Exact remaining work

| Block                                      | Exact remaining scope                                                                                                                                                                                                       | Explicit exclusions                                                                                         | Completion result                                                        |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `1 · C5 color closeout`                    | Select the exact authoritative Light/Dark values and four-role mapping for the already required distinct Normal/Hard/Expert/Real colors; then approve comparison-local score-band, FAST/SLOW, series, and threshold colors. | Entire viewer/editor; Basic/Recital, rank/achievement, and genre remain neutral unless separately reopened. | Difficulty and local data-color decisions approved or explicitly closed. |
| `2 · Iconography`                          | Define ordinary-UI icon grammar, icon-only/label policy, size/stroke, and accessible-name rules.                                                                                                                            | Entire viewer/editor and final logo drawing.                                                                | Approved ordinary-UI iconography rules.                                  |
| `3 · Motion`                               | Define ordinary-UI motion purpose, duration/easing roles, and reduced-motion alternatives.                                                                                                                                  | Entire viewer/editor, renderer timing, transport, and editor motion.                                        | Approved ordinary-UI motion contract.                                    |
| `4 · Data visualization`                   | Define ordinary-data visualization axes, legends, exact values, tooltip/focus behavior, and non-color/table fallback.                                                                                                       | Entire viewer/editor and its chart rendering.                                                               | Approved ordinary data-visualization anatomy.                            |
| `5 · Foundation promotion and reusable UI` | Run final regression on eligible `S1`, `S2`, `S3`, and `S5` fixtures, verify Pretendard JP delivery/fallback, promote Foundation v0.1, and consolidate validated component aliases, patterns, and templates.                | No `S4`, no `S6`, no new structural page work, and no reopening completed inputs.                           | Approved Foundation and reusable ordinary-UI guidance.                   |
| `6 · Handoff and milestone export`         | Consolidate existing page briefs into screen requirements, implementation mapping, accessibility/localization QA, and Claude Design handoff; decide PDF language/packaging and export the stable versioned milestone.       | Do not reopen page briefs, add viewer/editor changes, or create the final high-fidelity production design.  | Complete downstream handoff and versioned distribution artifact.         |

Research, specimens, browser checks, bilingual updates, and export steps inside a block
are subtasks. They must not be split into new top-level work to enlarge the count.

## Block-level execution cadence

These six blocks are also the only user-facing units of execution. After starting one
block, continue its research, candidate comparison, specimens, browser checks, required
material user decisions, bilingual documentation, and consolidation within that same
block. Pause only for a material decision that belongs to the user, missing authority,
or completion of the block. An approval inside a block changes the block's state and
then work resumes in that block; it never creates a new package or remaining item.
Internal documents, candidates, gates, and validation passes must not be announced as
the next task.

## Difficulty UI Gate explained

The open difficulty comparison does not concern chart notes, hand colors, or any
viewer/editor element. In eligible ordinary UI, `Normal/Hard/Expert/Real` must remain
visibly distinguishable through four different persistent colors, reinforced by name,
numeric level, fixed order, and explicit selection. This requirement is approved and
must not be reopened as a color-versus-neutral decision.

Block `1` first decides the exact authoritative Light/Dark values and four-role
mapping. Document `56` compares eleven exact published candidates; its first-pass
shortlist is Spectrum S2, Carbon, and SAP, while the final mapping remains unapproved.
Neutral `DU-D0` is rejected and is not a candidate. If no candidate satisfies
provenance, contrast, and content requirements, return the exact-value failure to the
user rather than silently removing the required color distinction.

## Superseded, downstream, and closed items

| Earlier item                                                              | Current disposition                                                                        |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Document `24` `FBR-05` and `S6` editor specimen                           | `Superseded`; do not execute.                                                              |
| Document `24`/`26` integrated `S1`–`S6` future validation                 | `Superseded`; block `5` uses ordinary fixtures `S1`, `S2`, `S3`, and `S5` only.            |
| Document `52` future `S4` viewer or `S6` editor material regression       | `Superseded`; do not execute.                                                              |
| Viewer/editor DOM shell, controls, responsive or accessibility adaptation | `Prohibited by preservation lock`; not a remaining task.                                   |
| Final NosLog mark drawing                                                 | `Downstream Claude Design deliverable` under `ITA-C`; not a Codex Foundation gate.         |
| Finding a Radix shell placement                                           | `Closed`; there is no approved placement and no search resumes unless the user reopens it. |
| Complete final high-fidelity pages or Figma screen suite                  | `Downstream Claude Design deliverable`; outside this session.                              |
| Production NosLog 2.0 implementation                                      | `Future Codex implementation session`; outside this session.                               |

## Not remaining work

Do not count or restart the completed product/IA/page-brief/foundation scopes,
`S1`–`S5` structural design,
approved neutral/focus/identity/action/material decisions, rejected `FCM-11` or
`SIG-07`, the legacy NOSTORY Figma, viewer/editor redesign, a new `S6`, final mark
drawing, final high-fidelity screens, or application implementation.

## Current material decision

Block `1` currently needs the user's choice of the exact authoritative Light/Dark
source and four-role mapping for the already approved distinct
Normal/Hard/Expert/Real colors. After that choice, continue the same block with the
ordinary local data-color comparison. Blocks `2`–`6` start only after block `1` is
complete; internal decisions do not become separate remaining work.

## Decision log

| ID       | Entry                                                                                                       | Status                                                     |
| -------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| `RWA-01` | Make this audit and the README table the current remaining-work authority.                                  | `Approved scope correction — 2026-08-10`                   |
| `RWA-02` | Preserve the entire existing viewer/editor and cancel every active `S4`/`S6` design task.                   | `Approved correction — 2026-08-10`                         |
| `RWA-03` | Keep the fixed eighteen-package denominator and `12.5/18 = 69%` progress.                                   | `Rejected and superseded — unsupported count`              |
| `RWA-04` | Classify final mark drawing as downstream and Radix placement exploration as closed.                        | `Scope classification`                                     |
| `RWA-05` | Keep four different ordinary difficulty colors; leave only their exact source values and role mapping open. | `Requirement approved; exact mapping awaiting user review` |
| `RWA-06` | Track six evidence-backed remaining blocks without a completion percentage or fractional credit.            | `Corrected after Git-history audit — 2026-08-10`           |
