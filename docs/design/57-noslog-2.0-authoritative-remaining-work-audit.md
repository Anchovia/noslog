# NosLog 2.0 Authoritative Remaining-Work and Preservation Audit

## Document control

- Status: `Approved scope correction — authoritative remaining-work ledger`
- Canonical language: English
- Korean companion:
  [57-noslog-2.0-authoritative-remaining-work-audit.ko.md](./57-noslog-2.0-authoritative-remaining-work-audit.ko.md)
- Date: 2026-08-10
- Inputs: root `AGENTS.md`, root `README.md`, documents `01`–`56`, current
  repository evidence, and the user's explicit viewer/editor preservation correction
- Purpose: prevent stale future checklists from reviving completed or prohibited work

## Authority rule

This document and the root `README.md` progress table are the current remaining-work
authority. Earlier documents remain evidence for decisions already made, but their
future-tense checklists do not create work when this audit marks them superseded,
downstream, closed, or prohibited.

## Locked chart viewer/editor boundary

The existing chart viewer and chart editor are preserved in their entirety. This lock
includes their pages, DOM shell, controls, labels, accessibility behavior, responsive
composition and containment, PixiJS/WebGL Falling renderer, Canvas Full-sheet
renderer, note and left/right-hand palettes, geometry, animation, chart mathematics,
and editor rendering model.

No remaining package may redesign, recolor, restyle, reorganize, replace, reinterpret,
or create a NosLog 2.0 variant of either experience. Earlier viewer/editor page briefs
are historical functional records, not active redesign scope. Only a later explicit
user decision naming the reopened scope can change this lock.

## Fixed management progress

- Complete packages: `12`
- In-progress packages: `1`, counted as the single authorized `0.5`
- Current progress: `12.5 / 18 = 69%`
- Exact remainder: package `13` completion plus packages `14`–`18`, or `5.5` fixed
  management packages

This is a management percentage, not an estimate of time or effort.

## Exact remaining work

| Package | Exact remaining scope                                                                                                                                                                                                                                                           | Explicit exclusions                                                                                               | Completion result                                                            |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `13`    | Approve neutral ordinary difficulty UI, or explicitly authorize a custom semantic-mapping exception; then research and approve comparison-local score-band, FAST/SLOW, series, and threshold colors.                                                                            | Entire viewer/editor; Basic/Recital, rank/achievement, and genre remain neutral unless separately reopened.       | `13B` and `13C` approved or explicitly closed; package becomes `Complete`.   |
| `14`    | Define icon grammar, icon-only/label policy, size/stroke and accessible-name rules; ordinary UI motion and reduced-motion rules; ordinary data-visualization axes, legends, tooltip/focus behavior, and non-color/table fallback.                                               | Entire viewer/editor; no renderer, transport, editor-control, or editor-motion specimen.                          | Approved ordinary-UI iconography, motion, and data-visualization rules.      |
| `15`    | Run the final Foundation regression on eligible completed ordinary-UI fixtures `S1`, `S2`, `S3`, and `S5`, checking only drift caused by later approved appearance rules; promote Foundation v0.1.                                                                              | No `S4`; no `S6`; no new structural specimen; no reopening completed typography/layout/color inputs.              | Regression record and approved Foundation v0.1 promotion.                    |
| `16`    | Consolidate ordinary application component aliases, anatomy, states, patterns, templates, and desktop adaptations: shell/navigation, actions, forms, selectors, rows/cards, feedback, tables, pagination, overlays, empty/loading/error, and relevant administrative workflows. | Entire viewer/editor; no final high-fidelity page suite.                                                          | Approved reusable ordinary-UI system and template rules.                     |
| `17`    | Consolidate the already approved page briefs into screen requirements, implementation mapping, accessibility and Korean/Japanese/English acceptance, browser QA, and a Claude Design handoff package.                                                                           | Do not reopen completed page briefs; do not add viewer/editor changes; do not create the final production design. | Complete downstream handoff with conflicts and acceptance criteria explicit. |
| `18`    | Decide PDF language and packaging, then export the stable editable guide as a dated, versioned PDF milestone.                                                                                                                                                                   | No export before canonical English and complete Korean companions match.                                          | Versioned distribution artifact and archive note.                            |

Expected fixed milestones after each package completes are `13/18 = 72%`, `14/18 =
78%`, `15/18 = 83%`, `16/18 = 89%`, `17/18 = 94%`, and `18/18 = 100%`.

## Difficulty UI Gate explained

The open difficulty comparison does not concern chart notes, hand colors, or any
viewer/editor element. The current ordinary NosLog UI repeatedly colors
`Normal/Hard/Expert/Real` in music cards, music-detail selectors, profile rows, exam
UI, and tier/admin UI. Package `13B` therefore has to decide whether NosLog 2.0:

1. keeps a restrained redundant color cue as a small marker (`DU-D1`); or
2. removes persistent difficulty color and relies on visible name, numeric level,
   fixed order, pattern, and explicit selection (`DU-D0`).

The proposed Spectrum values are published adaptive color values, but Adobe does not
define them as NOSTALGIA difficulty semantics. Assigning green/orange/red/purple to
Normal/Hard/Expert/Real is an invented NosLog mapping and fails the current rule to
adopt both published values and semantic mapping intact. Therefore `DU-D0` is the only
approvable candidate under current provenance rules. Choosing `DU-D1` would first
require the user to explicitly authorize a custom semantic-mapping exception.

## Superseded, downstream, and closed items

| Earlier item                                                              | Current disposition                                                                        |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Document `24` `FBR-05` and `S6` editor specimen                           | `Superseded`; do not execute.                                                              |
| Document `24`/`26` integrated `S1`–`S6` future validation                 | `Superseded`; package `15` uses ordinary fixtures `S1`, `S2`, `S3`, and `S5` only.         |
| Document `52` future `S4` viewer or `S6` editor material regression       | `Superseded`; do not execute.                                                              |
| Viewer/editor DOM shell, controls, responsive or accessibility adaptation | `Prohibited by preservation lock`; not a remaining task.                                   |
| Final NosLog mark drawing                                                 | `Downstream Claude Design deliverable` under `ITA-C`; not a Codex Foundation gate.         |
| Finding a Radix shell placement                                           | `Closed`; there is no approved placement and no search resumes unless the user reopens it. |
| Complete final high-fidelity pages or Figma screen suite                  | `Downstream Claude Design deliverable`; outside this session.                              |
| Production NosLog 2.0 implementation                                      | `Future Codex implementation session`; outside this session.                               |

## Not remaining work

Do not count or restart completed packages `1`–`12`, `S1`–`S5` structural design,
approved neutral/focus/identity/action/material decisions, rejected `FCM-11` or
`SIG-07`, the legacy NOSTORY Figma, viewer/editor redesign, a new `S6`, final mark
drawing, final high-fidelity screens, or application implementation.

## Open user decisions

1. Package `13B`: approve `DU-D0` fully neutral difficulty UI, or explicitly reopen
   the provenance rule to authorize a custom NosLog difficulty-color mapping.
2. Package `13C`: approve a later exact-source comparison for ordinary local data
   colors.
3. Packages `14`–`18`: approve each material design or packaging gate when its evidence
   is presented; this roadmap does not pre-approve their design choices.

## Decision log

| ID       | Entry                                                                                     | Status                                   |
| -------- | ----------------------------------------------------------------------------------------- | ---------------------------------------- |
| `RWA-01` | Make this audit and the README table the current remaining-work authority.                | `Approved scope correction — 2026-08-10` |
| `RWA-02` | Preserve the entire existing viewer/editor and cancel every active `S4`/`S6` design task. | `Approved correction — 2026-08-10`       |
| `RWA-03` | Keep the fixed eighteen-package denominator and current `12.5/18 = 69%` progress.         | `Confirmed`                              |
| `RWA-04` | Classify final mark drawing as downstream and Radix placement exploration as closed.      | `Scope classification`                   |
| `RWA-05` | Keep the ordinary difficulty UI choice open and require explicit user approval.           | `Awaiting user review`                   |
