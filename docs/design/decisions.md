# 결정 기록

지금도 유효한 사용자 결정만, 새것부터. 규칙 자체는 [가이드](./README.md)에 있고 여기엔 **무엇을 왜** 만 적는다.
뒤집힌 결정은 지운다(이력은 git 에 있다). Figma 시절 결정 표는 2026-09-14 에 정리했다.

## 2026-09-26

- **이미지 내보내기 창 = 폰 · 태블릿 전체 화면(A, 사용자 · 추천대로)** — 프로필 카드 · 서열표 공유가 폰에서 가운데 창(334) 안에 이미지를 286 폭으로 가둬 답답하던 것을 바꿈. 서비스 44곳 · 가이드 6개 조사: 폰에서 작은 가운데 창에 이미지를 넣는 곳 0, 이미지 내보내기는 전체 화면(Instagram 프로필 카드 · Telegram QR · 카카오톡 QR · LinkedIn QR). 1055 이하 전체 화면 · 이미지 폭 끝까지(402 폭에서 카드 402 × 211) · 버튼 아래 고정, 1056 이상 가운데 창 그대로. 카드 · 서열표 그림과 버튼 구성은 그대로.
- **대화상자 = GitHub(Primer) 3칸 창(A, 사용자 · 추천대로)** — 334 한 폭 · 칸 없음 · 버튼 등폭 · 폰도 가운데 창이던 임시 모양을 바꿈. 서비스 29곳(모달이 열린 곳, 시도 90여 곳) · 가이드 7곳 실측: 머리 구분선 + 본문만 스크롤 = GitHub · GitLab · Airbnb + Primer · Fluent · Carbon, 발 버튼 오른쪽 「보조 → 주」 = Primer · Atlassian · Apple · Fluent · Material 3(서비스 실측은 Airbnb 하나라 가이드 근거), 좁은 화면 입력 창 = 전체 화면 · 그 밖 = 아래 시트(Primer). 크기 small 440 · medium 560 · large 768(440 · 560 은 실측 폼 창 폭 440–568 의 가운데 — 새 값 허락). 확인 창은 큰 제목 · 닫기 · 구분선 없음. Airbnb 가운데 제목(B)은 확인 창이 없고 폰 근거가 없어, 당근 큰 제목(C)은 칸 나눔이 없어 물림. 모서리는 GitHub 12 대신 기존 10.
- **대화상자 안 내용 = 종류별 개편(사용자 · 모두 추천대로)** — 3칸 틀만 바뀌고 안은 그대로라 36개 창을 확인 · 정보 · 고르기 · 폼 · 그 밖으로 나눠 다시 그림(서비스 35곳 · 가이드 9개). 확인 창은 유지하고 회원 탈퇴(입력 칸이 있음)만 기본 창 + 번호 단계로, 빙고 초기화 확정 버튼은 채운 위험. 정보 창 = 소제목 + 구분선 줄 목록(GitHub · GitLab · tldraw · Excalidraw 도움말 창). 고르기 창 = 칩 · 돋보기 검색 · 체크 줄(당근 지역 · Booking 통화 · Google 항공편), 관리자 과제곡 검색을 공용 창으로, 선호 오락실 링크 `/arcades` → `/gamecenter` 고침. 폼 창 = 로그아웃이면 로그인 안내(Instagram · Pinterest · Spotify), 버튼은 발로, 신고 사유는 라디오(Carbon · Material 3). 피드백 종류는 항목을 늘릴 거라 셀렉트 유지(사용자). 채보 뷰어 출처 · 정보 창과 이미지 내보내기 창은 다른 세션 작업 중이라 뒤로. 새 토큰 없음.
- **프로필 토큰 점검(사용자 · 추천대로)** — 프로필을 가이드 값과 대조(다른 세션 감사 결과 합침). 가이드 규격으로: 통계 막대 = 공용 트랙 8 · 모서리 4 · raised(랭크 분포 6 · 판정 16 둥근 막대를 버림, 판정은 `StackedBar` 라벨 없는 한 줄), 판정 색 표시 28 × 10 알약 → 원 8(악곡 상세와 같음), 성장 추이 여백 덮어쓰기 삭제(악곡 상세와 같은 모양), 본인용 공유 · 설정 = 보조 아이콘 버튼 M(32 고정 · 모서리 4 → 폰 36 · 모서리 8). 화면 그대로 공용으로: 국기 `size="large"` · 아바타 첫 글자 `nl-display`(승인). 뜻 있는 숫자(표 열 폭 · 표 머리 32 · 누르는 높이 24 · 범례 칸 12 · 구역 이동 76)는 유지하고 가이드에 적었다. 새 토큰 없음.
- **업적 · 공유 카드 정리(사용자 · 추천대로)** — 업적 줄 조건 · 날짜 글(`metadata`)의 고정폭 숫자를 뺀다(가이드: 고정폭은 metric 두 클래스만, E1). 촘촘한 격자 칸 최소 52 는 값 그대로 `44 + spacing-8` 로 쓴다(E2). 업적 고르기 창 칸 최소 88 은 유지하고 가이드에 적는다(고른 이유는 기록 없음, E3). 공유 카드 명판 금속 · 속 색은 `EXAM_TIER_DARK_HEX` 로 묶고 테스트가 tokens.css 와 대조한다(순위 색도 이 값을 읽음, E4). 새 토큰 없음 · 화면 그대로(E1 숫자 폭만).
- **활동 달력 안내 = 「최근 1년 기록」(사용자)** — 「옆으로 밀면 1년」 을 바꿈(en 「Past year of plays」 · ja 「直近1年の記録」). 설명 문구라 넓은 화면에서도 숨기지 않는다(사용자).
- **영상 추출 겹침 = 겹친 채로 넣고 선택해 두기(A, 사용자 · 추천대로)** — かぞへうた · 平均律 · パヴァーヌ 가 긴 테누토와 그 안 노트가 겹쳐 넣을 수 없었다. 영상 콤보로 보면 곡마다 가짜 쪽이 달라(パヴァーヌ 는 안쪽 노트, 平均律 은 테누토) 모양만으로 자동으로 못 고친다. 가져온 노트끼리의 겹침만 선택해 넣고 사람이 고친다(기존 초안 노트와 겹치면 예전처럼 막음 — 운영에서 기존 초안에 넣어 겹친 일이 있었다) · 버전 저장은 겹침을 고친 뒤(「영상 추출」 버전은 그때 남김). 콤보로 자동 판별(B)은 콤보 기록이 쌓인 뒤 따로, 막기(C)는 뺐다.
- **채보 뷰어 토큰 정리(사용자 · A2 만 가이드대로, 나머지 추천대로)** — 전수조사에서 가이드와 다른 값 · 토큰 없는 색 · 가이드에 없는 숫자를 찾았다. 가이드 값으로: 슬라이더 후광 8 → 6, 미디어 버튼 포커스 2px + 띄움 → 공용 1px 흰 선(추천은 미디어 예외 유지였지만 「가이드대로」), 폰 제목 → `page-title`, 「더보기」 굵기 빼기, 페이지 위아래 여백 덮어쓰기 삭제, 무대 폭 계산을 토큰으로, 고정폭 숫자 빼기, 이음새 글자 `content/pending`, 미니맵 틀 경고 노랑 · 모서리 4, 폰 작성자 줄 48 → 컨트롤 높이, 빈 자리 256 → 16:9. 새 토큰 3개: 무대 검정 세 가지(#05070c · #070910 · #0b0b10) → `stage/canvas` #070910, 손 색 세 벌 → `hand/left · right`(낙하형 노트 값 H1, 눌린 건반은 흰색 45%). 나머지 부품 크기와 캔버스 색은 가이드에 적어 공식화. 채보 에디터의 건반 색(`chartPianoColors`)은 손대지 않음.
- **채보 뷰어 ← → · 두 번 두드리기 = 한 마디(사용자 · 추천대로)** — 유튜브 값(5초 · 10초)은 채보 연습에 너무 멀고, 초 단위는 템포마다 박 사이에 멈춘다(BPM 150 에서 1초 = 마디의 62%). 마디선에 맞춰 이동 — 전체 악보 · 마디 번호와 같은 단위, Soundslice 도 마디선 이동 단축키를 둔다(도움말 실측). 1초는 쓰는 서비스를 찾지 못해 물림. 되감기 300ms 는 두 번 두드리기 간격과 같은 값.
- **채보 뷰어 폰 = 「더보기」 창 하나로 · 출처 ⓘ 삭제(M1, 사용자 · 추천대로)** — 「더보기」 와 공유 옆 ⓘ(출처)가 작성자 · 공개 버전 · 영상 추출을 둘 다 보여 겹쳤다. 유튜브 폰 설명 창처럼 출처(검수 · vid2bmap · 논문 · GitHub)를 「더보기」 창 끝에 합친다. ⓘ 하나로(M2)는 글자 입구가 없어지고 영상 추출이 아닌 채보에도 입구가 필요해 물림. 넓은 화면은 「더보기」 가 없어 「출처 보기」 그대로.
- **채보 뷰어 「더보기」 = 아래 시트(사용자 제안)** — 가운데 대화상자로 열려 유튜브 폰(설명 창이 아래에서 올라옴)과 달랐다. 공용 대화상자에 `sheet` 선택을 더해 1055 이하에서 화면 아래에 붙인다(면 · 머리 · 닫기 그대로, 움직임은 오락실 시트와 같은 ⑫). 끌어 내리기 닫기는 없음.
- **채보 목록 메타 줄(작성자 · 영상에서 추출 · 공개일) 삭제(사용자)** — 「들어가서 자세히 보면 되는 의미 없는 정보」. 줄은 자켓 · 이름 · 아티스트 · 난이도 칩만. 「최근 공개 순」 정렬은 그대로.
- **채보 뷰어 폰 머리 = 유튜브 폰 시청 화면(Y1, 사용자 · 추천대로) · 작성자 옆 동그란 프로필 이미지(사용자)** — 넓은 화면 머리를 폰에 그대로 접어 무대 아래가 272 높이 6줄 + 상자, 테두리 버튼 셋이 다음 줄에 뭉쳤다(「유튜브처럼 구현도 안 했다」). m.youtube.com 실측대로 제목 한 줄 · 메타 한 줄 + 「더보기」 · 48 줄(채널 · 글자 없는 아이콘 버튼) — 무대 아래 100. 손 범례 · 버전 · 영상 추출은 「더보기」 창으로. Spotify 폰(Y2)은 범례 둘 자리가 없어 물림. 넓은 화면(1056+)은 그대로에 프로필 이미지만.
- **채보 보기 방식 세그먼트 = 무대 위에 다시(V1, 사용자 · 추천대로)** — 개편(A)에서 조작 줄 안 두루마리 아이콘으로 옮겼더니 글자가 없고 재생 중에 숨어 「채보 뷰어는 어디 갔나 · 왜 낙하형만 있나」(사용자). 조작 줄 안 글자 전환(V2, Soundslice · ArcViewer)은 폰 폭에 들어가지 않아 다시 숨어서 물림. 무대는 세그먼트만큼 내려가도 재생 버튼은 첫 화면 안. 같은 날 Safari 낙하형 차단 · 경고도 지움(사용자 — 「막지 마」, 채보 에디터의 Safari 차단은 그대로).
- **공개 채보 뷰어 = 유튜브 시청 페이지 배치(A, 사용자)** — 와이어프레임 수준이던 화면을 다시 짰다. 1440 × 900 에서 재생 버튼이 y 1067(첫 화면 밖), 폰은 조작부 300 이 무대 201 보다 컸다. 무대 먼저 · 조작은 무대 위에 겹침(인라인 · 전체화면 같은 줄, 재생 중 3초 뒤 숨김) · 곡 정보 · 동작 버튼 · 정보 상자는 무대 아래. 서비스 22곳 · 가이드 6곳 시안에서 처음엔 아래 고정 도크(C, Songsterr 등 4곳)를 추천했지만 「곳 수」 로는 친숙함을 잴 수 없고(유튜브 조작은 모두가 손에 익음 · HIG 「시스템 플레이어를 따르라」), 16:9 상자인 무대는 영상과 같은 모양이며 전체화면 · 조작 방식이 이미 유튜브식이라 A 로 바꿈(사용자). 연습 중 속도는 조작 줄 속도 값 · Shift + > · < 로. 「채보 뷰어 캔버스는 그대로」 규칙은 폐기(사용자 — 「오래된 얘기」).
- **공지 · 이벤트 = 「소식」 한 입구 + 공지사항 · 이벤트 탭, 이벤트 상태는 칩(N1 · E1, 사용자)** — 두 게시판이 따로라 입구가 둘이었다. 실서비스 45곳 중 유저가 쓴 글을 공식 공지와 한 목록에 섞는 곳은 0곳(한 게시판 + 분류는 게임 8곳이지만 모두 운영자 글)이라 게시판 · 데이터 · 주소는 그대로 두고 두 목록 머리에 같은 1단 탭만 둔다(메이플 · 로아 · 던파 · Linear · Vercel 방식). 이벤트의 「진행 중 · 예정 · 종료」 탭은 탭 두 줄(E2)이 위계를 흐려 공지 분류와 같은 필터 칩으로 — 두 쪽이 「탭 → 칩」 같은 구조. 새 토큰 · 새 부품 없음.
- **공지 읽기 = 읽기 본문 16/28 · 소제목 역할 값 · 라벨 항목 · 목록 한 줄 · 월 제목 없음(H1 · L2 · C0 · R1 · G1, 사용자)** — 본문이 16/24 · 소제목 위아래가 문단과 같은 16 · 「라벨: 설명」 문단이 일반 문단과 같아 벽처럼 읽혔다. 실서비스 45곳(게임 공지 14 · 변경 기록 16 · 한국 서비스 15) · 가이드 7곳 실측: 한국어 긴 글 행간 중앙값 1.7(13곳 중 9곳 1.6–1.8) → 글자 표에 `nl-body-reading` 16/28 한 줄(새 값 없이 조합만, 공지 · 이벤트 본문 한정). 폭 640(한 줄 40자)도 그렸지만 읽기 폭 토큰(768)을 대화상자 · 개인정보 등이 함께 쓰고 768 은 읽기 사이트 680–870 범위 안이라 공지만 예외를 두지 않음(사용자). 소제목 · 라벨 간격은 시안 값(48/16 · 4/24) 대신 가이드 역할 값(32/12 · 8/16)으로 — 토큰 · 예외를 늘리지 않는다. 목록은 메이플 · 로아 · 네이버 식 한 줄(분류 · 제목 · 오른쪽 날짜), 날짜가 줄마다 있어 월 제목을 뺐다(한국 공지 월 묶음 0/13). 목차는 절이 둘뿐이라 두지 않음.

## 2026-09-25

- **활동 요약 「플레이한 날」 → 「연속」(사용자)** — 지금 이어지는 연속 플레이 일수(오늘 아직 안 쳤으면 어제까지를 이어지는 중으로).
- **프로필 구역 다듬기(S1 · R1 · L1 · A1, 사용자 · 추천대로)** — 베스트 성과 · 기록 탭의 2단 밑줄 탭이 위 구역 탭과 두 겹이라 세그먼트 M 으로(성장 추이와 같은 부품 · 같은 말 「Grade · Rating」). 레벨별 달성은 램프 · 랭크를 한 막대로 합치고 「실패」 · 「안 함」 표시를 뺐다(실패는 게임에서 의미 없음). 범례를 누르면 그 칸만 강조하고 % 가 그 칸 비율(사용자 제안), 고르지 않으면 친 채보 비율. 업적 한 줄은 구역 폭에 들어가는 만큼 육각, 더 있으면 마지막 칸 「⋯」(7칸 고정 아님). 폰 개요 순서는 성장 추이 → 레벨별 달성 → 고정 기록 → 베스트 → 최근 → 업적 → 기여. 고정 기록 줄에도 Grd(지금 모드). 기록 탭 「베스트 50」 → 「최고 기록」, 통계 탭 노트 종류별 성공률은 뺐다(의미 없는 통계). 수치 상자 세계 · 국가는 라벨 14 · 순위 16/600 으로 키우고 지구본 · 국기를 붙였다.
- **공식 Grd 상자 전환 = 라벨 자리 고르기(T1, 사용자 · 추천대로)** — 「공식 Grd ⌄ · NosLog 레이팅」, 바꾸는 대상(큰 숫자)과 손잡이가 같은 자리. 레이팅 순위는 랭킹 페이지 캐시에서 내 자리만 찾는다. 상자 위 2단 탭 · 모드 옆 세그먼트는 전환이 겹쳐 뺐다.
- **작은 정리(사용자)** — 성장 추이 전환 「Grd」 → 「Grade」(폭 넉넉), 업적 구역 「최근 달성」 은 가장 최근 하나만, 기여 구역 「내 제안 · 나에게만 보입니다」 머리 줄 빼기(의미 없는 안내).
- **공식 Grd 상자 = 칸 나누기(B1, 사용자)** — 왼쪽 절반 공식 Grd · 오른쪽 위아래 세계 · 국가, 칸 사이 선. 수치 띠와 같은 만듦새라 한 식구로 보인다. 레이팅 줄은 뺐다(레이팅은 성장 추이 · 베스트 전환에서). 상자 오른쪽 위 변환 아이콘은 무엇이 바뀌는지 안 보이고 순위 칸을 밀어서 두지 않았다 — Grd ↔ 레이팅 전환 자리는 따로 고른다.
- **본인에게는 숨긴 항목도 보인다(P1, 사용자 · 추천대로)** — 「플레이 활동 비공개」 를 켜면 본인도 자기 최근 플레이 · 활동을 못 보던 문제. 숨긴 값은 원래 아이콘 대신 자물쇠만(사용자) + 흐린 값, 숨긴 구역은 「나에게만 보입니다」. 공개 여부 요약 줄은 여전히 두지 않는다.
- **머리는 모든 탭에서 같다(사용자)** — 업적 · 활동 탭에서 모드 세그먼트 · 공식 Grd 상자를 숨기면 탭을 옮길 때 아래 내용이 튀어 올라 불편하다. v2 시안의 「모드와 관계없는 탭은 숨김」 을 되돌린다.
- **프로필 「기여」 숫자 줄도 고정폭 끄기(사용자 · 추천대로)** — 「5점 · Lv.2까지 5점」 · 내 제안 값 · 초안 점수 줄(12 흐린 글자)에 켜 두었던 고정폭 숫자를 끈다. 가이드는 고정폭을 metric 두 종류에만 쓰고 예외를 두지 않는다(T1 과 같은 처리).
- **등급 색 A+ · A · B = 게임 아이콘 색을 있는 토큰으로(C2a, 사용자)** — 가이드 등급 색은 S · P 뿐이었다. 막대 옆 등급 아이콘과 짝이 맞게 A 계열 빨강 · B 파랑, 새 값 없이 별칭만(`score-grade-a-plus` = 난이도 글자 빨강 · `score-grade-a` = 난이도 빨강 · `score-grade-b` = 차트 파랑). A+ 를 한 단계 밝게 해 레벨별 달성에서 A+ 와 A 가 갈린다. 랭크 분포와 레벨별 달성 「랭크」 보기가 같은 색.
- **프로필 남은 결정(T1 · T5, 사용자 · 추천대로)** — 작은 흐린 글자(순번 · 고정 기록 「#n」)에 켠 고정폭 숫자를 끈다(가이드: 고정폭은 metric 두 종류만). 최근 플레이 「New」 는 글자 대신 공용 상태 태그(성공) — 이벤트 「진행 중」 과 같은 부품이라 상태 표시로 읽힌다.
- **프로필 시안 대조 정리(2026-09-26, 사용자)** — 성장 추이는 가이드 그래프 그대로 · 요약 문장과 요약 줄(현재 · 변화 · 최고)을 뺀다(Grd 는 내려가지 않아 늘 「지금 = 최고」 인 중복) · 조작부는 M. 레벨별 달성은 「1–8」 을 한 줄로 · 긴 난이도 이름은 들어가면 그대로 · 막대 8 · 「REAL 1」 유지. 랭크 분포에 FC 막대, 막대 색은 가이드 등급 색. 곡 순위는 「#n」(「곡」 글자 없음). 활동 요약 · 달력 아래 줄은 그대로. 가이드 밖 값은 없앤다 — 모서리 2 → control 4, 달력 칸 19 · 15 · 17 → 16, 고정 기록 줄 위아래 12 → 8.
- **프로필 구역 「모두 보기 ›」 = 제목 줄 오른쪽 끝 · 전환은 다음 줄(A, 사용자 · 추천대로)** — 제목 옆에 붙인 것은 가이드 2절(이동 = 제목 줄 오른쪽 끝)을 어긴 구현이었다. 오른쪽 끝에 전환과 링크를 나란히 두는 안(B)은 좁은 폭에서 모양이 둘로 갈려 뺐다.
- **최근 플레이 새 최고 표시 = 「New」 · 초록(사용자)** — 「새 기록」 은 「새로 쌓인 플레이」 로도 읽혀 세 언어 모두 「New」(느낌표 없음). 색은 `feedback/success-marker` 유지 — 빨강 계열은 오류 뜻이고 같은 줄의 EXPERT · A+ 와 겹친다(비교 시안).
- **프로필 머리 정보 = 두 줄(H1, osu! · 사용자 · 추천대로)** — 한 줄 메타(아이콘 5)가 「아이콘이 많고 정돈이 안 돼 안 읽힌다」(사용자). 실측 5곳(osu! · BeatLeader · Chess.com · Lichess · GitHub)에서 링크 · 계정은 아이콘 4/4, 활동 상태는 아이콘 없는 라벨 3/3, 둘을 한 줄에 섞는 곳 0/5 — 두 규칙을 다 지키는 안 중 높이가 그대로이고 새 부품이 없는 osu! 방식. 라벨 표 상자(BeatLeader)는 높이가 약 96 늘어 뺐다.
- **고정 기록 S2(사용자 승인 · v2 시안)** — 옆 열 맨 위에 고른 기록 3개 + 한 줄 소감, 고르지 않았으면 베스트 상위 3곡. 고르기는 「프로필 업적」 칸과 같은 설정 칸 · 창(선호 오락실 검색 목록 재사용). 기록 행은 동기화로 바뀔 수 있어 채보 기준으로 고정(`UserPinnedRecord`).
- **프로필 개편 3단계 = 통계 · 활동 탭 · 개요 옆 열 레벨별 달성 · 머리 정리(사용자 · v2 시안 그대로)** — 통계 = 성장 추이 · 레벨별 달성 전체(램프 ↔ 랭크 · 난이도) · 판정 · 랭크 분포 · 노트 종류별 성공률, 활동 = 요약 띠 · 활동 달력(53주) · 최근 플레이 한 판씩 + 「새 기록」. 개요 옆 「기록 개요」 → 레벨별 달성 요약(R2), 랭크 분포 · 판정은 통계 탭으로. 머리의 「· Basic」 · 상위 % · 90일 변화는 뺐다(2026-09-26, 사용자 — 모드는 큰 세그먼트가 말하고, 변화는 성장 추이에 있다). 성장 추이의 순위 · 플레이 지표는 쌓인 이력이 없어 두지 않았다. 누적 막대는 줄마다 라벨 폭이 달라 막대가 어긋나 공용 부품을 subgrid 로 바꾸고 값 칸(선택)을 더했다.
- **프로필 개편 2단계 = 기록 탭 · 곡 순위 열(사용자 · 추천대로)** — 베스트 50 · 모든 기록을 2단 탭 | 고스트 정렬 한 줄로 본다. 검색칸 · 필터 · 「베스트 = 공식 Grd 상위 50곡」 설명 줄은 넣었다가 뺐다(2026-09-26, 사용자 — 「필요 없다 · 설명 없이도 안다」). 다른 순으로 봐도 베스트 순번을 남겨 「베스트 몇 위였나」 가 사라지지 않게. 곡 순위는 곡 상세 순위표와 같은 RANK(점수 공개자 사이, 리듬게임 3/11 이 행에 곡 순위). 개요 베스트 제목 옆 링크는 사람마다 곡 수가 달라 「전체 50곡」 대신 「모두 보기」. 날짜 칸은 줄마다 폭이 달라 열이 어긋나 고정 폭(88 · 104).
- **프로필 개편 = 머리 + 구역 탭(D2) · 폰 모드 전환 = 전폭 세그먼트(CM1) · 최근 플레이는 한 판씩(사용자 · 추천대로, 1단계)** — 실서비스 44곳 실측(리듬게임 12 · 경쟁 11 · 소셜 12 · 기록 10) · 디자인 시스템 10곳 조사 시안에서 골랐다. 전폭 정보 상자 3개 → 메타 줄(리듬게임 5/5 아이콘 줄), 같은 크기 수치 네 칸 → 공식 Grd 하나를 크게 + 순위 · 상위 % · 레이팅 · 90일 변화(리듬게임 8/12 · 상위 % 경쟁 5/11 · 기록 5/12), 전폭 모드 탭 → 머리 세그먼트(리듬게임 전폭 탭 1/7), 성장 추이는 요약 문장 먼저(차트 지침 4/4), 베스트 · 최근은 열 고정 표(NN/g — 비교는 표), 폰은 베스트가 첫 화면 바로 아래(경쟁 5/11 에서 옆 열이 먼저 쌓여 목록이 밀림). 1판 폰 모드 전환(큰 숫자 옆 34 높이)은 「쓸 수 없는 모양」 이라 버렸다. 최근 플레이 같은 곡 묶기(P2)는 「각각 확인하고 싶다 · 묶으면 더 혼란」 이라 뺐다. 리듬게임 기록 사이트는 대부분 한 페이지(9/12)라 탭은 반대 근거가 있다. 기록 · 통계 · 활동 탭 · 고정 기록은 다음 단계.
- **채보 목록 = 채보마다 한 줄 · 악곡 목록 줄 틀 · 오른쪽 난이도 칩(A2 · R1 · I2 · D1 · V0, 사용자 「모두 추천대로」)** — 곡 카드 안에 난이도마다 테두리 줄을 두던 와이어프레임을 다시 그림. 실서비스 38곳(리듬게임 12 · 음악 · 미디어 13 · 스토어 14) · 가이드 6곳 실측: 리듬게임은 곡 묶음이 10/12 지만, 이 목록의 정렬(최근 공개 · 레벨)과 작성자 · 출처가 채보 단위이고 가이드(M3 · Carbon · Fluent)는 항목 하나 = 동작 하나라 채보마다 한 줄(DXRating · arcade-songs 방식). 곡 단위는 「악곡」 보기가 맡는다. 2열 카드(R2)는 레일 옆 418 폭에서 제목이 잘리고, 표(R3)는 폰에서 넘쳐 물림. 노트 · BPM · 길이 열(I3)은 빈 곡이 남아 물림, 목록 안 채보 미리보기는 선례 0/12 라 두지 않음(V1 물림).
- **프로필 머리 공유 · 설정 = 이름 줄에만(사용자)** — 버튼이 머리 오른쪽 열 전체를 차지해 명판 · 배지 줄이 중간에서 끊겼다. 이름 줄 오른쪽 끝으로 옮기고 머리 격자를 2열로.
- **시작 타이밍 500ms = 영상 추출 가져오기에서도(B, 사용자 · 추천대로)** — 새 빈 초안만 500ms 라 그 전에 만든 海神 초안은 다시 넣어도 0ms 가 남았다. 시작 타이밍 카드에 「시작 시각을 500ms 로」(시작 시각 0ms 일 때만, 노트 없으면 기본 켬, 모든 포인트를 같이 옮김). 기존 채보 일괄 변경(C)은 음원에 맞춘 0ms 까지 밀어 뺐다.
- **업적 페이지 정렬 · 보기 = 악곡 · 서열표와 같은 결과 줄(사용자 제안 · 추천대로)** — 정렬 다섯(분류 · 높은 등급 · 최근 달성 · 희귀한 · 다음 단계에 가까운 순(본인)), 보기는 목록 · 촘촘한 격자 둘(「일반 격자는 굳이」). 촘촘한 격자 = osu! 메달 벽처럼 육각만, 눌러서 정보 카드.
- **영상 추출 박자선 = 노트와 같은 AI 박자선으로 박을 세고, 빈 곳은 원본 프레임으로 메움(1번, 사용자)** — 28곡을 노트마다 영상 시각과 대조하니 12곡이 AI 가 놓친 박자선 뒤로 한 박 가까이 통째로 밀렸다(海神 18마디 1박 → 358ms, 공개본 포함). 변환기는 경고만 하고 두 박을 한 박으로 두었고, 템포는 원본 프레임에서 따로 세어 박 수가 달랐다(페르마타를 두 박으로 쪼갬). 고친 뒤 한 박 밀림 0곡(海神 95% 22ms · 平均律 4,145 → 72ms), 판정 합계가 확인된 4곡 노트 수 그대로. 짝 없는 박을 AI 박자선 위치로 채우면 ±8프레임 흔들림이 템포로 들어가(427 BPM) 박 수로 고르게 나눈다.
- **옛 기준 단계 = 관리자 「다시 판정」 의 「기준에 못 미치는 단계도 빼기」 체크(R1, 사용자 · 추천대로, 기본 꺼짐)** — 기준을 바꾸면 임시 기준으로 받은 단계(PIANIST 6곡인데 피아니스트 I)가 남는다. 기준을 바꾼 날에만 켜고 누른다. 평소 동기화 · 체크 끔은 빼지 않음 그대로.
- **업적 5등급 · 업적마다 가진 등급 · 진행 막대 등급 색(C1, 사용자)** — 동 · 은 · 금 위에 플래티넘(판정 GOOD 청록) · 다이아(판정 ◆JUST 분홍)를 있는 토큰으로. 알약은 I–V. 「모든 업적이 다섯 단계는 아니다 — 다이아 하나만 · 동 · 은 · 금 셋만도 된다」. 기준: S 헌터 · 990k · 풀콤보 · 피아니스트 10 · 50 · 200 · 500 · 1000, 검정 7 · 5 · 3 · 2 · 1급, 나머지는 제안표. 빙고 업적 삭제(유저가 직접 체크하는 기록). 줄의 「다음 II」 · 「모든 단계 달성」 삭제. 진행 막대는 쫓는 등급 색 + ⑯ 움직임.
- **프로필 카드 FC = FC + Pianist(사용자)** — Pianist 도 풀 콤보인데 카드가 FC 램프만 세서 「P 1000 · FC 100」 처럼 FC 가 P 보다 적게 보였다. 저장 값(`score_f` = FC 램프만)과 동기화는 그대로 두고 카드에서 `score_f + score_p` 로 — 운영 데이터를 고치지 않고 모든 사용자에게 바로. S 는 그대로(랭크별 개수).
- **업적 금 · 은 · 동 개수 삭제(사용자)** — 업적 페이지 머리 · 프로필 「업적」 구역의 「금 N · 은 N · 동 N」 은 「필요 없는 정보」. 「업적 n / N」 만 남긴다.
- **빈 채보 음원 오프셋 기본값 = 500ms(사용자)** — 0 이면 1마디 1박 노트가 재생과 동시에 판정선에 있어 떨어지는 걸 못 봤다(海神 Real 영상 추출). 새 빈 초안(관리자 에디터 · 기여 초안)만, 기존 채보는 그대로.
- **영상 추출 가짜 노트 = 손을 못 읽은 것만 가져오기에서 빼기 + 공식 노트 수와 비교(F1′, 사용자 · 추천대로)** — 대기열로 뽑은 22곡 중 10곡이 「노트가 겹친다」 며 막혔다. 영상 10편을 다시 받아 콤보 숫자로 대조하니(일반은 칠 때, 테누토는 뗄 때 오름) 테누토 머리 뒤 ¾박 안 · 칸 안의 일반 노트는 확인한 곳 모두 가짜, 8박 넘게 겹친 테누토 일부는 영상에 아예 없었고(幻想即興曲 47마디 115박), 둘 다 거의 손이 비어 있었다(가짜 58개 중 50). 손을 읽은 노트는 남긴다 — 1박 뒤 노트는 진짜였고(かぞへうた 9마디 3박), 길이만 틀린 진짜 테누토도 있다. 7곡이 바로 넣을 수 있게 됐고 3곡(かぞへうた 38 · 平均律 9 · パヴァーヌ 5곳)은 남은 겹침 자리만 보여 준다 — 추출기 테누토 인식을 고치는 안(F3)은 원인 조사 · 재추출이 필요해 뒤로, 겹쳐도 넣기(F2)는 수십 곳을 손으로 고쳐야 해 물림. 판정 수는 글리산도를 가로대 수로 센다(1lIj 가 1,158 로 공식과 일치).
- **업적 첫 판정 = 관리자 「동기화 내역」 의 「업적 다시 판정」 버튼(B1, 사용자 · 추천대로)** — 업적은 동기화 끝에만 판정해 배포 직후 모두 0 이었다. 50명씩 끝까지 돌고, 기준 수치를 바꾼 뒤에도 다시 누른다. 얻은 단계는 빼지 않는다.
- **채보 뷰어 설명 창 · 「28칸 · 열당 4마디」 삭제(사용자)** — 「이제 필요 없다」. 두 번 두드리기 · 설정 창 바깥 누르기는 유튜브처럼 되게 고침(누르기 시작에서 손가락 · 창 열림을 가림).
- **검정 급 머리 수치 띠 = 요구 Grd. 넘으면 초록 · 「내 값」 삭제 · 보상은 명판(R1 · L1 · B2, 사용자)** — 넘었는지 라벨의 「내 5,725」 를 보고 머리로 비교해야 했고, 폭 360 에서 그 라벨이 세 줄로 접혔다. 값은 합격 체크와 같은 `feedback/success-marker`(모자란 양은 머리 태그 「Grd. N 부족」 이 말함). 칭호 보상은 제목과 같은 글자를 되풀이하는 대신 랭킹 · 프로필의 검정 명판을 풀 이름형으로(가장 긴 「RECITAL | 10급」 94px 도 360 칸에 들어감, 줄임형 「B」 는 뜻이 안 읽혀 물림).
- **낙하형 가로 박자선 = 박마다 같은 선(G1, 사용자 · 추천대로)** — 실제 게임처럼. 알려 주신 영상(海神 Real) 한 장면에서 레인 폭 전체를 가로지르는 가는 흰 선을 봤고, 영상 추출 코드도 「게임은 박마다 선을 그린다」(Altale 실측)를 근거로 쓴다. 마디선만(G2)은 느린 곡에서 드물어, 마디선 진하게(G3)는 게임에 없는 구분이라 물림(마디선 굵기 차이는 미확인). 진하기는 30%(O2 · 55% 는 진함, 20% 는 먼 곳이 안 보임).
- **전체화면 설정 = 톱니 → 떠 있는 창(C1, 사용자 · 추천대로) · 조작은 유튜브와 같게(사용자)** — 노트 속도 · 메트로놈 · 음량 · 엄밀한 연주를 한 줄에 모두(C2)는 재생 막대가 절반으로 줄고 좁은 창에서 넘쳐서, 두 줄(C3)은 무대를 두 배로 가려 물림. 무대 누르기 재생 · 일시정지 · 스페이스 · 폰 두 번 두드리기는 유튜브 조작 그대로(이동 폭은 2026-09-26 한 마디로 바뀜).
- **낙하형 무대 = 16:9 고정 · 폭에 정비례(A1) · 전체화면 = 무대만 + 겹친 조작 줄(B1, 사용자 · 추천대로)** — 무대 높이가 화면 높이를 따라가 기기마다 비율이 0.62–2.09(폰 · 노트북 실측)로 달라져 원근 · 노트 간격 · 내려오는 속도가 달랐다. 16:9 논리 무대를 폭에 맞춰 줄여 같은 그림을 준다. 좁은 화면만 4:3(A2)은 폰 · PC 그림이 갈려서, 4:3 하나(A3)는 PC 에서 852 로 화면을 넘어 물림. 폰 세로의 낮은 무대(358 × 201)는 전체화면으로 보완. 전체화면에 설정까지 함께 넣는 안(B2)은 무대가 조작부만큼 작아져 물림. 전체 악보도 같은 버튼. 게임 화면 비율 16:9 는 미확인.
- **업적 배지 카드 움직임 = ③ 떠 있는 메뉴 페이드(A1, 사용자 · 추천대로)** — 카드가 떠 있는 창 크기라 즉시 뜨면 튄다. 작은 용어 도움말(⑧)은 그대로 즉시. 시간 · 곡선은 기존 토큰(`--nl-motion-duration-popup` · `--nl-ease-enter`).
- **업적 배지 정보 창 = osu! 메달 창 순서의 카드(M1, 사용자 · 추천대로)** — 분류 → 큰 육각 58 × 64(그림 32, 새 크기 허락) → 큰 제목 → 조건 → 구분선 → 달성 인원 · 달성일. 사용자: 「아이콘 크게 · 제목 크게 · 설명 · 언제 달성」, osu! 실측 창과 같은 순서. 동작은 기여 라벨과 같은 `TermHelp`(창 안만 `content` 로 바꿈).
- **프로필 머리 = 업적 배지만(개수 글자 삭제) · 배지는 올리거나 누르면 정보 작은 창 · 본인용 「비공개 · …」 줄 삭제(사용자)** — 「업적 24 / 51」 은 보기 안 좋고, 배지는 osu! 처럼 누르면 그 배지 정보를 봐야 한다. 창은 기여 라벨과 같은 `TermHelp`. 비공개 줄은 「이런 걸 추가하는 서비스가 없다」.
- **업적 진열 고르기 = 설정 「프로필」 탭의 「프로필 업적」 칸(D1, 사용자 · 추천대로) · 업적 목록의 핀 삭제** — 사용자: 핀 아이콘은 사이트 어디에도 안 쓰고 업적 창에서 꾸미지 않는다. 사진 · 닉네임 · 선호 오락실과 함께 「프로필 꾸미기」 를 한곳에 모은다(Steam 도 「프로필 편집」 에서 진열장을 고름, 다른 서비스는 미확인). 칸 모양 · 저장은 선호 오락실 칸 그대로(「변경」 → 창 → 폼 값 → 「저장」).
- **영상 추출 BPM = 가장 단순한 값 중 구간 끝까지 맞는 값(B′, 사용자)** — 0.1 · 0.01 단위 곡(222.22)도 있어 정수 · 0.5 반올림(B)을 바꿨다. 구간 전체 직선 맞춤(60fps 로 0.01–0.02 BPM 정밀도)으로 재고 정수 → 0.5 → 0.1 → 0.01 순으로 영상 흔들림 + 0.5프레임 안에 드는 첫 값, 지금 시작 BPM 이 그 안이면 제안하지 않는다(사람이 넣은 222.22 를 지키려고). 측정값 그대로(C)는 정수 곡도 143.99 처럼 흔들린 값이 되어 뺐다. 海神 시작 구간은 70박째 3프레임 계단 때문에 통째로 재면 159.84 라 계단 앞뒤를 따로 맞추고(160), 느린 10박은 리타르단도라 처음과 끝 사이 평균(111). 네 곡 결과 90 → 83 · 144 · 180 · 160 → 111 → 160 → 120.
- **영상 추출 템포 = 벗어난 박만 박 단위(B, 사용자)** — 구간마다 BPM 하나면 海神 Real 느린 곳(45–48마디)이 111 하나로 뭉개져 영상과 최대 247ms 어긋났다. 꾸준한 곳은 구간 BPM(B′), 영상이 벗어난 박만 정수 BPM 으로 이어 붙여 모든 박을 영상 박자선 1.5–2.5프레임 안에(海神 40ms, 타이밍 10개). 유비트 메모 값(C)은 노스텔지어 박자선과 달라(17마디 80 BPM 박 = 45프레임, 영상은 24) 뺐다. 곡마다 값을 맞추지 않는 일반 규칙 — 시험은 가상 곡(멈칫 · 리타르단도 · 곡 전체 흔들림).
- **마디 중간 BPM 포인트 = osu! 식 마디선 맞춤(마, 사용자)** — 타이밍 포인트마다 마디를 새로 시작하는 규칙(osu! 와 같음)은 그대로 두고, 카드의 마지막 포인트가 마디 중간이면 다음 마디 첫 박에 같은 BPM 포인트를 하나 더 넣는다(海神 +2). BPM 바뀐 자리가 새 마디로 보이고 번호는 이어 세는 게 osu! 사용자에게 자연스럽다(사용자). 그대로(가)는 海神 48마디 2박–79마디 마디선 · 강박이 곡의 2박에 떨어지고, 마디 유지(다)는 osu! 와 달라 헷갈리고 공개 뷰어까지 바꿔야 해 뺐다.
- **영상 추출 다시 넣기 = 어긋난 기존 타이밍 포인트를 영상 타이밍으로 바꾸기(B, 사용자)** — 노트만 지우고 다시 넣으면 옛 포인트가 제안을 막고 옛 시각(ms)을 그대로 들고 있어 海神 이 영상과 116ms 어긋났다. 카드 하나로 바꾸고, 기본은 초안에 노트가 없을 때만 켬(손으로 맞춘 타이밍을 모르고 지우지 않게). 알리기만(A)은 창을 닫고 지우고 다시 넣어야 하고, 언제나 영상 우선(C)은 손 타이밍을 덮어 뺐다. 「영상과 맞음」 한도는 시안의 1.5프레임 대신 제안과 같은 2.5프레임 — 1.5면 제대로 넣은 결과(28–37ms)에도 카드가 뜬다.
- **운영자 태그 = 기본 태그 「Admin」(B3, 사용자)** — 한 · 일 · 영 모두 글자 「Admin」. 반전 블록(강조형)은 금색 명판 · 업적 육각형 옆에서 혼자 튀고 버튼처럼 보여 뺐다. 기여 라벨과 같은 기본 태그로 두고, 범용 방패 아이콘(A1)은 「만든 티」 가 나서, 「NosLog Admin」 앞글자(B1)는 길어서 물림. 누르면 뜨는 설명 글은 언어별 그대로.

## 2026-09-24

- **영상 추출 시작 BPM = 템포 제안 카드(A, 사용자)** — 새 초안은 BPM 120 이라 영상 BPM(アルストロメリア 144)을 사람이 먼저 바꾸지 않으면 다시 지우고 넣어야 했다. 기존 「BPM 이 바뀌는 곳」 제안 카드와 같은 모양으로 「시작 타이밍을 BPM ○ 로」, 초안이 비었으면 기본 켬 · 노트가 있으면 기본 끔(시작 BPM 을 바꾸면 기존 노트 시각이 전부 움직임). 자동(B)은 영상 BPM 이 틀린 곡에서 끌 수 없어 뺐다. 첫 구간은 처음 16박이 아니라 구간 전체로 잰다(144.83 → 144.0). 새 화면 없음.
- **영상 추출 박자표 = 강세로 추정해 시작 타이밍 카드에 함께(A, 사용자)** — 게임은 박마다 같은 선을 그려 마디선이 없다(영상 확인). 박 머리 노트 수(테누토 · 트릴 시작 ×3)가 3박 · 4박 중 어느 주기로 되풀이되는지로 박자와 마디 첫 박을 잡는다 — Altale 3/4(0.81 vs 0.05) · アルストロメリア 4/4(0.31 vs 0.06) 둘 다 맞고, 노트 수만이면 アルストロメリア 마디 첫 박이 틀린다. 켜면 빈 초안의 첫 박도 강세 위치로(Altale 첫 노트 1마디 3½박 = 공개 채보). 근거가 두 곡뿐이라 약하면 기본 끔. 새 화면 없음.
- **채보 트릴 = 게임식 B′(사용자)** — 사선 띠(A)는 짧은 트릴이 사선 한 조각이라 게임과 다르게 읽혔다. 게임은 머리가 범위 전체 · 육각형이 촘촘히 쌓이고 한쪽이 옅어져 좌우로 흔들린다(Altale 영상 74.9~75.3초). 번갈아 옮기는 육각형(B)이 아니라 범위 전체 폭 육각형의 반대편을 옅게(B′, 사용자 제안), 육각형은 게임처럼 촘촘히(사분음표 1/12, 영상 실측 약 50ms — 번갈아 치는 간격 값과는 따로). 에디터 캔버스도 같은 모양(뷰어만 바꿨다가 사용자 결정으로 에디터까지). 새 색 없음(마름모 · 바탕 · 손 색 기존 값).
- **업적 공개 범위 = 점수 비공개면 남에게 실력 · 수집 업적을 숨김 · 진행 값은 본인만(P1 · V1, 사용자 · 추천대로)** — 「피아니스트 II」 만으로 기록 범위가 드러나 점수 비공개와 어긋나고, 다 숨기면 점수와 무관한 의견 · 빙고 업적까지 사라진다. 진행 숫자 · 막대는 프로필에 없던 값(PIANIST 개수 등)을 새로 공개해 본인에게만. 남의 진행을 보이는 서비스가 있는지는 미확인.
- **업적 = 단계형(동 · 은 · 금) · 육각 · 동기화 뒤 자동 판정(T2 · I1 · L2 · K1+K3 · R1 · P5 · N1 · D1 · D-a, 배치 A1 · B1 · C1 · D1 · E1, 사용자 「모두 추천대로」)** — 37곳 실측 · 가이드 조사. 숨긴 업적 · 플레이 횟수 · 연속 일수 · 운 조건 없음(오락실은 한 판이 돈). 희귀도는 %가 아니라 달성 인원(상세만). 머리는 고른 3개(없으면 자동) + 숫자, 개수 배지는 두지 않는다. 달성일은 NosLog 가 확인한 날짜 그대로(「모든 서비스가 그렇게 한다」). 색은 검정 명판 금속 사다리 토큰 재사용(새 토큰 없음). 기준 수치는 임시 — 운영 분포로 다시 정한다.
- **다크 회색 정리 = 면 유지 · 옅은 선 합치기 · 상호작용은 흰색 반투명 겹침(S0 · L1 · I2 · T0, 사용자 · 추천대로)** — 다크 실측 37곳 · 디자인 시스템 10곳과 비교하니 면 · 글자 값은 Adobe Spectrum 2 와 12개 중 11개가 같아 그대로 두고, 선은 우리 5단 대 중앙값 1–3단이라 `subtle` · `empty-slot` 을 `divider`(#323232)로 합쳤다. `input`(#717171)은 `strong` 에 합치지 않고 둔다(사용자 · 추천대로) — 합치면 입력칸 올림(경계 → `strong`) 단서가 사라지고 태그 경계(09-23 재설계)가 밝아져, 선은 구분선 · 경계 · 입력 · 강조 4단. 올림 · 누름 · 선택은 불투명 회색이라 카드 · 떠 있는 창 위에서 오히려 어두워지거나 안 보였고, 실측 36곳 중 24곳이 반투명을 써서 흰색 6% · 10% 를 제 면 위에 겹친다(`background-image` + `@property --nl-state-layer`). 안 쓰는 토큰 3개(`content/subdued-interactive` · `surface/plate` · `interaction/selected-hover`)는 지웠다. 라이트 값은 그대로.
- **유저 기여 3단계 = 채보 초안 · 시각 댓글(A1 · B1 · C1 · D1, 사용자 「모두 추천대로」)** — 들어가는 자리는 뷰어 머리 「고치기 ›」 와 곡 상세 채보 칸(공개 채보가 없으면 「채보 만들기 ›」, 내 초안이 있으면 「내 초안 · 상태 ›」 — 시안은 영상 칸 자리에 그렸으나 영상 칸은 두고 채보 칸이 상태를 말하게 함). 기여자 에디터는 왼쪽 레일에 「검토」 탭(단계 줄 · 시각 댓글) — 오른쪽 속성 칸을 그대로 써 댓글을 보며 고친다. 운영자는 같은 에디터를 읽기 전용 검토 모드로(확대 · 속성으로 틀린 곳을 찾기 가장 좋음), 공개 뷰어는 조작부 아래 「채보 의견」 구역 + 진행 막대 눈금(캔버스 · 조작부 배치 그대로). 에디터 글은 ko · ja · en 으로 옮긴다(한국어 전용이면 일본어 · 영어 유저가 기여할 수 없음). 폰은 에디터 대신 「넓은 화면에서」 안내. 공개는 운영자만, 채보 공개 20점 · 해결된 의견 1점.
- **유저 기여 2단계 = 기여 점수 · 라벨 · 프로필 「기여」(N1 · L2 · P1, 사용자)** — 1 · 2단계를 함께 내보낸다(방침 개정 한 번 · 첫인상). 등급 이름은 숫자 「기여 Lv.1–6」 — 셈여림은 비직관적, 금속 이름 · 색은 osu! 에서도 실력 등급 표시라 검정 명판과 뜻이 섞여 물림. 라벨은 기존 태그(명판 틀 · 원형 · 아이콘 글자보다 섞이지 않고 새 모양 없음). 프로필에 탭이 없어(최종안 S3 는 잘못 그림) 본문 「기여」 구역(P1). 점수는 **점수 기록 테이블**에 적립 — 처리된 오락실 제보가 6개월 뒤 지워져도 점수가 남게(「점수는 깎지 않는다」), 같은 기여는 한 번만.
- **영상 추출 채보 출처 = 머리에 작성자 줄 + 「노트 배치 · 영상에서 추출(vid2bmap) · 출처 보기 ›」(C2, 사용자) · 기록은 버전 종류로** — 「출처는 꼭 남기고 언급도 꼭」 이라 도구 이름이 누르지 않아도 보여야 한다(⋯ 안에만 두는 C1 물림, 캔버스 아래 문장 C3 은 스크롤해야 보여 물림). 작성자 줄은 기여 제도 결정(이름 + 라벨) 그대로라 나중 기여자 채보도 같은 자리. 어느 채보가 영상 추출인지는 「초안에 넣기」 직후 저장하는 버전의 `kind = "vid2bmap"` 으로 남긴다 — 스키마 변경 없이(원 영상 주소 칸은 필요할 때 따로).
- **영상 추출 손 = main 실행의 노트 · 박자선 + `LR_classification` 브랜치 실행의 손만(사용자)** — LR 브랜치는 손을 가려내지만(Altale 232/232) 라벨 번호를 바꾸며 박자선 채널을 맞추지 않아 박자선이 깨진다(8,909줄). 알고리즘을 고치지 않고 두 번 돌려 실행 스크립트에서 합친다 — 같은 종류 · 칸에서 구간별 y 밀림을 보정한 뒤 ±4프레임 짝의 손(Altale 1,640개 중 모름 16). 손 열이 있으면 사이트가 그대로 쓰고, 없거나 모르면 전처럼 칸 위치로 추정해 가운데만 확인 대상으로 고른다. 영상에서 읽은 손은 초안과 비교할 때 차이로 센다.
- **영상 추출 스냅 = 박마다 격자 자동 + 에디터 「스냅 확인」(C, 사용자)** — 곡 전체를 1/6박 한 격자로 맞추면 1/8박 연타가 두 개씩 한 자리로 뭉쳐 겹노트가 되고(Altale 12곳), ¼ · ¾ 자리는 ⅙ · ⅓ 한가운데라 제멋대로 갈렸다. 스냅 없이 영상 위치 그대로 넣는 안(B)은 60fps 흔들림 때문에 osu! 기준(2ms)으로 1,563개 중 829개가 경고라 뺐다. 박마다 허용 2.5프레임(1.5 는 정답 14개 틀림, 3 은 뭉친 곳 10) · 떨어진 두 노트를 한 자리로 합치는 격자는 안 맞는 것으로 → 뭉친 곳 0 · 정답 232 그대로. 어느 격자에도 안 맞는 노트는 영상 위치 그대로 넣고, 에디터가 격자 밖 노트를 경고 노랑(`--nl-feedback-warning-marker`) 점선 · 노트 검사기 아래 목록으로 보인다(osu! 「Unsnapped hitobjects」, 원본 코드 확인). 새 토큰 없음.
- **영상 추출 글리산도 = 조각을 이어 한 노트로 가져오기(사용자)** — 사용자 재량으로 두면 채보에서 노트가 빠진다(Altale 영상에 6개). vid2bmap 은 가로대마다 조각을 주므로 12프레임 · 4칸 안의 다음 조각을 한 줄기로 잇고, 경로는 시작 · 끝 · 1칸 넘게 꺾이는 점만, 가로대 간격은 조각 간격. 경로 위 일반 노트는 영상에 띠 하나뿐이라(34마디 3박) 가로대를 두 번 읽은 것으로 보고 뺀다. 에디터의 기존 글리산도 형식 그대로 — 새 화면 없음.

## 2026-09-23

- **개인정보처리방침 2026-09-24 시행(사용자 · 추천대로)** — 기여 제안(값 · 근거 · 처리 결과)의 이용 목적 · 공개 범위(반영된 값만, 제안자 이름 · 근거 · 반려 건은 비공개) · 보유(탈퇴 시 제안 삭제, 반영 값과 근거는 연결 끊긴 출처로 남음)를 더하고 직전 2026-09-22 판을 `versions/` 로 보관. 제안자 이름 · 기여 등급 공개는 2단계에서 다시 개정한다.
- **유저 기여 1단계 = 채보 정보 제안 + 운영자 검토(사용자)** — 혼자 곡 데이터를 채우기 어려워 기여를 연다. 실서비스 12곳(Google 지도 · 나무위키 편집 요청 · 위키백과 · Stack Overflow · OSM · TMDB · Waze · MusicBrainz · Discogs · osu! · ScoreSaber · Kamaitachi) 조사 뒤 **역할은 기여자 · 운영자 둘뿐, 반영 · 공개는 운영자만**(중간 검수 역할은 사용자가 뺐다). 보상은 권한 없는 「기여 Lv.1–6」 라벨(숫자 레벨 — 금속 이름 · 색은 검정 명판과 뜻이 섞여 물림, 모양은 기존 태그)로 다음 단계. 첫 칸은 확인이 쉬운 BPM · 노트 수 · 길이 · 수록일, 근거(영상 · 공식 · 직접)를 함께 받는다. 채보 정보의 이 네 줄은 비어 있어도 두고 「추가」 를 보인다(값 있는 행만 규칙의 예외). 관리자 검토 화면은 관리자 화면 규칙대로 기존 모양.

- **템포 변화 = BPM 경고를 「타이밍 포인트 넣기」 제안으로(T2, 사용자)** — Altale 63마디부터 실제로 느려진다(영상 원본 프레임: 2–62마디 90.00 · 63마디부터 82.96, 녹화 중복 프레임 비율은 구간마다 4%로 같아 녹화 탓 아님). 같은 내용이 경고와 제안으로 두 번 나오지 않게 경고 줄을 제안으로 바꿨고(T1 별도 절 · T3 넣은 뒤 타이밍 화면은 물림), 측정은 보정된 박자선이 아니라 보정 전 실제 프레임(`beat_frames.json`)으로 — 보정된 값은 83.7 로 틀렸다. 제안 제목을 누르면 캔버스가 그 박으로 가고 노랑 점선으로 보인다.
- **태그 · 배지 재설계(N3 · R1 · S-c · C-a · X2, 사용자)** — 다크에서 무채색 태그가 안 보인다는 지적. 재 보니 체감이 아니라 측정이었다 — 글자는 8.61 로 괜찮지만 **형태를 알리는 테두리가 1.94 : 1** 로 3 : 1 미달이고 면은 없었다.
  다크 35곳 · 배지 100여 개를 실측하니 **무채색 테두리 하나로 두는 곳은 0곳**이었고, 배지 면 대비의 60%가 1.5 미만이라 면은 힌트일 뿐 **윤곽은 테두리가 담당**했다(pkg.go.dev 면 1.00 + 테두리 3.21).
  그래서 면 `surface/raised` + 테두리 `border/input` + 글자 `content/default` 12/600. 테두리를 `default` 로 두면 팝오버에서 1.63 이라, 어디서나 3 : 1 을 넘기는 `input` 으로 올리고 토큰 표의 쓰임 한 줄을 고쳤다(사용자 승인).
  상태는 **같은 면에 글자만 표시색**(S-c) — 실측 최다는 채운 면(7곳)이지만 우리 표시색으로 채우면 흰 글자가 1.57~3.50 으로 깨지고, 옅은 색 면(5곳)은 shadcn · Radix 기본값이라 흔하다는 사용자 판단. 분류는 색을 주지 않는다(C-a) — 색을 아껴야 상태가 보인다.
- **정렬 트리거는 모든 폭에서 고스트 · 방향은 항목에 녹임(T2 · D2 · L2, 사용자)** — 같은 부품인데 폰은 고스트 36, 넓은 화면은 테두리 40 이었고 오락실 상세 · 의견만 늘 고스트라 화면끼리 어긋났다.
  실측 16곳에서 정렬 방향을 **메뉴 안에서 따로 고르는 곳은 2곳**(GitHub 리포 목록 · Linear — 둘 다 개발자 도구)뿐이고, 기준에 방향을 녹이는 곳이 6곳 · 방향을 두지 않는 곳이 7곳이었다.
  트리거 왼쪽 아이콘을 방향 토글로 만드는 안(D4)은 실측 · 가이드 선례가 0곳이라 뺐다. 트리거에 방향 화살표를 쓰는 곳도 0곳이라 기준 이름이 곧 상태다(「레벨 높은 순」).
  악곡 검색 메뉴는 5줄 → 8줄이 되었고 「방향」 절과 그 2단 구조가 사라졌다. 주소(`sort` · `order`)는 그대로라 기존 링크가 열린다. 새 토큰 없음.

- **영상 추출(vid2bmap) 가져오기 = 오른쪽 패널 + 노랑 미리보기 · zip 그대로 · 박 단위 첫 박 맞추기 · 겹쳐 보기 + 달라진 곳 목록(L1 · F1 · B1 · D3, 사용자)** — 채보 에디터는 재설계하지 않으므로 검사기 자리 패널과 캔버스에 정의만 돼 있던 미리보기 노랑(`--color-score` 와 같은 값)을 썼다. 좌우 나란히(D1, 사용자 제안)는 28칸이 반쪽(약 13px/칸)이 돼 1칸 차이가 안 보여, 한 캔버스에 겹치고(초안은 흐리게 · 빠질 노트는 충돌 빨강 · 고른 곳은 흰 띠) 목록에서 곳마다 「내 것 / 가져온 것」 을 고른다(VS Code 의 다음 충돌 · 받기). 근거 실측: Altale Real 앞부분 232개가 박자선 기준 변환으로 박 · 칸 · 폭 · 종류 모두 일치(절대 시각으로는 61%), 칸이 다른 8개는 영상으로 보니 공개 채보 쪽 입력 실수.
- **글에 투표 붙이기(V2 · W1 · R1, 사용자)** — 유저 피드백(공지에 투표)과 패턴 경향 이름처럼 의견을 모을 자리가 필요했다. 16곳을 보니 **글에 딸린 데이터(본문 밖)가 7곳, 본문 자리표시자가 3곳**이라 더 보편적인 쪽을 골랐다(V2).
  만들기 창은 Discourse 소스 · Strawpoll 실측이 일치한 것만 담았다 — 종류 먼저 · 「선택지 추가」 와 엔터 · 여러 줄 붙여넣기 · 마감 기본값 없음 · 결과 공개는 「자세히」 · **창에 미리보기 없음**. 결과는 라벨 오른쪽 퍼센트(4:1 우세).
  규칙은 조사 결과 그대로다: 한 개만은 누르면 바로 · 여러 개는 「투표하기」(7:6 으로 갈려 Discourse 가 나눈 기준을 따름) · 비로그인은 결과만 · 표가 들어오면 질문 · 선택지 잠금(마감만 변경) · 마감 뒤 전면 잠금 · 선택지 2~20개 · (선택지, 사람) 한 쌍으로 중복 방지.
  공지는 세 벌 번역, 이벤트는 쓴 언어 한 벌로 저장하고 볼 때 채워진 벌 중에서 고른다.
- **빙고 상태 칩은 한 줄 그대로(사용자)** — 좁아지면 칩이 2행으로 늘어났다. 가이드는 「상태 칩 한 줄」 이라 줄바꿈은 규칙 위반이어서, 공용 `FilterChips` 에 한 줄 변형(`row`, 넘치면 가로 스크롤 — 밑줄 탭과 같은 문법)을 더했다. 창 · 팝오버 안 칩은 그대로 줄바꿈한다. 칩 문구 「풀보드 완료」 → 「완성」(ja 完成 · en Complete).
- **글쓰기 재설계 — 미리보기 · 도구 · 아래 바(P2 · L2 · C · T-c · M1 · K1 · B1 · TB1 · Q1, 사용자)** — 미리보기가 입력칸 테두리 안에서 글만 바뀌어 실제 글이 어떻게 보일지 알 수 없었고, 긴 글을 쓰면 저장 버튼이 화면 밖으로 사라졌다.
  실측 7곳 · 문서 17곳 · 가이드 8종을 봤다. 미리보기를 **별도 화면**으로 여는 곳(Ghost · Substack · Dev.to)은 트리거가 버튼이고 GitHub 처럼 제자리에서 바뀌는 곳만 탭인데, 사용자가 **탭 유지(A)** 를 골랐다(그 탭에 `aria-haspopup="dialog"`). 폭은 남들에게 근거가 없어(실측 4곳 모두 미리보기 폭과 글 폭 일치 미확인) **우리 읽기 폭 768** 을 따른다.
  도구는 **접지 않는다** — 서식 버튼을 폭 때문에 감추는 곳이 거의 없었고(Zulip 22개 · Discourse 12개 가로 스크롤, GitLab · Gitea · CodiMD 줄바꿈), ＋ 메뉴는 항목이 여럿일 때만 쓴다(GitLab 12가지). 투표를 본문 밖(V2)에 두기로 해 ＋ 에 넣을 것이 구분선뿐이라 **＋ 없이 툴바 버튼**으로 둔다(사용자 확인).
  인용 · 코드 · 표까지 그리기로 한 건 게시글 렌더러 13곳에서 **인용 · 코드 · 구분선이 공통 기반**이고 표도 대부분 그리기 때문이다(`remark-gfm` 추가, 사용자 승인). 고정폭은 글꼴 파일을 늘리지 않으려고 기기 글꼴을 쓴다.
  글자 수는 GOV.UK 를 따라 **75% 뒤에만** 보이고 한도를 넘겨도 입력은 막지 않는다(게시만 막는다). 아래 바는 Polaris(변경 있을 때 저장 바) · NN/g(자동 저장이 저장 버튼을 대신하지 않는다)를 따른다.
  **기울임은 합성을 켠다(I2, 사용자)** — 만들고 재 보니 한글 · 영문 모두 기울임과 보통의 폭이 같아(41.48 대 41.48) 버튼이 먹통이었다. 우리 글꼴에 기울임 글꼴이 없고 사이트가 합성을 꺼 둔 탓이라, 본문 `em` 한 곳에서만 합성을 허용한다.
- **패턴 경향 0–4 = 모든 곡 대비 세기(A1 + B2, 사용자)** — 유저 의견(phs, 09-20): 「모든 곡 기준인지 곡 안 비중인지 명시해 달라」. 곡 안 비중(A2)은 쉬운 곡과 어려운 곡이 같게 보이고 빨간 막대가 쉬운 곡에 붙으며, 같은 레벨 대비(A3)는 레벨끼리 비교가 안 된다. 모든 곡 대비는 DDR 그루브 레이더와 같은 고정 기준이고, 기존 설명문 「…의 부담」과 뜻이 같아 모인 평가의 의미가 바뀌지 않는다. 기준은 ⓘ 창에만 두지 않고 평가하는 자리(패턴 투표 첫 줄)에 한 줄로 늘 보인다(가이드 「기준은 늘 보이는 글」). 단계 이름은 사용자 문구 — 없음 · 조금 나옴 · 뚜렷하게 나옴 · 심한 편 · 악곡의 특색일 정도, 한 줄 요약은 「0 없음 – 4 많음」.
- **기준 창 단계 색 표시 = 8px 네모 · 모서리 없음(Q2, 사용자)** — 처음 시안의 12px 네모는 토큰 위반은 아니었지만 사이트의 다른 색 표시(판정 점 · 검정 범례 네모 · 상태 태그 점 · 대시보드 점)가 모두 8 이라 튀었다. 점(원)은 판정 · 상태 같은 낱개 값에 쓰고 네모는 막대 조각 범례(검정 합격까지)에 쓰므로, 막대 색을 설명하는 이 목록은 8px 네모. 작은 막대 모양(Q4)은 새 모양이 생겨 물림.
- **오락실 사진 뷰어 = 전체 화면(V3 · B2 · C2 · S2 · U2 · N2, 사용자 「모두 추천대로」)** — 작은 대화상자 안이라 사진이 폰 56% · PC 39% 만 차지해 답답했다.
  실측 19곳에서 **폰에 작은 가운데 모달을 쓰는 곳은 0곳**(전체 화면 오버레이 10 · 전체 화면 라우트 5 · 시트 2)이고 태블릿 768 에 따로 중간 모양을 쓰는 곳도 없었다(4곳).
  배경은 새 색 없이 `surface/canvas`(위키백과 #202122 · DeviantArt #06070d 처럼 암색 면). 격자 단계는 뺐다 — 2단을 쓰는 7곳은 모두 사진이 수십~수백 장인데 오락실은 몇 장이다.
  주소는 남기지 않고 뒤로 가기로만 닫는다(공용 전체 화면 창과 같은 방식, 공유 가치가 낮음). 페이지 위 캐러셀은 폰에서도 ‹ › 를 되살렸다 — WCAG 2.2 2.5.7 은 스와이프 단독을 금지한다.
  M3 는 「전체 화면은 compact 에서만」 이라 이 점은 어긋나고, Apple HIG(사진 보기를 전체 화면 모달의 대표 예) · 실측 쪽을 따랐다.

## 2026-09-22

- **검정 통과선 여유 색(M2, 사용자)** — 사용자 제안은 + 빨강(slow) · − 파랑(fast)이었으나 slow 는 주황이고 fast 파랑 #3278be 는 #111 위 글자 대비 4.11 로 4.5 미달이라, − 만 알림 파랑 #4688ec(5.39)로. 부호 · ✓/✗ 아이콘은 그대로라 색만으로 알리지 않는다. 위 곡 카드의 내 베스트 숫자도 그 통과선과 같은 색(사용자). 이어서 여유(±)는 통과선 줄에서 빼고 합격까지 눈금 아래(합격선 값 자리)로 옮김 · 범례는 곡별 베스트 점수 그대로(사용자). 문구도 「1st 925,000 · 누적 1,875,000」 으로 줄임.
- **빙고 재설계(사용자)** — 상세 판 칸이 좌표(A1)뿐이라 25줄 목록과 오가야 했고, 폰에선 목록을 내리면 판이 사라졌다(실측 세로 2,939). 레퍼런스 21곳(판 자체는 OoT Bingo · Bingo Baker 등 4곳)과 가이드 4곳 근거로 비교해
  사용자가 골랐다: 판 칸에 미션 글(폰 A · 넓은 화면 1:1 E · W1), **좌표 없음(P4 — 원본 빙고에 없는 숫자, 위치는 미니 판)**, 미션 목록은 판 아래 기본 펼침(「한눈에 보는 것」).
  목록은 필터 창 대신 상태 칩(게스트 없음) + 보기 전환(격자 기본 · 목록형), 격자 미니 판은 우하단, 목록형 진행 막대는 글 아래(R1b · 위 간격 `spacing-4`), 넓은 화면 목록형 두 열(T2). 새 토큰 없음.
- **내보내기 창에서 구간 고르기(R2 · 기본 선택 추천대로, 사용자)** — 500곡을 넘으면 경고만 뜨고 창을 닫아 필터를 바꿔야 했다(사용자 지적). 「담기는 것」 줄 오른쪽 「구간 고르기」 → 레일과 같은 구간 체크 목록, 넘으면 처음부터 펼치고 위부터 이어서 500곡 안까지 미리 체크(가장 큰 구간 434라 늘 저장 가능). 고른 구간은 이미지에만. 목록 · 칩 · 범위 셀렉트 · 여러 장 나누기(R1 · R4 · R3 · R5)와 비교. 다른 서비스 사례는 미확인.
- **검정 재설계(K1 · G1-a · H2 · T7 · P2-a · C2 · E1 · W1, 사용자)** — 급이 드롭다운에 숨고 상태가 사실 칸에 묻히고, 누적 합격선을 머리로 계산해야 했다(1차 조사 실서비스 24 · 가이드 5, 2차 17). 급 고르기는 세로 사다리(6곳)를 추천했으나 사용자가 드롭다운 유지를 골랐고, 명판(S3)은 셀렉트 안에서 어색해 「급 · 상태」 로. 심사 중 = 기존 「검토 대기 = 경고 점」(다크에서 노랑에 가까움, 주황 빌리기 G1-b 는 안 함). 과제곡은 게임 선택 화면의 곡 사이 통과선에 LiveSplit 식 여유(±)를 얹음. 합격까지는 게임 결과 화면의 기준선 축에 곡별 조각 색(차트 범주 색 1~3)을 더함 — 좁은 폭(330)에서 가운데 맞춤 눈금 글자가 겹쳐 모두 눈금 왼쪽 맞춤으로. 상태 「완료」 → 「합격」(ko · ja · en). 새 토큰 없음.
- **서열표 제목 줄 ⋯ 더보기 메뉴(S7-a, 사용자)** — 이미지 내보내기를 결과 줄 끝(S5)에 두었다가 사용자가 ⋯ 메뉴로 바꿨다(「가장 보편적」). 실측 약 30곳(웹)에서 ⋯ 안 공유는 1곳(Apple Music) · 가장 많았던 자리는 제목 아래 동작 줄 4곳이라 근거는 약하고, 앱 쪽 빈도는 미확인. 메뉴에 항목 하나면 숨길 이유가 약해 「링크 복사」(새 기능) · 「서열표 안내」(메타 줄과 같은 창)를 함께 둔다. 공용 `ActionMenu` 그대로(항목 아이콘 없음) · 새 토큰 없음. 공용 아이콘 버튼 M 크기(`icon-sm`)는 부품에 남겨 둔다.
- **선 두께 토큰 두 단계(사용자)** — CSS 26개 파일에 흩어진 1px · 2px 선(약 175곳)을 `--nl-stroke-default` 1 · `--nl-stroke-strong` 2 로 묶었다. 값은 그대로라 화면은 같다(주요 21화면 × 1280 · 390 전후 캡처 비교). 채보 뷰어 캔버스 위 표시와 슬라이더 손잡이의 투명 여백(선이 아님)은 뺐다.
- **서열표 이미지 내보내기(E3 · D1 · I1 · W1 · JPEG, 사용자)** — 리듬게임 기록 도구는 「내 기록 이미지」로 사이트가 퍼진다(PIU Scores 서열표 ⬇ · CHUNITHM/maimai 베스트 이미지 · V-ARCHIVE B30 봇, LoL 티어 사이트는 없음). 자리는 추천 E1(결과 줄) 대신 사용자가 프로필 카드와 같은 제목 줄 오른쪽(E3)을 골랐다.
  창은 프로필 카드 창을 그대로(공용 `nl-image-share` 로 옮김) · 한 장 500곡(가장 큰 구간 434) · 폭 1200 · 한 줄 12(PIU 1000 · 6 · V-ARCHIVE 842 · 6 실측과 비교) · X 5MB 때문에 JPEG · 표시 선택은 브라우저 기억.
- **목표 미달성 = 자켓 흑백(B, 사용자)** — 「disabled 처럼」 제안을 disabled 정의(Material 3 · Carbon 「못 누름」) 때문에 자켓 그림만 흑백으로 좁혔다. 글자 · 판 · 누름은 그대로. 리듬게임 표본에서 흑백 사례는 못 찾음(근거 약함) · 업적(Steam 미달성 아이콘) 방식.
- **자켓 위 띠 = 공식 Grd · 레이팅 셀렉트(A, 사용자 · 기본 Grd · 주소)** — V-ARCHIVE 성과표의 「점수 ⌄」 셀렉트(점수 · 레이팅 · DJ POWER · 라이벌) 방식, 점수는 자켓 아래 그대로. 기존 `CompactSelect` · 이미지 위 글자 토큰만 사용. 값 없는 곡은 띠 없음.
- **오락실 시설 · 기체 태그 · 기체 특징(사용자: T2 · F3 → B2 · S1 · V2 · L1)** — 태그는 아이콘 + 글자(실측 약 8곳으로 가장 흔함 · 스페이스클라우드 류). 오락실 시설은 따로 구역(숙박 · 식당 · 뮤즈캣), 기체 태그는 카드 안 흐린 12(위치 메모와 같은 급). 특징은 정해진 항목 키-값 — 값은 3단계 보기(세 언어 번역 · 기체끼리 비교) + 「기타」 자유 글 한 줄. 새 토큰 없음 — 시안의 13/18 글자는 척도 밖이라 `body-secondary` 14/20.
- **의견 좋아요 · 답글 · 번역(L1 · R1 · T1 · P1, 사용자 「모두 추천대로」)** — 의견을 쓰기만 하고 반응할 방법이 없었다. 실서비스 22곳(댓글 12 · 번역 10) 실측 · 가이드 10곳. 좋아요 = 본문 아래 동작 줄 하트(댓글 좋아요 10/12, 본문 아래 6, 토글 · 색만으로 말하지 않기 가이드 4곳) — 2026-09-16 에 뺀 「추천」 을 좋아요로 되살림. 답글 = 한 단계 · 「답글 N개」 접힘 · 제자리 작성(1단 2 · 접힘 5). 번역 = 원문 + 「번역 보기」 → 같은 자리 교체 · 「○○어에서 번역됨 · Gemini · 원문 보기」(교체 3/3, NN/g). 번역은 공식 X 번역과 같은 방식 · 같은 무료 키로 **올릴 때 한 번 번역해 저장**(사용자 — 유료 전용 키 P1 대신). 무료 등급은 입력을 Google 이 서비스 개선에 쓰고 검토자가 볼 수 있어 방침 2026-09-22 에 그대로 적음.
- **오락실 사진 1056+ = 모자이크 · 「모두 보기」(P1, 사용자)** — ‹ › 가 왼쪽 큰 사진에만 떠서 넘기면 오른쪽 작은 사진과 같은 사진이 겹쳤다. 실측 모자이크 5곳(야놀자 · 여기어때 · Airbnb · Agoda · Booking) 모두 모자이크 위에 화살표가 없고 「전체 사진」 → 창에서 넘긴다 · Material 3 도 「Show all」. 모자이크는 넘기지 않는 자리로 두었다.
- **대시보드 오늘 막대는 보고 있는 수치로(사용자 · 추천대로)** — 오늘 보기에서 어떤 수치를 골라도 페이지뷰 시간대 막대만 나왔다(사용자 지적). 가입 · 동기화는 이미 있는 가입 · 동기화 시각에서 시각별로 세어 같은 막대를 제 색(C2)으로 그린다. 방문자는 하루 단위로만 세므로(H1) 같은 막대 틀(격자 · 시각 축)을 비워 두고 가운데 「시간대별로 모으지 않습니다」(B1, 사용자) — 안내 한 줄(A)은 전환 때 패널이 320 ↔ 140 으로 줄어 아래가 튀었다. 선 그래프 빈 틀 문법과 같고 안내는 공용 `nl-line-chart__state`. 새로 모으지 않아 개인정보처리방침은 그대로. 새 토큰 · 값 없음, 로딩은 같은 틀의 기존 스켈레톤.
- **악곡 자켓 = WebP 긴 변 512px · 품질 80(사용자)** — `public/bg` PNG 480장 162MB(평균 346KB, 최대 2.5MB)를 일반 `<img>` 로 원본 그대로 받아 목록이 느렸다. 화질 비교(원본 · 512 · 256)에서 512 는 상세 머리(272px, 고해상도 544)에서도 원본과 구분이 어렵고 256 은 흐려졌다 → 512. 14.5MB(9%), 목록 15장 5.55MB → 0.44MB. 새 PNG 는 `npm run convert:jackets`.
- **서열표 제목 아래 메타 한 줄(A, 사용자) — 「2,159곡 · 9월 2일 업데이트 · 안내」, 제목 옆 ⓘ 없앰** — 스위처 꺾쇠 뒤에 ⓘ 가 붙고 곡 수가 따로 떨어져 어색했다(사용자 지적). 실서비스 16곳 실측(폰 390)에서 제목 옆 ⓘ 는 0곳, 제목 아래 설명 10곳 · 업데이트 시각 6곳.
  Carbon · Material 3 는 툴팁에 중요한 정보를 숨기지 말라고 하고 Primer 는 설명을 제목 아래에 둔다. 10안 중 추천 A 를 골랐다. 기존 링크 · 메타 글줄 부품만 써서 새 토큰 · 값 없음, 업데이트 날짜는 안내 창에 이미 있던 값.
- **오락실 영업시간 토 · 일 색(사용자)** — 뮤즈캣 · 지도 서비스처럼 토요일 파랑 · 일요일 빨강. 새 토큰 없이 기존 글자용 값을 빌림: 토 `judgement/near`(캔버스 위 대비 다크 9.0 · 라이트 4.8), 일 `feedback/error-text`(다크 9.1 · 라이트 6.5). 요일 · 날짜 · 시간 줄 전체를 칠한다(사용자).
- **서열표 제목 스위처(④, 사용자) · 결과 수는 제목 아래 · 결과 줄 정렬(②, 사용자)** — 머리의 모드 「Basic ▾」 · 목표 「S 서열표 ▾」 두 셀렉트가 조합처럼 보여 어색했다(사용자 지적). 서열 · 티어 서비스 7곳 실측(탭 줄 + 드롭다운 4 · 칩 1 · 사이드 목록 1 · 제목 스위처 1)으로 네 틀을 비교했고, 추천(①)과 달리 사용자가 제목 스위처(④)를 골랐다 — 네 서열표를 한 목록에서 고르고 Recital 도 같은 자리.
  결과 수 · 정렬 · 보기는 악곡 목록과 같은 자리로 맞췄다(사용자: 「악곡 검색도 그렇게 돼 있으니까」). 정렬 기준은 세 안 중 추천 ②(공통 3종 + 로그인 점수 낮은 순 — 목표를 못 채운 곡을 구간마다 앞으로)를 골랐다. 새 토큰 · 새 값 없음, 구간 응답에 읽기(가나)를 더했다.
- **서열표 머리 = 검색줄 + 결과 줄 두 줄(B, 사용자) · 곡 검색 추가** — 커밋한 머리는 모양이 다른 테두리 상자 네 개(세그먼트 · 셀렉트 · 버튼 · 아이콘 세그먼트)가 두 줄에 쌓여 어색했고(사용자 지적), 특정 곡의 서열을 찾을 길이 없었다.
  서열 · 티어 · 곡 목록 서비스 15곳 실측(12곳이 목록 위나 헤더에 검색) · 가이드 4곳(Material 3 · Carbon · Apple HIG · Atlassian). 목록 탭 한 줄(A)과 비교했으나 사용자가 줄 수가 적은 B를 골랐다(3행 → 2행, 첫 구간 305 → 265 시안).
  악곡 목록 폰 머리와 같은 부품(검색 + ☰ 필터 44 · 보기 전환 M)이라 새 부품 · 새 값 없음(둘째 줄 모드 · 목표 셀렉트는 같은 날 제목 스위처로 바뀜). 검색 필드 · 방식도 악곡 목록과 같다. 서버 조회에 검색어 조건(`q`)을 더했다.
- **서열표 새 단장(B안 · 4열 · 목록 보기 · 0곡 구간 숨김, 사용자 「모두 추천대로」)** — 실측해 보니 부품 · 토큰은 이미 맞았고(어긋난 곳은 필터 꺾쇠 · 결과 수 자리 · 구간 밑줄 #444 · 0곡 구간 넷), 문제는 폰 구성과 밀도였다(첫 구간 530px · 첫 화면 약 1곡).
  서열 · 티어 · 곡 목록 서비스 16곳(V-ARCHIVE · 3icecream · osu! · ScoreSaber · LoL/TFT 티어 등) 실측 공통점 = 조작부 한두 줄 · 한 화면에 많이 · 이름을 볼 방법. 모드 · 목표 한 줄, 필터 · 보기 한 줄(모두 L), 설명 · 안내는 ⓘ 창으로 → 첫 구간 280px.
  폰 격자 4열(새 배치 값, 사이는 기존 8). 「상세 보기」 2열 카드 대신 목록 행 — 자켓은 40 시안과 비교해 이미 쓰는 48(채보 묶음)로(새 크기 없음, 사용자). 행 오른쪽은 점수와 기여 한 줄(공식 Grd, 없으면 레이팅)이라 예전 상세 카드의 등급 메달 · FC 마크는 빠지고 FC 는 자켓 테두리 그라데이션이 말한다.
  375 미만 4열 카드(66)는 「Expert 10」 판이 자켓 밖으로 나가 기존 난이도 약칭 규칙(N · H · EX · R)을 판에도 적용.
- **남은 조회·지도 최적화를 한 묶음으로(사용자)** — 악곡 상세의 미사용 옛 평가 집계를 제거하고 독립 조회를 병렬화한다. 오락실은 검색 결과 계산을 재사용하고 선택 때 기존 핀만 갱신한다. 공지는 공개 검증을 유지하며 목록 메타데이터와 글별 본문 캐시를 나눈다. 표시 내용·개인정보·예약 공개·토큰은 유지한다.
- **표시 결과를 유지하며 불필요 조회·선로딩 제거(사용자)** — 프로필 머리의 미사용 기록 조회를 제거하고 악곡 필터 미리보기는 동일 조건의 개수만 조회한다. 관리자 지표는 같은 기간에 받은 값을 주소와 함께 전환하고, 기간 변경·새로고침은 서버에서 갱신한다. 글꼴·토큰·레이아웃은 유지하며 기존 Pretendard의 전역 preload만 끈다.

## 2026-09-21

- **모바일 서열표 구간은 20개씩 점진 렌더링(사용자 성능 지적)** — 기존 구현은 첫 화면에서 카드 DOM 2,159개와 스켈레톤 2,080개를 만들고, 12.0 구간 진입 때 434개를 실제 카드·자켓으로 한꺼번에 교체해 스크롤 입력을 막았다. 화면 밖 구간은 기존 카드 스켈레톤 6개, 구간 진입 뒤 첫 20개, 끝 240px 전 자동 20개씩 세 번(80개), 이후 기존 「더 보기」로 제한한다. 데이터 의미와 카드 모양은 그대로 둔다.
- **관리자 탭 이동도 공용 위쪽 진행 막대(사용자)** — 관리자 탭은 화면 전체가 바뀌는 사이트 안 이동이므로 영역 스켈레톤을 더 만들지 않고, 일반 셸과 같은 `NavigationProgress` 를 관리자 셸에도 둔다. 기존 100ms 지연 · 2px 막대 · 이전 화면 유지 규칙과 토큰을 그대로 쓴다.
- **관리자 대시보드 서버 조회 중 기존 스켈레톤 규칙(사용자)** — 새 토큰 · 인라인 로딩 값 없이 `SkeletonText` · `nl-skeleton` · `nl-loading-delay` 만 쓴다. 제목 · 전환 버튼 · 패널 라벨은 유지하고 수치 · 그래프 · 목록을 현재 패널과 같은 크기로 바꿔, 서버 응답을 기다리는 상태를 구분한다. 「로그인 여부만 셉니다」 같은 고정 안내는 유지하되 뒤의 기간 값은 글자 스켈레톤으로 가린다.
- **관리자 대시보드 그래프도 공용 움직임 그대로(사용자)** — 새 애니메이션이나 Recharts 시간 옵션을 만들지 않고, 정해 둔 `nl-chart-reveal` · `nl-chart-bar` 만 추이 선 · 시간대 막대 · 가입자/손님 비율 · 막대 목록에 적용한다.
- **관리자 계정은 대시보드 이용 통계에서 제외(사용자)** — 운영 확인 방문과 관리 작업이 실제 이용자 흐름을 오염시키므로 페이지 · 방문자 · 시간대 · 상위 경로 · 브라우저 외부 호출 · API와 가입 · 동기화 · 전환 · 기여 합계에서 현재 관리자 역할을 뺀다. 처리 대기 · 실패 감시는 운영 상태라 유지한다. 개인 식별자를 남기지 않은 과거 방문 합계는 소급 분리하지 않는다.

## 2026-09-20

- **관리자 대시보드 확장(2026-09-20 사용자 · 추천대로)** — 실측(Plausible · GoatCounter · Matomo)에서 공통인 시간 · 유입 · 기기 · 국가 중 **시간대별부터**. 오늘은 시간대별 페이지뷰 막대(H1 — 방문자는 하루 단위 유지), 가입자 · 손님 합계, 전환 흐름 · 기여 활동(이미 있는 데이터). 개인 식별은 하지 않기로 함(사용자). 실시간 · 오류 추이는 저장이 더 필요해 보류.
- **대시보드 그래프는 수치마다 제 색(C2, 2026-09-20 사용자)** — 방문자 categorical-1 · 페이지뷰 categorical-2 · 가입 categorical-3 · 동기화 categorical-5, 수치 칸에도 같은 색 줄. 기존 bucket-1 은 파랑 여섯 단계 중 가장 어두워 카드 바탕과 구분이 약했다. 글자가 얹히는 목록 막대는 대비 때문에 그대로.
- **개인정보처리방침 2026-09-20 시행(사용자)** — 남는 합계에 시간대별 조회 수 · 로그인 여부별 합계가 늘어 문구를 고치고, 직전 2026-09-13 판을 `versions/` 로 보관.
- **Pass 없이도 저장된 기록으로 동일 기능 이용(사용자)** — 최근 플레이를 중복 없이 개인 최고 기록·횟수·Basic Grd·Rating·랭킹·성장 이력에 반영. Pass 만료 후 기존 전체 기록을 이어 갱신하고 재가입 시 공식 값으로 보정. 없는 Recital 값은 `—`, 출처 배지 추가 없음. [운영 적용 안내](../operations/recent-record-projection/README.md).

## 2026-09-19

- **악곡 상세 탭 스켈레톤 = 탭별 실제 틀(2026-09-19 사용자 지적)** — 네 탭이 높이 416 상자 하나를 쓰던 것을 로딩 결정 S1(실제 틀) 대로. 표본 글자 스켈레톤은 `overflow: clip` · 줄 높이 1 로 기준선 정렬 줄(판정 값)에서도 2px 어긋나지 않게(공용).
- **기록 없을 때 틀 유지(E1 · K1, 2026-09-19 사용자)** — 「등록된 기록이 없습니다.」 한 줄 대신 내 기록은 다섯 구역 그대로 값만 「—」(가이드의 「플롯 틀 유지」 · 값 자리 「—」 선례), 랭킹은 머리 줄 + 「아직 플레이된 기록이 없습니다.」 · 점수 자는 1명 이상부터. 판정 평균이 곡 기록의 Grd 를 기준으로 해 기록이 없으면 사라지던 것을 유저 Grd 로 읽게 함(평균의 뜻은 그대로).
- **뜻이 바뀌는 전환은 스켈레톤(S1, 2026-09-19 사용자 · 추천대로)** — 성장 추이에서 「NosLog 레이팅」 을 눌러도 이전 Grd 선이 그대로 남아 헷갈렸다. 흐린 이전 선(S2)도 모양이 보여 같은 문제. 지표 · 기간 · 모드 전환만, 300ms 지연 뒤 같은 틀 스켈레톤. 베스트 성과 지표 · 모드 전환도 같음.
- **로딩 표시(로딩 시안 결정 1–5 · 추천대로, 사용자)** — 이동 = 위쪽 진행 막대 + 이전 화면 유지(osu! · GitHub · YouTube), 영역 = 실제 틀의 스켈레톤(고정 부분은 실제 글자), 펄스, 스켈레톤 안 스피너 없음(실측 15곳 중 0곳 · 가이드 4곳 「표시는 하나」), 첫 진입 지연 없음 · 다시 불러오기 300ms 지연 · 최소 표시 없음. 스켈레톤 채움은 `surface/sunken` 이 다크 바탕과 같아(대비 1.00) 보이지 않아 `border/subtle` 로.

- **공식 X 소식 수집 분리(사용자)** — 최신 ID 이후만 12시간 주기로 확인하고 원문·성공한 번역을 DB에 보관한다. 홈은 캐시 조회만 하며 외부 API를 기다리지 않는다. Hobby 예약은 GitHub Actions, 실행 중복은 DB에서 차단한다.

- **패턴 경향 축 이름: 변속 → 즈레 · 동시치기 → 변박(사용자, 유저 의견)** — 노스탤지어는 스크롤 속도가 고정이라 「변속」은 거의 쓰지 않고, 가까운 타이밍을 어긋나게 치는 「즈레」가 훨씬 자주 보인다(설명문은 원래 즈레 뜻이었다). 동시치기는 11.0 이상이면 거의 모든 패턴에 있어 기준을 잡기 어려워, 그 자리를 「변박」(박자 · 마디 길이 변화)으로 바꿨다. 일본어 ズレ · 変拍子, 영어 Offset · Meter changes 로 맞춤(설명문도).

## 2026-09-18

- **플레이 점수 비공개(S3 · 사용자)** — 켜면 랭킹에서 아예 빠진다(익명 줄로 남기면 최상위권은 누구인지 짐작된다). 본인은 공개된 사람들 사이 자기 순위를 본다. 기본 공개(D1) · 가입 온보딩에서도 고른다(처음부터 선택권). 값은 서버에서 빼서 응답에 싣지 않는다.
- **피드백 답변(F1 · 사용자)** — 알릴 통로(이메일 · 봇)가 없어 사이트 안에서: 피드백 창 「새로 쓰기 · 내 제보」, 관리자는 처리 완료 때 답변 칸(선택). 새 답변 점은 메뉴 줄과 탭에.
- **Grd 구간 색을 모든 곳에(2026-09-19 사용자 · 추천대로)** — 유저 랭킹 값 · 공유 카드(토큰 밖 #facc15 노랑 예외를 없앰, 순위도 금 · 은 · 동) · 성장 추이 요약 시작 · 현재. 기준은 보이는 숫자(반올림한 Grd).
- **프로필 Grd · 레이팅 구간 색(B2 · R1, 2026-09-19 사용자)** — 공식 랭킹 상위 25명(Basic 6,776–7,756 · Recital 7,195–8,550) 참고, 두 모드 같은 기준 6,000 · 6,500 · 7,000 · 7,500 · 8,000(6,000 대부터 색). 색은 아래 판정 NEAR · GOOD(빌림 예외) → 위 점수 목표 색 S · 990k · Pianist. 레이팅도 같은 기준. 순위 칸은 순위표 색.
- **진행 막대 움직임(2026-09-19 사용자 · 추천대로)** — 빙고 상세 진행 · 악곡 머리 그레이드 막대 = 그래프와 같은 드러남 + 옮겨 감(누른 결과 · 난이도 비교가 보임). 빙고 목록 카드 막대는 없음(값이 안 바뀌고 여러 장이 한꺼번에 움직여 장식). 그레이드 그라데이션은 옮겨 가는 동안 흔들리지 않게 트랙 폭(cqw) 기준.
- **판정 추이 툴팁 선 이름 = 선 색(T1, 2026-09-19 사용자)** — 이름(◆JUST · MISS · FAST · SLOW)만 선과 같은 변수의 색, 값은 기본색. 라이트 SLOW(#c87b00) 글자 대비 3.3 : 1 은 알고 받아들임.
- **그래프 움직임 = 나타날 때 + 값 바뀔 때(A, 2026-09-19 사용자)** — 실측 데이터 서비스는 대부분 정적(8곳 중 등장 1 · 값 바뀜 0)이라 C 를 추천했으나 사용자가 A 선택. 나타남 800 · 바뀜 300, 곡선은 들어올 때 감속, 동작 줄이기에서 0.
- **움직임 목록 · 토큰(2026-09-19 사용자 · 추천 그대로)** — 가이드 6곳 · 실서비스 19곳 실측 근거. 데이터 · 랭킹 서비스는 올림 색 · 로딩에만 움직임을 쓰고 스크롤 연출은 0곳이라 상태 · 위치 · 로딩만: ① 올림 색 100 · ③ 메뉴 열림 150 페이드(닫힘 즉시 — 셀렉트 · ≡ 메뉴가 닫힘 움직임을 못 받아 모두 맞춤) · ④ 대화상자 · ⑤ 토스트 200 → 150 · ⑩ 스피너 · ⑪ 펄스 · ⑫ 헤더 · 시트 200 · ⑮ 지도. 값은 `tokens.css` 움직임 토큰만(테스트로 막음), 동작 줄이기는 토큰을 0 으로 한 곳에서.
- **움직임 모두 빼기(2026-09-19 사용자)** — 값이 제각각(버튼 50 · 헤더 200 · 스피너 1s · 펄스 2s · 토스트 400)이고 올림 색도 버튼만 움직여 섞여 있었다. 틀은 유명 디자인 가이드 3개 이상 · 실제 서비스 15개 이상 조사 뒤 새로 정한다. 가이드에 있던 「페이지 이동 위 2px 막대」 는 구현된 적이 없어 지움.
- **그래프 가리킨 점 = 선 색으로 채운 반지름 4(P1, 2026-09-19 사용자)** — 점을 끈 그래프에서 툴팁이 가리키는 자리. 속 빈 링(악곡 상세 점 모양)은 osu! 와 달라 보였다. 선(2)보다 확실히 크고 툴팁을 가리지 않는 크기.
- **기록 추이 그래프 = 주황 선 · 바닥선만 · 면 없음(C1 · G1 · B1, 2026-09-19 사용자)** — 세로축 숫자를 숨긴 그래프의 위 · 가운데 선은 읽을 값이 없고, #1b1b1b 면은 바탕과 차이가 작아 탁했다. 파랑 personal 은 어두운 바탕에서 안 보여 osu! 계열 주황(categorical-2, 선이 하나라 「평균 · SLOW」 뜻과 부딪히지 않음). 프로필 성장 추이 툴팁도 악곡 상세와 같은 「값 · N일 전」 으로 맞춤(빠져 있던 것).
- **악곡 목록 · 빙고 악곡 칸에서 들어가는 난이도 = 그 곡의 가장 높은 난이도(2026-09-19 사용자)** — 가장 낮은 난이도는 관심이 적다. Real 이 있으면 Real, 없으면 Expert(Expert 고정안에서 바꿈).
- **피드백 창 정리(C · 2026-09-19 사용자)** — 세그먼트 두 줄이 겹쳐 읽기 어려움: 창 구역 「새로 쓰기 · 내 제보」 = 1단 밑줄 탭, 입력 「종류」 = 셀렉트(종류가 늘어도 그대로).
- **해금 조건 = 원문 저장 · 화면에서 난이도별로 풀기(번역 89개 사용자 승인)** — 원문(BEMANIWiki)을 그대로 두고 이름만 사전으로 옮긴다. 원문이 바뀌거나 새 이벤트가 생겨도 사전 한 줄만 더하면 되고, 출처와 대조할 수 있다. 星屑 은 「별가루 / Stardust」(사용자) · 한국어 라벨 「언락 조건」 → 「해금 조건」(사전과 같은 말, 사용자).
- **본문 이미지 · 홈 이벤트 빈 상태(사용자 지적 → 추천안)** — 공지 · 이벤트 글에 이미지가 없던 것은 빠진 기능. 편집기 「이미지」 버튼 · 붙여 넣기 · 끌어다 놓기로 우리 공개 저장소(announcements/ · events/ 폴더)에 올리고, 렌더러는 그 주소만 그린다(외부 이미지 · 쿼리 붙은 주소는 안 그림 — 추적 차단). 보이는 모양 I1(본문 폭 이하 · 모서리 8 · 캡션 없음).
  홈 「진행 중인 이벤트」 는 이벤트가 없어도 숨기지 않는다 — 숨기면 홈에서 이벤트로 갈 길이 없다. 빈 카드 + 「이벤트 글쓰기」(E1).
- **시안 대조 감사 뒤 확정(사용자)** — 이벤트 게시 요청 창 기간은 시작일 · 종료일 두 칸 세로(한 줄이면 334 창에서 날짜가 잘림) · 이벤트 목록 머리 줄에 로그인 시 「내 글」 고스트 M · 공지 게시 창 분류는 셀렉트(가장 흔한 방식 · 분류가 늘어도 대응) · 공개 주소는 입력칸 + 「제목에서 만들기」 + 도움말(앞머리 붙은 입력칸은 만들지 않음).
- **난이도 글자색(다크) 한 칸 밝게(A, 사용자)** — 배포 전 axe 점검에서 난이도 판(raised #222) 위 Normal 4.20 · Expert 4.22 로 4.5 미달. 같은 색상각에서 명도만 올려 네 난이도 모두 판 위 4.6(Normal #239d68 · Hard #e26500 · Expert #db675b · Real #ae6be9). 판을 어둡게 하는 안(B)은 「판 = 올린 면」 규칙을 바꿔서 물림.
- **이벤트 게시판 · 유저 글 승인 구현** — 글 하나에 작성 중인 판과 승인된 공개판을 따로 둔다(공개 글을 고치는 동안 이전 공개본 유지). 반려는 글 전체를 내린다(공개판도 지움) · 수정 요청은 공개판을 그대로 둔다.
  상태 태그는 **테두리 태그 + 알림색 점(T3)** — 알림 표시색을 글자에 쓰면 12px 대비 4.5 미달 조합(다크 정보 4.1 · 라이트 성공 3.2 · 경고 3.1 · 정보 3.5)이 있고 새 글자색은 만들지 않는다(사용자). 점은 그래픽 3 : 1 을 두 테마 모두 넘는다.
- **홈 「진행 중인 이벤트」 = 큰 배너 카드 위 글자(H1, 사용자)** — 실서비스에서 가장 흔한 모양. 글자 아래 어두운 층 + 옅어지는 흐림으로 사진이 밝아도 읽히고 경계가 딱딱하지 않게.
- **이벤트 글 삭제 = 고치기 화면 아래 왼쪽(D1) · 공개 중이어도 작성자가 바로 삭제(P1, 사용자)** — 관리자 공지 작성과 같은 자리. 자기 글은 자기가 지운다(의견 · 투표와 같은 규칙).
- **홈 공지 줄 = 목록과 같은 제목 굵기 + 「고정」 태그(N2 변형, 사용자)** — 고정을 글자 대신 분류 태그 옆 테두리 태그로. 면은 홈에 두지 않는다(맨 위 중대 공지 배너와 겹침 · 09-10 결정 유지).
- **공지 게시판 B1 · 글쓰기 · 유저 글 승인 방향(사용자 결정)** — 공지 목록은 지금 구조에 제목 굵게 · 분류 필터(태그를 누르게 하지 않고 목록 위 — GOV.UK · Carbon) · 활성 중대 공지 맨 위 면 고정.
  요약 한 줄 없음(제목이 내용을 말함) · 월 묶음 유지 · 상세 끝 이전 / 다음 + 목록으로(다 읽고 다시 올라가지 않게). 상태 태그는 색 있음(태그 가족의 면 + 색 글자).
  글쓰기는 E2 = 마크다운 + 「쓰기 / 미리보기」 탭(GitHub 구조를 우리 규격으로 — 탭 줄 = 컨트롤 L · 서식 버튼 L · 입력 16/24, 고정폭 글자 예외는 만들지 않음) · 설정은 「게시」 창 · 유저 버튼은 「게시 요청」.
  유저 글 상태 5개(임시저장 · 검토 대기 · 수정 요청 · 공개 · 반려) · 사유는 편집 화면 맨 위 · 기록을 동기화한 사람만 작성 · 공개 글 수정 중엔 이전 공개본 유지. 이벤트 목록은 L1 = 상태 탭 + 배너 카드(모바일 이벤트 페이지 실측 3곳 모두 큰 배너 카드 · 2곳 상태 탭).
- **피드백 · 오류 제보 창 다시 그림(F2, 사용자 결정)** — 실서비스 16곳(12곳 실측) · 디자인 시스템 7곳 조사. 종류(오류 / 제안)를 먼저 고르고(실측 12곳 중 8곳), 가이드대로 정리한다.
  폰 전체 화면은 가이드에 있었는데 구현이 빠져 있었다(폭 334 창으로 뜸) → 공용 `FullScreenDialog` 로. 데스크톱 폭은 Wide 768 유지(480 새 폭 안은 물림).
  보내기는 늘 켜 두고 누르면 칸 아래 오류(D2 — GOV.UK · Atlassian · Primer. Carbon 만 짧은 폼 비활성 권장). 최소 10자 삭제(실측 0 / 12). 첨부는 파일 줄(A2) · 첨부 버튼은 L(M 이면 붙이는 순간 8 튐 · 입력칸 역할). 보낸 뒤는 창 안 완료 화면.
  자리표시자는 라벨 반복이라 없앴다(GOV.UK · Carbon · Atlassian · Primer). 글자 수는 「쓴 수 / 1,000」 하나. 오락실 제보는 같은 스키마를 이어받지만 원래 규칙(10~1000자 · 종류 없음)을 지킨다.
- **서열 변경 이력 = 날짜마다 카드(C1, 사용자 결정)** — 구역 제목 · 날짜 줄 · 항목이 같은 왼쪽 선에서 시작해 세 단계가 한 단계처럼 보였다(사용자 지적). 들여쓰기(H1 · H2)와 컨테이너(C1 · C2 · C3)를 시안으로 비교.
  들여쓰기 계단(H2)을 잠시 넣었다가 되돌리고, 날짜 묶음을 공용 펼침 `card` 로 감싸는 C1 을 골랐다 — 이력은 시간이 갈수록 날짜가 늘어나는 데이터라 상자가 더 분명하고, 「펼친 내용은 들여쓰지 않는다」 는 규칙도 건드리지 않는다.
  근거로 Material 3 목록 규정(담긴 목록은 간격 · 담지 않은 목록만 구분선, 반복 형식은 여백으로 충분)이 지금 조합(카드 사이 8 · 카드 안 항목 구분선)과 맞는다.
- **접히는 구역 사이에 구분선을 넣지 않는다(사용자 결정)** — 구역을 접게 만들면서 공용 펼침 규칙(펼침 + 펼침 = 위에 1px 구분선)이 딸려 와 화면이 계속 어긋났다. 가이드에 없던 장식이라 **공용 규칙 자체를 지웠다**.
  접히는 구역 사이도 다른 구역과 같은 「구역 사이 32」. 이 과정에서 잠시 R1(구분선 + 간격 0)을 넣었다가 되돌렸다.
- **구역을 접을 수 있게(사용자 결정)** — 내 기록의 「최고 기록」 · 「누적 요약」, 평가 탭의 「패턴 투표」 · 「서열 투표」 · 「의견」 을 모두 구역 펼침으로. 기본은 펼침(`open`).
  제목 줄이 펼침 줄이 되면 그 줄에 버튼 · 링크를 둘 수 없다(누르면 접힘). 그래서 패턴 투표의 저장 상태 · 「선택 해제」, 서열 투표 · 의견의 로그인 링크는 **펼친 내용 첫 줄 오른쪽 끝**으로 내렸다.
  공용 `Disclosure` 에 `titleId` · `titleRef` 를 더해 구역 이름표(aria-labelledby)와 저장 뒤 포커스 되돌리기를 그대로 유지했다.
- **확인 창의 삭제 확정 버튼은 모두 채운 위험 버튼(사용자 지적)** — 의견 · 투표 삭제만 빨간 테두리라 패턴 평가 · 계정 · 공지 삭제와 어긋났다. `destructiveFilled` 를 종류와 무관하게 준다.
- **점수 자는 면 · 제목 없이, 좌우는 순위표와 같은 선(사용자 지적)** — 카드 면과 제목 「악곡 랭킹」 을 지우고, 좌우 안쪽 12 로 자 시작선을 순위표 줄(「1」 · 아바타)과 맞췄다. 높이도 척도로 다시 잡았다(80 → 72 = 48 + 8 + 16).
  끝 점수의 핀이 자 밖으로 반쯤 나가 안쪽으로 16 당긴다(점은 제자리). 기준선 라벨 「S 950k」 → 「950k」. 순위표 머리글 「플레이어」 는 아바타가 아니라 **이름 첫 글자**에 맞춘다(곡 상세만 · 유저 랭킹 페이지는 그대로).
- **랭킹 탭에서 글자를 걷어냄(사용자 결정)** — 카드 제목은 「점수 분포」 → **「악곡 랭킹」**(Chart Ranking · 楽曲ランキング), 「참가자 N명」 · 구역 제목 「순위」 · 「내 순위 N / M」 은 모두 삭제.
  「난잡한 이유가 저 쓸데없는 글자」(사용자). 내 순위는 순위표 안 내 행이 이미 말하고, 참가자 수는 목록의 「전체 N명 중 …」 이 말한다. 구역 이름은 낭독용 sr-only 제목으로만 남기고, 로그아웃 링크는 목록 위 한 줄로 옮겼다. `ranking.participants` 문구는 지웠다.
- **곡 순위 · 핀 창 목록 점수 칸 = 만점 폭 고정(A, 사용자 결정)** — 만점 「1,000,000」 행만 점수가 아홉 글자라 등급 아이콘이 왼쪽으로 밀렸다(실측 순위표 13 · 모달 12, 사용자 지적). 점수 칸을 7ch + 쉼표 2 로 고정하니 편차 0.
  여섯 자리 점수는 등급과 한 글자만큼 벌어지는데(09-17 에 이 틈 때문에 고정 폭을 물렸다), 이번에는 아이콘이 어긋나는 쪽이 더 눈에 띈다는 판단. 등급을 점수 오른쪽으로 옮기는 안(B)은 공용 순위표와 순서가 달라져 물림.
- **점수 분포 = 분포를 그리지 않고 점수 자 위에 사람만(V1+V4, 사용자 결정)** — 유저 둘이 같은 곳에서 막혔다. 「출렁이는 높이가 뭐냐」 · 「핀이 꼭대기에 있기도 살짝 위에 있기도 한 건 무슨 뜻이냐」.
  원인 셋: 세로축에 라벨 · 눈금이 하나도 없다 · 참가자가 한 자릿수라 밀도 곡선의 봉우리가 사람 수만큼 생긴다(기록 있는 채보 대부분이 1명) · 핀 y 가 밀도라 실력으로 읽힌다.
  그래서 세로축을 버리고 divider 선 하나 위에 참가자를 점으로 찍고 사진 핀을 한 줄로 세운다(svg 190 → 80 · 카드 222 → 112). 09-17 D1(분포 곡선) 폐기.
  **사진끼리 겹치지 않는다** — 「나는 늘 따로」 규칙 탓에 내 핀만 거리 규칙 밖이라 옆 사람과 겹쳤다(사용자 지적). 나도 같은 거리 규칙을 따르되, 내가 낀 핀은 대표가 나가 되고 나머지는 「+N」 으로 접힌다(내 자리는 늘 보인다).
  핀 묶음 거리는 28 → **52**(핀 폭 + 사진 사이 여백) — 사진이 옹기종기 붙어 보여 더 뭉치라는 사용자 지적. 실측(390 폭, 실제 채보 11명): 40 이면 핀 6개 · 사진 사이 최소 22, 52 면 5개 · 34, 72 면 4개 · 73.
  레퍼런스로 실측한 lichess 레이팅 분포(67만 명)는 세로축 라벨 + 누적 곡선을 같이 두지만 표본 조건이 우리와 다르다. osu! · BeatLeader 는 404 · 앱 렌더 실패로 미확인.
- **점수 분포 곡선에서 내 위치 표시 제거(A)** — 세로선 · 「나 · 상위 N%」 글자 · 면 농도 차이를 모두 없애고, 내 자리는 곡선 위 핀 색(판정 JUST 노랑 `--nl-judgement-just`)으로만 가리킨다(사용자 결정).
  선 · 글자가 핀과 같은 자리를 두 번 말해 의미가 없어졌고, 새 색을 만들지 않으려고 기존 판정 색에서 골랐다. `userTopPercent` 는 상세 API · 서버 계산에서도 뺐다.
- **글자 13종 예외 없음(C)** (2026-09-26 읽기 본문 한 줄을 표에 더해 14종 — 표 밖 금지는 그대로) — 「13종 밖 금지 · 고정폭은 숫자 클래스 둘만」 을 그대로 지킨다(사용자: 예외를 만들지 않기로 했다). 수치 띠 값 · 그레이드 숫자의 고정폭 해제(같은 자리수도 폭이 달라져 자리 맞춤은 포기) · 그레이드 소수 700 → 600(16/24 · 600) · 점수 곡선 「나」 라벨 600 → 400 · 목록 소제목 600 → 400 · 동작 칸 「control 600」 은 이름을 `emphasis-label` 로 정리.
  가이드 글자 절에 「예외를 만들지 않는다」 한 줄을 넣고, 다른 절이 조합을 따로 정하던 문장을 모두 고쳤다. 남은 고정폭(공용 순위표 머리 · 서열표 · 빙고 · 필터 · 차트 축)은 그 페이지 작업에서 함께 정리한다.
- **왼쪽 열 잉크 12 정렬 · 카테고리 배지 = 태그 크기 + 판 색(사용자)** — 1056+ 난이도 항목 잉크가 9(트랙 1+3 · 항목 4)라 그레이드 · 수치 칸 · 동작 칸의 12 와 어긋났다 → 항목 안쪽 `8 − 1`(A · 트랙 모양은 유지). 행 높이는 40 유지(아래 수치 칸과 같은 리듬).
  카테고리 배지는 태그 규격(24 · 좌우 8 · 모서리 4)에 자켓 판의 면 · 카테고리 글자색, 경계 없음(C) — 규격은 태그, 색은 목록 카드와 이어짐.
- **악곡 상세 검수 후속 결정(사용자)** — 핀 카드 · 목록 점수 무색(순위표와 같음, 메달 칸도 16 으로 간격 8 통일) · 「내 순위」 요약 상자 삭제(제목 줄 하나) · 평가 탭 2:1 · 열 사이 16 · 「패턴 경향 기준」 창 Compact 유지 · 최근 판정 추이 전환은 세그먼트 유지 ·
  판정 분석 · 최근 판정 추이 · 최근 플레이에서 NEAR 제외(거의 켜지 않는 옵션 · MISS/NEAR 지표는 MISS 로) · 제목 줄 동작 = 고스트 M · 평균 없는 서열 투표 줄 = 「—」 + 인원(가운뎃점 대신 간격) · 펼침 구역 제목 → 내용 16 유지.
- **악곡 상세 가이드 검수 반영** — 전용 CSS · 실측(320 · 390 · 768 · 1280 × 네 탭) · 컴포넌트 24개 대조 결과를 가이드대로 고침(사용자 「추천대로」).
  판정 분석 소구역 사이 = 24 로 통일(24 · 16 섞임 → 가장 많던 24, 사용자 결정) · 점수 핀 「+N」 = 필터 개수 배지 모양(원 20 · raised · metadata 12 + 1px 경계, 11px 알약 물림).
  서열 투표 분포의 세로 막대는 「세로 막대 분포 금지」(점수 분포 얘기) 대상이 아니라 그대로 둔다. 공용 순위표 규격(메달 18 · FC 표시 · 페이지 번호 44)은 이번에 손대지 않음.
- **그레이드 진행 막대(Q3) — 0 → 최대 Grd, 채움 = 공식 등급 아이콘 색 순서 그라데이션** — 사용자 제안(B 파랑 → A 빨강 → S → 990k → Pianist, 사이는 그라데이션). 경계는 점수 → Grd 산술식을 찾지 못해(판정 비율 1 · 0.7 · 0.5 가정 9,035건 중 1,373건만 일치) 운영 기록 14,848건의 「내 Grd ÷ 최대」 등급별 하위 5%(B+ 40.3 · A 49.5 · A+ 59.6 · S 73.7 · 990k 81.9), Pianist 시작 87.9% = 8.7 ÷ 9.9(P 가 아니면 넘을 수 없음 · 990k 최대 87.8%).
  빨강은 A~A+ 에만(사용자: 빨강 구간이 길다). 섞기는 oklch — sRGB 는 파랑 → 빨강 사이가 탁한 회보라(Q1 물림). 파랑 = 판정 NEAR · 빨강 = `danger-marker` 를 빌림(사용자 결정). 라벨 「그레이드」 = control(막대 목록 라벨 규칙), 「Grd」 단위는 라벨과 겹쳐 뺌. B · B+ 경계는 기록 8건뿐이라 쌓이면 다시 잰다. 내 Grd 숫자 = 막대의 그 위치 색 · 최대 Grd 숫자 = Pianist 색(사용자 결정).
- **악곡 머리 「그레이드」 줄 = 「그레이드」 제목 + 내 Grd / 최대 Grd + 진행 막대, 점수 · 메달 삭제 · 제목 옆 등급 아이콘 원복(펼치면 숨김)** — 사용자 결정(09-17 B3 「내 최고 기록」 줄 · 제목 옆 아이콘 삭제를 뒤집음 — 수치 상자 맨 위 자리(레이아웃을 흔들지 않음) · Grd 정수만 크게(TETR.IO) · 로그아웃이면 로그인 링크(GOV.UK)는 유지. nosdata 자료는 쓰지 않음). 최대 Grd = 전부 ◆JUST · 풀콤보 · P(랭크 계수 9.9)의 Basic Grd, 운영 기록 풀콤보 221건 · Pianist 6건과 일치.
  최대값은 같은 줄 「/ 111.47」 — 점수를 뺀 뒤라 272 폭에도 한 줄. 공식 레벨 · 노트 수가 없으면 「/ —」(공식 레벨 빈 칸 「—」 과 같은 규칙).
- **새 Real 채보 상수 = 비워 둠** — 옛 기본값 `◆ + 10` 은 운영 기록 역산에서 Real 258곡 중 89곡이 틀림(12.5 · 13.5 · 9 · 10). 추측값 대신 비워 두고 관리자 「상수 미설정」 에 띄운다(사용자 결정). 악곡 머리 「공식 레벨」 칸은 없애지 않고 흐린 「—」(「그레이드」 줄 기록 없음 「—」 과 같은 모양, 사용자 결정) — 서열 값 빈 칸은 그대로 숨김. ◆ 구간 최솟값은 ◆1 대부분(63/82)이 11이라 물림.

## 2026-09-17

- **점수 분포 곡선 위 사진 핀(E)** — 사용자 제안(SoundCloud 파형 위 사진처럼). 핀 끝이 점수 자리를 가리키고, 가까운 사람은 대표 1명 + 「+N」(사용자 제안), 30명 이하 모두 · 넘으면 상위 3 + 나(추천 채택).
  누른 뒤 창은 조사 16곳(사람 카드 11 · 묶음 5)에서 — 사진 왼쪽 가로 카드가 가장 많음(6 · ScoreSaber 는 버튼 없이 카드 = 링크), 「창 → 다시 카드」 두 층은 0곳이라 한 사람 = 가로 카드 · 묶음 = 목록 한 장으로.
  폰은 탭하면 열고 한 번 더 눌러야 스크롤(곡선 보다 화면이 튀지 않게), 데스크톱은 올리면 열리고 누르면 바로 스크롤. 말풍선 · 막대 줄기 · 묶음 없이 쌓기는 상위권 핀이 상자 밖으로 넘쳐 물림.
- **악곡 머리 「채보 보기 · 플레이 영상」 = 수치 띠 상자 아래 칸 · 반반 · 글자 + ›(I1C)** — 1차 조사 20곳(머리 버튼 자리) · 2차 17곳(상자에 붙는 버튼) · 가이드 7곳(Material 3 · Apple · Carbon · Fluent · Atlassian · GOV.UK · NN/g).
  상자 안 아래 배치 6곳. 반반 글자만 있는 칸은 누를 수 있는 사례 0(멜론은 누를 수 없는 줄)이라 › 를 더함 — Chess.com(카드 아래 줄 + ›) · › = 이동(Apple · Carbon). 버튼 안은 글자만인 곳이 가장 흔해 아이콘 · ↗ 는 뺌.
  상자 밖 테두리 묶음(B3)은 모양이 떠 보여 물림(사용자). 채보 · 영상이 없어도 칸은 두고 비활성 글자색. 영상 새 탭 여부는 정하지 않음(같은 탭).
- **숫자 척도 — 값을 고른 줄의 나머지 숫자를 한 단계 흐리게(S1)** — 고른 값이 채움만으로는 덜 튐(사용자 지적). 비활성 색(S2)은 누를 수 없어 보이고, 테두리 없앰(S3)은 줄마다 모양이 흔들려 물림. 빈 줄은 그대로. 사용자 결정 — 실측한 설문 도구 3곳(Tally · Typeform · SurveyMonkey NPS)은 고른 뒤 나머지를 흐리지 않음(사례 없음).
- **서열 값 색 = 연속 그라데이션(G1)** — 머리 수치 띠의 공식 레벨 · 서열 값. 1.0 밝은 Normal → 3.5 Normal → 6.0 JUST 노랑 → 8.0 Hard → 11.25 Expert → 13.05 Real → 14.5 분홍(#ff5fd2, 새 색 사용자 승인)을 oklch 로 이음(osu! 난이도 색 참고).
  서열표 구간 머리 숫자에도 같은 색(사용자 결정). 6 근처 노랑이 없으면 초록 → 주황 사이가 탁해짐(G2 물림). 라이트 테마 양끝은 흰 섞음 대신 기본 글자색 섞음 · ◆JUST 분홍.
- **점수 분포 = 분포 곡선 + 내 위치 선(D1)** — 「나는 어디쯤」 그래프 계열(Chess.com · Monkeytype · Strava). 인원이 늘어도 선 하나라 복잡해지지 않고, 내 오른쪽 면이 「상위 N%」 를 면적으로 보여 줌.
  점 쌓기(D2)는 몰린 구간이 폰 높이를 넘고, 순위 곡선 면(D3)은 몰림이 기울기로만 읽혀 물림. 인원이 적어도 점 대신 완만한 곡선 하나(사용자 결정 — 곡선 폭 = Silverman 기준의 0.4배 · 축 4% 하한. 1.06배는 너무 밋밋, 0.25배는 사람마다 봉우리가 생겨 물림).
- **판정 분석 = 기록 탭 펼침 맨 위 · 기본 펼침**, 「내부 레벨」 → 「공식 레벨」 — 사용자 결정.
- **그래프 툴팁 = 값 위 · 「N일 전」 아래(osu! 방식)** — 성장 추이 · 최근 판정 추이 모두. 날짜 줄은 뺀다(사용자 결정, osu! 프로필 순위 그래프 사례).
- **수치 띠 값 색** — 내 순위 = 순위표 1 · 2 · 3 색과 그 밖 색, 최고 점수 = 서열표 S · 990k · Pianist 목표 색(FC 무시), 등급 = S · P 색(라벨 「스코어 등급」 → 「등급」). 「베스트 스코어 · 베스트 기록」 → 「최고 점수 · 최고 기록」(악곡 상세, 한국어), 최대 콤보 FC = FC 초록. 사용자 결정.
- **곡 순위 결과 칸 = 등급 · 점수 · FC 사이 8로 붙임, 머리 「점수」 = 점수 숫자(7자리) 가운데** — 등급이 점수 바로 왼쪽에 붙고 간격이 일정한 모양(사용자 제시 사례). 점수 칸 고정 폭은 자릿수가 짧을 때 등급과 틈이 생겨 물림.
- **제목 옆 등급 아이콘 = 제목 줄 높이(32 · 40)** — 24는 글자 크기와 같아 작아 보임(사용자 지적). 번역 줄 끝 선도 같은 폭으로.
- **패턴 투표 잠금 = 흐림 5px + 가운데 카드(L2)** — 잠금 사례 16곳: 작은 입력 칸은 대개 흐림 없이 한 줄 안내(Letterboxd · Steam · GitHub · osu!), 흐림 + 가운데 카드는 IMDb 리뷰 · Patreon.
  사용자 결정으로 L2. 투명도는 낮추지 않음(흐림 + 투명 60% 조합 L0은 사례 없어 물림). 카드는 떠 있는 창 규격 그대로. 흐림은 **입력 영역 잠금에만** 쓴다 — 공개 결과에는 쓰지 않는다.
- **패턴 평가 삭제 확인 = 채운 위험 버튼** — 되돌릴 수 없는 기록 삭제라 테두리 위험 버튼 대신 빨간 면(사용자 결정). 서열 투표 · 의견 삭제 확인은 그대로.
- **패턴 경향 막대 = 평균의 가까운 정수 단계 색(E2)** — ColorBrewer RdYlBu 5단계 순서(파랑 → 하늘 → 노랑 → 주황 → 빨강)를 새 색 없이 기존 토큰으로: `local-data/bucket-3` · `judgement/near` · `judgement/just` · `difficulty/hard` · `difficulty/expert`. 판정 · 난이도 색을 다른 뜻으로 빌리는 예외(사용자 결정, E1은 노랑 · 주황이 가까워 물림).
- **최근 판정 추이 선 색(B)** — ◆JUST = 판정 ◆JUST 분홍, MISS/NEAR = 빨강(판정 색에 빨강이 없어 `feedback/danger-marker` 를 빌림 — 데이터에 상태 색을 쓰는 예외, 그레이드 진행 막대 A 구간도 같음), FAST = 판정 NEAR 파랑, SLOW = 주황 그대로. 사용자 결정.
- **패턴 경향 평균은 평가 1명부터** — 사용자 결정(이전 3명). 서열 투표 분포 · 평균의 3명 기준은 그대로.
- **패턴 투표는 바로 저장(A)** — 저장 방식 조사 25곳+: 평점 하나는 대부분 바로 저장(Goodreads · 왓챠 · Netflix 등 약 9), 본문 · 필수 칸이 붙은 리뷰 한 건은 맨 아래 제출(RateMyProfessors · Airbnb 등 약 7),
  축마다 따로 집계되는 투표는 항목마다 바로 저장(MyAnimeList 작품 페이지 · AniList · Fragrantica 추정 — 3, 근거 약함). 상태는 제목 줄 오른쪽 한 곳, 전부 지우기만 확인창.
- **패턴 투표 = 축마다 숫자 버튼 0~4(A1) · 슬라이더 폐기** — 「여러 항목 × 작은 숫자 척도」 실측 20곳: 숫자 버튼 한 줄(Tally · Typeform · Jotform) · 표(Tally · Jotform · Fillout · Qualtrics),
  슬라이더는 Fragrantica 1곳. Carbon · Atlassian · Fluent · NN/g — 아주 작은 범위 · 정확한 값에는 슬라이더를 권하지 않음. 별은 1부터라 0(없음)과 맞지 않음.
  버튼 = 컨트롤 M · 모서리 8 · 사이 8(실측 6~8), 고른 값은 채움(Jotform 형, 링은 덜 보여 물림). 카드 면 없음 · 양끝 말 라벨 삭제(사용자 결정) · 해제는 같은 값 다시 누르기 + 제목 줄 「선택 해제」.
- **악곡 머리 제목 · 번역 · 아티스트** — 번역 버튼 + 팝오버를 제목 아래 한 줄로(실서비스 10곳 중 버튼 뒤에 숨기는 곳 0 · MyAnimeList · Letterboxd · VocaDB 형), 계층 T3(번역 = 본문 보조 기본 글자색 · 제목에 붙임, 아티스트 흐리게 8 띄움).
  긴 제목은 줄바꿈해 다 보여 주는 곳 12 · 자르는 곳 Apple Music · YouTube였지만 사용자 결정으로 **제목 · 번역 · 아티스트 모두 한 줄, 넘치면 끝 페이드 32**(YouTube 형 · 24 · 48 · 64 · 10% 비교), 누르면 모두 펼침.
  두 줄 자르기(단어 단위 줄바꿈)는 두 줄 제목 옆 아이콘이 줄 사이에 떠 보여 물림. 제목 글자 줄이기(24 → 20 → 16) 삭제 — 계층 규칙 밖.
  등급 아이콘은 제목 글자 끝 8 뒤 · 제목 줄 세로 가운데(크기는 제목 줄 높이 32 · 40), 번역은 등급 자리 앞에서 끝, 아티스트는 전체 폭. 자켓은 위 고정(옆 글자 사례 5곳). 카테고리 배지는 자켓 위 겹침에서 제목 바로 위 왼쪽으로(사용자 결정 · 목록 카드는 그대로).

## 2026-09-16

- **악곡 머리에 내 등급 = 제목 바로 옆(C)** — 이름 곁 배지 4곳(GitHub Public · npm DT · AtCoder · Chess.com GM), 제목 행 오른쪽 끝은 행동 버튼 3곳(GitHub · Speedrun.com · Blind)이라 A(오른쪽 끝 40) 물림. 크기는 09-17 결정(제목 줄 높이).
- **서열 변경 이력의 날짜 묶음 = 보조 펼침(기본 접힘)** — 사용자 결정. 이력이 쌓여도 구역을 펼쳤을 때 높이가 날짜 줄 수만큼만 늘게.
- **패턴 경향도 집계가 없으면 빈 막대 · 「—」** — 「집계 데이터가 부족합니다」 · 줄마다 「집계 중」 삭제. 빈 추이 틀과 같은 원칙(데이터가 없어도 그래프 자리는 그린다).
- **판정 분석 = 요약(나 · 평균 막대, 값 없으면 비활성 회색 한 줄) → 판정 목록 · 음표별 성공률** · **성장 추이 · 최근 판정 추이의 날짜 · 값 표는 화면 읽기용으로만** · **성장 추이 · 최근 플레이도 기본으로 접힌 구역 펼침(판정 분석과 한 묶음)** — 사용자 결정(그래프 · 툴팁과 중복, 목록이 길어지면 불편).
- **판정 분포 = 누적 막대 두 줄(나 · 평균) + 목록(A)** — GitHub 언어 막대 형. 항목 막대(Goodreads · Google Play)는 ◆JUST 외 조각이 1~~5%라 안 보이고,
  도넛 · 빗금 차이(D1 · D2 · A′)는 차이 0.1~~1.9%가 1px 미만 · 새 시각 요소라 물림(사용자: 「빗금은 별로」). 음표별 성공률은 95~100%에 몰려 글자 그대로. 조각 사이 틈은 두지 않음.
- **판정 분석 평균 비교는 켜기 없이 늘(A) · 빈 최근 판정 추이는 빈 틀 + 「기록 없음」(E2)** — 실서비스 14곳: 기준값을 늘 붙이는 곳 5(OP.GG · Monkeytype · LeetCode ·
  Chess.com · Plausible), 도구 줄 · 범례 3, 제목 줄 전환 1(PageSpeed), 내용 위 체크박스 1(AtCoder · 거르기용). Material 3 · Carbon · NN/g — 즉시 적용 켜기에 체크박스를 쓰지 않음.
  빈 차트는 Plausible(틀 그대로) · osu!(자리에 한 줄). 「0플레이 기준」 · 「…추이를 확인할 수 있는 기록이 없습니다」 삭제, 빈 틀은 위 · 아래 격자선만.
- **유사 Grd 비교 최소 인원 5 → 1** — 사용자 결정. 나를 뺀 기록이 한 명이라도 있으면 비교, 없을 때는 문장 · 평균 줄 모두 없음.
- **선 그래프 점 하나 = 틀 그대로 · 점 하나(A)** — 대회 1회 실제 사용자: AtCoder · Codeforces 모두 틀 · 축 · 날짜를 두고 점만, 설명 문장 없음.
  「기록이 1건이라 추이를 그리지 않습니다」 와 점수 한 줄(표 · 베스트 기록과 중복) 삭제. 한 건일 때 구역을 숨기는 사례는 확인 못 함(B 물림).
- **서열 투표 로그인 유도 = 제목 줄 오른쪽 끝 제목 링크(D)** — 로그아웃 실측: 블러 + 가운데 버튼은 잡플래닛 · 왓챠피디아 코멘트 · LinkedIn처럼 콘텐츠를 숨길 때만,
  투표 · 평가 칸은 결과를 그대로 두고 버튼 · 한 줄로 유도(왓챠 별점 · Goodreads · Stack Overflow · Blind · Letterboxd · Backloggd · osu!).
  공개 집계를 가리지 않고, 개요 「평가하기 ›」 규칙을 공용 `nl-heading-row` · `nl-heading-link` 로 옮겨 함께 쓴다. 목록 아래 떠 있던 링크는 삭제.
- **서열 투표 설명 문장 삭제** — 투표 · 평점 입력 곁에 자격 설명 문단을 두는 곳 0곳(Metacritic · Goodreads · IGDB · osu! · Letterboxd · Steam — 라벨 하나 또는 없음).
  「아직 공개할 투표 분포가 없습니다」 · 「첫 투표를 남길 수 있습니다」 · 「달성 기록이 있어야」 · 「플레이 기록이 필요합니다」 · 「1.0 ~ 14.5 · 0.1 단위」 삭제,
  자격이 없으면 입력을 숨김(A · Steam 형 — 잠근 입력 B 는 물림). 우리 화면은 설명 문장이 많다는 사용자 지적 — 새 화면도 문장보다 상태로 말한다.
- **의견 구역 — 작성 칸을 의견 쪽으로 · 상자 없는 목록 · 추천 삭제** — 실서비스 12곳(확인 9) · Atlassian Comment · Carbon Forms.
  작성 칸은 목록 구역 위(osu! · YouTube · Goodreads · Amazon — 평점 곁은 Metacritic · IGDB 2곳), 모양은 한 줄 → 펼침(YouTube 형 · 120자 짧은 글이라
  「버튼 → 따로 화면」 리뷰형은 과함). 의견 줄은 상자 없는 목록(5/6 — 카드는 V-ARCHIVE 1). 저장을 「평가 저장」(슬라이더만) · 「의견 저장」(의견만)으로
  나눔 — 같은 행이라 데이터 의미는 그대로, 「평가 삭제」 는 의견을 남긴다. 추천은 자리만 차지해 사용자가 삭제 — 「추천순」 도 뜻이 없어져 최신순 하나(추천 데이터 · API 는 남김).
- **악곡 상세 재설계** — 실서비스 28곳(리듬게임 DB 8 · 기록 · 게임 · 영화 · 앱 스토어) · 가이드 9 체계 + NN/g · Baymard · SWD 조사, 사용자 선택.
  머리를 ≈490 → ≈300 으로(자켓 144 → 80 · 태그 줄 없음 · 타일 100 → 난이도 세그먼트 44). 난이도는 **이름 + 색 레벨 숫자 세그먼트**
  (리듬게임 DB 6/6 이 항목 안 레벨 숫자 · 드롭다운 0). 서열 값 · 내부 레벨은 **머리 수치 띠 하나**(핵심 수치는 머리 아래 25/28) — 탭 안
  「서열 배치」 칸은 중복이라 삭제. 탭은 4개 유지 「개요 · 내 기록 · 랭킹 · 평가」, **패턴 경향은 유저가 가장 먼저 보는 정보라 개요 맨 위**
  (평가 탭으로 밀었던 안은 사용자가 물림), 「평가하기 ›」 는 별도 상자 대신 제목 줄 오른쪽. 채보 정보 · 이력은 개요 아래(채보 정보가 위).
  레이더 → 가로 막대(레이더 0/28 · NN/g 「면적 · 각도라 정량 비교에 약함」). 점수 분포는 세로 막대가 비직관적이라 물림(모양은 09-17 D1 곡선).
  내 기록 큰 숫자(32)는 **새 글자 단계를 만들지 않고** 수치 띠로. 순위표는 랭킹 페이지 공용이라 범위 밖. 데스크톱은 **왼쪽 머리 열 + 오른쪽
  탭**(AniList · Kitsu · Goodreads 형 — 전체 폭 머리 안은 사용자가 물림). 값 없는 항목(BPM · 노트 · 길이 · 영상 · 미리보기 — 운영 DB 2,167 채보 중 0)은
  자리를 만들지 않는다. 영어 탭 「Ratings」 · 띠 라벨 「Constant」 는 390 한 줄에 맞추려고 줄였다.
- **부품 안쪽 규격 — 입력 · 전체 화면 필터 · 메뉴 · 모달** — 실서비스 입력 38 · 폰 필터 22 · 메뉴 25 · 모달 12(+데모 8) · 가이드 13/6/13/14 조사.
  **입력:** 첫 · 마지막 잉크 12 · 사이 8 (아이콘 inset ≈12 최빈 53 · 78% · Primer · Fluent 12 · M3 「with icons 12」). 검색창이 8/16 이라 12 로,
  지우기는 면 M 정사각 · × 중심 22 = 셀렉트 꺾쇠. 글자 16/400 · 흐린 플레이스홀더는 관례(81~88%)와 같아 유지.
  **전체 화면 필터:** 「초기화」 를 머리 오른쪽(22곳 중 1)에서 발 왼쪽 고스트 L(9곳 · Baymard 「Clear all 은 왼쪽」)로. 머리 60 · 여백 16 · 제목 16/600 · 선 24 는 실측 안이라 유지.
  **메뉴:** 소제목은 그룹 둘 이상일 때만(목록형 메뉴 소제목 1/17 · Atlassian 「grouped items … title」). 판 8 · 행 44/40 · 14/400 은 실측 최빈 또는 M2 결정 유지.
  **모달:** 닫기 아이콘 20 → 24(가이드 「창 머리 가장자리 24」 · M3 · Carbon) `IconButton` 으로 전체 화면 창과 통일, 피드백 · 제보 창의 덮어쓰기 7개 제거.
  액션 「672 미만은 폭을 채움」 을 공용 규칙으로 올려 예외를 없앰. 안쪽 24 · 제목 16 · 스크림 .6 은 데모 · 가이드와 같아 유지.
- **악곡 목록 개편 — 폰 두 줄 툴바 · 난이도 판** — 목록 화면 58곳 · 리듬게임 12곳 · 가이드 9체계 조사.
  폰: 「정렬 ▾ | 필터 ▾」 반반 줄(30곳 중 Nintendo 1)을 없애고 검색줄 옆 ☰ 아이콘(Amazon · YouTube · Vercel · 오락실) +
  결과 줄 「정렬 고스트 · 보기 전환 M」(정렬 왼쪽 Dribbble · itch · GitHub 검색), 개수는 제목 아래 메타 글줄(itch · Nintendo · Notion 처럼 제목 곁).
  검색 →12→ 결과 줄 →12→ 목록(컨트롤 묶음 안 12). 데스크톱 구조는 유지(레일 9/28 · 정렬 결과 열 위 6).
  카드: 상자 유지 — 음악 앱은 맨 행이지만 레벨 4개를 든 리듬게임 카드는 8/8 상자. 레벨은 맨 색 숫자(사례 0) → 색 판 4칸
  (maimai · CHUNITHM · 온게키 · zetaraku), 판 24/20 · 숫자 metric-value · 색은 「난이도 = 색 글자」 그대로 · 없는 난이도는 자리만 비움(테두리 판은 다크에서 구멍처럼 보여 뺌).
  격자 이름 14(격자 14곳 중 8) · 촘촘 안쪽 8(111 칸에 판 4개 92 가 들어가야 함) · 격자 비율 1:1.5(Bandcamp · KREAM). 판을 자켓 위 띠로
  두는 안은 사례 0 · M3 「이미지 위 글자 피함」 이라 물림. 필터 아이콘 트리거는 `nl-filter-icon-trigger` 로 오락실과 공용.
- **빙고 목록 폰 툴바 = 악곡과 같은 배치** — 「정렬 ▾ | 필터 ▾」 반반 줄이 남아 있던 마지막 화면. 검색줄 옆 ☰ 44 + 정렬 고스트 줄로 통일, 빙고 상세 초기화 창의 안쪽 16 덮어쓰기도 제거.
- **기체 카드 두 열** — 이름/위치 메모 왼쪽, 상태/확인 시각 오른쪽. 확인 시각은 기체마다 따로 확인되는 근거라 카드 안 · 상태 밑
  (카카오맵 충전기 = 상태 옆 시각, GitHub · Slack 상태 페이지 = 이름 아래 보조 글). 셋째 줄에 떠 있던 「N일 전 확인」 을 없앴다.
- **영업시간 펼침 내용 — 위 8 · 아래 8 · 값 열 맞춤** — 가이드에 열린 내용 규칙이 없어 0 · 12 · 32 가 굳어 있었다.
  들여쓰기는 규격 0 대신 값 열(Google 지도 · 캐치테이블도 라벨/값 열에 맞춤).
- **hover 는 포인터에서만** — 폰에서 탭한 줄 · 버튼 · 카드가 켜진 채 남던 것(사이트 전체 44 규칙). `@media (hover: hover)` 로 감쌈,
  `:not(:hover)` 색 규칙 2개는 밖에 둠. 관리자 CSS 는 손대지 않음.

## 2026-09-15

- **페이지 제목 아래 간격 — 설명 · 메타 글줄 8, 첫 내용 16** — 37 페이지 실측: 제목 → 설명 · 메타 글줄은 8 이 최빈(24곳 중 8 · GitHub Docs · Notion ·
  Figma · Spotify · YouTube · Radix · Atlassian · USWDS). 제목 → 내용 블록은 6곳이 12–57 로 흩어져 사례로 못 정해 사다리(상자 8 · 구역 12 · 페이지 16)로.
  전에는 페이지마다 8 · 12 · 16 · 24 · 32 가 섞여 있었다(악곡 12 · 서열표 · 랭킹 · 공지 24 · 동기화 32 → 16).
- **컨트롤 높이 두 단계 — L 44/40 · M 36/32** — 디자인 시스템 17곳 · 서비스 33곳 DOM 실측(폰 390 · 데스크톱 1280):
  시스템 15/17 이 2–3 단계(작은 = 기본 − 8 · 큰 = 기본 + 8), 데스크톱 기본 32 가 6곳 · 36–38 이 5곳 · 40 이 3곳,
  Spectrum 은 터치 40 / 포인터 32 두 배율, 폰 카드 안 · 보조 버튼은 32–36 이 흔함(Google Play · Figma · 여기어때 · NOL · Steam 32, Notion 36),
  폰 주 CTA · 입력은 44–48. 우리 L 은 그대로, M 을 −8 로 더해 「32 예외」(기체 버튼 · 전체 지우기)를 없앴다.
  값이 시스템 기본보다 한 단계 큰 건 폰 CTA 44–48 · HIG 44 때문(사용자 확인). 바뀐 곳: 기체 버튼 폰 32 → 36 ·
  전체 지우기 32 → 36/32 · 필터 칩 36 → 36/32 · 초기화 · 영역 초기화 · 다시 시도 44/40 → 36/32. 조사 원자료 `docs/research/research-control-heights.md`.
- **오락실 상세 세 가지** — ① 이름 아래 「영업 중 · 24:00까지 · 지역」 줄 삭제: 60px 아래 정보 행과 같은 글이 겹쳤다
  (카카오 · Bing 도 상태를 이름 옆에 두지만 정보 행이 화면 밖). 거리도 함께 빠짐(목록 카드에 있음).
  ② 사진 넘김 ‹ › 는 672 미만에서 없앰 — 폰 사례 3/3(유니클로 · 무신사 · 여기어때)이 밀어 넘김 + 카운터만.
  672 이상은 오른쪽 위 묶음 → 좌우 가장자리 세로 가운데(데스크톱 표본은 안 잼, 관례). 키보드 ← → 추가.
  ③ 기체는 구분선 줄 → 한 대 = 카드. 줄마다 버튼 2개가 딸려 덩어리로 읽혀야 한다(카카오맵 충전기 카드와 같은 구조 · 표본 1).
  「가동 확인」 은 보조 → 흰 주 버튼(카드의 주 액션 · 목록 카드 「자세히 보기」 와 같은 서열), 안의 건수는 버튼 글자 색.
  ④ 버튼 아이콘 — 「가동 확인」 · 「고장 신고」 · 「길찾기」 는 글자만. 폰 상세 22곳: 주 버튼 글자만 15 · 아이콘은 글자 없는
  보조 버튼 15 · 선 아이콘 15/21. 「고장 신고」 빨간 테두리는 유지(22곳 중 신고를 위험색으로 한 곳은 0 이었지만 사용자가 빨강을 택함).
- **오락실 672 미만 툴바** — 「정렬 ▾ | 필터 ▾」 반반 줄을 없애고 필터는 검색줄 옆 아이콘 버튼, 정렬은 시트 요약 줄 오른쪽,
  지도 · 시트는 좌우 끝까지. 실측 9곳(Google · 네이버 · 카카오 · Bing 지도, 에어비앤비 · 아고다 · 여기어때 · 호갱노노 · 스타벅스)
  중 반반 버튼 줄 0곳, 첫 화면 지도 4/4 전체 폭, 정렬은 있는 3곳 모두 목록 머리 줄(표본 적음). 다른 목록 페이지는 그대로 —
  지도를 더 보여야 하는 오락실만의 예외(사용자 결정).
- **오락실 기체 색 점** — 가동 중이면 상태를 따른다: 양호 초록 · 보통 노랑 · 주의 빨강(이용 불가와 같은 색, 글자로 구분).
  전에는 가동 여부 기준이라 「보통」 이 양호와 같은 초록이었다. 주황 토큰은 없어 보통은 `feedback/warning`.
- **도움말(부품 ④)** — 용어 = 점선 밑줄 + ? 16, 기준 설명 = 라벨 뒤 ⓘ 16, 둘 다 마우스 올림 · 포커스 · 탭
  (실제 서비스 Stripe · Slack 이 올림으로 연다). 오락실 기체 상태 이유는 줄 끝을 맞추려고 ? 없이 점선만(9/13 이유 유지).
  「패턴 경향 기준」 은 다섯 항목 정의로 길어 ⓘ → 대화상자(누를 때만) — 작은 창은 390 에서 레이더를 거의 다 가린다.
  프로필 판정 기준은 펼쳐도 거의 같은 문장이라 도움말을 없애고 「N개 채보의 베스트 기록 기준」 을 늘 보이게.
- **태그 하나(부품 ⑤)** — 공지 분류를 공용 `nl-tag` 로 합쳤다(같은 값을 따로 선언하던 것). 동기화 1단계 「처음 한 번」 알약 태그는
  AI 가 만든 느낌이라 삭제(사용자 결정). 필터 개수 배지는 개수 전용 원 20 · 옅은 면 유지(GitHub 도 라벨과 개수를 다른 모양으로 나눈다).
- **메뉴 행 높이(부품 ⑥)** — 작은 메뉴 · 목록 행 = 컨트롤 높이(폰 44 · 데스크톱 40), 헤더 메뉴 이동 줄 = 폰 48 · 데스크톱 40.
  12곳 줄 간격 실측: 작은 메뉴 데스크톱 32–40(GitHub · YouTube · Dribbble), 폰은 YouTube 48 / GitHub 32 로 갈림 —
  폰 44 는 우리 높이 규칙과 맞춘 것. 폰 이동 메뉴는 9곳 중 7곳이 46 이상(중앙 52), 데스크톱 한 줄 이동 메뉴는 32 · 41(2곳).
- **ja · en 로케일 패스** — 공개 17종 × 320 · 390 · 1280 검사에서 넘침 셋: 홈 공식 소식 en 날짜(번역 버튼 「Japanese original」 152 가
  칸을 좁힘 → 라벨 「Original」 로, 날짜 형식은 그대로 · 사용자 결정) · 오락실 기체 줄 en 버튼(「Confirm」 · 「Report fault」) ·
  빙고 로그인 버튼 ja(「ログインして進行を保存」). 9/10 이후 추가한 ja · en 문구 79개는 제안대로 확정.
- **격자 4열 유지 · 선으로 나눈 묶음 사이** — 셸 1200 에서도 5열 단계는 열지 않는다(1055 5열 → 1056 4열 → 1276 5열로 두 번 뒤집힘,
  촘촘한 격자가 5열을 준다). 선으로 나눈 묶음은 선 위아래를 같게: 필터 레일 24/24 → 16/16(팝오버와 같게), 개인정보 요약 12/8 → 12/12.
- **레이아웃 — 본문 1200 · 여백 16/24/32 · 672/1056 유지 · 레일 1/4 유지** — 사이트 45곳 × 4폭 실측: 앱 화면 컨테이너
  16곳 중 1200–1280 이 9곳(Steam · V-ARCHIVE · GitHub · Linear · Airbnb …), 1000 근처 3곳. 1280 근처 컨테이너 미달 여백은
  32–44 가 6곳 · 24 가 3곳. 가이드 9곳 브레이크포인트는 576–768 / 992–1200 에 흩어져 672 · 1056 을 바꿀 근거 없음.
  레일은 레퍼런스 240–300 고정인데 1/4 이 셸 1200(안쪽 1136)에서 272 라 유지. 동기화 구역 펼침은 붙여 쌓지 않고 구역 사이 32 로 마감.
- **간격 역할값** — 구역 사이 32 · 구역 제목→내용 12 · 상자 제목→내용 8 · 카드 16(항목 12 · 데스크톱 큰 카드 24) · 목록 줄 사이 8.
  우리 눈금은 디자인 시스템 7곳 공통(12 · 16 · 24 · 32 · 48)과 같고 Radix 와 동일. 사이트 30곳 실측: 앱 화면 제목→내용 8–16 ·
  구역 사이 24 / 40–48 두 덩어리(32 는 가운데) · 카드 24(큰 카드) / 16(폰) / 12(항목) · 떨어진 줄 8. 검정 · 프로필 · 공지 · 개인정보의
  24 · 40 · 48 · 56 을 32 로, 16 · 24 인 제목 간격을 12 로, 필터 그룹 · 검정 인증 · 동기화 단계 12 를 8 로 맞췄다.
- **글자 전수 조사 — 13종 유지** — 공개 페이지 16개 · 글자 2,501개가 13종 안(예외는 카카오 지도 축척뿐). 서비스 31곳 실측 결과
  본문 16/14 · 최소 12 · 구역 제목 20 · 굵기 4단 · 버튼 14/500 은 그대로. **페이지 제목은 1056 이상에서 모든 페이지 32/40**
  (홈 · 프로필만 32 이던 것을 통일 — 앱 화면 h1 은 32–34 가 4곳 · 24 가 2곳). 악곡 상세 서열 투표는 「커뮤니티 서열 투표」
  20 제목을 보이게 하고 Basic · Recital 은 16(같은 탭의 패턴 투표 · 의견과 단계를 맞춤). 보조 설명 줄높이 14/20 유지.
- **떠 있는 창 한 규칙(부품 ⑦)** — 대화상자 · 의견 「⋯」 메뉴 · 번역 보기 창 · 차트 툴팁의 경계를 `subtle` → `overlay`,
  대화상자에 그림자, 차트 툴팁 모서리 4 → 10. 툴팁 모서리 근거는 약함(서비스 Stripe 8 · Slack 4) — 예외 없는 한 규칙을 택했다.

## 2026-09-14

- **펼침(부품 ③)** — 화살표 줄 끝 ∨→∧ · 구역 48 / 보조 컨트롤 높이 · 내용까지 16 / 8 · 들여쓰기 0
  (Carbon · MUI · Chakra · Radix · Bootstrap · Stripe · Slack 모두 0) · 면은 역할대로 ·
  올림은 줄 전체 면(Carbon · Radix · Asana — 글자만 밝히면 #dbdbdb → #f2f2f2 라 거의 안 보인다).
  「전체 랭크 보기」 는 고스트 버튼으로 바꾸면 배치가 흔들리고 어울리지 않아 24 글자 버튼 유지.
- **가이드 새로 씀** — Figma · 옛 브리프를 권위에서 뺐다. 값은 코드에서, 문서는 짧게.
- **밑줄 탭 한 모양 두 단계** — 1단 `AreaTabs` 48 · 전폭 구분선 · 모든 폭 탭(넘치면 가로 스크롤) / 2단 `MetricSwitch` 구분선 없음.
  근거 Material 3 주/보조 탭 · Spectrum 기본/조용한 탭 · GitHub · YouTube 실측. 좁은 폭 셀렉트는 없앴다.
- **아이콘 버튼 `IconButton` 하나** — 44 고정 · 모서리 4 · 올림 없는 `nl-icon-button` 폐기. 데스크톱 40 규칙을 따른다.
- **판정 이름 대문자** — `◆JUST · JUST · GOOD · NEAR · MISS`. 빙고 DB 문구 362칸은 사용자가 Neon 에서 SQL 로 고쳤다.
- **목록 흰 선은 키보드일 때만** — 마우스로 열어도 선택 항목에 흰 선이 뜨던 것을 `inputModality` 로 막았다.
- **의견 정렬 = `SortMenu`** — 보조 + 고스트 버튼 쌍은 보조 버튼이 입력칸 모양이 되며 선택이 입력칸처럼 읽혔다.
- **모서리 8 확대** — 페이지 번호, 패턴 투표 0–4 버튼(떨어진 누르는 상자). 칩 · 적용 조건도 8, 태그는 4.
- **입력 컨트롤 한 언어(B′ · R3 · F2 · M2 · B2 · H2)** — 면 #1b1b1b + 경계 #717171 · 모서리 8 · 포커스 후광 ·
  모든 목록을 정렬 메뉴 모양으로 · 보조 버튼 = 입력칸 모양 · 높이 토큰 하나(44/40). 조사 자료 `docs/research/`.
- **홈 구역 제목 20/28** — 다른 페이지와 같게. 입력 글자는 16 유지(모바일 확대 방지).
- **오락실 상세 「위치」 · 「기체」 제목 20** — 레일 상자 안 「최근 확인」 은 16.
- **서열표 카드** — 테두리 = 이 표 달성(기준 색, 달성 + FC 는 초록 → 기준 색 그라데이션, Pianist 는 기준 색 하나, 달성 못 한 FC 는 테두리 없음 — 09-17),
  자켓 위엔 오른쪽 아래 난이도 판만, 달성한 곡 점수는 기준 색(S 노랑 · 990k 살구 · Pianist 분홍).
- **곡 상세 순위표 = 유저 랭킹 행** — 검정 명판은 넣지 않는다. 320 에서 닉네임 말줄임은 알고 수용.
- **필터 항목 글자 색** — 카테고리 · 난이도 · 내 기록(S 는 JUST 노랑)을 해당 색으로, 올리면 기본 글자색.
- **자켓 수집 = 관리자 북마클릿** — 자켓 없는 곡만 관리자 동기화 때 받아 저장, 사용자 오버레이엔 알리지 않는다.
  관리자 곡 편집에서 자켓 교체 · 되돌리기.

## 2026-09-13

- **검정 명판** — 금속 사다리(흑연 · 브론즈 · 실버 · 골드 · 흑단), Basic 직각 / Recital 파인 모서리, 높이 24 유지.
- **오락실 상세** — 제목 위 사진 자리(없으면 자리표시자, 672 미만 전체 폭) → 정보 → 위치 → 기체.
  1055 이하 하단 고정 액션 바 [길찾기][♡ N][제보], 1056 이상 레일. 선호는 토글(채운 하트).
- **오락실 영업시간 펼침** — 오늘부터 7일 · 날짜별 예외 반영 · 마감 24:00, 넘기면 다음 날 시각.
- **오락실 기체 줄** — (2026-09-15 카드 · M 높이로 바뀜) 상태 색 점 + 「가동 확인」 · 「고장 신고」 반반 전체 폭. 상태 이유는 점선 밑줄 도움말.
- **오락실 목록 카드** — 영업 상태 줄은 「영업 중 · 24:00까지」 만, 기체 줄은 「기체 N대」 하나. 정렬은 가까운 순 · 이름순 · 선호 인원순.
- **영업시간 확인 제거** — 입력된 영업시간을 믿고, 틀리면 제보로 고친다.
- **홈** — 3열(672 미만)에만 9번째 칸 「피드백」, 「전체 공지」 뒤 꺾쇠(잉크 기준 광학 보정).
- **빙고 검색창** — 제목 · 과제곡 · 아티스트 · 번역 제목으로 찾는다.
- **점수 분포 인원 숫자** — 화면에선 숨기고 스크린 리더에만.
- **관리자** — 대시보드(자체 집계 · 운영 배포에서만 셈 · 개인정보처리방침 개정), 오락실 편집(기체 · 영업시간 · 사진 · 예외).

## 2026-09-12

- **눌림에 scale 없음 · 새 움직임 금지 · 공용 부품 규격을 페이지에서 덮어쓰지 않기.**
- **슬라이더 · 라디오 · 체크박스 모양** — 채운 점 + 후광 / 링 + 점 / 테두리 2 · 체크 선 2. 라디오는 올림 변화 없음.
- **공지 분류 칩은 제목 위 줄** — 제목 시작선을 고정.
- **필터 팝오버 머리 줄** — 왼쪽 제목 + 오른쪽 「초기화」. 오락실 「영업 중」 은 필터 안.
- **오락실 재설계** — 지도 · 목록 한 집합, 카드 누름 = 지도 이동 + 펼침(상세는 「자세히 보기」).
  제보 창은 672 미만 전체 화면 · 이상 768 모달. 「고장 신고」 는 빨간 테두리.
- **목록 더 불러오기** — 악곡은 자동 3번 뒤 버튼, 빙고는 전부 표시.
- **서열표 구성** — Basic = S · 990k · Pianist, Recital = 표 하나.
- **북마클릿 = 짧은 로더** — 본체는 `/api/bookmarklet` (모바일 5,000자 잘림 대응).

## 2026-09-11

- **필터 · 정렬 공통 문법** — 정렬 분리 · `FilterGroup` · 모바일 칩 · `AppliedTokens` · 그릇 유지(바텀시트 없음).
- **같은 줄 컨트롤 높이 = 컨트롤 높이 토큰** — 행 단위 일치가 역할 구분보다 먼저.
- **셸 1000 이후 재유도** — 레일 1/4, 격자 4열 672 · 5열 삭제 · 카드 최소 156.
- **보기 전환 아이콘 3단** — 목록 · 격자 · 촘촘한 격자.
- **자켓 카테고리 레이블** — 플레이트 유지, 글자만 카테고리 색, 오른쪽 아래.
- **빙고 상세** — 칸 팝오버 상세 · 줄 단위 목록 · 「빙고 정보」 펼침.
- **채보 전체 악보** — 세로 띠 하나 + 미니맵, 읽기 줄은 「현재 시각 / 전체 길이」.

## 2026-09-10 이전 (지금도 유효한 기초)

- **검정 없음 배지** (09-10) · **피드백 대화상자 334/768** · **Compact 액션 등폭** · **푸터 840 미만 가운데** (09-07).
- **떠 있는 창 경계 토큰 `border/overlay`** · **로딩 표시(펄스 스켈레톤 · busy 스피너 · 위 2px 막대)** (09-04).
- **난이도 = 색 글자** `difficulty/text-*` (09-02).
- **`content/pending` · `surface/media-scrim` · `content/on-media`** — 보류 값은 흐리게(opacity 없이), 이미지 위 글자 (08-19).
- **포커스 = 컨트롤 자신의 1px 선** · **보편적 올림/선택 배경 없음** · **글자 13종(`emphasis-label` 포함)** (08-11~13).

- 2026-09-20: 최근 응답 Grd 0은 원본을 보존하고 검증한 양손 A/A+/S/P 계산으로 최고 기록을 보완한다. 기존 처리 건도 횟수 중복 없이 재검사하며 전체 연동 기준은 유지한다.
